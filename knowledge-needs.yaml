knowledge-needs:
- name: (Creating Application) Logic
  guid: b097750b-4f88-4094-a656-2a4359f14c85
  topics:
  - name: Creating Actions
    id: creating-actions
    subtopics:
    - name: What are service actions/when to use service actions
      id: service-actions-when-use
    - name: Server-side vs client-side logic
      id: server-vs-client-logic
    - name: How to create an action
      id: how-to-create-an-action
    - name: Logic Flows
      id: logic-flows
    - name: What are client actions/when to use client actions
      id: client-actions-when-use
    - name: What are server actions/when to use server actions
      id: server-actions-when-use
    - name: Screen-level actions
      id: screen-level-actions
      description: 'Wants to understand the two types of screen-level actions in
        OutSystems Traditional Web Apps: Preparation actions, which run automatically
        before a screen renders to fetch data and set variables, and Screen Actions,
        which run in response to user interactions like clicking a button, so that
        they can add the right logic to each when building a Traditional Web App
        screen.'
    - name: Use screen server actions
      id: use-screen-server-actions
      description: Wants to add a screen client action that runs a server action
        from a button click, pass the current form values as input parameters,
        and continue the flow after a successful save in a Traditional Web App.
    - name: Action properties reference
      id: action-properties-reference
      description: Wants to look up the configuration properties available for an
        action in OutSystems 11 Service Studio, including which are mandatory and
        their default values, so they can correctly configure and understand the
        action's behavior.
    - name: Server action exposure
      id: server-action-exposure
      description: 'Wants to determine when a server action can be exposed, based on whether it is defined in an App or is a Library action that uses a parameter defined with a non-exposed static entity, so they can expose only valid server actions.'
    - name: Use AI logic suggestions
      id: use-ai-logic-suggestions
      description: Wants to understand what logic suggestions are and how to use
        them in action flows, including starting and naming the flow to trigger
        suggestions, accessing and accepting suggestions via the AI radar, using
        specific and generic suggestion types, and applying tips to improve suggestion
        quality, so that they can build application logic faster with AI-assisted
        suggestions.
    - name: Enable logic suggestions
      id: enable-logic-suggestions
      description: Wants to enable or disable logic suggestions in Studio Preferences
        (called Code Mentor in Service Studio and AI logic suggestions in ODC Studio),
        so that they can control whether suggestions appear while building action
        flows.
    - name: AI logic suggestions model
      id: ai-logic-suggestions-model
      description: Wants to understand how the AI logic suggestions model works,
        including that it uses an OutSystems proprietary ML model trained on anonymized
        patterns running entirely within OutSystems infrastructure rather than external
        LLMs, so that they can make informed decisions about data privacy and tool
        selection.
    - name: Action types overview
      id: action-types-overview
      description: Wants to understand the different types of actions available
        in OutSystems 11 (OutSystems built-in actions, custom actions, and actions
        that handle system events) and when each applies, so that they can choose
        the right type of action when designing application logic.
    - name: Troubleshoot Code Mentor
      id: troubleshoot-code-mentor
      description: Wants to troubleshoot Code Mentor issues in Service Studio,
        such as no more suggestions appearing, needing to click again to get
        suggestions, the AI radar not showing up due to missing prerequisites,
        or the AI radar disappearing due to connectivity problems, so that they
        can resolve the issue and keep using logic suggestions.
    - name: Iterate a list
      id: iterate-a-list
      description: Wants to loop over each element in a collection using the
        For Each logic node in OutSystems 11, including setting the Record
        List, Start Index, and Maximum Iterations properties, so that they
        can perform logic recursively on each list item.
    - name: Create conditional paths
      id: create-conditional-paths
      description: Wants to branch logic execution in a screen action based
        on a condition, by using If and Switch nodes in Service Studio, in
        OutSystems 11 (O11) apps, so that different logic paths run depending
        on runtime values.
    - name: Conditional paths (ODC)
      id: create-conditional-paths-odc
      description: Wants to branch logic execution in an action based on a
        condition, by using If nodes in ODC Studio, in OutSystems Developer
        Cloud (ODC) apps, so that different logic paths run depending on
        runtime values.
    - name: Create action via widget
      id: create-action-from-widget-event
      description: Wants to create a new client action directly from a
        widget's event property (for example, a Dropdown's On Change), by
        selecting New Client Action from the property's drop-down list in
        Service Studio/ODC Studio, so that the widget is automatically
        wired to trigger the newly created logic without adding a separate
        action and connector afterward.
    - name: Build CRUD server action
      id: build-crud-server-action-odc
      description: Wants to create a server action in ODC Studio with an
        entity-typed input parameter, drag the entity's built-in
        CreateOrUpdate entity action into the logic flow, and set its
        Source property to the input parameter, so that data entered on a
        form screen (for example, Employee, Project, or project member
        details) creates or updates the corresponding database record when
        the user clicks Save.
    - name: Define async action
      id: define-async-client-action
      description: Wants to make a client action asynchronous and signal its
        completion or failure, by using the $resolve() and $reject() predefined
        functions inside a JavaScript element, in OutSystems 11 Mobile Apps and
        Reactive Web Apps, so that server calls or other async operations can
        resolve or reject a returned promise instead of the client action
        returning synchronously.
    - name: Call async action
      id: call-async-client-action
      description: Wants to consume the promise returned by calling an
        asynchronous client action, by chaining then(onFulfilled, onRejected)
        or catch(onRejected) on the $actions.ActionName() call, in OutSystems 11
        Mobile Apps and Reactive Web Apps, so that they can read the action's
        output parameters and handle unhandled errors or $reject() calls once
        the async action completes.
  - name: Calculation and algorithm examples
    id: calculation-and-algorithm-examples
    subtopics:
    - name: Calculate factorial
      id: calculate-factorial
      description: Wants to implement a factorial calculation in a server
        action, so that a button click returns the factorial for a number.
  - name: Using Events
    id: using-events
    subtopics:
    - name: System Events
      id: system-events
      description: Wants to look up when the On Begin Web Request system event
        executes and how to configure its Description property, in Service
        Studio, so that they can correctly document logic that runs on every
        server request in a Traditional Web App (O11).
    - name: How to use Events
      id: how-to-use-events
    - name: Screen Events
      id: screen-events
    - name: UI elements Events
      id: ui-elements-events
    - name: Handle block events
      id: handle-block-events
      description: Wants to handle events triggered by a block by mapping a screen
        action to the block's event and accessing the event input parameters, in ODC
        Studio, so that the screen can respond to user interactions happening inside
        the block.
    - name: Trigger block event
      id: trigger-block-event
      description: Wants to create an event on a block and trigger it using a
        Trigger Event node in a client action, in ODC Studio, so that the block
        can notify its parent screen when something occurs inside it.
    - name: Block events O11
      id: block-events
      description: Wants to expose an event on a block and handle it on a screen by
        triggering the event in the block and mapping a screen action to it, so that 
        they can pass data and trigger logic across UI components.
    - name: Screen action as callback
      id: screen-action-as-callback
      description: Wants to assign a screen action as a callback for a UI component's
        provider events using JavaScript and the OutSystems UI API (e.g., SetProviderEvent),
        in Service Studio, so that custom logic runs when a component fires events
        like open or close.
    - name: On Parameters Changed
      id: on-parameters-changed
      description: Wants to refresh a block's displayed information when
        parent-passed input parameters change, by implementing the block's
        On Parameters Changed event handler in Service Studio, so that the
        block reflects updates without needing a full screen refresh.
    - name: Fix OnRender loop warning
      id: fix-onrender-loop-warning
      description: Wants to fix the "Potential Loop at Runtime" warning caused
        by changing screen or block data inside an OnRender action, in Service
        Studio for OutSystems 11, by removing that data change from the
        OnRender action, so that the OnRender event doesn't retrigger itself
        and cause an infinite rendering loop.
    - name: Fix OnAfterFetch loop warning
      id: fix-onafterfetch-loop-warning
      description: Wants to fix the "Potential Loop at Runtime" warning caused
        by refreshing an Aggregate or Data Action inside its own OnAfterFetch
        action, in Service Studio for OutSystems 11, by removing that refresh
        from the OnAfterFetch action, so that the OnAfterFetch event doesn't
        retrigger itself and cause an infinite fetch loop.
  - name: Understanding Exceptions
    id: understanding-exceptions
    subtopics:
    - name: How to handle Exceptions
      id: how-to-handle-exceptions
    - name: Fix empty OnException
      id: fix-empty-onexception
      description: Wants to fix the "Empty 'OnException'" error in Service Studio
        for OutSystems 11, caused by an OnException action flow that doesn't begin
        with an Exception Handler node, by adding an Exception Handler node as the
        first node in the OnException action, so the flow compiles and publishes
        successfully.
    - name: Custom JS error handler
      id: custom-js-error-handler
      description: Wants to define and register a custom JavaScript error handler function,
        using OsRegisterExceptionHandler and its predefined error codes, so they
        can handle JavaScript errors that occur during an Ajax refresh of an
        unescaped expression or during the RunJavaScript action of the
        HTTPRequestHandler extension, in Traditional Web Apps (O11).
    - name: Fix Log Error warning
      id: fix-log-error-warning
      description: 'Wants to fix the audit suggestion warning "''Log Error''
        should be set to ''Yes'' for ''AllExceptions'' error handler at screen
        flow level" by setting the Log Error property to Yes on the screen
        flow''s AllExceptions error handler in Service Studio, so that
        OutSystems logs detailed exception information for troubleshooting,
        in OutSystems 11.'
    - name: Fix error handling warning
      id: fix-error-handling-warning
      description: 'Wants to fix the "Errors are not being handled in the web
        flow" warning in Service Studio for OutSystems 11, caused by a web
        flow lacking error handlers and its Theme lacking a web flow for
        error handling, by adding error handlers to the web flow or changing
        the Theme''s error handling Flow property to one that already
        implements error handling.'
  - name: Using Expressions
    id: using-expressions
    subtopics:
    - name: Operands
      id: operands
    - name: When and how to write expressions
      id: write-expressions-guide
    - name: Scope
      id: scope
    - name: Operators
      id: operators
    - name: Functions
      id: functions
    - name: Check string contains
      id: check-string-contains
      description: Wants to check whether one text string contains another using
        the built-in Index function in OutSystems O11, including a case-insensitive
        variant, so that they can replicate C# String.Contains behavior in logic
        expressions.
    - name: Function default values
      id: function-default-values
      description: Wants to understand why OutSystems built-in functions don't
        raise exceptions during execution, returning the default value of the
        output data type instead when an issue occurs, so that they can predict
        function behavior when writing expressions in ODC Studio.
    - name: Built-in math functions
      id: built-in-math-functions-odc
      description: Wants to know which built-in math functions are available in ODC Studio,
        including their parameters, return types, and availability in server-
        side logic, client-side logic, and database aggregates, so that they can
        perform mathematical calculations in expressions.
    - name: Built-in text functions
      id: built-in-text-functions
      description: Wants to know which built-in text functions are available in ODC Studio,
        including their parameters, return types, and availability in server-
        side logic, client-side logic, and database aggregates, so that they can
        manipulate text values in expressions.
    - name: Organization functions
      id: organization-functions
      description: Wants to know which built-in organization functions are available in ODC
        Studio, including their outputs and availability in server-side logic,
        client-side logic, and database aggregates, so that they can retrieve
        session locale, user agent, and app name information in expressions.
    - name: O11 math functions
      id: built-in-math-functions-o11
      description: Wants to know which built-in math functions are available
        in OutSystems 11 (Traditional Web, Mobile, and Reactive apps),
        including their parameters, return types, and availability in server-
        side logic, client-side logic, database aggregates, and Local Storage,
        so that they can perform mathematical calculations in expressions.
    - name: Built-in date functions
      id: built-in-date-functions-odc
      description: Wants to know which built-in DateTime functions are available
        in ODC Studio (e.g. GetTicks), including their parameters and return
        types, so that they can perform date and time calculations in expressions.
    - name: O11 date functions
      id: built-in-date-functions-o11
      description: Wants to know which built-in date and time functions are
        available in OutSystems 11 (Traditional Web, Mobile, and Reactive apps),
        including date/time arithmetic (AddDays, AddMonths), extraction (Day,
        Month, Year), and difference (DiffDays, DiffHours) functions,
        including their parameters, return types, and availability in server-
        side logic, client-side logic, database aggregates, and Local Storage,
        so that they can perform date and time calculations in expressions.
    - name: DST date calculations
      id: dst-date-calculations
      description: Wants to understand how Daylight Saving Time (DST) transitions
        affect date and time built-in functions in OutSystems 11, including
        unexpected results from client-side JavaScript Date arithmetic during
        DST changes and how server-side Diff* functions always ignore DST and
        use the platform server's time zone, so that they can predict outcomes
        when adding to or comparing dates across a DST boundary.
    - name: Data conversion functions
      id: built-in-conversion-functions
      description: Wants to know which built-in data conversion functions are
        available in OutSystems 11 (e.g. TextToInteger, DecimalToText,
        DateTimeToText, BooleanToInteger), including their parameters, return
        types, and availability in server-side logic, client-side logic, and
        database aggregates, so that they can convert values between data
        types in expressions.
    - name: Format numbers rounding
      id: format-numbers-rounding
      description: Wants to know how to format Currency, Decimal, and Percent
        values into Text (specifying decimal digits, decimal separator, and
        group separator) using FormatCurrency, FormatDecimal, and
        FormatPercent, and understand why rounding results differ between
        server-side logic (round half up) and client-side logic (round half
        to even), so they get consistent formatted output in OutSystems 11.
    - name: Text/phone formatting
      id: text-phone-formatting
      description: Wants to know how to format Text output using FormatText
        (padding a string to a minimum/maximum character count with a
        chosen side and padding character) and FormatPhoneNumber (splitting
        a phone number string into international code, area code, and phone
        number segments with custom separators), so they can build
        formatted text values in OutSystems 11.
    - name: FormatDateTime patterns
      id: formatdatetime-patterns
      description: Wants to know the formatting pattern characters (day,
        month, year, hour, minute, second, AM/PM designators) supported by
        the FormatDateTime function to convert a DateTime value into Text in
        a custom format, including how to escape literal characters, so
        they can display dates and times in a specific format in
        OutSystems 11.
    - name: Built-in URL functions
      id: built-in-url-functions
      description: Wants to know which built-in URL functions are available in OutSystems
        11 (e.g. AddPersonalAreaToURLPath, GetBookmarkableURL,
        GetPersonalAreaName, GetOwnerURLPath, GetExceptionURL), including their
        parameters, return types, and availability in server-side and client-
        side logic, so that they can manage Personal Area URL paths and retrieve
        the current screen's or last exception's URL in expressions.
    - name: O11 misc functions
      id: o11-misc-functions
      description: Wants to know which miscellaneous built-in functions are
        available in OutSystems 11 (GeneratePassword, If, IsLoadingScreen,
        CurrentThemeIsMobile), including their parameters, return types, and
        availability in server-side logic, client-side logic, and database
        aggregates, so that they can generate passwords, evaluate conditional
        expressions, detect screen loading state, and check for a mobile
        theme in expressions.
  - name: Library actions
    id: library-actions
    description: Wants to know which ODC library actions are available and what each
      library is for, so they can pick the right action for the logic task
      they need.
    subtopics:
    - name: Text library actions
      id: text-library-actions
      description: Wants to know which actions are available in the Text library and what
        each does, so they can pick the right action for text manipulation tasks
        in ODC Studio.
    - name: URL library actions
      id: url-library-actions
      description: Wants to know which actions are available in the URL library and what
        each does, so they can manipulate URLs correctly in ODC Studio.
  - name: Debugging and troubleshooting logic
    id: debug-troubleshoot-logic
    subtopics:
    - name: How to debug and troubleshoot application logic
      id: debug-troubleshoot-app-logic
  - name: Creating Variables
    id: creating-variables
    subtopics:
    - name: Site properties / Settings
      id: site-properties-settings
    - name: Input Parameters
      id: input-parameters
    - name: Local Variables
      id: local-variables
    - name: Output Parameters
      id: output-parameters
    - name: How to use Variables
      id: how-to-use-variables
    - name: Reset form after save
      id: reset-form-after-save
      description: Wants to reset a screen's form to a blank state after
        saving a record, and reuse the same identifier input parameter to
        view or edit an existing record, by adding an Identifier-typed
        screen input parameter, filtering the screen's Aggregates with it,
        and setting it via a Destination node to NullIdentifier() (reset)
        or GetX.List.Current.X.Id (edit), so that a single detail screen
        supports add, view, and edit flows for master-detail navigation in
        ODC Studio.
    - name: Record auto-assign
      id: record-auto-assign
      description: 'Wants to automatically populate a Record variable''s attribute
        values in Service Studio''s Assign Tool, by first assigning the record''s
        identifying attribute in the same Assign node, so they don''t have to
        manually enter each attribute value one by one.'
    - name: Client Variables
      id: client-variables
      description: 'Wants to store and share client-side key-value data across
        Mobile and Reactive Web apps using Client Variables, including how to
        create one in Service Studio, use it to filter an Aggregate or bind it
        to a widget, and understand when its value resets (on sign-out) or is
        shared (same User Provider, same browser, via Public Blocks/Client
        Actions), so that app context or configuration data persists
        client-side without exposing sensitive information.'
    - name: Runtime property fields
      id: runtime-property-fields
      description: Wants to look up the configurable properties of a Runtime Property
        element and which are mandatory, so they can correctly define a Runtime
        Property in Service Studio for OutSystems 11.
  - name: Using Structures
    id: using-structures
    subtopics:
    - name: How to create structures
      id: how-to-create-structures
    - name: What are structures
      id: what-are-structures
    - name: Select output structure
      id: select-output-structure
      description: Wants to use a Structure instead of an Entity in the output of a
        SELECT SQL Query, so they retrieve only the needed attributes and less
        data.
  - name: Logic performance tips
    id: logic-performance-tips
    description: Wants to apply best practices for optimizing OutSystems 11 application
      logic performance — including using Service Center reports to find bottlenecks,
      limiting timer/batch job run duration with checkpoints, simplifying screen
      preparations, treating Site Properties as constants, avoiding isolated Aggregates,
      and avoiding queries inside conditional preparation branches — so that Traditional
      Web, mobile, and Reactive Web apps run efficiently.
    subtopics:
    - name: Client event fetch
      id: client-event-fetch
      description: Wants to avoid performing local data fetch inside client events
        (On Initialize, On Ready, On Render) in OutSystems mobile apps, and instead
        fetch data via data fetch calls, using the On After Fetch event for dependent
        fetches, so that data fetching runs in parallel with screen rendering instead
        of being fully serialized.
    - name: Optimize mobile loading
      id: optimize-mobile-loading
      description: Wants to reduce a mobile screen's loading time when it depends
        on multiple server calls or long lists, by grouping related calls,
        caching infrequently changing data locally, and fetching additional list
        items with infinite scroll, so that the screen renders faster and stays
        responsive.
    - name: StringBuilder for large strings
      id: stringbuilder-large-strings
      description: Wants to concatenate many strings, or a few large strings, into
        one large string efficiently in OutSystems 11, using the StringBuilder API
        from the Text Extension (StringBuilder_Append) instead of plain string concatenation,
        so that operations that build large outputs, such as serializing large record
        sets, avoid severe performance degradation.
    - name: Avoid complex data types
      id: avoid-complex-data-types
      description: Wants to fix Service Studio scalability suggestion warnings
        about Binary Data, List, or Record-typed screen input parameters,
        session variables, and entity attributes, by avoiding those complex
        data types on screens/session state (or splitting a Binary Data
        attribute into a separate one-to-one entity), so that OutSystems 11
        apps avoid unnecessary performance overhead.
- name: (Creating Basic) Application Security
  guid: f89b0297-9ba2-48bd-9e84-b56614a7e620
  topics:
  - name: User Roles
    id: user-roles
    subtopics:
    - name: Anonymous
      id: anonymous
    - name: Anonymous screen access
      id: anonymous-screen-access
      description: Wants to grant anonymous and registered users access to selected
        Traditional Web App screens by assigning the Anonymous role to those
        screens, so that public screens open without sign-in during initial app
        setup.
    - name: Secure anonymous screens
      id: secure-anonymous-screens
      description: Wants to resolve the Service Studio warning about exposing
        a Server Action for public/anonymous access, by removing the
        Anonymous role from a screen or otherwise validating server-side
        data access, so that public endpoints don't expose sensitive data or
        enable cross-site request forgery attacks.
    - name: Check user role in code
      id: check-user-role-in-code
      description: Wants to verify whether the currently logged-in end-user
        has a specific public role (local or consumed from another app),
        inside a server action or service action, using the
        CheckEndUserRole built-in function with a RoleId input, so that
        business logic can conditionally allow or restrict behavior based
        on the user's role.
    - name: Check role of user
      id: check-role-of-user
      description: Wants to verify whether a given user (not necessarily the
        current one) has a specific role, inside server-side logic or in
        database/local storage aggregates, using the CheckRole built-in
        function with RoleId and UserId inputs, so that business logic can
        conditionally allow or restrict behavior based on any user's role, in
        OutSystems 11.
    - name: Grant role in logic
      id: grant-role-in-logic-o11
      description: Wants to grant a role to a user programmatically inside
        server-side logic (a Service Studio logic flow), in OutSystems 11,
        so that role assignment can be automated instead of done manually
        through the Users application.
    - name: Add role in Studio
      id: add-role-in-studio
      description: Wants to create a new application-specific role in a
        module, using the Logic tab's Roles folder and Add Role option, so
        that they can define a custom role (for example HRManager) to use
        later in screen and logic access control.
    - name: Restrict screen access
      id: restrict-screen-access
      description: Wants to restrict which end-users can open specific
        screens by enabling only a custom application role on the screen's
        roles property (for example, Screen Roles in OutSystems 11 or
        Accessible by in ODC Studio), so that only users holding the
        required role (for example HRManager) can access those screens.
    - name: Restrict logic with roles
      id: restrict-logic-with-roles
      description: Wants to restrict what end-users can do or see by using the
        generated Check<RoleName>Role() action, checking the role in an If
        condition to guard server-side logic or in an aggregate filter to
        restrict returned data, so that only users with the required role can
        execute actions or access sensitive data.
    - name: Access control overview
      id: access-control-overview-o11
      description: Wants to understand what access control means in
        OutSystems 11, including the difference between authentication and
        authorization, the built-in Anonymous and Registered roles versus
        application-specific roles, and how authorization is granted to
        users, either programmatically with Grant/Revoke actions or manually
        in the Users app, so they can reason about how permissions are
        structured before implementing them.
    - name: Built-in end-user access control choice
      id: builtin-end-user-access-control-choice
      description: Wants to decide whether to rely on OutSystems' built-in
        end-user roles and screen or logic permission checks to control
        access in an application, so that they know whether they need an
        external authorization layer before implementation.
    - name: REST endpoint permissions
      id: rest-endpoint-permissions
      description: Wants to understand how OutSystems automatically computes
        access control for generated REST endpoints tied to screens, blocks,
        global client actions, exception handlers, and application events,
        based on the security settings of the calling context, so that they
        know what permission level an endpoint enforces without configuring
        it manually.
    - name: Check permissions
      id: check-permissions-overview-o11
      description: 'Wants to understand how permissions are checked in
        OutSystems 11 applications, using the Screen Roles property to
        restrict screen access and the generated Check<RoleName>Role(UserId)
        action to guard server-side logic, so they can reason about the
        built-in mechanisms available for enforcing role-based authorization
        before implementing them.'
    - name: Check role actions ODC
      id: check-role-actions-odc
      description: 'Wants to check whether a user has a specific role in ODC,
        using the generated Check<RoleName>Role(UserId) client and server
        actions inside If conditions on server-side logic, or in a screen
        widget''s If condition, Enabled, and Visible properties, so they can
        enforce role-based authorization on both logic and UI elements.'
    - name: Manage menu access
      id: manage-menu-access
      description: Wants to hide menu entries with a Data Action that combines
        an HRManager role check and a custom Server Action that detects
        whether the current user manages employees, so that the Vacations app
        menu only shows the links the user is allowed to see.
    - name: ODC access control overview
      id: odc-access-control-overview
      description: Wants to understand what access control means in ODC,
        including the difference between authentication and authorization and
        how application-specific roles determine what an end user can do after
        signing in, so they can reason about permissions before implementing
        them.
    - name: Hierarchical roles choice
      id: hierarchical-roles-choice
      description: Wants to decide whether to use hierarchical roles instead
        of OutSystems' default static roles when designing an application's
        security model, weighing benefits (hierarchical authorization, reduced
        role explosion, runtime configurability) against implementation costs
        (configuration overhead, performance overhead, security risks, lack of
        built-in product support), so they can choose the appropriate role
        model for their app.
    - name: Hierarchical roles demo
      id: hierarchical-roles-demo
      description: Wants to implement the hierarchical roles authorization
        approach in a demo sales application by restricting screen access
        with Screen Roles, controlling displayed information with
        HasAccessByFunctionId, and controlling available actions with
        GetPermissionsByUserId, so they can apply the three authorization
        scenarios (screen access, content visibility, action permissions)
        using hierarchical roles in OutSystems 11.
    - name: Implement hierarchical roles
      id: hierarchical-roles-implementation
      description: Wants to implement a hierarchical roles authorization solution in
        OutSystems 11 by designing a supporting data model, structuring the app
        architecture into layered modules, building BackOffice management
        screens to manage functions and permissions, and creating server actions
        for checking user permissions and function-level access, so they can
        enforce granular, configurable authorization across their app without
        relying on OutSystems' default static roles.
- name: (Creating an Application) Architecture
  guid: 9ce4fe11-dcc9-4b0f-b3ea-545557accdec
  topics:
  - name: Why is architecture relevant?
    id: architecture-relevance
    subtopics:
    - name: Basic architecture concepts
      id: basic-architecture
    - name: Importance of having good architecture from day one
      id: good-architecture-value
  - name: How to build app architecture
    id: app-architecture-basics
    subtopics:
    - name: Map OS concepts with "rest of the world" concepts
      id: map-os-to-global-concepts
    - name: Programming model intro
      id: programming-model-intro
      description: Wants to understand the OutSystems programming model and how
        reactive SPAs differ from traditional web models, so they can map Java
        web app concepts and client-side logic to OutSystems apps when choosing
        an app model.
    - name: Application layers overview
      id: application-layers-overview
      description: Wants to understand how OutSystems organizes an application
        into Data, Logic, and Interface layers, and how those layers map to
        Java persistence, business logic, and presentation layers, so they can
        place each concern in the correct layer when building an OutSystems
        app.
  - name: Modules
    id: modules
    subtopics:
    - name: What are modules?
      id: what-are-modules
    - name: Difference between applications and modules
      id: apps-vs-modules
    - name: Clone a module
      id: clone-a-module
      description: 'Wants to clone a module and move it to another application
        in OutSystems 11 Service Studio, using the Module menu''s Clone option
        and the Independent Modules app''s Move action, so that a module built
        in one app can be reused in another without recreating it from
        scratch.'
    - name: Module naming by layer
      id: module-naming-by-layer
      description: Wants to choose the correct naming convention for foundation, core, and
        mobile modules, so they can name modules consistently by layer in an
        OutSystems app.
    - name: Module naming rationale
      id: module-naming-rationale
      description: 'Wants to understand why module naming conventions matter,
        including how they reveal each module''s purpose, enforce the reference
        architecture, and normalize common patterns, so they can decide when
        to apply a convention.'
    - name: Module properties reference
      id: module-properties-reference
      description: Wants to look up the configuration properties available for
        a Traditional Web App module in OutSystems 11 Service Studio, covering
        general settings (DBMS, user provider, icon), Web settings (theme,
        jQuery version, exception handler), validation and upgrade messages,
        and advanced settings (cookies, multi-tenancy, rendering mode),
        including default values, so they can correctly configure and
        understand module behavior.
    - name: Reference element properties
      id: reference-element-properties
      description: Wants to look up the properties available for a Reference
        element, a reference to another module shown in the workspace tree,
        in Service Studio for OutSystems 11, including Name, Description,
        Icon, and jQuery Version and which are mandatory, so they can
        correctly interpret a module reference.
    - name: End-user module names
      id: end-user-module-names
      description: Wants to keep end-user module names short, direct, and
        meaningful so the module names work well in URLs and stay easy for
        users to understand.
  - name: Multi-tenant architecture
    id: multi-tenant-architecture
    description: Wants to understand what multi-tenancy is and how OutSystems 11
      implements it through user providers and tenants, so they can design applications
      that isolate data, end-users, and sessions for multiple customers on a single
      deployed application.
    subtopics:
    - name: Set Is Multi-tenant
      id: set-is-multi-tenant
      description: Wants to configure the Is Multi-tenant property on a module's
        Entities, Site Properties, and Timers in OutSystems 11, deciding whether
        each should isolate data per tenant or share it across tenants, so they
        can correctly implement data segmentation when building a multi-tenant
        application.
    - name: Physical tenant isolation
      id: physical-tenant-isolation
      description: Wants to implement physical segregation of tenants in
        OutSystems 11 by giving each tenant a separate database accessed
        through a database access layer, so they can achieve full data
        isolation between tenants at the cost of higher infrastructure and
        maintenance overhead compared to logical segregation.
    - name: Secure tenant data
      id: secure-tenant-data
      description: Wants to prevent one tenant from accessing another tenant's
        data in a logically segregated OutSystems 11 application, by
        controlling use of the "Show Tenant Identifier" property and the
        TenantSwitch action, and by auditing where these are referenced, so
        they can guard against accidental cross-tenant data exposure.
- name: (Modeling, Consuming and Using) Data
  guid: 184681b2-96da-4711-9efd-53172730bbed
  topics:
  - name: Showing data on a screen/block
    id: display-data-screen-block
    subtopics:
    - name: Replace sample data in a UI element
      id: replace-ui-sample-data
    - name: Bind data to a UI element
      id: bind-data-to-a-ui-element
    - name: Header row widget behavior
      id: header-row-widget-behavior
      description: Wants to understand how the Header Row Widget automatically
        integrates with a Table Records Widget, including how it auto-adds/removes
        Cells to match each attribute and how to hide it via the Show Header
        property, in Service Studio for OutSystems 11 Traditional Web Apps,
        so they can correctly structure and configure table headers.
    - name: Table pagination setup
      id: table-pagination-setup
      description: Wants to add pagination and records-per-page control to a Table
        widget in an OutSystems 11 Screen, so that users can navigate through large
        datasets in configurable page sizes.
    - name: ODC table pagination
      id: odc-table-pagination-setup
      description: Wants to add pagination and records-per-page control to a Table
        widget in ODC Studio, so that users can navigate through large datasets in
        configurable page sizes.
    - name: Table sorting setup
      id: table-sorting-setup
      description: Wants to implement user-triggered column sorting on a Table widget
        using a Dynamic Sort in an Aggregate, so that users can reorder displayed
        records by clicking header cells.
    - name: Sort SQL-fed table
      id: sql-table-sorting-setup
      description: Wants to enable dynamic column sorting on a Table widget whose
        data source is a raw SQL query (not an Aggregate), by setting up an OnSort
        action with an attribute-encoding function in Reactive Web, or the List
        Sort Column Rich Widget in Traditional Web, in OutSystems 11, so users can
        reorder SQL-query-driven table results safely without SQL injection risk.
    - name: Customize table columns
      id: customize-table-columns
      description: Wants to customize the columns of a Table widget — adding, deleting,
        renaming, editing column expressions, and adjusting text alignment — in an
        OutSystems 11 Screen, so that the Table displays the right data fields with
        appropriate labels and formatting.
    - name: Set up Table widget
      id: set-up-table-widget
      description: Wants to add a Table widget to a Screen, bind it to an Aggregate
        as its data source, and configure the displayed columns — including adding,
        deleting, renaming, editing expressions, and adjusting alignment — in ODC
        Studio, so that the employee list is presented correctly in the browser.
    - name: Add Table widget
      id: add-table-widget
      description: Wants to add a Table widget to a Screen and bind it to an Aggregate
        as its data source, so that the screen displays database records in a tabular
        layout.
    - name: Fixed header table
      id: fixed-header-table
      description: Wants to create a vertically scrollable Table Records widget with
        a fixed header row that remains visible while users scroll, by adding a separate
        Table Widget as a static header, applying TableRecords style classes, and
        enclosing the body in a CSS-styled container with overflow-y auto, in Service
        Studio for O11 Traditional Web Apps, so that large datasets are navigable
        without losing column context.
    - name: Responsive table setup
      id: responsive-table-setup
      description: Wants to add and configure the Responsive Table UI Pattern in Service
        Studio for O11 Traditional Web Apps, including binding data to the
        placeholder and configuring the row behavior so the table header stays
        fixed while rows either scroll or expand.
    - name: Optimize mobile list data loading
      id: optimize-mobile-list-data-loading
      description: Wants to tune how a mobile app list fetches records on demand,
        by setting the initial fetch size, the On Scroll Ending increment, and the
        infinite-scroll-threshold attribute, and by keeping list items visually
        simple, in OutSystems 11, so that scrolling stays smooth and list rendering
        avoids visual glitches.
  - name: Securing Data
    id: securing-data
    subtopics:
    - name: Security validations on CRUD
      id: security-crud-validation
  - name: Getting Data from your systems of records
    id: fetch-data-system-records
    subtopics:
    - name: Get data from external db
      id: get-data-from-external-db
    - name: Create SAP entity actions
      id: create-sap-entity-actions
      description: Wants to generate default entity actions for a
        non-hierarchical external SAP OData connection, by selecting
        non-navigation-property attributes in the ODC Portal and adding
        the entity as a public element in ODC Studio, so that they can
        use the generated Create, Update, and Delete entity actions in
        their app logic.
    - name: Create SAP deep insert
      id: create-sap-deep-insert
      description: Wants to generate a custom deep insert server action
        for an external SAP OData connection with hierarchical
        (navigation property) attributes, by selecting those attributes
        in the ODC Portal and adding the resulting server action as a
        public element in ODC Studio, so that they can insert an entity
        and its related entities in a single atomic transaction.
  - name: Adding test data
    id: adding-test-data
    subtopics:
    - name: How to bootstrap test data from an excel file
      id: bootstrap-test-data-excel
    - name: How to edit data in Service Studio
      id: edit-data-service-studio
    - name: Manage sample data
      id: manage-sample-data
      description: Wants to export, import, and reset the sample data provided
        by the OutSystemsSampleDataDB module using its public Server Actions
        and Service Center timers, so they can customize or refresh the data
        used by Screen Templates in Service Studio.
    - name: Reference sample data
      id: reference-sample-data
      description: Wants to add the OutSystems Sample Data public elements to
        an app in ODC Studio, so they can use the sample entities and actions
        that Screen Templates rely on when building screens.
  - name: Modeling Data
    id: modeling-data
    subtopics:
    - name: Entity schema mapping
      id: entity-schema-mapping
      description: Wants to understand how OutSystems represents database tables as entities, including attributes, identifiers, reference attributes, indexes, records, relationships, delete rules, and entity diagrams, so they can model database structures correctly in Service Studio.
    - name: Static Entities
      id: static-entities
    - name: Static entity data migration
      id: static-entity-data-migration
      description: Wants to understand the considerations for migrating Static
        Entity data between OutSystems environments, including how Entity
        Records get environment-specific Data IDs, how the SS_Key combined
        with the Espace SS_Key uniquely identifies a record across
        environments, and how the Is_Active flag marks soft-deleted records,
        so they can correctly map static entity foreign keys and avoid
        data-integrity issues during migration.
    - name: Entity record table reference
      id: entity-record-table-reference
      description: Wants to look up the physical table name and dependencies
        of the Entity Record table (OSSYS_ENTITY_RECORD), including its data
        source (OSSYS_ESPACE) as producer and its lack of downstream
        consumers, so they can understand where entity record data
        originates when investigating or migrating static entity data.
    - name: Static entity record example
      id: static-entity-record-example
      description: Wants to see a worked example of how creating, removing
        (soft-deleting), and re-adding Static Entity records changes the
        OSSYS_ENTITY_RECORD and physical table data (Data_Id, SS_Key,
        Is_Active), so they can interpret real entity record state when
        troubleshooting or verifying static entity data during migration.
    - name: Create/manipulate entities
      id: create-edit-entities
    - name: Import data model from Excel
      id: import-data-model-excel
      description: Wants to create an app's entity and initial data by
        importing a spreadsheet through Import New Entities from Excel, so
        that the data model exists without manually creating entities and
        attributes one by one.
    - name: CRUD wrapper accelerator
      id: crud-wrapper-accelerator
      description: Wants to understand what CRUD wrappers are and use the ODC
        Studio accelerator to auto-generate Create, CreateOrUpdate, Update,
        and Delete server (and service) action wrappers for an entity,
        including their built-in mandatory-attribute validation, so they can
        add business rules and validations to CRUD operations without
        manually building wrapper logic.
    - name: Non-AutoNumber CRUD Ids
      id: non-autonumber-crud-ids
      description: Wants to know how the ODC Studio CRUD wrapper accelerator
        handles entities whose Id attribute doesn't use AutoNumber, requiring
        the developer to supply a unique identifier (for example via
        GenerateGuid()) in the Create and CreateOrUpdate wrappers, so records
        aren't created without a valid, unique Id.
    - name: CRUD wrapper auditing
      id: crud-wrapper-auditing
      description: Wants to enable automatic auditing in ODC Studio CRUD
        wrappers by naming entity attributes CreatedOn, CreatedBy,
        UpdatedOn/ModifiedOn, and UpdatedBy/ModifiedBy, so the generated
        wrapper logic automatically fills in who created or last updated a
        record and when.
    - name: Data types
      id: data-types
      description: Wants to understand what data types are available in ODC, their
        default values, and their minimum/maximum constraints, so that variables and
        entity attributes can be correctly typed during app development.
    - name: Entity properties
      id: entity-properties
      description: Wants to look up the configurable properties of an Entity, including
        which are mandatory, their default values, and constraints such as
        Expose Read Only still allowing SQL-based writes, so they can correctly
        configure an entity in Service Studio for OutSystems 11.
    - name: Entity attribute properties
      id: entity-attribute-properties
      description: Wants to look up the configurable properties of an Entity Attribute,
        including which are mandatory, their default values, and constraints, so
        they can correctly define an attribute's behavior in Service Studio.
    - name: Entity diagram properties
      id: entity-diagram-properties
      description: Wants to look up the configurable properties of an Entity Diagram
        element, including which are mandatory, so they can correctly document
        their data model in Service Studio.
    - name: DateTime UTC behavior
      id: datetime-utc-behavior
      description: Wants to understand how DateTime values are stored in UTC and
        displayed in the device's local time zone — including how client-side,
        server-side, and SQL calls differ — so that time-sensitive data is handled
        correctly across regions.
    - name: Null DateTime behavior
      id: null-datetime-behavior
      description: Wants to understand why the null DateTime value (#1900-01-01
        00:00:00#) behaves differently in mobile apps depending on the Platform
        Server version at first installation, including the UTC time zone
        adjustment applied in environments installed before 10.0.804.0, so that
        they can predict how the null DateTime value is stored, read from local
        storage, and sent to the server.
    - name: Change null DateTime
      id: change-null-datetime-behavior
      description: Wants to change the null DateTime value behavior in mobile apps
        so it matches new installations of Platform Server 10.0.804.0 or later, by
        inserting the Compiler.MobileRuntime.ApplyTZForNullDates parameter into the
        OSSYS_PARAMETER table on-premises (or contacting Support in cloud
        environments), so time zone adjustments stop being applied to the null
        DateTime value.
    - name: Fix invalid data type
      id: fix-invalid-data-type
      description: 'Wants to fix the "Invalid Data Type" error family in Service
        Studio for OutSystems 11, caused by using incompatible data types with
        an operator, setting a property or operator to the wrong data type, or
        assigning a Null Value of a different data type than the Input widget''s
        variable, by matching the operator, property, or Null Value to the
        correct data type, so that the expression or widget configuration
        compiles and publishes successfully.'
    - name: Fix expression overflow
      id: fix-expression-overflow
      description: Wants to resolve the "Expression Overflow" error family in Service
        Studio for OutSystems 11, caused by an invalid Date or Time value, a
        Decimal, Integer, or Long Integer value exceeding its allowed range, an
        expression exceeding its maximum allowed length, or a Text literal
        exceeding its maximum length, so they can correct the underlying value
        or expression and continue publishing or running the app without the
        error.
    - name: Fix invalid indexer
      id: fix-invalid-indexer
      description: 'Wants to fix the "Invalid Record List Indexer" error in Service
        Studio for OutSystems 11, caused by indexing a Record List with a
        non-Integer value or indexing an element that isn''t a List, by
        matching the index value''s data type to Integer or using the []
        operator only on List elements, so that the module compiles and
        publishes successfully.'
    - name: Fix invalid numeric value
      id: fix-invalid-numeric-value
      description: 'Wants to fix the "Invalid Numeric Value" error family in
        Service Studio for OutSystems 11, caused by setting a numeric property
        or entity attribute (Decimals or Length) to a value outside its
        allowed range or of the wrong type, by editing the property or
        attribute to a valid numeric value, so that the module compiles and
        publishes successfully.'
    - name: Fix reserved attribute
      id: fix-reserved-attribute-name
      description: Wants to fix the "'<attribute>' is an invalid attribute name in
        <database>." warning caused by naming an entity attribute using an
        Oracle reserved word, by renaming the attribute to a non-reserved word,
        so that the attribute compiles successfully against an Oracle database,
        in OutSystems 11.
    - name: Fix entity warning
      id: fix-unexpected-entity
      description: 'Wants to fix the "Unexpected Entity" warning family in
        Service Studio for OutSystems 11, caused by an entity composed
        solely of an Auto Number attribute, having more than one Binary
        Data attribute, or specifying an invalid physical table name, by
        adding another attribute, moving binary data to a new entity, or
        correcting the table name, so that the entity compiles and
        publishes successfully.'
    - name: Fix table name changed
      id: fix-table-name-changed
      description: 'Wants to fix the "Entity physical table name was set to..."
        warning in Service Studio for OutSystems 11, caused by the entity''s
        physical table name colliding with an existing external table name in
        the database, by acknowledging Service Studio''s automatic rename to
        a non-conflicting name, so that the entity compiles and publishes
        successfully without a database naming clash.'
    - name: Fix data type warning
      id: fix-unexpected-data-type-warning
      description: 'Wants to fix the "Unexpected Data Type" warning family in
        Service Studio for OutSystems 11, caused by comparing Entity
        identifiers of different data types, using an incorrect data type in
        an expression or Built-in function, or referencing a Static Entity
        Record with the wrong attribute data type, by matching the data types
        involved or setting the correct <Static Entity Name> Identifier type,
        so that the expression or entity compiles and publishes successfully.'
    - name: Fix database warning
      id: fix-database-type-warning
      description: 'Wants to fix the "module ''Database'' property (<database>)
        does not match Platform Server database type (<database>)" warning in
        Service Studio for OutSystems 11, caused by the module''s Database
        property not matching the server''s configured database type, by
        setting the property to the correct database type or "(Both)" and
        validating that SQL queries are compliant with the chosen database,
        so that the module compiles without the mismatch warning.'
    - name: Fix unused attribute
      id: fix-unused-attribute
      description: 'Wants to fix the "Consider deleting the value <value>. It''s
        never used because it refers to the missing attribute <attribute>."
        warning in Service Studio for OutSystems 11, caused by a value being
        set for an attribute that no longer exists on the entity, by deleting
        the unused value entry and verifying the attribute deletion was
        intended, so that the module compiles and publishes successfully.'
  - name: Data relationships
    id: data-relationships
    subtopics:
    - name: One-to-many relationship
      id: one-to-many-relationship
      description: Wants to create a one-to-many relationship between two entities in
        Service Studio, with one entity as the master and the other as the
        detail entity, so that each detail record belongs to one master record
        and each master record can have many detail records.
    - name: One-to-one relationship
      id: one-to-one-relationship
      description: Wants to create a one-to-one relationship between two entities in
        Service Studio, with the dependent entity holding the foreign key, so
        that each record has exactly one related record.
    - name: Many-to-many relationship
      id: many-to-many-relationship
      description: Wants to create a many-to-many relationship between two entities in
        Service Studio by introducing a junction entity with the necessary
        foreign keys and any extra attributes, so that each record can relate to
        multiple records on both sides.
  - name: Querying data
    id: querying-data
    subtopics:
    - name: Query fundamentals
      id: query-fundamentals
      description: Wants to understand what a query is and the ways it can be used
        to work with data (retrieving results, performing calculations, combining
        data from different entities, and adding, deleting, or changing data), so
        they can choose the right query approach for a data task in OutSystems.
    - name: Joins
      id: joins
    - name: Fix cross join warning
      id: fix-cross-join-warning
      description: Wants to fix the Service Studio scalability suggestion
        warning that an aggregate is being calculated as a cross join for
        each entity, by adding the missing join condition between the
        entities in the aggregate, so that the query performs an inner or
        outer join instead of unnecessarily evaluating every combination.
    - name: Filter data
      id: filter-data
    - name: Multi-entity aggregate
      id: multi-entity-aggregate
      description: Wants to create an aggregate that joins multiple entities and applies
        filters for a specific input parameter, so that a screen can display related
        records from several data sources in a single query.
    - name: Master-detail aggregates
      id: master-detail-aggregates
      description: Wants to implement a master-detail pattern in a Screen using two
        Aggregates with different Fetch properties (At start and Only on demand),
        so that a list loads immediately while detail data loads only when needed.
    - name: Trigger aggregate refresh
      id: trigger-aggregate-refresh
      description: Wants to trigger an on-demand Aggregate to refresh and update
        the UI asynchronously, by using an Assign node to set a filter parameter
        followed by a Refresh Data node inside a client action triggered by a UI
        event, so that the Screen displays updated data without a full page reload.
    - name: Refresh Data properties
      id: refresh-data-properties-reference
      description: Wants to look up the properties of the Refresh Data node and understand
        that it runs synchronously, blocking the execution flow until the data
        source is refreshed, in OutSystems 11 Service Studio, so they can
        correctly configure a Refresh Data action to update a widget's data on
        demand.
    - name: Chain aggregate fetches
      id: chain-aggregate-fetches
      description: Wants to sequence multiple On Demand aggregates so each fetches
        only after the aggregate it depends on has data, using the On After Fetch
        event on each aggregate, so that dependent dropdowns and fields don't try
        to filter using data that isn't loaded yet when a screen first opens.
    - name: Optional aggregate filter
      id: optional-aggregate-filter
      description: Wants to add an optional filter condition to an aggregate using
        a NullIdentifier() comparison against a screen variable, in Service Studio
        for OutSystems 11, so that filtering is skipped when no value is selected,
        in traditional web apps, mobile apps, and reactive web apps.
    - name: Fix invalid aggregate
      id: fix-invalid-aggregate
      description: 'Wants to fix the "Invalid Aggregate" error in Service Studio
        for OutSystems 11, caused by joining entities from multiple databases,
        having duplicate join conditions on the same entities, or applying an
        aggregation function to a calculated column with an incompatible data
        type, so that the module compiles and publishes successfully.'
    - name: Fix invalid dependency
      id: fix-invalid-dependency
      description: 'Wants to fix the "Invalid Data Dependency" error in Service
        Studio for OutSystems 11, caused by conflicting Fetch property values
        between dependent data sources, a data source depending on more than
        one other data source, or a circular reference between data sources,
        by adjusting Fetch properties, merging or referencing data via a
        client variable, or removing the circular dependency, so that the
        module compiles and publishes successfully.'
    - name: Fix maintenance warning
      id: fix-maintenance-suggestion-warning
      description: Wants to fix the "Complete enumeration of <entity> attributes
        found" maintenance suggestion warning in Service Studio, caused by a
        SQL query selecting all attributes individually instead of using the
        entity wildcard, by replacing the attribute list with <entity>.*, so
        that the query is easier to maintain, in OutSystems 11.
    - name: Fix unused query warning
      id: fix-unused-query-warning
      description: Wants to remove an unused Aggregate or SQL query that is not
        referenced anywhere in the app, so the app does not run unnecessary
        queries and the codebase stays easier to maintain.
  - name: Aggregates
    id: aggregates
    subtopics:
    - name: Create aggregate
      id: create-aggregate
      description: Wants to create an aggregate to fetch data from the database
        using the Search field or drag-and-drop entity placement in the
        Aggregate editor, so a screen can display the records it needs.
    - name: Sort aggregate data
      id: sort-aggregate-data
      description: Wants to sort an Aggregate's returned records in ascending
        or descending order using the Sorting tab's Add Sort feature, in
        Service Studio for OutSystems 11, so they can retrieve information
          such as the first or last record when data is ordered alphabetically.
    - name: Aggregate outputs
      id: aggregate-outputs
      description: Wants to understand the records list returned by an
        Aggregate, including the Current and CurrentRowNumber iterator
        values, the Length and Empty properties, and the Count output, so
        they can use aggregate results correctly in screen logic.
    - name: Aggregate properties
      id: aggregate-properties
      description: Wants to understand the Aggregate properties that control
        returned data, including Max. Records and the read-only Executed SQL
        property, so they can limit results and inspect the generated SQL
        without affecting Count.
    - name: Aggregate SQL translation
      id: aggregate-sql-translation
      description: Wants to understand how an Aggregate is translated into
        SQL during compilation, including how Sources, Filters, Sorting,
        and Test values map to SELECT, FROM, WHERE, and ORDER BY clauses, so
        they can reason about the generated query.
  - name: Sharing Data defined in OutSystems
    id: share-data-outsystems
    subtopics:
    - name: Allow other apps to change data
      id: allow-apps-edit-data
    - name: Allow other apps to read data
      id: allow-apps-read-data
  - name: Exporting data
    id: exporting-data
    subtopics:
    - name: Export data to Excel
      id: export-data-to-excel
      description: Wants to export the data of an entity to an Excel worksheet
        using the Record List To Excel action and enable users to download the
        generated file, in OutSystems 11 Reactive Web and Traditional Web apps,
        so that entity records can be shared as an Excel file.
    - name: Extract PE data via REST API
      id: extract-pe-data-rest-api
      description: Wants to extract data from an OutSystems 11 Personal Environment
        when direct access to the front-end or database servers is unavailable,
        by exposing a REST API, so they can retrieve their data through a
        supported API-based mechanism.
    - name: Extract PE data via Excel download
      id: extract-pe-data-excel-download
      description: Wants to extract data from an OutSystems 11 Personal Environment
        when direct access to the front-end or database servers is unavailable,
        by generating an Excel download, so they can retrieve their data
        through a supported file-based mechanism.
- name: API testing
  guid: 195d823e-8f16-4d32-a66d-7766c6bdd032
  topics:
  - name: Fundamentals of API testing strategies
    id: api-testing-strategy
    subtopics:
    - name: API testing strategies
      id: api-testing-strategies
  - name: Manual API testing
    id: manual-api-testing
    subtopics:
    - name: Manual API tests
      id: manual-api-tests
  - name: Automated API testing
    id: automated-api-testing
    subtopics:
    - name: Automated API tests
      id: automated-api-tests
      description: Wants to design automated API integration tests in OutSystems,
        using syntax testing and multi-step test scenarios and structuring them
        as reusable, data-driven test blocks, so that the test suite stays
        maintainable and scalable as APIs evolve.
- name: Access Control and Privileges
  guid: ccc4b848-0f34-4854-bdc3-76e15fd4f42c
  topics:
  - name: Case Management Access Control
    id: case-management-access-control
    subtopics:
    - name: CMF access control
      id: cmf-access-control
      description: Wants to understand what access control is in the Case Management
        Framework in OutSystems 11, including what it restricts, how the default-deny
        behavior works when enabled, and what read and write permissions mean, so
        that they can decide whether and how to apply it to their case app.
    - name: Grant case definition access
      id: grant-case-definition-access
      description: Wants to grant or revoke access to a case definition for a user
        or group in OutSystems 11 using the Case Management Framework server actions,
        so that all existing and future case instances of that definition are accessible
        to the right users.
    - name: Grant case instance access
      id: grant-case-instance-access
      description: Wants to grant access to a specific case instance for a user or
        group in OutSystems 11 using the Case Management Framework server actions
        (Case_GrantAccessToUser / Case_GrantAccessToGroup), so that only the intended
        users or groups can access that individual case instance.
    - name: CMF login requirement
      id: cmf-login-requirement
      description: Wants to resolve the OS-CMFR-GEN-20001 error ("It looks like
        you don't have access. Make sure you're logged in with a registered
        user."), caused by calling a CaseServices_API Get service action
        without an active logged-in user session, by ensuring the user logs in
        before executing these actions, so that the Case Management Framework
        can successfully execute the action.
    - name: CMF access denied
      id: cmf-access-denied
      description: 'Wants to resolve the OS-CMFR-GEN-20002 error ("You need
        <write/read> access to the Case to perform the <action-name> action."),
        caused by the Case Management Framework''s access control being enabled
        while the user lacks the required permission level for the action, by
        granting the user or group access via CaseDefinition_GrantAccessToUser,
        CaseDefinition_GrantAccessToGroup, Case_GrantAccessToUser, or
        Case_GrantAccessToGroup, so that the action can be executed
        successfully.'
    - name: Fix sample user login
      id: fix-sample-user-login
      description: 'Wants to resolve the OS-CMFR-GEN-20006 error ("Couldn''t
        log in. Make sure you''re accessing as a sample user."), caused by
        passing a non-sample user identifier to the SampleUser_Login action,
        by ensuring the UserId input parameter belongs to a valid sample
        user, verified with Service Studio''s Debugger, in OutSystems 11, so
        that the Case Management Framework can successfully execute the
        action.'
    - name: Fix missing access grant
      id: fix-missing-access-grant
      description: 'Wants to fix the "There''s no access control for that
        <user/group>/<case/case definition> pairing." error by verifying the
        user/group and case/case definition identifier values passed to the
        Case Management Framework''s access-revoking or access-updating
        action using Service Studio''s Debugger, in OutSystems 11, so that
        the action targets an access control record that actually exists.'
    - name: Fix UserId GroupId
      id: fix-userid-groupid-conflict
      description: 'Wants to resolve the "It''s not possible to fill in both
        UserId and GroupId attributes." error, caused by passing values for
        both the UserId and GroupId inputs to a Case Management Framework
        internal access-control action instead of the correct CaseServices_API
        action, by using the correct CaseServices_API action, so that Case
        Management Framework access control functions correctly.'
- name: Accessibility
  guid: d4c7eefa-c2fc-445e-81d4-270bbf3ad604
  topics:
  - name: Design for accessibility
    id: design-for-accessibility
  - name: Implement accessibility features (screen readers, contrast...)
    id: accessibility-screen-reader
    subtopics:
    - name: Generate HTML tags
      id: generate-html-tags
      description: Wants to generate semantic HTML tags (h1, nav, fieldset,
        etc.) around UI elements for accessibility, using the HTML Element
        widget's Tag property in Mobile/Reactive Web Apps or the OSTagName
        Extended Property on Container/Placeholder widgets in Traditional Web
        Apps, in Service Studio for O11, so the generated markup uses proper
        semantic tags instead of generic span/div elements.
    - name: Accessible master detail
      id: accessible-master-detail
      description: Wants to make the Master Detail UI Pattern accessible to
        assistive technologies, by mapping the list and detail content to
        ARIA tabs roles (tablist/tab/tabpanel), aria-controls,
        aria-labelledby, and tabindex attributes in Service Studio, so that
        screen readers announce the relationship between selectable items
        and the detail panel and keyboard users can navigate predictably, in
        mobile apps and reactive web apps (O11).
    - name: Accessible Alert pattern
      id: accessible-alert-pattern
      description: Wants to make the Alert UI Pattern accessible to assistive
        technologies, by understanding its built-in ARIA role behavior (alert
        for Error/Warning, status for Success/Info), changing the role with
        the SetAccessibilityRole action, and toggling the aria-hidden
        attribute with SetAriaHidden when showing or hiding the alert, in
        Service Studio, so that the pattern meets WCAG 2.2 AA accessibility
        requirements, in mobile apps and reactive web apps (O11).
    - name: Accessible Notification pattern
      id: accessible-notification-pattern
      description: Wants to make the Notification UI Pattern accessible to
        assistive technologies, by choosing between the alertdialog role
        (for interactive notifications with buttons) and the alert role
        with aria-live="assertive" (for non-interactive status messages),
        and applying the correct ARIA attributes via a JavaScript node in
        the Initialized client action when needed, in Service Studio, so
        that the pattern meets WCAG 2.2 AA accessibility requirements, in
        mobile apps and reactive web apps (O11).
    - name: Table sort accessibility
      id: table-sort-accessibility
      description: Wants to make Table widget sorting accessible to screen readers
        by enabling the WCAG 2.2 aria-sort attribute via Factory Configuration, and
        to declare a non-default initial sort direction using the
        default-sort-direction Extended Property on a Header Cell, in OutSystems 11,
        so custom OnSort actions report the correct sort state.
    - name: Accessible Card Background pattern
      id: accessible-card-background-pattern
      description: Wants to improve text contrast on the Card Background UI
        Pattern by darkening the gradient mask applied to the lower half of
        the card via custom theme CSS, in Service Studio, so that overlaid
        text meets WCAG 2.2 AA contrast requirements (4.5:1 for normal text,
        3:1 for large text) without altering the app's color palette, in
        mobile apps and reactive web apps (O11).
    - name: Accessible Tag pattern
      id: accessible-tag-pattern
      description: Wants to fix low-contrast color combinations on the Tag UI Pattern by
        identifying the theme's low-contrast OutSystems UI colors and updating
        the theme's CSS color variables in Service Studio, so that text and
        background combinations meet WCAG 2.2 AA contrast requirements (4.5:1
        for normal text, 3:1 for large text) without altering the app's color
        palette, in mobile apps and reactive web apps (O11).
    - name: Accessible Badge pattern
      id: accessible-badge-pattern
      description: Wants to fix low-contrast color combinations on the Badge UI
        Pattern by identifying the theme's low-contrast OutSystems UI colors
        and updating the theme's CSS color variables in Service Studio, so
        that text and background combinations meet WCAG 2.2 AA contrast
        requirements (4.5:1 for normal text, 3:1 for large text) without
        altering the app's color palette, in mobile apps and reactive web
        apps (O11).
    - name: Accessible Animated Label pattern
      id: accessible-animated-label-pattern
      description: Wants to make the Animated Label UI Pattern accessible to
        assistive technologies, by associating the Input widget with its
        Label widget via the Label Properties' Input Widget dropdown, in
        Service Studio, so that screen readers correctly announce the
        input's purpose, meeting WCAG 2.2 AA accessibility requirements, in
        mobile apps and reactive web apps (O11).
    - name: Accessible Lightbox Image pattern
      id: accessible-lightbox-image-pattern
      description: Wants to make the Lightbox Image UI Pattern accessible to
        assistive technologies, by providing a meaningful aria-label or alt
        attribute for each image displayed within the lightbox dialog, in
        Service Studio, so that screen readers announce an accessible name
        for each image, meeting WCAG 2.2 AA Non-text Content (1.1.1) and
        Name, Role, Value (4.1.2) requirements, in mobile apps and reactive
        web apps (O11).
    - name: Accessible Carousel
      id: accessible-carousel-pattern
      description: Wants to make the Carousel UI Pattern accessible to
        assistive technologies by wrapping static content, images, and OutSystems
        UI components in Containers, adding meaningful aria-label values to slide
        containers, and keeping Image alt text on each image, so that screen
        readers announce Carousel slides correctly in mobile apps and reactive
        web apps (O11).
    - name: Carousel WCAG fix
      id: carousel-wcag-fix
      description: Wants to update the Carousel UI Pattern for OutSystems UI by increasing
        the pagination control target size, enclosing direct Image widgets in
        Containers to prevent incorrect roles, and assigning list and listitem
        roles to carousel lists in an OnReady client action, so that the pattern
        meets WCAG 2.2 AA accessibility requirements in mobile apps and reactive
        web apps (O11).
    - name: Accessible Range Slider Interval
      id: accessible-range-slider-interval-pattern
      description: Wants to make the Range Slider Interval UI Pattern
        accessible to assistive technologies, by adding a JavaScript node
        to the OnReady client action that assigns distinct aria-label
        attributes ("Lower value handle" and "Upper value handle") to each
        .noUi-handle element, in Service Studio, so that screen readers can
        distinguish between the lower and upper handles, meeting WCAG 2.2
        AA accessibility requirements, in mobile apps and reactive web apps
        (O11).
    - name: Accessible Submenu pattern
      id: accessible-submenu-pattern
      description: Wants to increase the pointer target size of Submenu UI
        Pattern headers, by adding a custom CSS rule (.osui-submenu__header)
        that sets a minimum height and width of 24px in the theme's style
        sheet, in Service Studio, so that the pattern meets WCAG 2.2 AA
        accessibility requirements for touch and pointer target size, in
        mobile apps and reactive web apps (O11).
    - name: Accessible Breadcrumbs pattern
      id: accessible-breadcrumbs-pattern
      description: Wants to confirm the Breadcrumbs UI Pattern remains
        accessible by relying on its default WCAG 2.2 AA-compliant behavior
        and validating any custom changes in Service Studio for O11, so
        that customized breadcrumbs continue to meet accessibility
        requirements in mobile apps and reactive web apps.
    - name: Accessible Wizard pattern
      id: accessible-wizard-pattern
      description: Wants to make the Wizard UI Pattern accessible to
        assistive technologies, by removing redundant aria-label attributes
        and hiding the icon wrapper from screen readers, assigning the
        tabpanel role to each Wizard item when steps navigate between
        panels, or setting aria-current="step" on the active item when the
        Wizard is used as a progress tracker, via a JavaScript node in the
        OnReady client action, in Service Studio, so that the pattern meets
        WCAG 2.2 AA accessibility requirements, in mobile apps and reactive
        web apps (O11).
    - name: Accessible Counter pattern
      id: accessible-counter-pattern
      description: Wants to fix low-contrast color combinations on the Counter
        UI Pattern by identifying the theme's low-contrast OutSystems UI
        colors and updating the theme's CSS color variables in Service
        Studio, so that text and background combinations meet WCAG 2.2 AA
        contrast requirements (4.5:1 for normal text, 3:1 for large text)
        without altering the app's color palette, in mobile apps and
        reactive web apps (O11).
    - name: Accessible Progress Circle pattern
      id: accessible-progress-circle-pattern
      description: Wants to make the Progress Circle UI Pattern accessible to
        assistive technologies, by hiding the decorative circle with
        aria-hidden="true" on an enclosing container and adding a
        descriptive text alternative via an Expression widget styled with
        the wcag-hide-text CSS class, in Service Studio, so that screen
        readers announce accurate progress feedback, meeting WCAG 2.2 AA
        accessibility requirements, in mobile apps and reactive web apps
        (O11).
    - name: Accessible Accordion
      id: accessible-accordion-pattern
      description: Wants to make the Accordion UI Pattern accessible to
        assistive technologies when used with a List widget, by adding a
        client action with a JavaScript node that assigns list and
        listitem roles to the Accordion Item's list element via the
        AccordionInitialized event, in Service Studio, so that screen
        readers correctly announce list structure, meeting WCAG 2.2 AA
        accessibility requirements, for OutSystems UI versions earlier
        than 2.29.0, in mobile apps and reactive web apps (O11).
    - name: Accessible Blank Slate pattern
      id: accessible-blank-slate-pattern
      description: Wants to make the Blank Slate UI Pattern accessible to
        assistive technologies, by setting the correct ARIA role (region
        for a static empty state, status for one that updates
        dynamically) via a JavaScript node in the OnReady action, in
        Service Studio, so that screen readers announce the pattern
        correctly, meeting WCAG 2.2 AA accessibility requirements, for
        OutSystems UI versions earlier than 2.29.0, in mobile apps and
        reactive web apps (O11).
    - name: Accessible User Avatar
      id: accessible-user-avatar-pattern
      description: Wants to make the User Avatar UI Pattern accessible to
        assistive technologies, by adding a JavaScript node in the OnReady
        client action to set an informative or empty alt attribute
        depending on context, in Service Studio, so that the pattern
        meets WCAG 2.2 AA accessibility requirements, for OutSystems UI
        versions earlier than 2.29.0, in mobile apps and reactive web
        apps (O11).
    - name: Accessible User Avatar contrast
      id: accessible-user-avatar-contrast
      description: Wants to fix low-contrast color combinations on the
        User Avatar UI Pattern by identifying the theme's low-contrast
        OutSystems UI colors and updating the theme's CSS color
        variables in Service Studio, so that text and background
        combinations meet WCAG 2.2 AA contrast requirements (4.5:1 for
        normal text, 3:1 for large text) without altering the app's
        color palette, in mobile apps and reactive web apps (O11).
    - name: Accessible Tooltip pattern
      id: accessible-tooltip-pattern
      description: Wants to make the Tooltip UI Pattern accessible to
        assistive technologies, by configuring the Trigger property to
        OnClick in Service Studio, so that the Tooltip opens automatically
        on keyboard focus and closes with Esc, meeting WCAG 2.2 AA
        accessibility requirements, in mobile apps and reactive web apps
        (O11).
    - name: Dropdown Tags NVDA behavior
      id: dropdown-tags-nvda-esc
      description: Wants to understand why NVDA screen reader users need to
        press Esc twice to close the Dropdown Tags dropdown, since the first
        Esc exits focus mode and the second collapses the control, so that
        they can confirm this is expected behavior rather than a bug and
        explain it to end users, in mobile apps and reactive web apps
        (O11).
    - name: Accessible Action Sheet pattern
      id: accessible-action-sheet-pattern
      description: Wants to make the Action Sheet UI Pattern accessible to
        assistive technologies, by setting aria-expanded, aria-controls,
        and aria-haspopup on the trigger button and removing the redundant
        aria-expanded attribute from the Action Sheet container via a
        JavaScript node in the OnRender client action, in Service Studio,
        so that the pattern meets WCAG 2.2 AA accessibility requirements,
        for OutSystems UI versions earlier than 2.29.0, in mobile apps and
        reactive web apps (O11).
    - name: Accessible Tabs pattern
      id: accessible-tabs-pattern
      description: Wants to make the Tabs UI Pattern accessible to assistive
        technologies, by moving the tabpanel role and related ARIA
        attributes from the article element to an inner container via
        JavaScript nodes added to the OnReady and OnDestroy client actions,
        in Service Studio, so that screen readers correctly associate tabs
        with their panels, for OutSystems UI versions earlier than 2.29.0,
        in mobile apps and reactive web apps (O11).
    - name: Accessible Inline SVG pattern
      id: accessible-inline-svg-pattern
      description: Wants to make the Inline SVG UI Pattern accessible to
        assistive technologies, by adding aria-hidden and focusable
        attributes for decorative SVGs, a role and aria-label for
        informative SVGs, or a role, tabindex, aria-label, and keyboard
        event handlers for interactive SVGs, via a JavaScript node in the
        Initialized client action, in Service Studio, so that the pattern
        meets WCAG 2.2 AA accessibility requirements, in mobile apps and
        reactive web apps (O11).
  - name: Translating apps
    id: translating-apps
    subtopics:
    - name: Migrate Multilingual
      id: migrate-multilingual
      description: Wants to migrate translations, clean up client-action logic
        (GetLocale, SetLocale, GetTranslation, AddTranslations,
        AddTranslationsFromResource), and remove the module dependency from
        the deprecated Multilingual Forge component to the built-in
        multilingual feature, in Service Studio for OutSystems 11, so that
        the app's translations and logic keep working without the
        unsupported Forge component.
    - name: Enable RTL support
      id: enable-rtl-support
      description: Wants to enable right-to-left layout in an OutSystems 11 app by
        setting the locale to an RTL language such as Arabic, so that the
        platform automatically applies the is-rtl class and renders the UI
        right to left without manual CSS injection.
    - name: Fix RTL display
      id: fix-rtl-display
      description: Wants to fix right-to-left (RTL) languages displaying left-to-right in
        apps, by creating a client action that adds or removes the CSS is-rtl
        class based on the current locale and calling it from the OnRender
        action of the Menu block used on all screens, in Service Studio for
        OutSystems 11, until the official fix is applied.
    - name: Use IsRTL action
      id: use-isrtl-action
      description: Wants to use the IsRTL action to branch screen logic based on
        whether RTL is applied, including filling input parameters,
        controlling container visibility, and driving If conditions and
        client actions, so that the app adapts its content when the UI
        switches between left-to-right and right-to-left.
- name: Advanced app architecture
  guid: d59b4004-582f-44ee-a1ae-eb75787ba1e0
  topics:
  - name: SOA and Microservices
    id: soa-and-microservices
  - name: Distributed transactions
    id: distributed-transactions
    description: Wants to manage distributed transactions in ODC apps by
      combining service actions and events while keeping state consistent
      after failures and retries.
    subtopics:
    - name: Choreography tradeoffs
      id: choreography-tradeoffs
      description: Wants to compare choreography with orchestration for
        distributed transactions so they can keep the process centralized,
        keep apps loosely coupled, and make testing and troubleshooting
        easier.
    - name: Retry compensation
      id: retry-compensation
      description: Wants to handle distributed transactions with service
        actions so they can retry transient failures, compensate
        unrecoverable errors, and restore a consistent state across
        concurrent transactions.
    - name: Event compensation
      id: event-compensation
      description: Wants to handle distributed transactions with events so
        they can track transaction state, emit compensation events, and
        recover consistent state after failure callbacks or exception
        flows.
  - name: ODC Events
    id: odc-events
    subtopics:
    - name: ODC event limits
      id: odc-event-limits
      description: Wants to look up the platform limits for ODC events — maximum payload
        size per event, maximum queue capacity per event type, delivery retry
        count and backoff intervals, and concurrent execution/duration limits —
        so that they can design event payloads, queues, and consumer logic
        within ODC's supported constraints.
  - name: ODC cloud architecture
    id: odc-cloud-architecture
    description: Wants to understand how OutSystems Developer Cloud (ODC) separates
      its development platform and app runtime into independent, multi-tenant
      Kubernetes clusters that auto-scale based on CPU and RAM usage, so that
      they can reason about how apps are built, deployed, and run in ODC.
  - name: ODC network architecture
    id: odc-network-architecture
    description: Wants to understand OutSystems Developer Cloud (ODC)'s network
      architecture and security components, including the CDN, WAF, Identity
      Service, Load Balancer, TLS encryption, and network isolation between
      customers, so that they can design and secure Platform and Runtime apps
      accordingly
  - name: ODC self-hosted architecture
    id: odc-self-hosted-architecture
    description: Wants to understand the ODC self-hosted deployment model,
      including why customers use it, how cloud and self-hosted
      responsibilities split, and what runtime, connectivity, monitoring,
      and infrastructure prerequisites are required, so that they can
      evaluate and plan a self-hosted deployment.
    subtopics:
    - name: Install flow overview
      id: install-flow-overview
      description: Wants to understand the end-to-end process for installing a
        self-hosted ODC stage, including infrastructure provisioning,
        database setup, custom registry preparation, configurator
        installation, inbound traffic setup, and final portal configuration,
        so that they can plan and follow the installation in the correct
        order.
  - name: Data Fabric data handling
    id: data-fabric-data-handling
    description: Wants to understand how Data Fabric handles customer data
      when connecting to external systems in ODC, including what's kept only
      in memory versus cached in NoSQL storage, how connection secrets are
      encrypted and never stored in clear text, the encrypted channels used
      for data in transit, and what's deleted when a connection or environment
      is removed, so that they can assess data-handling and security
      practices for external system integrations.
  - name: Trigger ODC event
    id: trigger-odc-event
    description: Wants to create a public event with an input parameter in
      ODC Studio and trigger it from a server action using an IF condition
      and a Trigger Event node, so that the event fires only when a new
      record is created, providing the data a workflow needs to start.
  - name: ODC data platform
    id: odc-data-platform
    description: Wants to understand what the ODC data platform is, which
      categories of platform features send data to it, so that they can
      reason about where their platform data lives and how to access it for
      monitoring and compliance.
  - name: ODC Portal log and metric filtering
    id: odc-portal-log-metric-filtering
    description: Wants to understand how logs and metrics are filtered by
      stage and portfolio in ODC Portal, so that they can access platform
      data for monitoring and compliance.
- name: Advanced data handling
  guid: c6a945c1-deaa-4266-9b94-d7b31d9fdf07
  topics:
  - name: When to use Aggregates vs SQL Nodes
    id: agg-vs-sql
  - name: Stored procedures
    id: stored-procedures
  - name: File data type
    id: file-data-type
    subtopics:
    - name: File vs BinaryData
      id: file-vs-binarydata
      description: Wants to understand why ODC stores File attribute content in
        cloud storage instead of the database, and how the File type differs
        from BinaryData in loading behavior, size limits, and reference
        semantics, so they can choose the right data type for large content in
        reactive web and mobile apps.
    - name: File type properties
      id: file-type-properties
      description: Wants to look up the File data type's properties and restrictions,
        including its maximum size and where it isn't supported, so they can
        correctly configure File attributes in ODC Studio.
    - name: Display cloud images
      id: display-cloud-images
      description: Wants to bind a File-typed entity attribute to the Image
        widget (Type = File or Binary Data) in ODC Studio, understanding that
        the File must already be saved to the entity before binding and that
        each image loads asynchronously via its own request after the
        aggregate or data action completes, so that images stored in cloud
        storage render correctly on screen.
    - name: Configure image caching
      id: configure-image-caching
      description: Wants to control caching for images bound to File values
        in ODC, understanding the default 'private, no-store' behavior and
        how to enable CDN-level caching by generating a signed URL via
        GetFileSignedURL and setting ResponseHeaders.CacheControl, so that
        images can be cached appropriately without exposing user-scoped
        content publicly.
    - name: Scoped image URLs
      id: scoped-image-urls
      description: Wants to understand that a File image's generated URL is
        tied to the requesting user's session, so that they avoid sharing or
        reusing image URLs across sessions or users, since such URLs fail to
        load outside the original session.
    - name: Use Download node
      id: use-download-node
      description: Wants to add the Download node to a screen action in ODC
        Studio to let users download a File-typed variable directly from
        cloud storage to their device, understanding it works for any file
        size without routing through the app server, doesn't require
        authentication (unlike uploads), and is only available in screen
        actions (not client or server actions), so that they can implement
        file downloads correctly in reactive web and mobile apps.
    - name: File data preview
      id: file-data-preview
      description: Wants to understand how ODC Studio previews File attribute
        values as MIMEType metadata (not content) in Edit Data, Advanced SQL
        test queries, the debugger's variable inspector, and the data type
        picker, so they can correctly interpret File data during
        development, testing, and debugging.
    - name: Download content type behavior
      id: download-content-type-behavior
      description: Wants to understand that the browser decides whether to
        open a downloaded file in-browser (e.g. PDF viewer) or save it to
        disk based on the file's MIMEType, set at upload time and not
        controlled by ODC, so they can predict download behavior for files
        handled by the Download node.
    - name: File reference lifecycle
      id: file-reference-lifecycle
      description: Wants to understand how a File attribute's reference
        moves through Uploaded, Active, and Pending deletion states as
        entity records are created, updated, and deleted, and how ODC's
        reference counting keeps a shared File value from being deleted
        while other committed records (including via cascade delete)
        still reference it, so they can predict when File content
        actually gets removed.
    - name: Unsaved upload lifecycle
      id: unsaved-upload-lifecycle
      description: Wants to understand how ODC handles a file uploaded through the Upload
        widget before it's saved to an entity attribute, including that aborted
        or interrupted uploads are deleted immediately and unsaved uploads are
        automatically deleted after a fixed retention window, so they can save
        the file reference in time to prevent it being removed.
    - name: File storage operations
      id: file-storage-operations
      description: Wants to understand how File storage behaves
        operationally in production, including that File content follows
        the same disaster-recovery retention as the database, that
        removed or replaced File content still counts against storage
        usage until the retention period ends, and that the platform
        automatically reclaims content from incomplete or rolled-back
        operations, so they can plan storage capacity and DR expectations
        correctly.
    - name: Aggregate File results
      id: aggregate-file-results
      description: Wants to understand that an aggregate returns File
        metadata and a direct cloud-storage download URL instead of
        binary content, so screen logic can use that URL like any other
        string and the app server never handles the file's binary data.
    - name: Generate signed URLs
      id: generate-signed-urls
      description: Wants to generate a time-limited signed URL for a File value using
        GetFileSignedURL or for multiple files using GetMultipleFilesSignedURLs,
        configuring the URLExpiration TTL within a bounded default and maximum
        duration enforced at runtime rather than publish time, so they can grant
        direct, temporary cloud-storage file access without routing bytes
        through the app server.
    - name: Signed URL safe usage
      id: signed-url-safe-usage
      description: Wants to follow safe-usage rules for signed URLs
        (generate on demand, use immediately, never store or reuse,
        keep TTLs short, never share via email or messages) and
        understand that a signed URL is a bearer token nobody's
        identity is checked, so they can avoid leaking file access.
    - name: Handle expired signed URLs on idle screens
      id: expired-signed-url-idle-screens
      description: Wants to handle requests for an expired signed URL
        on a long-idle screen when it fails silently without an ODC
        error code, so they can refresh URLs correctly on long-idle
        screens.
    - name: Use ODC Upload widget
      id: use-upload-widget-odc
      description: Wants to add the Upload widget to a screen in ODC
        Studio, bind its File Content property to a File-typed entity
        attribute, and call CreateOrUpdate in a server action to save
        the uploaded file, understanding that only authenticated users
        can upload (anonymous users see a login-required banner unless
        the screen's Accessible by property requires authentication),
        so they can implement file uploads to cloud storage in ODC
        reactive web and mobile apps.
    - name: ODC upload size limit
      id: odc-upload-size-limit
      description: Wants to resolve the "File with X bytes cannot be uploaded because it is
        above the maximum size of Y bytes" error shown by default when a file
        exceeds the Upload widget's size limit in ODC, by checking Size() on the
        selected File value before the form submits and showing a custom
        message, so end users get a friendlier size-limit warning.
  - name: Aggregating data
    id: agregating-data
    subtopics:
    - name: Dynamic sort setup
      id: dynamic-sort-setup
      description: Wants to implement dynamic sorting on calculated attributes in
        an aggregate that uses external entities, so that the sort works correctly
        at runtime without errors or empty results.
    - name: Create calculated attribute
      id: create-calculated-attribute
      description: Wants to create a calculated attribute in an Aggregate that
        concatenates an employee's last and first names into a FullName value,
        so the result can be used as a sort key and displayed on the
        EmployeeDashboard screen.
    - name: Aggregation functions
      id: aggregation-functions
      description: Wants to understand which aggregation functions are available
        in an Aggregate, what the output contains after applying them, and how
        Group By affects which columns are returned, in OutSystems, so that
        queries return only the expected calculated results.
  - name: SQL
    id: sql
    subtopics:
    - name: Query parameters
      id: query-parameters
    - name: Use SQL
      id: use-sql
    - name: Insert null via SQL
      id: insert-null-via-sql
      description: Wants to insert a null value into a database field when the
        built-in create/update actions default missing values instead (for
        example, writing 0 for an undefined decimal), by using a SQL node with
        Expand Inline and EncodeSql, in OutSystems Service Studio (O11), so
        that null is correctly stored in the database.
    - name: Implement dynamic SQL statements correctly
      id: dynamic-sql-correctly
    - name: SQL operator reference
      id: sql-operator-reference
      description: Wants to know which ANSI-92 operators and functions — comparison,
        logical, arithmetic, string, date/time, conditional, aggregate, and window —
        are supported in SQL queries with external entities in ODC, so they can write
        correct queries without accidentally using unsupported syntax.
    - name: SQL type conversion
      id: sql-type-conversion
      description: Wants to know which type conversion functions (such as CAST,
        DECIMAL_TO_TEXT, TEXT_TO_INTEGER) are available and what behavior to expect
        when converting between data types in SQL queries with external entities in
        ODC, so they can avoid runtime errors and produce correct results.
    - name: SQL NULL behavior
      id: sql-null-behavior
      description: Wants to understand how NULL values behave in SQL operators and
        functions when querying external entities in ODC — including how NULL handling
        differs between push-down and ODC-handled execution in mashup queries — so
        they can write reliable queries that handle NULLs correctly across different
        external systems.
    - name: ANSI-92 syntax basics
      id: ansi-92-syntax-basics
      description: Wants to understand the ANSI-92 SQL syntax rules used in ODC SQL
        nodes — including entity and attribute notation, identifier casing behavior,
        and how to run multiple statements in a single SQL node with transaction
        semantics — so they can write correct queries against external entities without
        syntax errors.
    - name: SQL query execution
      id: sql-query-execution
      description: Wants to understand how ODC parses, normalizes, and executes SQL
        queries against external entities — including push-down behavior for
        single-system queries and performance considerations for mashup queries
        spanning multiple external systems — so they can write efficient and correct
        queries.
    - name: Update SQL statement
      id: update-sql-statement
      description: Wants to update records in an external entity using the ANSI-92
        UPDATE statement in an ODC SQL node — including SET clause syntax, WHERE
        filtering, permissions, return behavior, and data source compatibility —
        so they can correctly modify data in external systems.
    - name: Delete SQL statement
      id: delete-sql-statement
      description: Wants to delete records in an external entity using the ANSI-92
        DELETE statement in an ODC SQL node, including WHERE filtering,
        required permissions, and data source compatibility, so they can
        correctly remove data from external systems.
    - name: Insert/upsert statement
      id: insert-upsert-statement
      description: Wants to create or update records in an external entity using
        the ANSI-92 INSERT or UPSERT statement in an ODC SQL node — including
        sourcing values via VALUES or SELECT, single versus multiple record
        semantics, required permissions, and returned primary key behavior —
        so they can correctly write data to external systems.
    - name: Insert/upsert support
      id: insert-upsert-support
      description: Wants to know which external data sources supported by ODC support
        single-record insert, multiple-record insert, upsert, and insert/upsert
        from a SELECT in an ODC SQL node, so they can determine which operations
        are available before writing a query against a given external system.
    - name: Non-select query setup
      id: non-select-query-setup
      description: Wants to configure an ODC SQL Tool non-SELECT query with an
        Output Entity or Structure and know that DELETE, INSERT, and UPDATE
        statements are supported, so they can run data-changing SQL correctly.
    - name: SQL best practices
      id: sql-best-practices
      description: Wants to follow best practices when writing SQL nodes in ODC
        Studio — using input parameters, limiting the number of records fetched,
        avoiding queries inside cycles, selecting only the required attributes
        instead of the whole entity, and defining an output structure that
        matches the expected result set — so their SQL queries stay secure,
        performant, and maintainable.
    - name: Test Query differences
      id: test-query-runtime-differences
      description: Wants to understand why running Test Query on an Aggregate
        or SQL node in ODC Studio can produce different results than the
        published app at runtime, because lower record volumes in
        Development skew cost-based execution-plan estimates and Test
        Query runs through a separate execution path from the app, so
        they know when to also validate a query in the target environment
        with representative data.
    - name: SQL test query commits
      id: sql-node-test-query-commits
      description: Wants to understand that for SQL nodes querying external
        entities, INSERT, UPDATE, DELETE, and CALL statements commit
        immediately when running Test Query in ODC Studio, so they can
        avoid unintended data changes while validating a SQL node's logic.
  - name: Compound data
    id: compound-data
  - name: Binary data actions
    id: binary-data-actions
    description: Wants to know which actions the BinaryData library provides in ODC,
      including their inputs, outputs, and supported encodings, so that they can
      convert and manipulate binary content correctly in logic flows.
  - name: Zip library actions
    id: zip-library-actions
    description: Wants to know which actions the Zip library provides in ODC,
      including their inputs, outputs, and the required action order for creating,
      updating, and reading Zip archives in logic flows, so that they can manage
      Zip files correctly.
  - name: Text dictionary actions
    id: text-dictionary-actions
    description: Wants to know which actions the TextDictionary library provides in ODC,
      including how to create and manage dictionaries and their keys and values,
      so that they can manage text dictionaries in logic flows.
  - name: JSON Deserialize action
    id: json-deserialize-action
    description: Wants to look up the configurable properties, such as JSON String,
      Data Type, and Date Format, and the runtime Data output of the JSON
      Deserialize action, in Service Studio for OutSystems 11, so that a JSON
      string can be converted into an Entity, Static Entity, Structure, or List
      record.
  - name: JSON Serialize action
    id: json-serialize-action
    description: Wants to look up the configurable properties, such as Data, Serialize
      Default Values, and Date Format, and the runtime JSON output of the JSON
      Serialize action, and understand how enabling Serialize Default Values
      affects the JSON equivalents produced for empty values of certain data
      types, in Service Studio for OutSystems 11, so that a Record or List of
      Records can be correctly converted into a JSON string.
  - name: JSON Serialize large numbers
    id: json-serialize-large-numbers
    description: Wants to understand why serializing or deserializing a Long
      Integer or Decimal value larger than JavaScript's MAX_SAFE_INTEGER
      (9007199254740991) fails when the operation runs on the client side, and
      how running the operation in a Server Action avoids the issue, in
      OutSystems 11, so that large numeric values can be serialized or
      deserialized without errors.
  - name: Dealing with rollbacks
    id: dealing-with-rollbacks
    subtopics:
    - name: Force update conflict
      id: force-update-conflict
      description: Wants to let a user override a detected concurrent-update
        conflict and force-save their changes after being warned, in OutSystems 11,
        so that data isn't silently lost but the user can still choose to overwrite
        when confident their information is current.
    - name: Detect update conflicts
      id: detect-update-conflicts
      description: Wants to implement a pessimistic concurrency strategy that
        detects concurrent update conflicts before saving a record, using
        GetForUpdate to lock the record and comparing audit attributes (such as
        UpdatedOn) between the loaded and current record, in OutSystems 11, so
        that users don't unknowingly overwrite each other's changes.
    - name: Isolation level basics
      id: isolation-level-basics
      description: Wants to understand which operations start and end a database
        transaction in ODC (write operations like Create, Update, Delete, GetForUpdate;
        and Commit, Rollback, or end of Request), and how the Read Committed isolation
        level prevents dirty reads by hiding uncommitted changes from concurrent
        transactions, so that they can reason about data visibility during concurrent
        database operations.
- name: Advanced data modeling
  guid: 9a38cf20-a3dc-491a-8a25-c261c830e9c7
  topics:
  - name: Data performance, caching
    id: data-performance-caching
    subtopics:
    - name: Invalidate app cache
      id: invalidate-app-cache
      description: Wants to invalidate server-side caches (from Aggregates,
        Advanced SQL queries, and server actions using Cache in Minutes) for
        the calling application across all replicas in ODC, using the
        ApplicationInvalidateCache system action, so that stale cached data
        doesn't linger after underlying data changes.
    - name: Connection pool usage
      id: connection-pool-usage
      description: Wants to understand when and how to use connection pooling when
        connecting to external databases (SQL Server, Oracle, PostgreSQL, DB2, MySQL)
        via extensions in OutSystems 11 — including connections from OutSystems
        extensions back to the OutSystems database — so that frequent connections do
        not degrade application performance as load scales up.
    - name: Optimize data transfer
      id: optimize-data-transfer
      description: Wants to understand how OutSystems 11 optimizes server-to-client
        data transfer for Screen Aggregates, Data Actions, and Server Actions used
        in client-side Screen Actions logic in Mobile Apps and Reactive Web Apps,
        so that they know when and why the platform automatically reduces the data
        sent to the client.
    - name: Database query slowdown
      id: database-query-slowdown
      description: Wants to diagnose and fix database queries that slow down after
        deleting or updating a significant number of rows, by checking and updating
        outdated table statistics and checking and defragmenting the table in
        Oracle or SQL Server, in OutSystems 11, so that the query optimizer builds
        accurate access plans and performance recovers.
    - name: Get AWR/ADDM reports
      id: get-awr-addm-reports
      description: Wants to generate and interpret Oracle AWR (Automatic Workload
        Repository) and ADDM (Automatic Database Diagnostic Monitor) reports for
        an OutSystems 11 Oracle-backed platform database, using SQL*Plus commands
        with begin/end snapshot IDs, including ADDM automated diagnostics and
        recommendations, so that they can identify the root cause of database
        performance problems such as CPU statistics, wait time statistics, and
        workload profile issues.
  - name: Indexes
    id: indexes
    subtopics:
    - name: Index FK reference attribute
      id: index-fk-reference-attribute
      description: Wants to fix the Service Studio scalability suggestion
        recommending an index on an entity's Foreign Key reference
        attribute, by creating that index when the attribute's Delete Rule
        is set to Protect or Delete, so that the OutSystems 11 app avoids
        the performance cost of an unindexed reference lookup.
    - name: Choose entity indexes
      id: entity-index-selection
      description: Wants to identify which entity attributes to index based
        on query conditions, join usage, and data sparsity, in OutSystems
        11, so that query performance improves without excessively slowing
        down inserts and updates.
  - name: Enforcing data integrity
    id: enforcing-data-integrity
    subtopics:
    - name: Fix foreign key data
      id: fix-foreign-key-data
      description: Wants to fix foreign key creation failures by checking the child table for values with no matching parent record and correcting or removing those rows, so the relationship can be created during deployment.
    - name: Review delete rules
      id: review-delete-rules
      description: Wants to review foreign key delete rules to remove circular dependencies or multiple cascade paths that block foreign key creation during deployment, so the module can publish successfully.
    - name: Troubleshoot FK timeout
      id: troubleshoot-fk-timeout
      description: Wants to increase the SQL query timeout or check database locks when foreign key creation times out during deployment, so the relationship can be created successfully.
  - name: Exposing data via REST webservices
    id: rest-webservice-data
  - name: Data isolation
    id: data-isolation
  - name: Data archiving / cold data
    id: data-archiving-cold-data
  - name: Data synchronization
    id: data-syncronization
- name: Advanced integrations
  guid: fb5d71a9-9ac6-4fda-adea-e89546e0af89
  topics:
  - name: Webservice customization
    id: webservice-customization
    subtopics:
    - name: Customize request and response
      id: custom-request-response
    - name: API rate limit
      id: api-rate-limit
    - name: Customize headers
      id: customize-headers
    - name: Choose log level
      id: choose-log-level
      description: Wants to choose the appropriate logging level for REST APIs and consumed
        SOAP Web Services in OutSystems 11, understanding what each level logs
        and that higher levels increase database storage and may expose
        sensitive parameter values in the environment management console, so
        that they can balance troubleshooting needs against performance and
        security.
    - name: New API version
      id: new-api-version
      description: Wants to create a new REST API version when changing an
        endpoint path, authentication, method name, or a mandatory
        parameter/field's presence or type, so that existing consumers keep
        working on the old contract during a migration period.
  - name: Database connections
    id: database-connections
    subtopics:
    - name: Configure database connections
      id: configure-database-connections
      description: Wants to configure an external database connection per
        environment in Service Center and map the extension's logical database
        to it, so that applications using extension modules can read and write
        data from the correct external database after deployment.
    - name: Import external tables
      id: import-external-tables
      description: Wants to import entities from external tables or views in Integration Studio by selecting the database connection, selecting the tables and views, defining the logical database, reviewing the data types, and publishing the extension so that they create entities from external database data.
    - name: Set physical table names
      id: set-physical-table-names
      description: Wants to enter the three-part Physical Table Name for each
        external database Entity during a LifeTime deployment, and copy the
        source value when both environments use the same external database or
        schema, so that the deployed app points to the correct target database.
  - name: Integration Patterns
    id: integration-patterns
    subtopics:
    - name: Consume O11 entities
      id: consume-o11-entities
      description: Wants to understand how an O11 entity's exposure settings determine
        which entity actions are available when the entity is consumed as an
        external entity in ODC Studio, so that they can correctly use O11 data
        in ODC apps.
    - name: O11 entity handling
      id: o11-entity-handling
      description: Wants to consume O11 entities in ODC apps while accounting for
        static-entity IDs, explicit tenant filtering, transaction boundaries, and
        load-related performance limits, so they can keep reads and writes correct.
    - name: Oracle empty text
      id: oracle-empty-text
      description: Wants to handle Oracle empty-text behavior when consuming O11
        Oracle entities in ODC apps, including single-space placeholders, quoted
        default Email and Phone values, and empty-string filters in data mashup
        queries, so they can avoid unexpected query results and UI output.
    - name: Connect O11 infrastructure
      id: connect-o11-infrastructure
      description: Wants to connect their ODC organization to an O11
        infrastructure in the ODC Portal, by adding the infrastructure with
        its LifeTime base URL and creating a bound LifeTime service account
        with the Administrator role and ODC organization ID, so that they
        can consume O11 entities and logic in their ODC apps.
    - name: Fix org ID mismatch
      id: fix-org-id-mismatch
      description: Wants to resolve the OS-EBRG-40301 "authentication token
        doesn't match the ODC organization ID" validation error when
        connecting an ODC organization to an O11 infrastructure, by creating
        a new LifeTime service account scoped to ODC with a matching ODC
        organization ID, so that the connection can be established
        successfully.
    - name: Edit or remove O11 infrastructure
      id: edit-remove-o11-infrastructure
      description: Wants to edit the name and authentication token of a
        connected O11 infrastructure, or remove it, in the ODC Portal,
        understanding that the LifeTime base URL can't be changed and that
        infrastructures with associated O11 data connections or secure
        connections can't be removed, so that they can manage O11-ODC
        connections over time.
    - name: Set up interop DB access
      id: setup-interop-db-access
      description: Wants to set up the initial database access for O11-ODC
        data interoperability in a self-managed infrastructure by creating a
        dedicated database user and granting it read access to O11 system
        tables (application, entity, module metadata, plus SQL Server or
        Oracle-specific system tables), for each O11 environment mapped to
        an ODC stage.
    - name: Grant DB access on entity expose
      id: grant-db-access-entity-expose
      description: Wants to grant the interoperability database user
        read/write access to the new database views and tables created when
        an O11 application entity is exposed or promoted, and read access to
        the views/tables for the User and Tenant system entities, so ODC can
        consume the data.
    - name: Expose O11 entities
      id: expose-o11-entities
      description: Wants to configure the baseline environment and select
        which O11 entities to expose to ODC, setting the Public and Expose
        Read Only properties in Service Studio, using the O11 LifeTime
        console, so that ODC apps can reuse O11 data.
    - name: Promote exposed entities
      id: promote-exposed-entities
      description: Wants to promote exposed O11 entities from the baseline
        environment to the next environment in the O11 infrastructure
        pipeline using the LifeTime console, so that the exposed entities
        become available in that ODC stage.
    - name: Update exposed entities
      id: update-exposed-entities
      description: Wants to rename, remove, or update the database view of
        an already-exposed O11 entity in the baseline environment via the
        LifeTime console, so that changes to the entity's definition are
        reflected before promoting it to other environments.
    - name: O11-ODC interop overview
      id: o11-odc-interop-overview
      description: Wants to understand the three areas of O11-ODC
        interoperability, data, logic, and user, and what each enables,
        before deciding how to extend an O11 app with ODC capabilities.
    - name: Decide ODC extension
      id: decide-odc-extension
      description: Wants to decide whether to extend an existing O11 app
        with new ODC apps, using data, logic, and user interoperability, or
        continue evolving it in O11, by weighing use cases such as agentic
        workflows, high-scale B2C apps, and low-dependency apps against
        heavy-dependency or UI-extension scenarios, so that they can choose
        the right platform for a new use case.
    - name: Reuse logic O11-ODC
      id: reuse-logic-o11-odc
      description: Wants to reuse business logic between O11 and ODC by
        exposing it as a REST API on one platform, setting the API Consumer
        property to identify the other platform as the intended consumer in
        Service Studio or ODC Studio, and then consuming that REST API on
        the other platform by setting the API Producer property, so that
        O11 and ODC apps can call each other's logic exclusively through
        REST.
    - name: Secure logic reuse connection
      id: route-logic-reuse-secure-connection
      description: Wants to route the REST API requests of an O11-ODC logic
        reuse integration through a secure connection instead of over the
        public internet, by using the secure Base URL an administrator
        created for that stage and setting it as the URL of the Consumed
        REST APIs in the ODC app's configuration for each deployed stage,
        so that logic reuse traffic between O11 and ODC stays private.
    - name: Add secure connection
      id: add-secure-connection
      description: Wants to add a secure connection in ODC Portal for an O11
        cloud infrastructure, when consuming O11 logic from an ODC Cloud
        organization or the Development stage of an ODC self-hosted
        organization, so that ODC apps can route O11 REST API requests over a
        private channel and developers can use the resulting Base URL instead
        of the public O11 environment URL.
    - name: Reflect O11 changes
      id: reflect-o11-changes
      description: Wants to reflect an O11 data model change (new entities,
        changed attributes, or exposure settings) in their ODC apps, by
        updating the exposed entities in LifeTime, refreshing the O11
        connection in the ODC Portal, and updating dependencies in ODC
        Studio, following the O11-to-ODC development lifecycle order (dev,
        QA, production) for each environment, so the ODC app doesn't fail
        at runtime looking for database columns or views that don't yet
        exist.
    - name: O11 breaking changes
      id: o11-breaking-changes
      description: Wants to identify which O11 data model changes break an
        ODC app (removing an exposed entity, or removing an entity
        attribute that's used in the app) versus safe changes (exposing a
        new entity, renaming an exposed entity, adding a non-mandatory
        attribute, or removing an unused attribute).
    - name: Troubleshoot O11 interoperability
      id: troubleshoot-o11-interoperability
      description: Wants to troubleshoot O11-ODC data interoperability when
        an O11 connection shows Missing prerequisites, fails with Error
        connecting after token rotation, or exposes only the baseline
        environment or a Platform Server version requirement error in
        LifeTime, so they can fix the connection or version issue and
        continue exposing O11 entities to ODC.
  - name: Extensions
    id: extensions
    subtopics:
    - name: Choose connector option
      id: choose-connector-option
      description: Wants to choose an approach for migrating from the third-party
        ardoPostgreSQL connector to the OutSystems supported PostgreSQL connector
        in OutSystems 11 (adapting existing extensions and republishing consumers,
        creating new extensions and updating consumer modules, or creating new
        extensions and consumers from scratch), so that they can weigh development
        effort against consumer-testing risk before starting the migration.
    - name: Migrate DB connector
      id: migrate-db-connector
      description: Wants to migrate from the ardoPostgreSQL connector to the
        supported PostgreSQL connector in OutSystems 11 by creating a new database
        connection, updating each affected extension's connection reference,
        refreshing entities, and republishing and testing consumer modules, so
        that existing extensions and consumers keep working under the new
        supported connector.
    - name: Integrate using custom code
      id: custom-code-integration
    - name: Use NuGet package
      id: use-nuget-package
      description: Wants to add a public .NET assembly to an OutSystems 11 extension by using Visual Studio's NuGet Package Manager, ensuring the referenced DLL is copied locally and packages.config is included, so that the extension can call the package from C# code.
    - name: Declare File parameters
      id: declare-file-parameters
      description: Wants to declare File input and output parameters in an
        External Library function using the IOSFile type from the
        OutSystems External Libraries SDK, so their custom code can receive
        File values an ODC app provides and return new File values back to
        the calling app.
    - name: Read write file content
      id: read-write-file-content
      description: Wants to read a File input parameter's content and write
        a File output parameter's content in an External Library function
        using the IFileStorage interface from the FileStorage NuGet
        package, choosing the right read or write method based on file size
        and access pattern, so they can process file content correctly
        without loading unsupported amounts of data into memory.
    - name: Library file lifecycle
      id: library-file-lifecycle
      description: Wants to understand that files created by an External
        Library function are stored temporarily and isolated per function
        invocation, and that the calling app must save the returned File
        reference to an entity to promote it to permanent storage, so they
        can correctly manage file retention and access from custom code.
    - name: Consume via extension
      id: consume-webservice-via-extension
      description: Wants to consume a Web Service that can't be natively imported
        into OutSystems, by creating an Extension in Integration Studio with an
        Action per method, building a matching data model, importing the WSDL
        via Visual Studio's Add Web Reference dialog, and implementing the action
        to call the generated proxy class, so that Service Studio can call the
        Web Service like a built-in action, in OutSystems 11.
    - name: Open extension editor
      id: open-extension-editor
      description: Wants to open the Extension Editor in Integration Studio to
        view and edit an extension's properties, by double-clicking the
        extension node in the Multi-tree Navigator or selecting its tab in the
        Multi-tab Editor, so that they can modify extension properties and
        manage its elements (actions, structures, entities, and resources).
    - name: Fix invalid assembly
      id: fix-invalid-assembly
      description: 'Wants to fix the OS-ELG-MODL-05001 error ("File does not
        contain a valid compiled external library, or the compiled assembly
        was not built using a supported .NET version") by verifying their C#
        project targets a supported .NET version and packaging the compiled
        project into a ZIP correctly before uploading, so that they can
        successfully publish the external library.'
    - name: Fix ZIP compression
      id: fix-zip-compression-method
      description: 'Wants to fix the OS-ELG-MODL-05029 error ("The package was compressed using an unsupported compression method: <CompressionMethodName>") by using the Deflated or Stored compression method when creating high code package ZIP files, so that the external library uploads and publishes successfully.'
    - name: Fix ZIP uncompress error
      id: fix-zip-uncompress-error
      description: 'Wants to fix the OS-ELG-MODL-05028 error ("The file <FileName> can not be uncompressed.") by removing unnecessary files from the external library ZIP package or contacting Support when the file is required, so they can upload and publish the external library successfully.'
    - name: Fix OSIgnore field error
      id: fix-osignore-field-error
      description: 'Wants to fix the OS-ELG-MODL-05012 error ("The property/field
        decorated by OSIgnore ... is not public") by changing the access modifier
        of an OSIgnore-decorated struct property/field to public in their C#
        external library project, so that they can successfully publish the
        external library.'
    - name: Fix OSInterface duplicate
      id: fix-osinterface-duplicate
      description: 'Wants to fix the OS-ELG-MODL-05008 error ("The interface
        decorated with OSInterface ... is implemented by multiple classes") by
        ensuring only one class implements the OSInterface-decorated interface
        in their C# external library project, so that they can successfully
        publish the external library.'
    - name: Fix name length error
      id: fix-name-length-error
      description: 'Wants to fix the OS-ELG-MODL-05019 error ("The name <Name> is
        not supported as it has more than 50 characters.") by shortening the
        external library name to 50 characters or fewer, so they can
        successfully publish the library.'
    - name: Fix leading-digit error
      id: fix-leading-digit-error
      description: 'Wants to fix the OS-ELG-MODL-05020 error ("The name <Name> is not supported as it begins with a number.") by renaming the C# external library so the name starts with a letter before uploading it, so they can successfully publish the external library.'
    - name: Fix invalid characters error
      id: fix-invalid-characters-error
      description: 'Wants to fix the OS-ELG-MODL-05021 error ("The name: ''<Name>'' is not supported as it has the following invalid characters ''<InvalidCharacters>''.") by renaming the C# external library so the name uses only letters, numbers, and underscores before uploading it, so they can successfully publish the external library.'
    - name: Fix underscore names
      id: fix-underscore-names
      description: 'Wants to fix the OS-ELG-MODL-05022 error ("The following name: <InterfaceName/MethodName> begins with underscores") by renaming C# interface or method names so they do not start with underscores, so they can successfully publish the external library.'
    - name: Missing file warning
      id: missing-file-warning
      description: 'Wants to resolve the "The file ''<file>'' was not found on
        the file system" warning that occurs when a previously added resource
        file can''t be found on the file system during extension creation,
        merge, or save in Integration Studio, by re-opening the extension to
        recover the files, so that the extension resource is restored.'
    - name: Overwritten file warning
      id: overwritten-file-warning
      description: 'Wants to resolve the "The file ''<file>'' was overridden"
        warning in Integration Studio by understanding that a merge operation
        replaces file-system files with newer, extension-generated ones when
        Last Modified timestamps differ, and by locating the automatic backup
        saved in the extension''s Backups folder, so that they don''t lose
        manual file-system changes.'
    - name: Fix source code warning
      id: fix-source-code-warning
      description: 'Wants to resolve the "The Extension has no [.NET] source
        code to update/compile" warning in Integration Studio by adding at
        least one action to the extension, so that the extension has
        associated source code to update and compile, in OutSystems 11.'
    - name: Fix missing options warning
      id: fix-missing-options-warning
      description: 'Wants to resolve the "The .NET Software Development Kit
        options are not correctly defined" warning that blocks compiling and
        1-Click Publishing an extension, by setting the .NET SDK options
        correctly in the Options Window, in Integration Studio, so that the
        extension compiles and publishes successfully.'
    - name: Fix resource warning
      id: fix-resource-warning
      description: 'Wants to fix the "The Resource <file> should not be included
        in the Extension" or "...has a reserved file name..." warnings in
        OutSystems 11, caused by naming an extension resource with the reserved
        OutSystemsHubEdition prefix or a reserved system filename (for example,
        compressor or web.config), by renaming the resource file to remove the
        prefix or reserved name, so that the resource deploys correctly.'
    - name: Authenticate mTLS library
      id: authenticate-mtls-library
      description: 'Wants to implement mutual TLS (mTLS) authentication in an
        external library''s HTTP client, by loading a client certificate and
        validating the server certificate, so that an OutSystems Developer
        Cloud (ODC) app can securely consume a mTLS-protected REST API.'
    - name: Parse XML XPath
      id: parse-xml-xpath
      description: Wants to extract filtered data from an XML file using XPath expressions
        in a Server Action, by loading the XML document, selecting nodes with an
        XPath expression, and iterating over the matched nodes to read their
        values, using the OutSystems 11 XML Extension, so that only the data
        matching specific criteria is available for downstream business logic
        without processing the entire document.
    - name: Process XML content
      id: process-xml-content
      description: Wants to create, load, query, modify, save, and transform XML
        documents and elements with the ODC XML actions, so that a server action
        can build and process XML content end to end.
    - name: Regex text search/replace
      id: regex-search-replace
      description: Wants to use the Regex_Search and Regex_Replace server actions
        from the Text extension to search for and replace regular expression
        patterns in text, wrapping them inside a server action instead of a
        client action, so that the regex pattern stays secure server-side in
        a reactive OutSystems app.
    - name: Validate GUID with regex
      id: validate-guid-regex
      description: Wants to build a reusable server action that uses Regex_Search
        with a GUID-matching pattern to check whether a text input is a valid
        GUID, so that they can validate identifiers are properly formatted
        before use in OutSystems 11.
    - name: Extract GUID with regex
      id: extract-guid-regex
      description: Wants to build a reusable server action that uses Regex_Search
        with a GUID-matching pattern to detect and extract a GUID embedded
        within a larger text string, so that they can retrieve the GUID value
        from unstructured text in OutSystems 11.
    - name: Fix SAP connection
      id: fix-sap-connection-error
      description: Wants to fix the "Invalid SAP Connection" error caused by a
        SAP connection with no remote functions linked (never imported, or all
        deleted), by importing at least one remote function or deleting the
        connection, so that the SAP connection compiles and publishes
        successfully in OutSystems 11.
    - name: Fix SAP decimal warning
      id: fix-sap-decimal-warning
      description: Wants to resolve the "This can result in data loss" warning
        caused by an SAP remote function parameter mapped to Decimal that can
        exceed the Decimal data type's supported range or digit length, by
        checking whether the SAP data fits within Decimal's boundaries and
        switching the parameter to Text instead, so that the integration
        avoids runtime errors or silent rounding in OutSystems 11.
    - name: Configure deploy action
      id: configure-deploy-action
      description: Wants to choose the correct Deploy Action for an extension resource in
        Integration Studio based on the resource's file type, so that the
        resource deploys to the right Platform Server location, or is excluded,
        when the module is published.
    - name: Compare resource template
      id: compare-resource-template
      description: Wants to compare a resource file stored on the file system
        against its template file in Integration Studio's Resources tree,
        using the Compare With Template operation and a diff tool set in the
        Options Window, so that they can review and merge changes made
        outside the Resource Editor when Update Source Code can't
        auto-merge them.
    - name: Check import details
      id: check-import-details
      description: Wants to check the Import Details report for an extension
        resource that was added by importing actions from a .NET assembly,
        in Integration Studio's Resource Editor, so that they can review
        additional information about the imported resource.
    - name: Import assembly actions
      id: import-actions-from-dotnet-assembly
      description: Wants to import actions from a .NET assembly in Integration Studio by
        browsing for the assembly, selecting the assembly members to import, and
        reviewing the generated report, so that the extension creates the
        corresponding actions and resources automatically.
    - name: Structure element properties
      id: structure-element-properties
      description: Wants to know the properties available when defining a Structure element
        in Integration Studio, including which are mandatory or optional and
        their default values, so that they can correctly configure a structure
        inside an extension.
    - name: File menu operations
      id: file-menu-operations
      description: Wants to know what operations the File menu provides in Integration
        Studio, so that they can manage an extension's lifecycle while working
        in Integration Studio.
    - name: Edit menu operations
      id: edit-menu-operations
      description: Wants to know what operations the Edit menu provides in Integration
        Studio, so that they can perform clipboard actions and access
        environment editing options while working on an extension.
    - name: Help menu operations
      id: help-menu-operations
      description: Wants to know what operations the Help menu provides in Integration
        Studio, so that they can access help resources, product updates, and
        feedback tools while working on an extension.
    - name: Configure .NET SDK options
      id: configure-net-sdk-options
      description: Wants to configure the .NET IDE, .NET Compiler Tool, and
        .NET Compiler Tool Options paths in Integration Studio's Options
        window .NET tab, so that they can compile and edit .NET extensions
        with their preferred development environment and build settings, in
        OutSystems 11.
    - name: Configure proxy auth
      id: configure-is-proxy-auth
      description: Wants to configure proxy authentication settings (enabling
        proxy authentication and setting the proxy user name and password)
        in Integration Studio's Options window Connection tab, so that
        Integration Studio can connect to a Platform Server behind an
        authenticating proxy, in OutSystems 11.
    - name: Configure recompilation
      id: configure-recompilation
      description: 'Wants to enable or disable the "Enable server extension
        recompilation on upgrade" option in the Environment Configuration
        screen of Service Center, understanding it''s disabled by default,
        unavailable in production, and only safe to enable temporarily for
        trusted extensions, in OutSystems 11, so that they control whether
        the Platform Server can automatically recompile extensions during
        publishing.'
    - name: Select Server window
      id: select-server-window
      description: Wants to know what information the Select Server window requires in
        Integration Studio, so that they can connect to a specific Platform
        Server, in OutSystems 11.
    - name: 1-Click Publish window
      id: 1-click-publish-window
      description: Wants to know what areas and action buttons the 1-Click Publish window
        provides in Integration Studio, so that they can monitor progress,
        review errors and warnings, and manage extension publishing to a
        Platform Server, in OutSystems 11.
    - name: Verify window
      id: verify-window
      description: Wants to know what areas and buttons the Verify window provides in
        Integration Studio, so that they can review verification errors and
        warnings and continue extension publishing, in OutSystems 11.
    - name: Download version window
      id: download-version-window
      description: Wants to know what information and buttons the Download Version from
        Server window provides in Integration Studio, so that they can review
        available versions and download a specific extension version from the
        Platform Server they're connected to.
    - name: Download window
      id: download-window
      description: Wants to know what information and buttons the Download from Server
        window provides in Integration Studio, so that they can review
        extensions they have Download access to and download the latest
        published version from the Platform Server they're connected to.
  - name: Integrate with legacy systems
    id: legacy-systems-integration
    subtopics:
    - name: Access Lotus Notes data
      id: access-lotus-notes-data
      description: Wants to access data from a legacy Lotus Notes/Domino application
        using ODBC and a SQL Server Linked Server, on a 64-bit SQL Server environment,
        so that OutSystems applications can query that legacy data at runtime.
  - name: Private Gateway
    id: private-gateway
    subtopics:
    - name: Activate private gateway
      id: activate-private-gateway
      description: Wants to activate an ODC Private Gateway for an environment
        stage in ODC Portal and configure the Cloud Connector using the generated
        token and address, so that ODC apps can securely connect to private
        network endpoints.
    - name: Configure private gateway
      id: configure-private-gateway
      description: Wants to configure a private gateway for an O11
        self-managed infrastructure, when consuming O11 logic from an ODC Cloud
        organization or the Development stage of an ODC self-hosted
        organization, so that ODC apps can route O11 REST API requests through
        the gateway and developers can use the resulting secure-gateway:<port>
        endpoint instead of the public O11 environment URL.
    - name: Private gateway routing for O11 consumption
      id: private-gateway-routing-o11-consumption
      description: Wants to route O11-consumption requests in ODC through an
        already activated private gateway by selecting that gateway in the O11
        infrastructure connection configuration, so that traffic stays on a
        private network path.
  - name: Generate PDF reports
    id: generate-pdf-reports
    subtopics:
    - name: Set up HTML2PDF component
      id: setup-html2pdf-component
      description: Wants to install and configure the HTML2PDF Forge component in
        OutSystems 11, including downloading the wkhtmltopdf/wkhtmltoimage binaries
        matching the component's documented version, uploading them in the
        component's admin panel, and adding references to GeneratePDF,
        MakeAbsoluteURL, and GetEntryURL in Service Studio, so that the environment
        is ready to generate PDF reports.
    - name: Create PDF report HTML2PDF
      id: create-pdf-report-html2pdf
      description: Wants to build and generate a PDF report using the HTML2PDF
        component by creating an anonymous screen with a Table Records widget bound
        to an aggregate, then calling GeneratePDF with an absolute URL and a
        Download action from a separate screen, in OutSystems 11, so that end users
        can export screen data as a downloadable PDF file.
    - name: Customize HTML2PDF output
      id: customize-html2pdf-output
      description: Wants to work around HTML2PDF/wkhtmltopdf rendering limitations,
        such as table rows breaking across pages and missing header/footer/pagination
        support, by applying the page-break-inside CSS rule, passing header/footer
        URLs via OtherArgs, and adding pagination JavaScript, in OutSystems 11, so
        that the generated PDF report renders correctly and completely.
  - name: Integration Builder
    id: integration-builder
    subtopics:
    - name: Learn Integration Builder
      id: learn-integration-builder
      description: Wants to understand what Integration Builder is, what problem it solves
        (connecting OutSystems 11 apps to external Systems of Record), and which
        types of external systems it supports, so that they can decide whether
        to use it and where to start.
    - name: Authorize Dataverse IB
      id: authorize-dataverse-ib
      description: Wants to authorize Integration Builder to access their
        Microsoft Dataverse data through their Microsoft account, so that
        Integration Builder can retrieve environment, table, and column
        metadata and let them test a newly created Dataverse integration in
        the development environment.
    - name: Connect Dataverse
      id: connect-dataverse
      description: Wants to authorize a Microsoft Dataverse connection in
        Integration Manager, either automatically (with Microsoft Entra
        administrator permissions, generating a certificate and Microsoft
        Entra app) or manually (without administrator permissions, using
        parameters supplied by a Microsoft Entra administrator), so that
        Server Actions in the generated integration module can authenticate
        requests at runtime.
    - name: Edit Dataverse connection
      id: edit-dataverse-connection
      description: Wants to edit an existing Microsoft Dataverse connection in
        Integration Manager by authorizing with a Microsoft account that has
        access to Microsoft Entra, so that they can update the connection tied
        to the Microsoft Entra app registration.
    - name: Connect Salesforce
      id: connect-salesforce
      description: Wants to set up a Salesforce connection for Integration
        Builder by having a Salesforce administrator create a permission set
        and an external client app (with OAuth scopes, JWT flow, and
        permitted policies) in Salesforce, then share the Consumer Key (and
        certificate) with the OutSystems developer, so that Integration
        Manager can complete the connection configuration.
    - name: Fix recursive refs
      id: fix-recursive-refs
      description: 'Wants to resolve the "The attribute <EntityName>.<AttributeName> has recursive references, which are not supported by OutSystems. Please deselect it before proceeding." error in Integration Builder by reopening the SAP OData Add Entities step and clicking Next so Integration Builder auto-unselects recursive attributes, or by consuming the service through an extension in Integration Studio when the recursive attribute is required, so that they can save or publish the integration.'
  - name: Handling webservice errors
    id: handle-webservice-errors
    subtopics:
    - name: Fix builder connection
      id: fix-builder-connection
      description: 'Wants to resolve the "We were unable to reach your environment
        and we couldn''t sign you into <Integration/Workflow> Builder" error in
        OutSystems 11, caused by an incorrect environment name or by
        Integration/Workflow Builder being unable to reach the environment over
        the network, by verifying with their IT department that
        Integration/Workflow Builder has network access to the correct source
        and destination per OutSystems network requirements, so that they can
        sign in to Integration Builder or Workflow Builder.'
    - name: Fix builder credentials
      id: fix-builder-credentials
      description: 'Wants to resolve the "It was not possible to sign into
        <Integration/Workflow> Builder" error in OutSystems 11, caused by
        invalid credentials or a blocked user, by verifying the username and
        password, confirming with a LifeTime administrator that the username
        exists, and asking a LifeTime administrator to unblock the username,
        so that they can sign into Integration Builder or Workflow Builder.'
    - name: Fix IM unreachable
      id: fix-im-unreachable
      description: 'Wants to resolve the "Integration Manager couldn''t be
        reached" network error in Integration Builder by verifying Integration
        Manager is installed and up to date in the environment''s Dependencies
        settings, and checking that Integration Builder''s network/firewall
        configuration allows access to the environment''s
        OSIntegrationManager_API endpoint, so that they can navigate from
        Integration Builder to Integration Manager to configure integration
        connections.'
    - name: Fix Salesforce API error
      id: fix-salesforce-api-error
      description: 'Wants to resolve a Salesforce error message
        (OS-INBL-API-20000) returned when Integration Builder calls the
        Salesforce API, by retrying later and contacting the Salesforce
        administrator or Salesforce support if the error persists, so that
        they can restore the connection to Salesforce.'
    - name: Fix username not found
      id: fix-username-not-found
      description: 'Wants to resolve the "User with username <Username> was
        not found" error (OS-INBL-API-00004) in Integration Builder, caused
        by an unregistered username in the API call, by verifying the
        username shown in the error matches the one used to sign into
        Integration Manager and signing into Integration Builder with the
        correct username, or contacting OutSystems support if the username
        is already correct, so that they can create or edit connections in
        Integration Manager.'
    - name: Fix integration key error
      id: fix-integration-key-error
      description: 'Wants to resolve the "No integration was found for the
        ''Key'' with <value>" error (OS-INBL-API-00008) in Integration
        Builder, caused by the integration key no longer existing (a deleted
        integration or an incorrect key used in the API call), by refreshing
        the integration list in Integration Builder and recreating the
        integration if it no longer exists, or contacting OutSystems support
        if the integration still exists, so that they can assign a
        connection to the integration in Integration Manager.'
    - name: Fix environment not authorized
      id: fix-environment-not-authorized
      description: 'Wants to resolve the "Environment <Hostname> isn''t
        authorized to access the Integration Builder" error
        (OS-INBL-API-00011), caused by the environment not being registered
        in Integration Builder, by going to Integration Builder''s Settings >
        Environment setup and adding the environment, so that Integration
        Manager can create connections and redirect to Integration Builder.'
    - name: Fix Salesforce access
      id: fix-salesforce-access
      description: 'Wants to resolve the "The request has been refused"
        Salesforce error (OS-INBL-API-00021) returned when Integration
        Builder verifies the Salesforce user during Connection creation,
        caused by the account lacking necessary permissions (or exceeding
        API request limits, if the error code shown is
        REQUEST_LIMIT_EXCEEDED), by authorizing Integration Manager with a
        different Salesforce account or manually setting up a Connected App
        in Salesforce and filling in the connection form in Integration
        Manager, so that they can restore the Salesforce connection.'
    - name: Fix Salesforce token error
      id: fix-salesforce-token-error
      description: Wants to resolve the OS-INBL-API-00050 Salesforce error,
        where the session ID or OAuth token is expired or invalid because
        the Salesforce authorization has expired or been revoked, by
        reauthorizing the Salesforce integration in Integration Builder, so
        that Integration Builder can retrieve Salesforce metadata and create
        the integration.
    - name: Fix AAD delete error
      id: fix-aad-delete-error
      description: 'Wants to resolve the "connection was deleted locally, but
        we could not delete the connection on the Microsoft''s servers" error
        (OS-INBL-API-00030) in Integration Manager, caused by the
        corresponding Application already being deleted in Microsoft Azure
        Active Directory, by manually deleting the Application in Azure
        Active Directory if it still exists, so that they can fully remove a
        connection tied to an Azure AD app registration.'
    - name: Fix AAD update error
      id: fix-aad-update-error
      description: 'Wants to resolve the "connection was updated locally, but we
        could not update the connection on the Microsoft''s servers" error
        (OS-INBL-API-00028) in Integration Manager, caused by the matching
        Application in Microsoft Azure Active Directory already being deleted
        or otherwise unavailable for editing, by updating the connection
        manually in Azure Active Directory or deleting the connection in
        Integration Builder and creating a new one if the current connection
        still does not work, so that they can keep the connection in sync with
        the remote application registration.'
    - name: Fix identity token error
      id: fix-identity-token-error
      description: 'Wants to resolve the "An error occurred while getting a
        token from the Microsoft Identity Platform" error (OS-INBL-API-00038)
        in Integration Builder, caused by a Dataverse, Dynamics, or SharePoint
        connection''s Microsoft Entra App Registration having an expired or
        mismatched certificate, by creating a new connection automatically
        (if a Microsoft Entra administrator) or verifying the App
        Registration''s Application, Object, and Tenant IDs and certificate
        match the connection''s configuration in Integration Manager, so that
        they can restore the connector''s ability to get data from the
        external system.'
    - name: Fix IM sign-in error
      id: fix-im-signin-error
      description: 'Wants to resolve the "Internal server error" (OS-INBL-API-00061)
        that blocks signing into Integration Manager in OutSystems 11, caused
        by broken references between Integration Manager and the modules it
        consumes, by using Service Center to republish the Integration
        Manager application and its dependent modules, for example by
        creating a Solution with the "OsIntegrationManager" module and the
        "include dependencies as components" option checked and publishing
        its running version, so that they can sign into Integration Manager
        again.'
    - name: Fix publish error
      id: fix-publish-error
      description: 'Wants to resolve the "Publish failed with an error:
        <OriginalErrorMessage>" error (OS-INBL-GEN-00032) in Integration
        Builder, caused by an issue during publishing an integration, by
        checking the Service Center Error logs of the remote environment for
        the original error message and contacting OutSystems support if the
        logs don''t provide enough information, so that they can resolve the
        publish failure and successfully publish the integration.'
    - name: Fix module gen error
      id: fix-module-gen-error
      description: 'Wants to resolve the "unexpected error" (OS-INBL-GEN-50002)
        or "There was an unexpected error" message (OS-INBL-GEN-50004) that
        blocks publishing an integration in Integration Builder, caused by a
        module generation failure, by returning to the Review Integration
        screen and clicking Publish again, or contacting OutSystems support
        if the issue persists, so that they can successfully publish the
        integration.'
    - name: Fix missing fields
      id: fix-missing-fields
      description: 'Wants to resolve the "There was an unexpected error. If the problem persists, contact OutSystems Support." message in Integration Builder while creating or updating an integration, caused by missing mandatory fields, by filling in the missing values on the screen and then retrying the step, so that the integration can be saved or updated successfully.'
    - name: Fix internal service error
      id: fix-internal-service-error
      description: 'Wants to resolve the OS-INBL-API-00069 "There was an error contacting an internal service." error in Integration Builder by verifying the server address configured in Service Center can be resolved through DNS, ensuring it matches the HTTPS certificate, and checking the Service Center Error logs for more details, so that Integration Manager can contact the internal service.'
  - name: Workato Connector
    id: workato-connector
    subtopics:
    - name: Install Workato connector
      id: install-workato-connector
      description: Wants to install and configure the OutSystems Workato Connector
        by installing the Forge component, creating an API key with entity access
        rules in the OutSystems environment, and creating a corresponding app
        connection in Workato, so that Workato can securely connect to their
        OutSystems environment.
    - name: Build OutSystems trigger recipe
      id: build-workato-outsystems-trigger-recipe
      description: Wants to build a Workato recipe that triggers on a new or
        updated OutSystems entity record to run an action in another
        application, so that OutSystems changes propagate to other connected
        apps.
    - name: Build Upsert trigger recipe
      id: build-workato-upsert-trigger-recipe
      description: Wants to build a Workato recipe that triggers on an external
        app event to create or update an OutSystems entity record via the
        Upsert record action, so that external app changes are reflected in
        OutSystems.
    - name: Map Workato foreign key
      id: map-workato-foreign-key
      description: Wants to obtain the Id value of a related OutSystems entity
        (a foreign key) before calling the Upsert record action in a Workato
        recipe, by adding a Search records action for the related entity and
        reordering it ahead of the upsert step, so that foreign key attributes
        are populated correctly when creating or updating records from another
        application.
- name: Advanced security
  guid: dedf532a-fe2a-4e93-8316-731133ad6ba1
  topics:
  - name: Handling sensitive data
    id: handling-sensitive-data
    subtopics:
    - name: Data masking
      id: data-masking
    - name: Data encryption
      id: data-encryption
    - name: Secure random generation actions
      id: secure-random-generation-actions
      description: Wants to use the Security library to generate secure random
        values, so that an app can produce unpredictable inputs for sensitive
        workflows in ODC.
    - name: Cryptographic key generation actions
      id: cryptographic-key-generation-actions
      description: Wants to use the Security library to generate cryptographic
        keys, so that an app can prepare key material for protected operations
        in ODC.
    - name: Data confidentiality actions
      id: data-confidentiality-actions
      description: Wants to use the Security library to protect data
        confidentiality, so that an app can encrypt and decrypt sensitive
        information in ODC.
    - name: Data integrity and authenticity actions
      id: data-integrity-authenticity-actions
      description: Wants to use the Security library to protect data integrity
        and authenticity, so that an app can trust protected content in ODC.
    - name: Search encrypted data
      id: search-encrypted-attribute
      description: Wants to search an encrypted searchable attribute in
        OutSystems 11, by adding the EncryptSearchableAttribute action
        before the query and setting its Text and IndexType inputs to match
        the attribute's encryption IndexType, so that the search string is
        encrypted before it's used in the database query.
    - name: Secure data in transit
      id: secure-data-in-transit
      description: Wants to protect OutSystems app communications from Man-in-the-Middle
        attacks by using HTTPS/SSL/TLS for Web Flows and Web Services and enabling
        secure cookies with HSTS headers, so that sensitive data and session cookies
        aren't transmitted in plain text.
    - name: Enable TLS protocols
      id: enable-tls-protocols
      description: Wants to enable supported TLS protocols for OutSystems integrations by
        adding Schannel client registry keys and setting SchUseStrongCrypto in
        the .NET Framework registry hives, so that requests can negotiate a
        supported SSL/TLS protocol when the defaults cause secure channel
        errors.
    - name: Secure custom cookies
      id: secure-custom-cookies
      description: Wants to add the Secure flag to a custom cookie in OutSystems
        11, using the CookieSecure parameter of the SetCookie Server Action in a
        server-side context or by appending '; Secure' in client-side JavaScript,
        so that custom application cookies are only sent over HTTPS.
    - name: Secure REST headers
      id: secure-rest-headers
      description: Wants to fix the security vulnerability where headers marked
        for removal in a consumed REST service's OnBeforeRequest callback
        aren't actually removed from the outgoing request, by enabling the
        Allow headers removal inside onBeforeRequest REST callback setting in
        the Factory Configuration Forge component and republishing affected
        modules, so that sensitive information in headers isn't
        unintentionally exposed to the server, logs, or network during
        transit, in OutSystems 11.
  - name: External access control
    id: external-access-control
    subtopics:
    - name: ACLs
      id: acls
    - name: Webservice authentication
      id: webservice-authentication
    - name: Authenticate REST with client cert
      id: authenticate-rest-client-cert
      description: Wants to secure a consumed REST API by sending a client-side
        certificate through the REST Extensibility API, customizing the
        request with OnBeforeRequest, and optionally forcing a TLS version for
        legacy servers, so that an OutSystems 11 app can authenticate to a
        client-certificate-protected endpoint.
    - name: Restrict internal access
      id: restrict-internal-network-access
      description: Wants to restrict access to UI Flows, exposed SOAP Web
        Services, and REST APIs to users authenticated on an internal
        network IP range, by setting the Internal Access Only property to
        Yes in Service Studio, so that sensitive elements are reachable
        only from within the internal network, in OutSystems 11.
    - name: JWT REST authentication
      id: jwt-rest-authentication
      description: Wants to secure REST APIs exposed or consumed between O11
        and ODC using JWT-based token authentication, so that only trusted
        callers can invoke sensitive operations across platform boundaries.
    - name: Choose JWT key type
      id: choose-jwt-signing-key-type
      description: Wants to choose between symmetric (HMAC) and asymmetric
        (RSA/EC) signing when designing key management for JWT-based
        authentication of exposed REST APIs, weighing shared-secret
        simplicity against multi-consumer key rotation and untrusted-client
        verification needs, so that they pick the right signing approach for
        their trust and deployment scenario.
    - name: JWK rotation practices
      id: jwk-key-rotation-practices
      description: Wants to represent JWT signing keys using the JWK format
        and rotate them by publishing new keys alongside existing ones and
        accepting both during a transition period, while keeping
        token-signing keys separate from claim-encryption keys, so that keys
        can be rotated without breaking validation and the blast radius of a
        compromised key is minimized.
    - name: Store signing keys
      id: store-jwt-signing-keys
      description: Wants to choose where to store JWT signing keys based on
        their architecture and platform version, so that keys are stored and
        retrieved securely.
    - name: Choose token flow
      id: choose-token-flow
      description: Wants to select the right JWT token flow (service account,
        credentials-only, or credentials + service account) for an ODC exposed
        REST API based on whether callers are applications, users via an app,
        or users directly, so that authentication matches the caller identity
        model.
    - name: Validate JWT in OnAuthentication
      id: validate-jwt-onauthentication
      description: Wants to implement token validation in an ODC exposed REST
        API's OnAuthentication callback, extracting the bearer token from the
        Authorization header and verifying its signature and standard claims
        (iss, aud, nbf, exp), so that unauthorized or tampered requests are
        rejected before business logic executes.
    - name: JWT security practices
      id: jwt-security-practices
      description: Wants to apply security best practices for JWT
        token-based exposed REST APIs, covering short token lifetimes,
        minimizing claims and sensitive data in the payload, planning token
        revocation, protecting tokens in transit and at rest, and defending
        against replay attacks, so that the token-based authentication
        pattern resists common token-related attacks.
    - name: JWT library actions
      id: jwt-library-actions
      description: Wants to use the Security library JWT actions to create,
        read, and validate tokens, including toggling exception PII output and
        filling the TokenPayload and ValidationParameters records, so that an
        app can handle JWTs when implementing token-based authentication in
        ODC.
    - name: Manage token lifecycle
      id: manage-token-lifecycle
      description: Wants to centralize token acquisition, secure storage,
        request attachment in OnBeforeRequest, and refresh handling for
        consumed REST APIs that use JWT-based token authentication, so that
        apps reuse valid tokens and recover from 401 or 403 failures without
        scattering token logic.
    - name: Protect access tokens
      id: protect-access-tokens
      description: Wants to secure access tokens used by consumed REST APIs
        by encrypting them at rest, keeping them out of logs and error
        messages, and limiting privileges when an ODC app stores JWTs for
        outbound API calls, so that token leakage and misuse are reduced.
    - name: Credential delegation
      id: credential-delegation
      description: Wants to enable Integrated Authentication credential delegation
        from the browser through OutSystems to a consumed Web Reference, by setting
        the Web Reference's Integrated Authentication property, ensuring
        integrated-auth context in the calling screen/flow/block/action, and
        configuring the OutSystems server as trusted for Kerberos delegation to the
        Web Service server in Active Directory, in OutSystems 11, so that the Web
        Service receives the client's actual domain credentials instead of the
        application's service account.
    - name: Create API client
      id: create-api-client
      description: Wants to create an API client in the ODC portal, configuring its
        identifying details, secret expiration and reminder settings, and per-
        stage permissions, so that OAuth 2.0 client credentials are generated
        for accessing ODC REST APIs and data.
    - name: Copy API client credentials
      id: copy-api-client-credentials
      description: Wants to copy the client ID and client secret of a newly created
        API client immediately after creation, so they can configure them in their
        app to request an access token, since the credentials can't be retrieved
        again once the credentials page is left.
    - name: Suspend API client
      id: suspend-api-client
      description: Wants to suspend access to an API client from the ODC portal,
        so its secrets and access tokens immediately become invalid and can no
        longer be used to access ODC REST APIs.
    - name: Resume API client
      id: resume-api-client
      description: Wants to resume access to a suspended API client from the ODC
        portal, so its secret and access token become valid again, or generate a
        new secret if expired, restoring API access.
    - name: Edit API client permissions
      id: edit-api-client-permissions
      description: Wants to edit the permissions of an existing API client in the
        ODC portal after it was created, so its access to ODC REST APIs and data
        can be updated.
  - name: Prevent CSRF attacks
    id: prevent-csrf-attacks
    description: Wants to prevent cross-site request forgery attacks in OutSystems
      11 by relying on the built-in anti-CSRF protections for traditional and
      reactive web apps and by validating API requests server-side, so that
      forged requests from another site are rejected.
  - name: Injection attacks
    id: injection-attacks
    subtopics:
    - name: Injection attack types
      id: injection-attack-types
      description: Wants to understand what injection attacks are, why they are dangerous,
        and what categories exist, including SQL injection, XSS, interpreter injection,
        file system attacks, and buffer overflow, in the context of OWASP web security,
        so that they can recognize and reason about this vulnerability class when securing
        ODC applications.
    - name: XSS attack types
      id: xss-attack-types
      description: Wants to understand the three categories of Cross-Site Scripting
        attacks, specifically reflected, stored, and DOM-based XSS, including how each
        propagates and their relative severity, so that they can identify the type of
        XSS risk present in their application and apply the right mitigation.
    - name: Encode custom HTML and JavaScript
      id: encode-custom-html-javascript
      description: Wants to safely include end-user input in custom HTML or
        JavaScript, including concatenated HTML attribute values, by using the
        appropriate EncodeHtml() or EncodeJavascript() built-in function
        whenever escape content is disabled, so that untrusted values are
        rendered as text instead of executing in the browser.
    - name: Prevent URL parameter injection ODC
      id: prevent-url-parameter-injection-odc
      description: Wants to prevent unsafe end-user input from altering URL
        parameter values in ODC applications by wrapping dynamic parameter
        values with EncodeUrl() before including them in a URL, so that
        special characters are encoded safely instead of breaking the
        generated URL.
    - name: Fix JS injection warning
      id: fix-js-injection-warning
      description: Wants to resolve the Service Studio warning that an expression
        using end-user input isn't protected against JavaScript injection, by
        wrapping it with the EncodeJavascript() built-in function or
        VerifyJavascriptLiteral() from the Sanitization extension, in OutSystems
        11, so that malicious script content can't be injected via that
        expression.
    - name: Fix LDAP injection warning
      id: fix-ldap-injection-warning
      description: Wants to resolve the Service Studio warning that an LDAP
        filter built from end-user input isn't protected against LDAP
        injection, by encoding each dynamic filter value with
        LdapFilterEncode() before assembling LDAP_Search, LDAP_Search_WithAuthenticationType,
        or LDAP_Login filters, so that attacker-controlled values can't alter
        the LDAP filter structure.
    - name: Fix open redirect warning
      id: fix-open-redirect-warning
      description: Wants to resolve the Service Studio warning that an
        HTTPRequestHandler input parameter derived from end-user input is
        susceptible to open redirect vulnerabilities, by wrapping the
        parameter with the ReplaceURLDomain() function, replacing the
        'External Site' element with a well-known page, or replacing it with
        an External Site element that doesn't take a URL input, in
        OutSystems 11, so that malicious redirect URLs can't be injected via
        that parameter.
    - name: Prevent open redirects
      id: prevent-open-redirects
      description: Wants to protect OutSystems apps from unvalidated redirect
        and forwarder vulnerabilities that let attackers redirect users to a
        malicious site via dynamic URL parameters, by avoiding dynamic URL
        redirects to external sites or validating the input URL against an
        allowlist, in OutSystems 11.
    - name: Fix SQL injection warning
      id: fix-sql-injection-warning
      description: Wants to resolve the Service Studio warning that a SQL
        Query Parameter has Expand Inline enabled, including the specific
        case of a parameter used after ORDER BY or GROUP BY, by disabling
        Expand Inline where possible or ensuring the parameter value doesn't
        contain end-user input, in OutSystems 11, so their application isn't
        vulnerable to SQL injection.
    - name: Prevent SQL injection
      id: prevent-sql-injection
      description: Wants to prevent SQL injection in custom queries by keeping
        untrusted input in query parameters instead of concatenating it into
        inline SQL, and by wrapping untrusted variables with EncodeSql() or
        VerifySqlLiteral() and avoiding Expand Inline on end-user input when
        inline SQL is unavoidable, so that attacker-controlled input can't
        alter the query's SQL structure.
    - name: ODC injection defense overview
      id: odc-injection-defense-overview
      description: Wants to understand the main layers of injection defense
        in ODC, including design-time warnings, context-appropriate
        sanitization, safe query parameterization, default output escaping,
        and Content Security Policy, so they can choose the right mitigation
        for each injection risk in an ODC application.
  - name: Access control risks
    id: access-control-risks
    description: Wants to protect OutSystems 11 applications from access control
      and permissions vulnerabilities, covering how to secure actions (avoiding
      UI-only disabling/hiding, validating permissions server-side rather than
      only in Preparation, checking permissions before executing code, using
      non-guessable IDs) and information (checking permissions in Preparation
      before displaying data), so that only authorized users can perform actions
      or view data.
    subtopics:
    - name: Protect block parameters
      id: protect-block-parameters
      description: Wants to keep GetUserId and other identity values out of client
        Block widget parameters so that client-side parameter changes cannot
        impersonate another user or undermine server-side role checks when the
        block calls backend actions or queries.
    - name: Secure GetUserId use
      id: secure-getuserid-use
      description: Wants to keep GetUserId and other identity values out of
        client-side action parameters so server-side logic retrieves identity on
        the server, preventing tampering and impersonation.
  - name: Content security policy
    id: content-security-policy
    subtopics:
    - name: CSP directive impacts
      id: csp-directive-impacts
      description: Wants to understand the impacts of disabling the unsafe-eval and unsafe-
        inline CSP directives via Factory Configuration settings, covering
        effects on client-side components and widgets that rely on eval() or
        inline scripts/styles in Reactive Web Apps on OutSystems 11, so that
        they can identify what breaks and how to fix it before disabling these
        directives.
    - name: Configure CSP for mobile app iframes
      id: configure-csp-mobile-app-iframes
      description: Wants to configure Content Security Policy directives with the explicit
        https:// protocol and the correct frame-ancestors values, including
        adding the outsystems:// scheme alongside https:// when iframe content
        works on Android but is blocked on iOS, so that CSP-protected
        resources and iframes load successfully in native mobile apps.
    - name: CSP mobile app propagation
      id: csp-mobile-app-propagation
      description: Wants to understand how activating, updating, or
        deactivating a CSP for a stage propagates to already-installed ODC
        mobile apps, including that no new build is required, changes are
        delivered via a native caching system over HTTP headers, a bundled
        fallback policy applies if the app launches offline right after
        install, and updates are fetched the next time the app is online, so
        that they can predict when CSP changes take effect for existing
        installs.
    - name: CSP misconfiguration risks
      id: csp-misconfiguration-risks
      description: Wants to understand common CSP misconfiguration risks, including missing
        policies breaking functionality, overly permissive wildcard sources
        enabling malicious redirects, omitting the explicit https:// protocol,
        and exceeding the per-stage directive character limit, so that they can
        plan and test a CSP configuration that's neither too restrictive nor too
        permissive.
    - name: Configure CSP in O11
      id: configure-csp-o11
      description: Wants to configure Content Security Policy in OutSystems 11 by enabling
        CSP in LifeTime or Service Center, setting directive values at the
        environment or app level, republishing affected apps after changes,
        and monitoring blocked resources, so that approved sources load
        correctly and the policy takes effect as expected.
    - name: Activate CSP for a stage
      id: activate-csp-stage
      description: Wants to activate a Content Security Policy for a stage
        in ODC Portal by toggling CSP status to active, requiring CSP
        Management permissions, so that default restrictive directives are
        automatically applied to protect all apps in that stage.
    - name: Configure CSP directive values
      id: configure-csp-directive-values
      description: Wants to customize CSP directive values, or reset them
        to defaults, in ODC Portal to match their app's specific external
        service dependencies, plugins, and resources, so that the CSP
        allow list permits necessary domains while blocking everything
        else.
    - name: CSP default and required values
      id: csp-default-required-values
      description: Wants to understand what each CSP default value and required value does
        and the impact of removing them, so that they can safely customize their
        app's CSP without breaking functionality.
  - name: Security fundamentals
    id: security-fundamentals
    subtopics:
    - name: CIA security triad
      id: cia-security-triad
      description: Wants to understand the CIA (Confidentiality, Integrity,
        Availability) security triangle model, including the complementary
        concepts of authenticity and non-repudiation, so that they can
        reason about the core pillars used to guide an organization's
        information security policies.
    - name: OWASP top 10 lists
      id: owasp-top-10-lists
      description: Wants to find the major security vulnerabilities
        affecting web and mobile applications by consulting OWASP's
        periodically published Top 10 Web Application Security Risks and
        Top 10 Mobile Risks lists, so that they can use a recognized
        vulnerability baseline when assessing and securing their
        applications.
    - name: Design-time security warnings overview
      id: design-time-security-warnings-overview
      description: Wants to understand which security warnings OutSystems
        surfaces at design time in Service Studio, and what kinds of issues
        those warnings help catch before deployment, so that they can evaluate
        whether the built-in guidance covers their team's secure coding
        baseline.
- name: ODC Workflows
  guid: 8f3c2a1d-6e4b-4a7f-9c0d-2b5e7f1a4c9e
  topics:
  - name: Workflow editor
    id: workflow-editor
    subtopics:
    - name: Configure workflow start
      id: configure-workflow-start
      description: Wants to configure how a workflow begins execution by assigning
        a triggering event to the Start node in ODC's Workflow editor, so that
        the workflow launches automatically when that event occurs.
    - name: Configure conditional start
      id: configure-conditional-start-odc
      description: Wants to add a Conditional Start activity to an existing
        ODC workflow and link it to a business event in ODC's Workflow editor,
        so that the workflow triggers only when that event occurs.
    - name: Workflow instance labels
      id: workflow-instance-labels
      description: Wants to create a custom instance label expression on the Start node,
        using the start event's input parameters, built-in runtime properties
        for identifying the instance, and built-in functions, in ODC's Workflow
        editor, so that workflow instances are easier to identify and
        troubleshoot in logs and the Portal.
    - name: Workflow built-in functions
      id: workflow-built-in-functions
      description: Wants to know which built-in function groups are available in ODC
        workflow expressions, so they can choose expressions that work in
        workflows.
    - name: Add decision activity
      id: add-decision-activity
      description: Wants to add a Decision activity to an ODC workflow, defining
        conditions on the outcomes to route execution down different paths
        based on data values, in ODC's Workflow editor, so that the workflow
        follows the correct path for each case.
    - name: Configure automatic path
      id: configure-automatic-path
      description: Wants to configure an automatic path in an ODC workflow
        using automatic activities to run service actions and send
        notifications, including when continuing from a Decision outcome or
        after a conditional start event, in ODC's Workflow editor, so that the
        process completes without manual intervention when conditions are met.
    - name: Parallel workflow tasks
      id: parallel-workflow-tasks
      description: Wants to run multiple workflow tasks in parallel by
        adding parallel branches in ODC's Workflow editor, so that
        independent activities execute at the same time.
    - name: Add human activity
      id: add-human-activity
      description: Wants to add a Human activity to an ODC workflow in the
        Workflow editor, configuring assignment, destination screen, display
        message, close conditions, and timeout so the workflow can pause for
        manual input and continue when the activity completes.
    - name: Human activity states
      id: human-activity-states
      description: Wants to understand the human activity statuses in an ODC
        workflow and how HumanActivityOpen, HumanActivityAssign,
        HumanActivityRelease, ProcessTerminate, and close events change the
        node between Running, Waiting, Open, Completed, Terminated, and Error
        states, so they can predict runtime transitions.
    - name: Manual task notifications
      id: manual-task-notifications
      description: Wants to build custom notifications for pending human
        activities so users know when a manual workflow task needs attention.
    - name: Human activity SLA
      id: human-activity-sla
      description: Wants to configure an SLA for a human activity in an ODC
        workflow so the task must complete within a defined timeframe.
    - name: Pause for manual review
      id: pause-for-manual-review
      description: Wants to pause a workflow with a Wait activity until a
        business event occurs, then hand off to a Human activity assigned to
        a role that closes on another event, in ODC's Workflow editor, so that
        a person can complete a required task before the workflow continues.
    - name: Add wait activity
      id: add-wait-activity
      description: Wants to add a Wait activity to an ODC workflow in the
        Workflow editor and configure its close event, conditions, and
        timeout so the workflow pauses until the wait ends.
    - name: Create a workflow
      id: create-workflow
      description: Wants to implement a workflow in ODC Portal by creating it
        in the workflow editor, giving it a unique name, selecting the event
        that triggers the Start node, adding steps through to the End node,
        and publishing it, so that the workflow is ready to run in the app.
    - name: End workflow
      id: end-workflow
      description: Wants to understand how an ODC workflow finishes when
        execution reaches the End node, including how conditional start
        flows must also reach the End node before the instance status changes
        to Done.
    - name: Terminate workflow
      id: terminate-workflow
      description: Wants to understand how a Terminate node stops an ODC
        workflow execution, including how ongoing automatic activities finish
        before the instance status changes to Done.
    - name: Search workflow editor
      id: search-workflow-editor
      description: Wants to open the workflow editor's search menu using
        Ctrl+K (or Cmd+K on a Mac) to search and execute commands, find
        flow-steps or properties, edit expressions and outcomes, or add new
        flow-steps, so that they can navigate and edit complex workflows
        faster.
    - name: Go to flow step
      id: go-to-flow-step
      description: Wants to use a Go to a flow step in an ODC workflow to loop back to a
        previous node and understand where it is valid and which variables
        remain available after the jump, so that they can model controlled loops
        without losing required data.
    - name: Manage workflow errors
      id: manage-workflow-errors
      description: Wants to identify, locate, and resolve issues in a
        workflow using the workflow editor's notification panel, by
        reviewing the error count, searching and filtering errors by
        activity, property, or message, and using one-click navigation to
        jump to the affected node and property, so that they can debug
        workflow errors efficiently.
    - name: Manage workflow history
      id: manage-workflow-history
      description: Wants to view the full history of published workflow revisions, and
        discard unpublished changes or restore the workflow to any previous
        published revision, in ODC's Workflow editor, so that they can track and
        recover prior states of a workflow.
  - name: Test a workflow
    id: test-workflow
    description: Wants to verify a workflow works as expected before deploying
      it, by triggering its execution from the app and reviewing instance
      details and errors in the Instances tab of the ODC Portal, so that
      issues are caught before the workflow reaches production.
  - name: Deploy a workflow
    id: deploy-workflow
    description: Wants to deploy a workflow and its app to the QA stage and
      then to the Production stage, understanding that an impact analysis
      runs in the background before each deployment, so that the workflow
      becomes available to end users.
  - name: Workflow activity filters
    id: workflow-activity-filters
    description: Wants to create an aggregate that filters workflow activity
      instances by process and activity definition keys so they can build
      dashboards or task boxes that show only the relevant workflow work.
- name: Advanced workflows
  guid: 7a84e09e-c48c-404f-8d9f-728b9f78c88c
  topics:
  - name: Using callbacks
    id: using-callbacks
  - name: Best practices for scalable processes
    id: scalable-process-best-practices
  - name: Extend processes (process entities, APIs, reports)
    id: extend-processes-entities
  - name: Event driven processes
    id: event-driven-processes
  - name: Case Management Framework
    id: case-management-framework
    subtopics:
    - name: CMf overview
      id: cmf-overview
      description: Wants to understand what the Case Management Framework is, how it
        extends Business Process Technology, and its core features, in
        OutSystems 11, so that they can decide whether it fits their case
        management app.
    - name: CMf app examples
      id: cmf-app-examples
      description: Wants to understand the Simple, Medium, and Complex
        use-case categories for Case Management Framework apps, with example
        scenarios for each (e.g. approvals, support cases, claims handling),
        in OutSystems 11, so that they can gauge the complexity and scope of
        their own case management app.
    - name: Understand subcases
      id: understand-subcases
      description: Wants to understand what a subcase is, how it relates to a
        parent case via the CaseRelation entity, and when to use one to
        abstract a complex set of steps from the parent case, in OutSystems
        11's Case Management Framework, so that they can decide whether to
        model part of their process as a subcase.
    - name: Track subcase completion
      id: track-subcase-completion
      description: Wants to track subcase completion from the parent case's
        process by adding a Wait node with a relevant timeout and an On Close
        callback action to the parent's Business Process Technology flow, in
        OutSystems 11's Case Management Framework, so that the parent case can
        react once the subcase finishes.
    - name: Bootstrap CMF app
      id: bootstrap-cmf-app
      description: Wants to create the foundational modules of a case management app
        (core services, workflow, and optional UI) using the Case Management Framework
        in OutSystems 11, so that the basic app structure is in place and ready for
        further case development.
    - name: Bootstrap case config
      id: bootstrap-case-config
      description: Wants to create a bootstrapping timer action that registers the
        case definition and case status definitions into the Case Management Framework
        every time the core services module is published in OutSystems 11, so that
        the configuration is automatically kept in sync with the module.
    - name: Define case status
      id: define-case-status
      description: Wants to define a new case status by adding a record to the
        CaseStatusConfiguration static entity in the core services module in
        OutSystems 11, so that the bootstrapping action can register it with the
        case definition and it becomes available for assignment to case instances.
    - name: Enable CMF milestones
      id: enable-cmf-milestones
      description: Wants to enable milestone definitions for a case definition
        by creating a MilestoneConfiguration static entity with the required
        attributes and adapting the Bootstrap_CaseConfiguration timer action in
        the core services module in OutSystems 11, so that milestone definitions
        are fetched and included in the case definition's setup data.
    - name: Add CMF milestones
      id: add-cmf-milestones
      description: Wants to add milestone definitions and associate them with a case
        definition by creating records with the required attributes in the
        MilestoneConfiguration static entity in OutSystems 11, so that the
        Bootstrap_CaseConfiguration timer automatically creates milestone
        instances for new case instances.
    - name: Update case status
      id: update-case-status
      description: Wants to update the status of an existing case instance using
        the Case_UpdateStatus action from the CaseServices_API module in OutSystems
        11, so that a case moves to a new valid status when the required conditions
        (valid case ID, valid status, valid state-machine transition) are met.
    - name: Manage CMF delegation
      id: manage-cmf-delegation
      description: Wants to enable users to create, update, or delete a case or
        activity delegation in OutSystems 11 using the Delegation_Create,
        Delegation_CreateOrUpdate, Delegation_Update, and Delegation_Delete actions
        in the CaseConfigurations_API module, so that work can be reassigned to
        another user either for a set time period or indefinitely.
    - name: Use CMF delegation
      id: use-cmf-delegation
      description: Wants to validate an active delegation, retrieve existing
        delegations by ID or user, and correctly pass the DelegatedBy attribute
        when calling case and activity actions in OutSystems 11 using the
        CaseServices_API module, so that delegated actions are properly authorized
        and logged in case history.
    - name: Understand GroupExtended
      id: understand-group-extended
      description: Wants to understand what the GroupExtended entity is in the
        Case Management Framework in OutSystems 11, including how it extends the
        Group system entity, how it supports assigning activities to groups,
        managing group-based case permissions, defining group hierarchies, and
        associating calendars, so that they can decide how to model user groups
        in their case management app.
    - name: Bootstrap CMF groups
      id: bootstrap-cmf-groups
      description: Wants to set up extended case management groups by creating
        a GroupExtendedConfiguration static entity, a Bootstrap_Groups timer,
        and wiring the GroupExtended_CreateOrUpdate action in OutSystems 11,
        and then add group records to the configuration module, so that groups
        are registered in the Group and GroupExtended entities when the module
        is published.
    - name: List case activities
      id: list-case-activities
      description: Wants to retrieve a list of activities assigned to a logged-in
        case worker by adding the Case_GetActivities action and related static
        entity dependencies to the UI module and wiring a Data Action in
        OutSystems 11, so that the activity list can be used as the data source
        for a Table or List widget.
    - name: Enrich activity list
      id: enrich-activity-list
      description: Wants to enrich an activity list with case business entity
        data by modifying the Data Action output parameter to a Record list and
        iterating with a For Each, Aggregate, and ListAppend in OutSystems 11,
        so that case workers can see supplementary case information (such as a
        support ticket subject) alongside each activity.
    - name: List requester cases
      id: list-requester-cases
      description: Wants to retrieve a list of cases created by a logged-in
        requester by adding the Case_GetCases action and RequesterScope static
        entity dependencies to the UI module and wiring a Data Action in
        OutSystems 11, so that the case list can be used as the data source for
        a Table or List widget.
    - name: Enrich requester case list
      id: enrich-requester-case-list
      description: Wants to enrich a requester case list with case business
        entity data by modifying the Data Action output parameter to a Record
        list and iterating with a For Each, Aggregate, and ListAppend in
        OutSystems 11, so that requesters can see supplementary case information
        (such as a support ticket subject) alongside each case they created.
    - name: Translate CMF entities
      id: translate-cmf-entities
      description: Wants to add locales to the Data tab and translate the CMF
        static entities (CaseDefinitionConfiguration, CaseStatusConfiguration,
        Milestones, CaseActions) in the core services module in OutSystems 11,
        so that the Case Management Framework app displays text in the correct
        supported language for end users.
    - name: Enable CMF locale
      id: enable-cmf-locale
      description: Wants to create a SetupCaseConfiguration_Locale server action
        that calls SetCurrentLocale and appends translation records for the case
        definition and case status static entities in OutSystems 11, so that the
        case management app resolves labels in the correct language for a given
        locale at runtime.
    - name: Adapt CMF bootstrap
      id: adapt-cmf-bootstrap
      description: Wants to call the SetupCaseConfiguration_Locale action once
        per target locale in the Bootstrap_CaseConfiguration timer action,
        chaining each call's SetupData output as the next call's input in
        OutSystems 11, so that all desired locale translations are registered
        every time the core services module is published.
    - name: Understand rules engine
      id: understand-rules-engine
      description: Wants to understand the four components of the rules engine
        in the Case Management Framework (Rule, Rule Group, Rule Line, Rule
        Group Output), how they relate to each other, and what result types a
        rule can return, in OutSystems 11, so that they can model business rules
        correctly before setting them up.
    - name: Set up CMF rule
      id: set-up-cmf-rule
      description: Wants to set up a rule in the Case Management Framework by
        creating the Rule, Rule Group, Rule Line, and Rule Group Output records
        using the CaseServices_API in OutSystems 11, so that a business rule is
        ready to be evaluated in a case management application.
    - name: Evaluate CMF rule
      id: evaluate-cmf-rule
      description: Wants to evaluate a rule in the Case Management Framework by
        calling the execution actions in the CaseServices_API Rules Engine folder
        in OutSystems 11, so that the rule result can be used in a case management
        application's logic.
    - name: Fix status mismatch
      id: fix-status-mismatch
      description: 'Wants to fix the "Both statuses must belong to the same
        Case Definition." error by making sure the CaseStatusId and
        NextCaseStatusId input parameters reference case statuses that
        belong to the same case definition, in OutSystems 11, so that the
        Case Management Framework can successfully create or update the
        case state machine.'
    - name: Fix duplicate transition
      id: fix-duplicate-transition
      description: 'Wants to fix the "We found more than one record for this
        transition" error by deactivating the duplicate CaseStateMachine
        record for a given CaseStatusId and NextCaseStatusId pair using the
        CaseStateMachine_Update action in OutSystems 11, so that the Case
        Management Framework can execute CaseStateMachine_GetByTransition
        successfully.'
    - name: Fix duplicate access grant
      id: fix-duplicate-access-grant
      description: 'Wants to fix the "Access permissions for that Case Definition
        have already been granted to that user/group" error by verifying the
        input parameters passed to the Case Management Framework''s access-granting
        action using Service Studio''s Debugger in OutSystems 11, so that the
        action isn''t called again for a user/group that already has access to
        that case definition.'
    - name: Fix CMF email destination
      id: fix-cmf-email-destination
      description: 'Wants to fix the "Set a destination to send the email" error
        by filling in at least one of the To, Cc, Bcc, ToGroupId, CcGroupId, or
        BccGroupId inputs before calling Email_Send in the Case Management
        Framework in OutSystems 11, so that the framework can successfully
        execute the action.'
    - name: Fix rule data type
      id: fix-rule-data-type
      description: 'Wants to fix the "There was an error processing this rule
        expression. <data-type> can''t be compared with <data-type>" error by
        rewriting the rule so its left and right attribute data types are
        comparable in the Case Management Framework in OutSystems 11, so that
        the rule executes successfully.'
    - name: Fix rule output type
      id: fix-rule-output-type
      description: 'Wants to fix the "The expected rule output type does not
        match this rule type" error by updating the RuleTypeId input value to
        match the configured rule type using the Rule_CreateOrUpdate action in
        the CaseConfigurations_API module in OutSystems 11, so that the Case
        Management Framework can successfully execute the rule.'
    - name: Fix activity action ID
      id: fix-activity-action-id
      description: 'Wants to fix the "Unknown activity action. It must match one
        from ActivityAction." error by verifying the ActivityActionId input
        value against the ActivityAction static entity in the
        CM_ProcessServices_BL module using Service Studio''s Debugger before
        calling Case_PerformActionActivity in the Case Management Framework in
        OutSystems 11, so that the action executes successfully.'
    - name: Fix case purge error
      id: fix-case-purge-error
      description: 'Wants to fix the "Case Definition with the Id
        <case-definition-identifier-value> can''t be purged. Change the
        PurgeTypeId to allow it." error by updating the case definition''s
        PurgeTypeId, DaysToPurgeCase, and DaysToPurgeProcess attributes using
        the CaseDefinition_Update action from the CaseConfigurations_API
        module, or by filling in the missing attributes in the Bootstrap/Setup
        input structures, in OutSystems 11, so that the Case Management
        framework can successfully execute the CaseDefinition_Purge action.'
    - name: Fix activity case mismatch
      id: fix-activity-case-mismatch
      description: 'Wants to fix the "Activity with Id
        <activity-identifier-value> does not belong to Case with Id
        <case-identifier-value>" error by verifying the Activity Identifier
        input value belongs to a process associated with the given case using
        Service Studio''s Debugger before calling Case_DiscardActivity in the
        Case Management Framework in OutSystems 11, so that the action
        executes successfully.'
    - name: Fix invalid GUID
      id: fix-invalid-guid
      description: 'Wants to fix the "<record> Id is not a valid GUID:
        <input-parameter-value>" error by verifying the input parameter''s
        value is a properly formatted GUID using Service Studio''s Debugger
        in OutSystems 11, so that the Case Management Framework action
        executes successfully.'
    - name: Fix access flag error
      id: fix-access-flag-error
      description: 'Wants to fix the "Either CanView or CanEdit must be set to
        True" error by setting at least one of the CanView or CanEdit input
        parameters to True before calling the DEPRECATED_Case_AddAccessToGroup
        or DEPRECATED_Case_AddAccessToUser action, verifying the values with
        Service Studio''s Debugger, in OutSystems 11, so that the Case
        Management Framework action executes successfully.'
    - name: Fix CMF email record
      id: fix-cmf-email-record
      description: 'Wants to fix the "We couldn''t retrieve the record
        <case-identifier> of object <entity-name>" error by verifying the
        case-identifier value passed to Email_ProcessPlaceholders or
        Email_Send using Service Studio''s Debugger in OutSystems 11, so
        that the Case Management Framework can find the matching record and
        execute the action successfully.'
    - name: Fix CMF no entities
      id: fix-cmf-no-entities
      description: 'Wants to fix the "We couldn''t find any entities." error
        by verifying the input values passed to the GetBusinessEntityAttributes,
        Email_ProcessPlaceholders, or Email_Send actions using Service Studio''s
        Debugger in OutSystems 11, so that the Case Management Framework can
        find the entities associated with the case definition and execute the
        action successfully.'
    - name: Fix duplicate case status
      id: fix-duplicate-case-status
      description: 'Wants to fix the "There''s already an active Case Status
        defined as initial" error by setting the existing initial case
        status''s IsInitial attribute to False, or deleting it using the
        CaseStatus_Delete action of the CaseConfigurations_API module, in
        OutSystems 11, so that the Case Management Framework can successfully
        create or update the new initial case status.'
    - name: Fix duplicate record
      id: fix-duplicate-record
      description: 'Wants to fix the "<record/attribute> already exists for
        that <record/attribute>" error by verifying the input parameter
        values passed to a Case Management Framework create or update action
        using Service Studio''s Debugger in OutSystems 11, so that the
        action doesn''t attempt to create a duplicate record.'
    - name: Fix initial status error
      id: fix-initial-status-error
      description: 'Wants to fix the "You can only have one initial case
        status" error, which occurs when more than one case status record
        has its IsInitial attribute set to True, by ensuring only one case
        status record has IsInitial set to True before running the
        Bootstrap/Setup actions in the Case Management Framework in
        OutSystems 11, so that the framework can successfully execute the
        action.'
    - name: Fix no initial status
      id: fix-no-initial-status
      description: 'Wants to fix the "The Case Definition must have an
        initial status defined" error by filling in the CaseStatusId input
        parameter of Case_Initialize with a case status that has IsInitial
        set to True, creating or updating one with the
        CaseStatus_CreateOrUpdate action from the CaseConfigurations_API
        module if needed, in OutSystems 11, so that the Case Management
        Framework can successfully initialize the case instance.'
    - name: Fix bootstrap GUID error
      id: fix-bootstrap-guid-error
      description: 'Wants to fix the "One or more <record> Ids aren''t valid
        GUIDs" error by verifying that the Identifier attribute of all
        records matches a valid GUID format before running the
        Bootstrap/Setup actions in the Case Management Framework in
        OutSystems 11, so that the framework can successfully execute the
        action.'
    - name: Fix requester conflict
      id: fix-requester-conflict
      description: 'Wants to fix the "ExternalRequesterDetails and
        CaseRequesterUserId input parameters cannot be both filled in" error
        by filling in only the CaseRequesterUserId input parameter when the
        requester is an internal user, or only the ExternalRequesterDetails
        input structure when the requester is external, before calling
        Case_Initialize, verifying the values with Service Studio''s
        Debugger, in OutSystems 11, so that the Case Management Framework
        can successfully execute the action.'
    - name: Fix activity access error
      id: fix-activity-access-error
      description: 'Wants to fix the "It looks like you don''t have access to
        the activity" error by verifying the user is assigned to the
        activity, belongs to the activity''s group, or has a valid
        delegation, using the Activity_ValidateUserAccess action of the
        CaseServices_API module, and passing the DelegatedBy input parameter
        when acting under delegation, in OutSystems 11, so that the Case
        Management Framework can successfully execute the action.'
    - name: Fix close activity error
      id: fix-close-activity-error
      description: 'Wants to fix the "The activity couldn''t be closed, either
        because you''re missing the required access or it was already
        closed." error by verifying the user has access to the activity
        using the Activity_ValidateUserAccess action of the CaseServices_API
        module, and passing the DelegatedBy input parameter when acting
        under delegation, before calling Case_CloseActivity in the Case
        Management Framework in OutSystems 11, so that the action executes
        successfully.'
    - name: Fix reference mismatch
      id: fix-reference-mismatch
      description: 'Wants to fix the "Not all <records> reference the same
        <record>. Change them accordingly." error by making sure the
        CaseDefinitionId attribute of all records passed as input to a
        Bootstrap action reference the same case definition, in OutSystems
        11, so that the Case Management Framework can successfully execute
        the action.'
    - name: Fix rule processing error
      id: fix-rule-processing-error
      description: 'Wants to resolve the generic "There was an error
        processing rule <rule-identifier>. <error-message>" message raised
        when the Case Management Framework''s rules engine fails to execute
        a rule, by checking the documentation for the specific
        <error-message> code shown, in OutSystems 11, so that the correct
        underlying fix can be applied.'
    - name: Fix missing rule config
      id: fix-missing-rule-config
      description: 'Wants to fix the "This rule isn''t configured or doesn''t
        exist" error by configuring the rule using the
        RuleGroup_CreateOrUpdate action from the CaseConfigurations_API
        module before executing it, in OutSystems 11, so that the Case
        Management Framework can successfully execute the action.'
    - name: Fix FromDate ToDate error
      id: fix-fromdate-todate-error
      description: 'Wants to fix the "FromDate must be set to a date before
        ToDate." error by setting the FromDate input parameter to a date
        earlier than the ToDate input parameter before calling
        Delegation_Create or Delegation_Update, verifying the values with
        Service Studio''s Debugger, in OutSystems 11, so that the Case
        Management Framework can successfully execute the action.'
    - name: Fix delegation group mismatch
      id: fix-delegation-group-mismatch
      description: 'Wants to fix the "The user you are trying to delegate
        from is not a member of the selected group." error by ensuring
        the FromUserId input parameter belongs to a user who is a member
        of the FromGroupId group before calling Delegation_Create or
        Delegation_Update, verifying the values with Service Studio''s
        Debugger, in OutSystems 11, so that the Case Management Framework
        can successfully execute the action.'
    - name: Fix unassigned activity error
      id: fix-unassigned-activity-error
      description: 'Wants to fix the "The activity must be assigned to a
        user before it can be released" error by assigning the activity
        to a user using the Activity_AssignToUser action from the
        CaseServices_API module before calling Case_ReleaseActivity, in
        OutSystems 11, so that the Case Management Framework can
        successfully execute the action.'
    - name: Fix takeover access error
      id: fix-takeover-access-error
      description: 'Wants to fix the "Couldn''t complete the Takeover. Either
        the user is missing access or the activity isn''t assigned to
        anyone." error by verifying the user has access to the activity
        using the Activity_ValidateUserAccess action, or assigning the
        activity to a user using the Activity_AssignToUser action, both
        from the CaseServices_API module, before calling
        Case_TakeoverActivity in the Case Management Framework in
        OutSystems 11, so that the action executes successfully.'
    - name: Configure case SLA
      id: configure-case-sla
      description: Wants to set and enforce a resolution/response time limit
        on a case, using the GenerateEvent, OverrideMinutesForCompletion, and
        CalendarId input parameters of the Case_AssociateProcess action, in
        OutSystems 11's Case Management Framework, so that an event fires
        when the case's SLA time limit is exceeded.
    - name: Set default case SLA
      id: set-default-case-sla
      description: Wants to set a default SLA response time for every new
        case instance by creating a ProcessSLAConfiguration static entity
        and wiring the ProcessDefinitionSLA_CreateOrUpdate action into the
        Bootstrap_CaseConfigurations timer, in OutSystems 11's Case
        Management Framework, so that the SLA starts counting automatically
        from case creation.
    - name: Act on overdue case SLA
      id: act-on-overdue-case-sla
      description: Wants to trigger custom logic (such as terminating the
        process or completing the case) when a case's SLA time limit is
        exceeded, by adding a Conditional Start based on the SLAOverdue
        ProcessEventType, in OutSystems 11's Case Management Framework, so
        that overdue cases are automatically handled.
    - name: Process still running
      id: process-still-running
      description: 'Wants to understand why a BPT process stays open with
        "The process is still running" error when it has pending or running
        events, caused by the Wait activity''s auto-retry after an
        unhandled exception, in OutSystems 11''s Case Management Framework,
        so that they can confirm no action is required.'
    - name: Fix invalid input
      id: fix-invalid-input
      description: 'Wants to fix the "The <input-parameter> you''ve entered
        is either incorrect or null." error by verifying the input
        parameter''s value using Service Studio''s Debugger before
        executing the action, in OutSystems 11''s Case Management
        Framework, so that the action executes successfully.'
    - name: Fix tag case mismatch
      id: fix-tag-case-mismatch
      description: 'Wants to fix the "The selected Tag and Case belong to
        different Case Definitions." error by verifying the Tag and Case
        input values passed to CaseTag_Create belong to the same case
        definition using Service Studio''s Debugger, in OutSystems 11''s
        Case Management Framework, so that the action executes
        successfully.'
    - name: Fix record not found
      id: fix-record-not-found
      description: 'Wants to fix the "<record> wasn''t found" error by
        verifying the input parameter''s value using Service Studio''s
        Debugger and confirming the referenced entity record exists,
        creating it if it doesn''t, in OutSystems 11, so that the Case
        Management Framework action executes successfully.'
    - name: Fix duplicate ID error
      id: fix-duplicate-id
      description: 'Wants to fix the "<record> with the Id
        "<input-parameter-value>" already exists." error by verifying the
        input parameter''s value is correct using Service Studio''s
        Debugger, and changing a create action to its update or
        create-or-update counterpart if appropriate, in OutSystems 11''s
        Case Management Framework, so that the action executes
        successfully.'
    - name: Fix duplicate tag
      id: fix-duplicate-tag
      description: 'Wants to fix the "This combination of Case and Tag already
        exists." error by verifying the Tag input parameter passed to
        CaseTag_Create doesn''t already correspond to a tag linked to the
        case, using Service Studio''s Debugger, in OutSystems 11''s Case
        Management Framework, so that the action executes successfully.'
    - name: Fix working hours error
      id: fix-working-hours-error
      description: 'Wants to fix the "WorkingHourStart must be set to a time
        before WorkingHourEnd." error by setting the WorkingHourStart input
        parameter to a time earlier than the WorkingHourEnd input parameter
        before calling Calendar_Create or Calendar_Update, verifying the
        values with Service Studio''s Debugger, in OutSystems 11, so that
        the Case Management Framework can successfully execute the action.'
    - name: Fix negative minutes
      id: fix-negative-minutes
      description: 'Wants to fix the "The number of minutes must be greater
        than or equal to zero." error by setting the Minutes input parameter
        to a value equal to or greater than zero before calling
        Calendar_GetEndDate, verifying the value with Service Studio''s
        Debugger, in OutSystems 11, so that the Case Management Framework
        can successfully execute the action.'
    - name: Fix unsupported database
      id: fix-unsupported-database
      description: Wants to fix the "We're sorry, but this database isn't supported." error
        by using a database platform that supports advanced queries for the
        environment before executing an action that requires an advance query,
        in OutSystems 11's Case Management Framework, so that the action
        executes successfully.
    - name: Fix milestone achieved
      id: fix-milestone-achieved
      description: 'Wants to fix the "The milestone has already been
        achieved." error by verifying the milestone input parameter passed
        to Milestone_SetAchieved isn''t already set to the achieved status,
        using Service Studio''s Debugger, in OutSystems 11''s Case
        Management Framework, so that the action executes successfully.'
    - name: Fix milestone pairing
      id: fix-milestone-pairing
      description: 'Wants to fix the "We couldn''t find a pairing for
        <case/case definition> and Milestone Definition." error by
        verifying the case/case definition and milestone definition
        identifier values passed to Case_IsMilestoneAchieved or
        CaseDefinition_UnlinkFromMilestoneDefinition using Service
        Studio''s Debugger, in OutSystems 11, so that the Case Management
        Framework can find the matching record and execute the action
        successfully.'
- name: App Configuration Management
  guid: 22a71e1c-da01-468c-9633-29c791104e7e
  topics:
  - name: App configuration
    id: app-configuration
    subtopics:
    - name: Configure app settings after a deployment
      id: config-app-after-deploy
    - name: App settings
      id: app-settings
    - name: Set app setting secret
      id: set-app-setting-secret
      description: Wants to mark or unmark an app setting as secret in ODC
        Studio and configure its per-stage value in the ODC Portal, so that
        sensitive setting values stay encrypted and hidden, understanding
        that reverting to non-secret clears the value on publish.
    - name: Configure app properties
      id: configure-app-properties
      description: Wants to configure an app, library, or mobile library's general
        properties in ODC Studio, covering identity/branding details and
        advanced behavior settings, so that the app looks and behaves as
        intended.
    - name: Customize validation messages
      id: customize-validation-messages
      description: Wants to customize the validation and upgrade messages shown to
        end users of an app, library, or mobile library, by editing the Messages
        settings page in ODC Studio, so that error and upgrade notifications match
        the app's language, tone, or branding requirements.
    - name: Configure app settings before a deployment
      id: config-app-before-deploy
    - name: Trace query origin
      id: trace-query-origin
      description: Wants to trace a slow SQL query generated by OutSystems back
        to the exact module, screen/action, and Aggregate or SQL element that
        produced it, by enabling the "Add Query Origin to Generated SQL" setting
        in the Factory Configuration component and republishing affected modules,
        in OutSystems 11, so that DBAs can inform developers of the precise
        location of an underperforming query.
    - name: Configure SQL query timeout
      id: configure-sql-query-timeout
      description: Wants to configure the platform-wide Default Query Timeout in
        the OutSystems 11 Configuration Tool, so that queries without an individual
        timeout setting fail only after an appropriate amount of database execution
        time.
    - name: Configure application request timeout
      id: configure-application-request-timeout
      description: Wants to configure the application request timeout in OutSystems
        11 by setting the `executionTimeout` parameter under `httpRuntime` in
        machine.config, so that IIS allows a Traditional Web request to run for an
        appropriate amount of time before aborting it.
    - name: Configure web service timeout
      id: configure-web-service-timeout
      description: Wants to configure the `Timeout in Seconds` property for a
        consumed web service method in OutSystems 11, so that the app waits an
        appropriate amount of time for an external service response before timing
        out.
    - name: Configure server request timeout
      id: configure-server-request-timeout
      description: Wants to configure the `Server Request Timeout` property for
        Reactive Web or Mobile app server calls in OutSystems 11, so that the client
        waits an appropriate amount of time for Data Actions, Server Actions, and
        Aggregates before triggering a Communication Exception.
    - name: Increase upload limit
      id: increase-upload-limit
      description: Wants to increase the maximum file upload size allowed by an
        OutSystems 11 application, by creating a Factory Configuration shared
        configuration that sets the maxRequestLength and maxAllowedContentLength
        web.config parameters and associating it with the affected eSpaces, so
        that large file uploads no longer fail with a "Maximum request length
        exceeded" error.
    - name: Factory Configuration overview
      id: factory-configuration-overview
      description: Wants to understand what the Factory Configuration Forge plugin is
        and how to install it from Forge for OutSystems 11, so that they can use it
        to manage web.config shared configurations and platform-level settings across
        their Service Center Factory.
    - name: Use settings in logic
      id: use-settings-in-logic
      description: Wants to reference an app setting's value in server-side
        logic (server actions) and client-side logic (client actions) in
        ODC Studio, using a Settings expression directly on the server and
        a wrapper server action for the client, so that hardcoded values
        are avoided and secret settings stay on the server.
    - name: Shared configurations
      id: factory-configuration-shared-configs
      description: Wants to create shared configurations in Factory Configuration using
        XSLT templates (from scratch or pre-made samples) and associate them with eSpaces
        in Service Center, so that web.config customizations persist across module
        deployments in OutSystems 11.
    - name: Platform configurations
      id: factory-configuration-platform-configs
      description: Wants to understand which internal platform configurations are available
        in Factory Configuration's Platform Configurations tab (such as brute force
        protection, slow query threshold, and remember login settings) and what database
        parameters they control, so that they can tune platform-wide behavior in
        OutSystems 11.
    - name: JSON Serialize consistency opt-in
      id: json-serialize-consistency-optin
      description: Wants to enable the "Enable Json Serialize consistency between
        Client and Server side" opt-in in Factory Configuration's Platform
        Configuration settings, requiring Platform Server 11.17.0 and Factory
        Configuration 11.0, so that JSON Serialize actions produce consistent
        null and default-value output between client and server, understanding
        this is a breaking change that requires republishing apps and updating
        any existing workaround code.
    - name: Disable role error logs
      id: disable-role-error-logs
      description: Wants to stop Registered Role Required error logs from being
        generated when expired sessions in reactive or mobile apps trigger server
        requests, by installing the Factory Configuration Forge component and
        unchecking the Enable Registered Role Required error logs option in the
        Platform Configurations tab, in OutSystems 11 Platform Server 11.21.0 and
        newer, so they can eliminate this specific log noise without suppressing
        other relevant exceptions.
  - name: Runbooks for app configuration management
    id: runbook-config-mgmt
    subtopics:
    - name: Manual runbooks for app configuration
      id: manual-runbook-config
  - name: Automated runbooks for app configuration management
    id: automated-runbook-mgmt
    subtopics:
    - name: Automated runbooks for app configuration
      id: automated-runbook-config
- name: App architecture
  guid: 6c1b124d-3ad6-456a-926c-0a843667cd0a
  topics:
  - name: Refactoring
    id: refactoring
    subtopics:
    - name: Convert to library
      id: convert-to-library
      description: Wants to convert an existing Mobile or Reactive module to a
        Library module in OutSystems 11, including resolving disallowed elements
        and TrueChange errors before conversion, so that the module publishes
        successfully as a Library.
  - name: Validating your application architecture
    id: validate-app-architecture
    subtopics:
    - name: Reduce module size
      id: reduce-module-size
      description: Wants to reduce the size of an OutSystems 11 module by identifying
        and fixing its main causes, monolithic single-module architecture, large
        images, large resources, and numerous custom icons, so that the module
        publishes, deploys, and runs efficiently.
    - name: Architecture canvas rules
      id: architecture-canvas-rules
      description: Wants to validate module architecture with the Architecture Canvas rules
        for modules, so they can keep module dependencies sound.
  - name: Architecture Canvas
    id: architecture-canvas
    subtopics:
    - name: Pattern layer overview
      id: pattern-layer-overview
      description: Wants to understand how the Architecture Patterns in OutSystems course groups patterns by architecture canvas layer, so they can follow the library module and core module sections in context.
  - name: Application composition
    id: application-composition
    subtopics:
    - name: Library module overview
      id: library-module-overview
      description: Wants to understand what a Library module is in OutSystems 11, including
        its stateless, database-agnostic design principles, typical use cases
        such as UI/style guides and reusable connectors or logic utilities,
        which Service Studio elements are available or restricted in a Library,
        and what reference restrictions apply to it, so that they can decide
        when and how to use Libraries in their application architecture.
    - name: Library module patterns
      id: library-module-patterns
      description: Wants to understand the extension and connector patterns used in
        OutSystems library modules, including wrapping external libraries,
        external database entities, and external APIs behind reusable
        consumer-facing modules, so that they can design library modules that
        hide implementation details and normalize business concepts.
    - name: Granularity of APIs
      id: granularity-of-apis
      description: Wants to split an integration service into smaller APIs by functional
        area when a single external system exposes too many operations, so
        that consumers avoid a single bottleneck and the integration stays
        easier to maintain.
    - name: Create a Library module
      id: create-library-module
      description: Wants to create a new Library module in Service Studio, by
        creating a Mobile or Reactive Web app (or adding a module to an existing
        one) and choosing Library as the module type, and knows to set the entry
        module to a consuming module when debugging it, so that they have a
        working Library ready to hold reusable stateless components.
    - name: ODC library overview
      id: odc-library-overview
      description: Wants to understand what a library is in OutSystems Developer
        Cloud (ODC), including its stateless design, common use cases such as
        wrapping an external system or centralizing style guides and themes, and
        its role as a shared, cross-portfolio building block with an independent
        lifecycle, so that they can decide when to use a library in their app
        architecture.
    - name: Module types overview
      id: module-types-overview
      description: Wants to understand the different module types available in OutSystems
        11 and how a producer module's exposed logic differs by type for the
        consumer modules that reference it, including modules with no UI, so
        that they can choose the right module type when structuring an app for
        reuse.
    - name: Modular programming
      id: modular-programming
      description: Wants to structure an ODC app so it encapsulates one aspect of
        functionality in a single app and keeps the code independent and
        replaceable, so they can organize a solution into modular pieces.
    - name: Producer and consumer apps
      id: producer-consumer-apps
      description: Wants to identify producer apps and consumer apps in ODC when
        apps share reusable elements across applications, so they can reuse
        features from one app in another and keep the app roles clear.
  - name: App naming and URLs
    id: app-naming-and-urls
    description: Wants to understand how ODC turns an app's display name into its internal
      URL path, including which characters are dropped or replaced, the naming
      limit, the Module fallback, and when two apps collide on the same path, so
      that they can choose names that produce predictable, unique URLs.
  - name: Core module patterns
    id: core-module-patterns
    subtopics:
    - name: Core entity pattern
      id: core-entity-pattern
      description: Wants to understand the core entity pattern in OutSystems 11 — how
        a core module stores data in entities exposed as read-only and centralizes all
        create/update operations — so that multiple applications can consume core business
        data consistently without bypassing the module's encapsulation.
    - name: ECS pattern variations
      id: ecs-pattern-variations
      description: Wants to understand the external core service (ECS) pattern and its
        variations — base ECS, local replica, isolated synchronization, direct integration,
        pub/sub, summary cache, and lazy load — in OutSystems 11, so that the right caching
        and synchronization strategy can be selected when an external system is the system
        of records.
    - name: Transparency service
      id: transparency-service-pattern
      description: Wants to understand the transparency service pattern in OutSystems 11,
        where an integration service with multiple drivers abstracts several external systems
        behind a uniform API, so that core services can retrieve and update data without
        knowing which external system holds it.
    - name: Module reference trade-offs
      id: module-reference-tradeoffs
      description: Wants to understand how to manage module references in OutSystems 11
        — including avoiding circular references, choosing between structures and entities
        for large record flows, and evaluating local web services versus public actions —
        so that the application's reference model stays simple, deployment times are
        minimized, and thread exhaustion is avoided.
    - name: Encapsulate REST APIs
      id: encapsulate-rest-apis
      description: Wants to understand why and how to encapsulate REST API consumption
        inside an ODC library, exposing server actions and public structures instead of
        consuming REST methods directly in the app, so that changes to the REST interface
        only need to be updated once and breaking changes can be managed through library
        versioning.
  - name: Session management
    id: session-management
    subtopics:
    - name: Session lifecycle
      id: session-lifecycle
      description: Wants to understand how a session is created, maintained, and
        ended in OutSystems Traditional Web Apps — including differences between
        Web, Web Service, and Timer sessions — so that they can reason about when
        session state is available during their application's execution.
    - name: Configure session timeout
      id: configure-session-timeout-o11
      description: Wants to configure how long an idle Traditional Web App session
        remains active before the Platform Server automatically ends it, by adjusting
        the session timeout value in the machine.config file, so that end users
        get a session duration appropriate to their app's needs.
    - name: Configure user session
      id: configure-session-timeout-odc
      description: Wants to configure the maximum session duration and idle
        session duration per stage in the ODC Portal, so that end-user app
        sessions in each stage meet their organization's security and
        compliance requirements.
    - name: Session variables
      id: session-variables
      description: Wants to understand how session variables persist data during
        a Traditional Web App session — including built-in read-only variables like
        Username and TerminalType, defining custom session variables, their behavior
        in asynchronous logic and consumer/producer module scenarios, and performance
        implications of storing large data — so that they can design session state
        usage correctly for their application.
    - name: Configure tenant sessions
      id: configure-tenant-sessions
      description: Wants to configure how OutSystems 11 binds an end-user's session
        to their tenant during login, including using the default Users application
        versus a custom User Provider with the TenantSwitch system action, so that
        queries and aggregates are automatically scoped to the correct tenant's
        data.
    - name: Keep session alive
      id: keep-session-alive
      description: Wants to keep a Traditional Web App session alive from a Reactive
        or Mobile Web app by issuing periodic asynchronous requests to a blank
        Traditional Web page, so that users do not get signed out when the
        Traditional Web app would otherwise idle out.
  - name: Mobile libraries
    id: mobile-libraries
    subtopics:
    - name: Mobile libraries overview
      id: mobile-libraries-overview
      description: Wants to understand what mobile libraries are in ODC, how they
        differ from general-purpose libraries in terms of target platform,
        extensibility configurations, and UI component options, and their key use
        cases such as plugin wrappers and reusable mobile UI components, so that
        they can decide when to build a mobile library instead of a general-purpose
        library.
  - name: Library versioning
    id: library-versioning
    subtopics:
    - name: Library revision vs. version
      id: library-revision-vs-version
      description: Wants to understand the distinction between a library's revision
        number and version number in ODC, so that they can choose the right
        update flow for each type of change.
    - name: Test a library revision
      id: test-library-revision
      description: Wants to test a library revision in a test app in ODC before
        releasing it, so that library changes don't break consumers.
    - name: Release a new library version
      id: release-library-version
      description: Wants to release a new library version in ODC using semantic
        versioning and release notes, so that consumers can adopt updates with
        clear compatibility expectations.
    - name: Deploy library version
      id: deploy-library-version
      description: Wants to understand whether a new library version in ODC can
        be deployed by itself from Development to QA, so they know that the
        library is packaged and deployed with a consuming app instead.
    - name: Review accepted library updates
      id: review-accepted-library-updates
      description: Wants to review accepted library updates in consuming apps or
        libraries in ODC, so that they can verify update impact before deciding
        on follow-up actions.
    - name: Roll back accepted library updates
      id: roll-back-accepted-library-updates
      description: Wants to roll back accepted library updates in consuming apps
        or libraries in ODC when needed, so that they can recover safely from
        breaking updates.
  - name: Multi-portfolio architecture
    id: multi-portfolio-architecture
    subtopics:
    - name: Portfolio overview
      id: portfolio-overview
      description: Wants to understand what a portfolio is in OutSystems
        Developer Cloud (ODC), including its place in the organization >
        portfolio > stage > asset hierarchy, the benefits of isolating
        assets by non-functional requirements or governance rules, and
        common use cases (different identity providers, compliance
        boundaries, department-specific delivery, shared building-block
        libraries), so that they can decide whether and how to adopt a
        multi-portfolio structure.
    - name: Identify cross-portfolio needs
      id: identify-cross-portfolio-needs
      description: Wants to identify which business concepts, logic/UI components,
        or data need to be shared across portfolios in a multi-portfolio
        organization, so that they can extract shared functionality into
        libraries or expose it via REST APIs.
    - name: Own shared portfolio libraries
      id: own-shared-portfolio-libraries
      description: Wants to establish clear ownership and governance for
        libraries shared across portfolios, including using a dedicated shared
        building blocks portfolio owned by a platform group or Center of
        Excellence (CoE), so that other portfolios can adopt library updates
        on their own schedule without affecting release cadence.
    - name: Dual-exposure pattern
      id: dual-exposure-pattern
      description: Wants to expose the same business logic both within a
        portfolio (via a service action) and to other portfolios (via a REST
        API) by wrapping both around a shared internal server action, so that
        logic isn't duplicated when assembling a complete multi-portfolio
        architecture.
    - name: Reuse across portfolios
      id: reuse-across-portfolios
      description: Wants to choose between a library (for shared logic) and a
        REST API (for shared data) to share an asset across portfolios in ODC,
        understanding which elements are available cross-portfolio and how to
        manage REST API breaking changes for other portfolios' consumers, so
        that they pick the correct mechanism and avoid disrupting
        cross-portfolio consumers.
    - name: Portfolio boundary design
      id: portfolio-boundary-design
      description: Wants to decide portfolio boundaries by grouping apps that
        share service actions, entities, or bounded-context logic together and
        choosing the correct portfolio at asset creation, since ODC doesn't
        support moving assets between portfolios afterward, so that consumer
        apps keep working and rework is avoided.
    - name: Per-portfolio configs
      id: per-portfolio-configs
      description: Wants to configure portfolio-specific settings, such as REST
        API endpoint URLs, identity providers, and connection strings or
        credentials, when consuming a REST API from another portfolio, so
        that each stage authenticates and connects correctly per portfolio.
    - name: Configure new portfolio
      id: configure-new-portfolio
      description: Wants to configure the default stage-level settings, such as domain,
        identity, connectivity, and security configurations, for a newly
        provisioned portfolio's stages in ODC, since new portfolios start with
        default configurations, so that the new portfolio's apps have the
        connections, domains, and security needed to work correctly.
    - name: Forge asset ownership
      id: forge-asset-ownership
      description: Wants to understand that in a multi-portfolio ODC
        organization, an installed Forge asset is permanently owned by the
        portfolio chosen at installation (following its stages,
        configurations, and access controls), and that install/update
        permissions, plus any dependencies Forge installs, are scoped to
        that same portfolio, so they select the correct portfolio and
        confirm they have the right permissions before installing or
        updating a Forge asset.
    - name: Clone Forge asset
      id: clone-forge-asset
      description: Wants to clone a locked, installed Forge asset into a
        different portfolio in a multi-portfolio ODC organization when they
        need to modify its implementation, understanding that the clone
        becomes an independent editable copy following the target
        portfolio's stages and access controls while apps keep referencing
        the original until updated, so they can safely customize Forge
        assets without affecting other portfolios.
    - name: Asset & config scope
      id: asset-and-config-scope
      description: Wants to know which types of assets and platform configurations are
        shared across portfolios versus scoped to a single portfolio or
        portfolio stage in ODC, so that they can plan where to create assets and
        apply configurations correctly.
    - name: Confirm portfolio structure for migration
      id: confirm-portfolio-structure
      description: Wants to confirm the target portfolio structure for an ODC
        organization that already has existing apps and libraries before
        starting an incremental migration, so that assets are grouped
        correctly and existing deployments keep working throughout the
        transition.
    - name: Refactor dependencies for portfolio migration
      id: refactor-portfolio-dependencies
      description: Wants to refactor dependencies among existing apps and
        libraries before separating them across ODC portfolios, so that
        cross-portfolio consumers keep working during an incremental
        migration.
    - name: Prepare stages for portfolio migration
      id: prepare-portfolio-stages
      description: Wants to prepare portfolio stages for migrating existing
        apps and libraries incrementally in ODC, so that each asset group can
        be moved and validated in the right environment sequence without
        disrupting active deployments.
    - name: Update roles for portfolio migration
      id: update-portfolio-roles
      description: Wants to update portfolio access roles before and during an
        incremental migration of existing apps and libraries in ODC, so that
        the right teams can manage each portfolio without blocking ongoing
        deployments.
    - name: Migrate asset groups incrementally
      id: migrate-asset-groups-incrementally
      description: Wants to migrate one asset group at a time to portfolios in
        an ODC organization that already has existing apps and libraries,
        validating end-to-end flows before retiring the prior
        implementation, so that existing deployments keep working throughout
        the transition.
- name: Asynchronous processes
  guid: d4964d80-c8da-468a-ab3c-8f59aab61aa5
  topics:
  - name: How to process data using Timers
    id: process-data-timers
    subtopics:
    - name: Timer overview
      id: timer-overview
      description: Wants to understand how OutSystems timers run asynchronous
        logic on a schedule compared with PHP cron jobs, including logs,
        retries, and DevOps-adjustable settings, so that they can use timers
        for background work.
    - name: Timer best practices
      id: timer-best-practices
      description: Wants to implement a long-running Timer correctly by applying best
        practices (timeout prevention, idempotent commits, completeness through batching,
        chaining execution to continue processing when more records remain, and data
        integrity), so that the Timer never aborts due to timeout and all records are
        eventually processed without data corruption or repeated work.
    - name: Configure timer settings (ODC)
      id: configure-timer-settings-odc
      description: Wants to configure a Timer's schedule, timeout, and active flag in ODC
        Portal per deployment stage, so the Timer runs at the right time, within
        resource limits, and with appropriate values in each environment.
    - name: Configure timer settings (O11)
      id: configure-timer-settings-o11
      description: Wants to configure a Timer's effective schedule, periodicity,
        priority, and timeout in Service Center per environment, so that the
        Timer runs at the right time and within execution limits.
    - name: Configure timer timeout (O11)
      id: configure-timer-timeout-o11
      description: Wants to configure the `Timeout in Minutes` property for an
        OutSystems 11 Timer in Service Studio, so that the platform allows the timer
        execution to run for an appropriate amount of time before aborting it.
    - name: Monitor timer logs
      id: monitor-timer-logs
      description: Wants to monitor timer execution by viewing timer logs in the ODC
        Portal, so that they can check whether a timer is running, review its last
        execution time and duration, and investigate errors using log details and
        related activities.
    - name: Holiday cache sync
      id: holiday-cache-sync
      description: Wants to use a timer to fetch public holiday data from a REST library
        into a public entity when the cached year has no rows, then read the cached
        records by month for a calendar screen, so that the app avoids repeated API
        calls and shows holidays in each month.
    - name: Scheduler job dispatch
      id: scheduler-job-dispatch
      description: Wants to understand how the Scheduler Service queues timer, email,
        and business process jobs, dispatches them to available front-end servers,
        and uses fixed thread pools without running the business logic itself, so
        that they can reason about background task execution in Application Services.
  - name: How to process massive amounts of data in Batch Processing
    id: batch-data-processing
- name: Basic integrations
  guid: c0892f27-0055-4fe6-bb6f-e5a308d37b2a
  topics:
  - name: Integration Patterns - Basics (decision tree)
    id: integration-pattern-basics
  - name: HTTP library actions
    id: http-library-actions
    subtopics:
    - name: Inspect HTTP request
      id: inspect-http-request
      description: Wants to read details of the current HTTP request, such as
        cookies, headers, HTTP method, URL, domain, and submitted files, using
        the ODC HTTP library's Request_Get* actions, in ODC Studio, so that
        server-side logic can react to incoming request data.
    - name: Customize HTTP response
      id: customize-http-response
      description: Wants to customize the current HTTP response by adding a header, setting
        a cookie, or setting the status code, using the ODC HTTP library's
        response actions, in ODC Studio, so that server-side logic can control
        what's sent back to the client.
    - name: Build and submit HTTP URLs
      id: build-submit-http-urls
      description: Wants to generate an absolute URL, replace a URL's domain,
        and submit an outbound HTTP GET request, using the ODC HTTP library's
        Request_AbsoluteURL, Request_ReplaceDomain, and
        Request_SubmitGetRequest actions, in ODC Studio, so that apps can
        normalize URLs and call external HTTP endpoints.
  - name: Expose webservices
    id: expose-webservices
    subtopics:
    - name: Define methods
      id: define-methods
    - name: Error codes
      id: error-codes
    - name: Document APIs
      id: document-apis
    - name: Develop the logic of web services
      id: webservice-logic-dev
    - name: Test SOAP with SoapUI
      id: test-soap-soapui
      description: 'Wants to test a SOAP web service method built in OutSystems by installing
        SoapUI, generating a project from the service''s WSDL, submitting a sample request,
        and inspecting the success/error XML response, so that the method''s behavior can
        be verified before considering it complete.'
    - name: Troubleshoot webservices
      id: troubleshoot-webservices
    - name: Fix REST param type
      id: fix-rest-param-type
      description: 'Wants to fix the "Cannot send a ''Binary Data''/''List''
        parameter" error in a REST API method request in Service Studio for
        OutSystems 11, caused by setting a request parameter to the Binary
        Data or List data type when the method''s request format doesn''t
        support it, by changing the parameter''s data type or the method''s
        request format, so that the REST API method compiles and publishes
        successfully.'
    - name: Fix REST date format
      id: fix-rest-date-format
      description: 'Wants to fix the "Incompatible Date Formats" error in a REST
        API method in Service Studio for OutSystems 11, caused by
        request/response example date values not matching the REST API''s
        configured Date Format property, by changing the API''s Date Format
        property or the example''s date values, so that the REST API method
        compiles and publishes successfully.'
    - name: Fix invalid request format
      id: fix-invalid-request-format
      description: 'Wants to fix the "Invalid Request Format" error for a REST
        API method in Service Studio for OutSystems 11, caused by an invalid
        or missing Request Format property, or by a mismatch between the
        Request Format property and the body input''s data type, by
        correcting the Request Format property or the input parameters, so
        that the REST API method compiles and publishes successfully.'
    - name: Fix invalid response format
      id: fix-invalid-response-format
      description: 'Wants to fix the "Invalid Response Format" error for a REST
        API method in Service Studio for OutSystems 11, caused by an invalid
        or missing Response Format property, by correcting the property or
        removing the output parameters, so that the REST API method compiles
        and publishes successfully.'
    - name: Fix invalid receive in error
      id: fix-invalid-receive-in-error
      description: 'Wants to fix the "Invalid Receive In" error for a REST API
        output parameter in Service Studio for OutSystems 11, caused by an
        output parameter''s data type only being allowed to be received in
        the request body, by correcting the parameter''s ''Receive In''
        property, so that the REST API method compiles and publishes
        successfully.'
    - name: Fix multipart invalid HTTP method
      id: fix-multipart-invalid-http-method
      description: 'Wants to fix the "Invalid HTTP Method" error for a
        multipart/form-data REST API method in Service Studio for OutSystems
        11, caused by the method using an HTTP method that doesn''t support
        multipart/form-data, by changing the method''s HTTP method, so that
        the REST API method compiles and publishes successfully.'
    - name: Fix multipart basic parts
      id: fix-multipart-basic-parts
      description: 'Wants to fix the multipart/form-data validation error for
        a REST API method in Service Studio for OutSystems 11 caused by the
        method defining zero or only one basic part, by adding the required
        basic parts to the method definition, so that the REST API method
        compiles and publishes successfully.'
    - name: Fix multipart nameless parts
      id: fix-multipart-nameless-parts
      description: 'Wants to fix the multipart/form-data validation error for
        a REST API method in Service Studio for OutSystems 11 caused by one
        or more parts not having a name, by naming every part in the method
        definition, so that the REST API method compiles and publishes
        successfully.'
    - name: Fix multipart JSON part type
      id: fix-multipart-json-part-type
      description: 'Wants to fix the multipart/form-data validation error for
        a REST API method in Service Studio for OutSystems 11 caused by a
        JSON part not using a Structure or List data type, by changing that
        part''s data type to a Structure or List, so that the REST API method
        compiles and publishes successfully.'
  - name: Consume webservices
    id: consume-webservices
    subtopics:
    - name: Authenticate SOAP service
      id: authenticate-soap-service
      description: 'Wants to fix a 401 Unauthorized error when consuming an
        authenticated SOAP web service in OutSystems 11 by saving the WSDL
        locally and importing it from the file system, and by setting the
        username and password with the EnhancedWebReferences extension''s
        SetWebReferenceCredentials action, so that the web service can be
        consumed successfully.'
    - name: Fix SOAP WSDL schema error
      id: fix-soap-wsdl-schema-error
      description: Wants to fix a SOAP web service import error in Service
        Studio caused by an invalid WSDL schema (for example, an undeclared
        XML namespace prefix), by editing the exported definition files to
        correct the schema and re-importing them, so that the web service
        imports successfully, in OutSystems 11.
    - name: Enable OSTrace SOAP import
      id: enable-ostrace-soap-import
      description: Wants to enable OSTrace logging for SOAP web service
        consumption in Service Studio, by adding the SoapConsume diagnostics
        switch and a listener file to Settings.xml, so that they can capture
        detailed logs when a SOAP import error message alone isn't enough to
        find the root cause, in OutSystems 11.
    - name: Fix SOAP name collision
      id: fix-soap-name-collision
      description: Wants to fix a SOAP web service import error in Service
        Studio caused by a name collision between XML schema elements (for
        example, a complexType and attributeGroup sharing the same name), by
        examining the OSTrace log's Name Collision Issue table to identify
        the conflicting elements and renaming one of them in the WSDL, so
        that the web service imports successfully, in OutSystems 11.
    - name: Fix SOAP compile error
      id: fix-soap-compile-error
      description: 'Wants to fix a CS0117 compilation error that occurs when
        publishing a module after consuming a SOAP web service with an
        unsupported list-attribute WSDL case, by finding the method and
        output parameter in the error details, locating the related
        complexType and maxOccurs definition in the WSDL, and adding a dummy
        element so the module publishes successfully, in OutSystems 11.'
    - name: Changing endpoints
      id: changing-endpoints
    - name: Consume and refresh methods
      id: consume-refresh-methods
    - name: Write SOAP data
      id: write-soap-data
      description: 'Wants to create and update contact records through a consumed SOAP web service by loading an existing contact with GetContact when the Email input parameter is set, calling CreateContact when it is empty, and saving the form data back to the external system, so that users can maintain contacts in the SOAP service.'
    - name: Show webservice errors
      id: show-webservice-errors
      description: Wants to handle the outcome of a webservice method call in the
        application logic by checking the method's Result.Success output and
        displaying the Result.ErrorMessage in a feedback message when it's false,
        so that end users are informed when an operation like deleting a record
        fails.
    - name: Handle REST status codes
      id: handle-rest-status-codes
      description: Wants to handle the outcome of a consumed REST web service call
        by checking the HTTP status code returned and branching on success and
        error cases, so that the application can react appropriately to 2xx, 4xx,
        and 5xx responses.
    - name: Structures
      id: structures
    - name: SOAP parameter reference
      id: soap-parameter-reference
      description: Wants to look up the properties of a deprecated SOAP web service's input
        parameter in Service Studio, so that they can correctly configure and
        understand input parameters when consuming a deprecated SOAP web service
        in OutSystems 11.
    - name: Multipart form inputs
      id: multipart-form-inputs
      description: Wants to consume a REST API method that uses multipart/form-data as its
        request format in Service Studio, understanding how parts from the
        OpenAPI specification become input parameters and their configurable
        properties, including binary dynamic lists and the auto-generated
        RequestPart data structure, so that they can correctly configure and use
        the consumed method's inputs.
    - name: Multipart content-disposition
      id: multipart-content-disposition
      description: Wants to understand how OutSystems generates the
        Content-Disposition header for a multipart/form-data REST API method
        depending on whether the RequestPart data structure is used, and how
        to manually add a filename parameter via the OnBeforeRequest callback
        when it isn't, so that file parts are correctly identified in the
        request.
    - name: Consume SAP OData
      id: consume-sap-odata
      description: Wants to consume an SAP OData service in OutSystems 11 by downloading
        the JSON Swagger specification from SAP API Business Hub and importing it via
        Service Studio's REST consumer in a dedicated module, so that SAP data and
        operations are available as server actions in the application.
    - name: Fix OData dollar params
      id: fix-odata-dollar-params
      description: Wants to work around SAP OData's $-prefixed system query parameter
        encoding issue by implementing an OnBeforeRequest handler in Service Studio
        that re-adds the dollar sign to matching URL query parameters before each
        request, so that filters, ordering, pagination, and other OData system options
        are transmitted correctly.
    - name: Handle SAP OData errors
      id: handle-sap-odata-errors
      description: Wants to implement an OnAfterResponse handler in Service Studio
        that parses SAP OData error responses across three formats (data errors, fault
        errors, and API errors) and throws typed exceptions, so that error handling
        logic in the consuming application can respond appropriately to different
        failure modes.
- name: Basic workflows
  guid: 368a1c9e-0f21-4970-9645-577590bbf1ca
  topics:
  - name: Emails
    id: emails
    subtopics:
    - name: How to send emails
      id: how-to-send-emails
    - name: How to create emails
      id: how-to-create-emails
    - name: Validate email address
      id: validate-email-address
      description: Wants to check whether a text string is a properly formatted
        email address using the EmailAddressValidate built-in function, which
        follows the HTML5 email validation spec, in Service Studio for O11, so
        that they can validate user input before using it in email operations.
    - name: Email link variable
      id: email-link-variable
      description: Wants to generate a direct link to a specific record by building
        the URL with GetEntryURL in the email's Preparation action and passing it
        as an input parameter, in Service Studio for O11 Traditional Web Apps, so
        that clicking the link in the email takes the user straight to that record's
        page instead of a generic landing page.
    - name: Translate email locale
      id: translate-email-locale
      description: Wants to translate the content of an OutSystems email that's
        sent in the default language despite the triggering screen being localized,
        by setting the locale in the email's Preparation action with SetCurrentLocale
        (optionally reading the prior locale via GetCurrentLocale), in Service
        Studio for O11, so that the email is sent in the same language as the
        user's session.
    - name: Find last sent email
      id: find-last-sent-email
      description: Wants to find the timestamp of the last sent email in
        OutSystems 11, either by referencing the Sent_Email and Email_Status
        entities' Sent attribute in Service Studio, or by checking the Email
        Log screen in Service Center's Monitoring tab, so that they can
        verify when an email was actually sent.
    - name: Attach file to email
      id: attach-file-to-email
      description: Wants to attach a file to an outgoing email by configuring the Attach
        File action's required and optional properties in Service Studio for
        O11, so that recipients receive the supporting file directly within the
        email rather than through a separate channel.
    - name: Concatenate emails
      id: concatenate-email-addresses
      description: Wants to combine multiple email addresses (or lists of email
        addresses) into a single comma-separated list using the
        EmailAddressesConcatenate built-in function, in Service Studio for O11,
        so that they can pass a combined recipient list to the Send Email
        element or activity.
    - name: Add email MIME headers
      id: add-email-mime-headers
      description: Wants to add custom header properties (MIME headers) to an
        outgoing email by using the Email element's Extended Properties
        (Property and Value) in Service Studio for O11, so that they can
        control message-level headers such as Precedence or Auto-Submitted
        for automated email handling.
    - name: Email render timeout
      id: email-render-timeout
      description: Wants to understand why an OutSystems 11 email fails to send
        with "The operation has timed out" when the Preparation action's
        rendering page takes too long, and that SetRequestTimeout has no effect
        on it, so they reduce the email's render time instead of trying to
        extend the timeout.
  - name: BPT
    id: bpt
    subtopics:
    - name: Connecting processes to the UI
      id: connect-process-ui
    - name: Understanding asynchronous processes
      id: async-processes-basics
    - name: Triggering processes, process flows
      id: trigger-processes
    - name: Execute sub-process
      id: execute-sub-process
      description: Wants to execute a sub-process from a parent process by using the Execute
        Process activity in OutSystems 11, so that the runtime creates a
        sub-process instance, pauses the parent until the sub-process terminates,
        and then resumes the parent flow.
    - name: Conditional process start
      id: conditional-process-start-o11
      description: Wants to configure a conditional start in an OutSystems 11 process so it starts on create or update entity events and only listens after a dependency source activity has executed.
    - name: Design process flow
      id: design-process-flow
      description: Wants to design the behavior of a process using the Process
        Flow editor after creating a process, selecting tools from the Process
        Flow Toolbox to be executed in the flow, so that the process's logic is
        defined.
    - name: Process upgrade impact
      id: process-upgrade-activity-impact
      description: Wants to understand how moving activities in a process flow
        affects executing process instances after publishing, in OutSystems 11,
        so that they can predict which instances get suspended and know to
        review them in Service Center.
    - name: Add activity impact
      id: process-upgrade-add-activity-impact
      description: Wants to understand how adding activities to a process flow
        affects executing process instances after publishing, in OutSystems 11,
        so that they know instances continue executing unaffected regardless
        of whether they've already passed the added activity, haven't reached
        it yet, or are in a newly added branch.
    - name: Delete activity impact
      id: process-upgrade-delete-activity-impact
      description: Wants to understand how deleting activities from a process
        flow affects executing process instances after publishing, in
        OutSystems 11, including using a Decision tool workaround to avoid
        suspension and checking active instances via BAM, Service Center, or
        the platform database, so that they can safely remove deprecated
        activities without suspending running processes.
    - name: BPMN notation
      id: bpmn-notation
    - name: Patterns for processes (automatic, human, decision)
      id: process-decision-patterns
    - name: Automatic activity errors
      id: automatic-activity-errors
      description: Wants to understand how errors are handled in an Automatic
        Activity in OutSystems 11, by knowing that on error the flow execution
        stops and automatically retries from the beginning until it succeeds
        or the process is terminated, so that they can monitor and resolve
        failing process activities through Service Center.
    - name: Automatic activity scope
      id: automatic-activity-scope
      description: Wants to know what elements are accessible when editing an
        Automatic Activity's flow in OutSystems 11 (process input/output
        parameters, local variables, invoked actions' and queries' output
        parameters), so that they can select the right variable in the
        Expression Editor or Select Variable window.
    - name: Archive BPT processes
      id: archive-bpt-processes
      description: Wants to archive old closed BPT Process instances by creating
        a mirrored set of meta-model entities and using Process_Delete or
        Process_BulkDelete from the BPT_API extension, in OutSystems 11, so
        that BPT queries stay performant as Process volume grows.
    - name: Fix BPT activity timeout
      id: fix-bpt-activity-timeout
      description: Wants to resolve a BPT automatic activity hitting the 5-minute
        timeout by moving the long-running batch logic into a Timer that signals
        the waiting process to proceed, in OutSystems 11, so that thread pool
        resources aren't exhausted and the process can continue.
    - name: Fix missing detail warning
      id: fix-missing-detail-warning
      description: 'Wants to fix the "Please set the ''<process>'' Process
        Detail property to display in Taskbox" warning in OutSystems 11, by
        setting the Detail property on the Human Activity or, if that''s
        unset, on the Process itself (directly, or via the Launch On
        entity''s Label Attribute), in Service Studio, so that a meaningful
        detail text appears under the activity in the Taskbox.'
    - name: End process activity
      id: end-process-activity
      description: Wants to end a process flow path using the End process
        activity in OutSystems 11, so that the process execution properly
        terminates once every flow path in the main process reaches its own
        End activity.
    - name: Output parameter access
      id: output-parameter-access
      description: Wants to understand which process activity can read and
        write a process flow's output parameters (the owning activity has
        read/write, others have read-only, and the Process Flow itself has
        read/write), in OutSystems 11, so that they correctly design data
        handoffs between process activities.
    - name: Output parameter defaults
      id: output-parameter-defaults
      description: Wants to understand how a process or process activity
        output parameter behaves when it's never set, using the default
        value from the currently published process definition (applied
        immediately if a new version publishes mid-execution, and the same
        rule applying to unset input parameters), in OutSystems 11, so that
        they know what value they'll read.
    - name: Entity action process events
      id: entity-action-process-events
      description: Wants to have an Entity Action issue events to all executing
        process and activity instances when it creates or updates a record, in
        OutSystems 11, so that a process can automatically launch or advance
        in reaction to an entity change.
    - name: Taskbox overview
      id: taskbox-overview
      description: Wants to understand what the Taskbox is and how it lists
        tasks, instructions, and actions for end users in Reactive and
        Traditional Web Apps, in OutSystems 11, so that they know where users
        interact with pending process tasks.
    - name: Terminate process property
      id: terminate-process-property
      description: Wants to force immediate termination of a process
        execution by setting the End activity's Terminate Process property
        to Yes in OutSystems 11, so that all activity instances stop right
        away regardless of other running flow paths, and can be restarted
        later from Service Center.
    - name: Light process overview
      id: light-process-overview
      description: Wants to understand what light process execution is and
        when to use it, for event-driven processes handling large volumes of
        database events at scale, knowing the trade-offs (no process
        history, no activity instances, unsupported Automatic Activity start
        dates), in OutSystems 11, so that they can decide whether a process
        qualifies for and benefits from this scalable database queueing
        mode.
    - name: Enable light process execution
      id: enable-light-process-execution
      description: Wants to enable light process execution for a process by
        meeting the required conditions (a single Start > Automatic Activity
        > End flow, a database event set as Launch On, and Expose Process
        Entity set to No) and turning on the Light Process Execution option
        in Service Center before republishing the module, in OutSystems 11,
        so that a qualifying process runs faster without process/activity
        tracking overhead.
    - name: DST BPT bug overview
      id: dst-bpt-bug-overview
      description: Wants to understand why some BPT activities scheduled during
        a Daylight Savings time transition may never execute on the .NET
        stack in OutSystems 11 (versions 7.0.1.25+, 8.0.1.8+, 9.0, 9.1),
        including the risk of the BPT system locking down entirely, so that
        they can assess whether their environment is affected.
    - name: Detect DST BPT issue
      id: detect-dst-bpt-issue
      description: Wants to detect whether their OutSystems 11 environment is
        affected by the Daylight Savings time BPT bug, by checking for
        Pending Activities in Service Center's Environment Health monitoring
        and running a diagnostic SQL query against the OSSYS_BPM tables, so
        that they can confirm the issue before applying a fix.
    - name: Fix stuck BPT activities
      id: fix-dst-bpt-activities
      description: Wants to fix BPT activities stuck due to the Daylight
        Savings time bug in OutSystems 11, by rescheduling the affected
        activities' NEXT_RUN value outside the missing hour via a SQL
        update, or by terminating the affected processes in Service Center
        or through the BPT API, so that normal BPT processing resumes.
  - name: Workflow Builder
    id: workflow-builder
    subtopics:
    - name: WFB overview
      id: wfb-overview
      description: Wants to understand what Workflow Builder is and who it's for,
        including business experts, analysts, process owners, IT managers, and
        developers, in OutSystems 11, so that they can decide whether it's the
        right tool for their business process app.
    - name: WFB app building steps
      id: wfb-app-building-steps
      description: Wants to build and publish a business process app in Workflow
        Builder by setting up a form, defining workflow steps, publishing to test,
        and sharing it with end-users via IT, in OutSystems 11, so that they can
        turn a business process into a working app.
    - name: WFB group naming
      id: wfb-group-naming
      description: Wants to understand how Workflow Builder automatically creates
        user groups in the OutSystems Users tool when an app is published to the
        development environment, including the naming conventions for requester and
        activity groups ([WB | App Name | Requesters] and [WB | App Name | Group name]),
        in OutSystems 11, so that IT teams can identify and configure the correct
        groups for end-user access.
    - name: WFB production handoff
      id: wfb-production-handoff
      description: Wants to understand the steps required to move a Workflow Builder
        app from development to production in OutSystems 11, including involving the
        IT team, sharing user group names, and having IT add users in the OutSystems
        Users tool, so that the app is ready for end users to access.
    - name: Update CM dependency
      id: update-cm-dependency
      description: Wants to install or update the Case Management framework
        dependency for a Workflow Builder app in OutSystems 11 by downloading
        the CMF .osp file from Workflow Builder's settings and publishing it
        through Service Center, so that Workflow Builder recognizes the app's
        dependencies as up to date.
    - name: WFB token error
      id: wfb-token-error
      description: Wants to resolve the "Token error" that occurs when Workflow
        Builder doesn't have a valid token to access the environment, by logging
        out and back in (or contacting support if the problem persists), in
        OutSystems 11, so that Workflow Builder can reconnect to the environment.
    - name: WFB app architecture
      id: wfb-app-architecture
      description: Wants to understand the architecture of a Workflow Builder
        generated app (built on Case Management Framework and Business Process
        Technology, including logic, UI, and data model) and how IT can extend
        it using Service Studio, in OutSystems 11, so that they can plan how to
        hand off and evolve the app after publishing.
    - name: WFB unknown error
      id: wfb-unknown-error
      description: 'Wants to resolve the "Unknown error" that occurs when
        Workflow Builder encounters an unspecified service disruption, by
        retrying the operation or contacting OutSystems Support if the
        problem persists, in OutSystems 11, so that they can continue using
        Workflow Builder.'
    - name: WFB delete version error
      id: wfb-delete-version-error
      description: Wants to resolve the "Cannot remove case application version
        since it's not in Draft" error when deleting an application in Workflow
        Builder, by logging out and back in (or contacting support if the problem
        persists), in OutSystems 11, so that they can successfully delete the
        application version.
    - name: WFB icon save error
      id: wfb-icon-save-error
      description: Wants to resolve the "Cannot save icon in the storage" error
        that occurs when Workflow Builder can't save an application icon in
        storage, by retrying later, publishing without an icon, or contacting
        OutSystems Support if the problem persists, in OutSystems 11, so that
        they can proceed without the icon-save failure blocking their work.
    - name: WFB storage error
      id: wfb-storage-error
      description: 'Wants to resolve the "Storage error. <OriginalErrorMessage>."
        error when Workflow Builder cannot access its internal storage, by
        retrying later or contacting OutSystems Support if the problem
        persists, in OutSystems 11, so that icon display and publishing
        succeed.'
    - name: WFB status update error
      id: wfb-status-update-error
      description: 'Wants to resolve the "Specified application version do not
        exists or the status update is invalid" error when performing an
        operation requiring an application status update in Workflow Builder,
        by logging out and back in (or contacting support if the problem
        persists), in OutSystems 11, so that they can successfully complete
        the status update.'
    - name: WFB icon size error
      id: wfb-icon-size-error
      description: 'Wants to resolve the "Application version icon is larger
        than <MaxSize>" error when uploading an icon that exceeds the max
        file size in Workflow Builder, by retrying with a smaller icon or
        contacting OutSystems Support if the problem persists, in OutSystems
        11, so that the application icon upload succeeds.'
    - name: WFB discard version error
      id: wfb-discard-version-error
      description: 'Wants to resolve the "Unable to discard version. It has
        published versions: <ApplicationVersionId>." error that occurs when
        trying to delete an application that''s already published but still
        displays as unpublished in Workflow Builder, by logging out and back
        in (or contacting support if the problem persists), in OutSystems 11,
        so that they can successfully delete the application.'
    - name: WFB attribute data error
      id: wfb-attribute-data-error
      description: 'Wants to resolve the "Attribute data is not valid:
        <OriginalErrorMessage>" error when creating or modifying a field
        with inconsistent data in Workflow Builder, by logging out and back
        in (or contacting support if the problem persists), in OutSystems 11,
        so that they can successfully save the field.'
    - name: WFB attribute type error
      id: wfb-attribute-type-error
      description: 'Wants to resolve the "Cannot change the type of an
        existing attribute: <Name> (<Id>)" error that occurs when trying to
        change the data type of a field that''s already published in
        Workflow Builder, by creating a new field with the desired data
        type instead, in OutSystems 11, so that the application field saves
        successfully.'
    - name: WFB attribute length error
      id: wfb-attribute-length-error
      description: 'Wants to resolve the "Cannot decrease the Length of an
        existing attribute: <Name> (<Id>)" error that occurs when trying to
        decrease the Length of a Text field that''s already published in
        Workflow Builder, by creating a new field with the desired Length
        instead, in OutSystems 11, so that the application field saves
        successfully.'
    - name: WFB field label error
      id: wfb-field-label-error
      description: 'Wants to resolve the "Field label cannot be higher than
        <Max_Length>." error in Workflow Builder in OutSystems 11 by
        shortening a field label that exceeds the maximum allowed length so
        the application can save.'
    - name: WFB dropdown id error
      id: wfb-dropdown-id-error
      description: 'Wants to resolve the "Cannot create new dropdown entry. Id
        is invalid for dropdown: <Name> (<Id>)" error that occurs when
        creating or updating a Dropdown field with inconsistent data in
        Workflow Builder, by deleting and re-creating the unpublished field
        (or contacting support if already published), in OutSystems 11, so
        that they can successfully save the field.'
    - name: WFB group create error
      id: wfb-group-create-error
      description: 'Wants to resolve the "Cannot create Group:
        <OriginalErrorMessage>" error that occurs when creating a group
        where the operation fails for some members, in Workflow Builder,
        by retrying the operation or contacting OutSystems Support if the
        problem persists, in OutSystems 11, so that they can successfully
        create the group.'
    - name: WFB EnvModule error
      id: wfb-envmodule-error
      description: 'Wants to resolve the "Cannot create EnvModule.
        <OriginalErrorMessage>." error that occurs when publishing an
        application in Workflow Builder fails because the system can''t
        validate external module dependencies, by retrying the publish
        operation or contacting OutSystems Support if the problem persists,
        in OutSystems 11, so that they can successfully publish the
        application.'
    - name: WFB module error
      id: wfb-module-error
      description: 'Wants to resolve the "Module error. <OriginalErrorMessage>."
        error that occurs when publishing an application in Workflow Builder
        fails because the system can''t obtain external entity dependencies
        from the user''s infrastructure, by verifying that the linked Entity
        and Module are correct and that the logged-in Workflow Builder user
        has download permission on those modules, in OutSystems 11, so that
        they can successfully publish the application.'
    - name: WFB app templates
      id: wfb-app-templates
      description: Wants to choose a prebuilt app template that matches their business
        scenario when starting a new app in Workflow Builder, in OutSystems 11,
        so that they can start from ready-made form fields and workflow logic
        instead of building the app from scratch.
    - name: WFB prerequisites
      id: wfb-prerequisites
      description: Wants to confirm they meet the requirements to use Workflow Builder,
        including a non-Free-Edition environment, the minimum supported Platform
        Server version (a higher minimum version for entity reuse), inbound
        network access on port 443, IT user permissions, and a supported desktop
        browser, in OutSystems 11, so that they can proceed with setup without
        hitting environment or access blockers.
    - name: WFB first login
      id: wfb-first-login
      description: Wants to log in to Workflow Builder for the first time by
        entering their OutSystems development environment address, username,
        and password, in OutSystems 11, so that they can access Workflow
        Builder using an existing IT user account.
    - name: WFB env registration
      id: wfb-env-registration
      description: Wants to register their OutSystems environment in Workflow
        Builder as the first user from their organization, by accepting the
        disclaimer, providing (or having Workflow Builder auto-collect) the
        Activation Code depending on their Platform Server release, and
        completing the automatic Case Management framework installation, in
        OutSystems 11, so that they can then set up the governance model and
        end-user hierarchy for the workflow app.
    - name: WFB onboarding tour
      id: wfb-onboarding-tour
      description: Wants to revisit the onboarding carousel introducing Workflow
        Builder's main features, by clicking the ? button in the top menu, in
        OutSystems 11, so that they can review the tool's core functionality
        after the initial registration.
    - name: WFB app statuses
      id: wfb-app-statuses
      description: Wants to understand the different app statuses shown on the
        My apps screen in Workflow Builder, including Published, Draft, Failed
        to publish, and Converted to Service Studio, in OutSystems 11, so that
        they can determine what action, if any, is needed for each app.
    - name: WFB template error
      id: wfb-template-error
      description: 'Wants to resolve the "Template error. <OriginalErrorMessage>."
        error that occurs when creating a new app from a predefined template
        fails in Workflow Builder, by retrying the operation or contacting
        OutSystems Support if the problem persists, in OutSystems 11, so that
        they can successfully create the app from the template.'
    - name: WFB refresh page error
      id: wfb-refresh-page-error
      description: 'Wants to resolve the "Please refresh the page and try
        again" error that occurs when validation issues prevent an
        application workflow from being stored in Workflow Builder, by
        logging out and back in (or contacting support if the problem
        persists), in OutSystems 11, so that they can successfully modify
        the workflow.'
    - name: WFB application error
      id: wfb-application-error
      description: 'Wants to resolve the "Application error.
        <OriginalErrorMessage>." error that occurs when an action over
        an app fails in Workflow Builder, by logging out and back in
        (or contacting OutSystems Support if the problem persists), in
        OutSystems 11, so that they can continue working in Workflow
        Builder.'
    - name: WFB invalid access error
      id: wfb-invalid-access-error
      description: 'Wants to resolve the "Invalid access.
        <OriginalErrorMessage>." error that occurs when a user lacks
        permission to perform an operation in Workflow Builder, by
        verifying their permission for the operation or logging out and
        back in (or contacting OutSystems Support if the problem
        persists), in OutSystems 11, so that they can continue working
        in Workflow Builder.'
    - name: WFB general error
      id: wfb-general-error
      description: 'Wants to resolve the "General error.
        <OriginalErrorMessage>." error that occurs when an operation
        causes an error in Workflow Builder, by examining the original
        error message and creating a case with OutSystems Support if
        the problem persists, in OutSystems 11, so that they can
        continue working in Workflow Builder.'
    - name: WFB communication error
      id: wfb-communication-error
      description: 'Wants to resolve the "Communication error.
        <OriginalErrorMessage>." error that occurs when the browser
        can''t contact the Workflow Builder servers, by verifying the
        workstation has connectivity to the Workflow Builder servers, in
        OutSystems 11, so that they can continue accessing Workflow
        Builder.'
    - name: WFB invalid application data error
      id: wfb-invalid-application-data-error
      description: 'Wants to resolve the "Invalid application data.
        <OriginalErrorMessage>." error that occurs when trying to update
        an application with inconsistent data in Workflow Builder, by
        logging out and back in (or creating a case with OutSystems
        Support if the problem persists), in OutSystems 11, so that
        they can successfully save the application.'
- name: Component testing
  guid: 7e2fb431-16fd-487e-8c9b-77fe350b092b
  topics:
  - name: Fundamentals of component testing strategies
    id: component-test-fundamentals
    subtopics:
    - name: Component testing strategies
      id: component-test-strategy
  - name: Manual component testing
    id: manual-component-testing
    subtopics:
    - name: Manual component tests
      id: manual-component-tests
  - name: Automated component testing
    id: component-test-automation
    subtopics:
    - name: Service virtualization
      id: service-virtualization
    - name: Thin screen actions
      id: thin-screen-actions
      description: Wants to keep screen actions focused on UI validations and user interactions by moving business validations and business logic into reusable server or public actions, so the logic can be tested without UI tests.
    - name: Isolate data access
      id: isolate-data-access
      description: Wants to keep complex algorithms testable by loading the required data before the action runs and passing it in as inputs, so the logic does not depend on direct database queries or database setup in tests.
    - name: Testing small units of code
      id: test-small-units
    - name: Automated component tests
      id: automated-component-tests
    - name: BDD test templates
      id: bdd-test-templates
      description: Wants to create BDD Framework test apps and screen suites from the provided application and screen templates, with the required blocks and actions, so that they can start component tests quickly.
    - name: BDD test best practices
      id: bdd-test-best-practices
      description: Wants to implement BDD Framework component tests by separating test code from business code, mirroring only relevant dependencies, organizing suites and blocks, using consistent names, and selecting the right assert type, so that the tests stay readable and maintainable.
    - name: BDD test setup
      id: bdd-test-setup
      description: Wants to set up a BDD Framework test app in ODC Studio by adding the component as a public element, creating a screen and BDD scenario, and implementing the test actions, so they can start component testing.
- name: Collaborative Development
  guid: ''
  topics:
  - name: Merging revisions
    id: merging-revisions
    subtopics:
    - name: Resolve merge conflicts
      id: resolve-merge-conflicts
      description: Wants to understand how merge conflicts are handled when multiple
        developers publish changes to the same app simultaneously, including
        when changes merge automatically, when conflicts in textual elements
        require manually choosing which changes to keep, and how to finish with
        Merge and publish or Merge, so that they can collaborate on an app
        without losing work.
    - name: Compare app revisions
      id: compare-app-revisions
      description: Wants to compare the currently open version of an app against
        a previously published revision or another revision from the server using
        ODC Studio's Compare and Merge options, so that they can review differences
        before deciding what to publish.
    - name: Recover previous merge
      id: recover-previous-merge
      description: Wants to resume or discard an in-progress merge that was automatically
        saved, using the Recover Previous Merge dialog after canceling a merge
        or an unexpected Service Studio or ODC Studio closure, so that they can
        continue resolving conflicts without losing earlier merge work or start
        fresh.
    - name: Compare workflow versions
      id: compare-workflow-versions
      description: Wants to compare different versions of a workflow (a previous
        revision, unpublished changes, or a conflicting published revision) in ODC,
        using the revision history panel, the reopen-workflow dialog, or the
        publish-conflict dialog, so that they can review differences before choosing
        which version to keep or restore.
    - name: Workflow diff color coding
      id: workflow-diff-color-coding-odc
      description: Wants to interpret the color-coded highlights and property-level changes
        shown in ODC's workflow comparison view, so that they can correctly
        identify what was added, modified, deleted, or in conflict between two
        workflow revisions.
    - name: Publish with message
      id: publish-with-message
      description: Wants to add a custom message when publishing a workflow in ODC,
        and see that message surface in the revision history and in merge-conflict
        or open-workflow conflict dialogs, so that they and their team understand
        the context behind each revision.
    - name: Merge color reference
      id: merge-color-reference-o11
      description: Wants to interpret the color-coded highlights shown in Service
        Studio's Compare and Merge window, to identify deleted, inserted, and
        modified or conflicting lines during a merge, in OutSystems 11.
  - name: Isolated workspaces
    id: isolated-workspaces
    description: Wants to choose a branching strategy and use a personal area for
      isolated debugging when collaborating on an OutSystems app, so that work
      stays short-lived and safe to share.
  - name: Version control workflows
    id: version-control-workflows
    description: Wants to use Visual Studio Team Explorer and Git repositories to
      connect to a server copy, sync local changes, and manage branches and
      merges when working on a module, so that they can version code in a
      Git-based workflow.
- name: Consume UI
  guid: 349f50f2-cb79-41c7-9e8b-f113cbb3e965
  topics:
  - name: Use a live style guide
    id: use-a-live-style-guide
  - name: Reuse UI
    id: reuse-ui
    subtopics:
    - name: Embed web screens iframe
      id: embed-web-screens-iframe
      description: Wants to embed Traditional Web Screens from an O11 web app into
        a Mobile App using an iframe, including creating the InternalBrowser block,
        implementing the FetchIframe action with correct CSRF headers, and working
        around the LifeTime Analytics incompatibility, so that existing web screens
        can be surfaced inside a mobile app without full migration.
    - name: Use Iframe pattern
      id: use-iframe-pattern
      description: Wants to add the Iframe UI Pattern to a Traditional Web App screen
        in Service Studio, handle a missing dependency if the widget does not
        appear, configure the SourceURL, Title, Width, and Height properties, and
        publish and test the module so that external app content displays in a
        bite-size preview inside the app.
    - name: Auto-login mobile user
      id: auto-login-mobile-user
      description: Wants to automatically log in an end user authenticated in the
        mobile app into the embedded Traditional Web App, by adding an OnException
        handler that calls Session_GetMobileAppLoginInfo and then the system Login
        action, in Service Studio for O11, so that users don't need to log in
        separately to the web app inside the iframe.
    - name: iframe link target
      id: iframe-link-target
      description: Wants to ensure links inside an embedded Traditional Web Screen
        that navigate back to the mobile app open in the top window by setting
        target="_top" on those links, in O11 Mobile Apps using an iframe block,
        so that the app avoids nested iframes (cascade syndrome).
    - name: Fix Safari cookie block
      id: fix-safari-cookie-block
      description: Wants to fix an embedded O11 Mobile App iframe that loads
        but doesn't behave as expected in Safari on iOS/macOS, caused by
        Intelligent Tracking Prevention blocking third-party cookies, by
        implementing OAuth 2.0 token forwarding to a first-party session or
        Apple's Storage Access API consent flow, so cross-domain iframe
        content keeps working.
  - name: Front end architecture
    id: front-end-architecture
    subtopics:
    - name: Style Guide purpose
      id: style-guide-purpose
      description: Wants to understand what a Style Guide is and why it matters,
        including its role in ensuring brand theme consistency, UI consistency,
        and usability across an organization's applications, so they can
        recognize the Style Guide's purpose within the OutSystems UI
        architecture.
    - name: UI architecture overview
      id: ui-architecture-overview
      description: Wants to understand the components of the OutSystems UI
        architecture (the OutSystems UI Framework, built-in screen templates,
        custom application templates, style guide, and applications) and how
        they relate to each other, so they can understand how OutSystems UI
        structures the user interfaces of their applications, in OutSystems 11.
    - name: Look and feel overview
      id: look-and-feel-overview
      description: Wants to understand what capabilities the OutSystems UI
        Framework provides for customizing an app's look and feel, including
        CSS style sheets, screen layout placeholders, the style editor,
        themes, the theme editor, UI patterns, right-to-left content support,
        and responsive design, in OutSystems 11, so they can determine which
        OutSystems UI Framework tool to use to enhance their app's user
        interface and user experience.
    - name: Utility scope
      id: utility-scope
      description: Wants to understand that the Utilities section applies only to
        Mobile Apps and Reactive Web Apps in OutSystems 11, so they know which
        app types the utilities content covers.
  - name: Customize style
    id: customize-style
    subtopics:
    - name: Customize UI patterns
      id: customize-ui-patterns
      description: Wants to customize a built-in OutSystems UI pattern's look,
        structure, or behavior, by writing additional CSS in a new module for
        style changes, or copying the pattern to a new module to add
        placeholders or change its code, in Service Studio, so they can adapt
        patterns beyond the officially supported CSS-only customization while
        understanding that copied patterns won't receive OutSystems UI
        upgrades.
    - name: UI pattern extensibility
      id: ui-pattern-extensibility
      description: Wants to understand how OutSystems UI Patterns that use
        external providers expose curated input parameters and client actions,
        and how to reach the providers' full extensibility through provider
        client actions or provider instance and JavaScript, so they can work
        with the latest patterns and know when deprecated patterns use
        AdvancedFormat instead.
    - name: Customize styles in your app
      id: customize-app-styles
    - name: Creating a theme
      id: creating-a-theme
    - name: Layout placeholder names
      id: layout-placeholder-names
      description: Wants to identify the layout placeholder names reserved by the
        OutSystems platform, so they avoid naming conflicts when building a
        theme layout or block in OutSystems 11.
    - name: BEM methodology
      id: bem-methodology
      description: Wants to understand the BEM (Block Element Modifier) approach used in OutSystems UI themes, including how blocks, elements, and modifiers keep CSS components small and reusable, so they can structure theme CSS without unintended side effects.
    - name: CSS best practices
      id: css-best-practices
      description: Wants to write maintainable CSS in an OutSystems app by
        avoiding inline styles and ID selectors, centralizing CSS in the
        theme, and using CSS variables, in Service Studio, so their app's
        styling stays maintainable and scalable.
    - name: Promote style changes
      id: promote-style-changes
      description: Wants to promote visual Styles Editor changes to a reusable CSS
        class and move the generated CSS to a screen style sheet, so the styled
        container can be reused on other widgets.
    - name: Combine CSS classes
      id: combine-css-classes
      description: Wants to assign multiple CSS classes to a single widget and
        split text, background, and border styles across those classes, so the
        widget combines the expected styling from each class and reflects
        screen and block style loading order.
    - name: Switch style classes
      id: switch-style-classes
      description: Wants to set a widget's Style Classes property with an
        expression that switches between CSS classes based on a Boolean screen
        variable, so the widget can change style dynamically at runtime.
    - name: Set stacking order with layer system
      id: set-stacking-order-layer-system
      description: Wants to understand and use the OutSystems UI Layer
        System's z-index CSS variables across its different layer tiers to
        set stacking order for custom CSS or UI patterns, in OutSystems 11
        Mobile Apps and Reactive Web Apps, so their custom UI elements stack
        correctly.
    - name: Adjust layer system scale
      id: adjust-layer-system-scale
      description: Wants to adjust the OutSystems UI Layer System scale by
        extending or changing the available z-index values for layer tiers,
        in OutSystems 11 Mobile Apps and Reactive Web Apps, so that custom
        interface elements fit the app's required stacking model.
    - name: Mitigate layer system z-index breaking changes
      id: mitigate-layer-system-z-index-breaking-changes
      description: Wants to identify and mitigate breaking changes caused by
        the OutSystems UI Layer System's new z-index values, in OutSystems
        11 Mobile Apps and Reactive Web Apps, so their app keeps working as
        expected after adopting the updated layer system.
    - name: Make header scroll
      id: make-header-scroll
      description: Wants to make the London theme's fixed header scroll with the page
        instead of staying pinned at the top, by overriding the .Header CSS
        class's position and top properties to static and removing the .Content
        class's top padding, in an OutSystems 11 web app's style sheet, so that
        the header doesn't remain fixed in place and obscure content as users
        scroll through longer pages.
    - name: Change app favicon
      id: change-app-favicon
      description: Wants to replace the default OutSystems favicon with a custom PNG
        or ICO image by changing the favicon resource in Service Studio, in a Reactive
        Web App for O11, so that the app displays a branded browser tab icon.
    - name: Choose icon library
      id: choose-icon-library
      description: Wants to select and switch between the available icon libraries for an
        app by setting the Icon Library property at the theme level, in ODC
        Studio, so that icon rendering matches design needs and compatibility
        with existing or new apps.
    - name: Resolve icon library conflicts
      id: resolve-icon-library-conflicts
      description: Wants to resolve icon library mismatches when a referenced
        block uses a different icon library than the app's theme, by updating
        the producer library's icon setting or manually replacing icons flagged
        with a TrueChange warning, in ODC Studio, so that referenced blocks
        display correctly without visual inconsistency.
    - name: Fix theme mismatch
      id: fix-theme-mismatch
      description: 'Wants to resolve the "Themes are not compatible" warning
        between a Screen Template and their module, by selecting a compatible
        Screen Template, reverting Theme changes, or migrating a Traditional
        Web App from Silk UI/the London Theme to the OutSystems UI framework,
        in Service Studio for O11, so the final screen renders as previewed.'
    - name: Fix grid overflow
      id: fix-grid-overflow
      description: 'Wants to resolve the "Grid Dimensions Overflow" error caused
        by a widget''s Width property (set in grid columns) exceeding the number
        of columns defined in the theme''s grid, by reducing the widget''s
        Width value or increasing the theme''s Grid property, in Service Studio
        for OutSystems 11, so they can continue publishing or running the app
        without the error.'
    - name: Uppercase input text
      id: uppercase-input-text
      description: 'Wants to display input text in uppercase at runtime while
        keeping the stored variable''s original letter case, by applying a
        text-transform: uppercase CSS style class to the Input widget and
        using the ToUpper function on the variable in logic, in Service
        Studio for OutSystems 11, so end users see uppercase text without
        altering the underlying data casing.'
  - name: UI framework migration
    id: ui-framework-migration
    subtopics:
    - name: Check OutSystems UI version
      id: check-outsystems-ui-version
      description: Wants to find out which version of the OutSystems UI
        framework their app is using, by opening the Dependencies icon,
        clicking In Use, and selecting OutSystemsUI, in Service Studio, so
        they can determine whether any patterns they use are deprecated and
        know when to migrate.
    - name: Migrate Silk patterns
      id: migrate-silk-patterns
      description: Wants to migrate each Silk Web UI pattern to its OutSystems UI
        equivalent — including remapping content placeholders, updating input parameters,
        and handling structural changes introduced in O11 — so that a Silk-based
        Traditional Web App can be upgraded to the OutSystems 11 Web UI Framework
        without breaking existing screen behavior.
    - name: Migrate CSS classes
      id: migrate-css-classes
      description: Wants to find the OutSystems Web UI CSS class equivalent for
        each Silk Web UI CSS class (background colors, text colors, buttons, and
        text styles), so that a Silk-based Traditional Web App keeps consistent
        visual styling after migrating to the OutSystems 11 Web UI Framework.
    - name: Migration rationale
      id: migration-rationale
      description: Wants to understand why they need to migrate a Silk UI Web
        application to the OutSystems 11 Web UI Framework, including how it
        affects Screen Template compatibility, so they can decide whether and
        when to migrate their apps.
    - name: Estimate migration effort
      id: estimate-migration-effort
      description: Wants to estimate the time and complexity of migrating a
        Silk UI Web application to the OutSystems 11 Web UI Framework, based
        on pattern complexity and whether the app uses a style guide, so they
        can plan and scope the migration work realistically.
    - name: Migrate app structure
      id: migrate-app-structure
      description: Wants to migrate the structural elements of a Silk Web
        application (the Common folder, base theme, and layout blocks) to the
        OutSystems 11 Web UI Framework, in Service Studio, so the
        application's navigation, theming, and layout continue working
        correctly after the UI framework upgrade.
    - name: Choose migration path
      id: choose-migration-path
      description: Wants to choose a migration approach (pattern-by-pattern,
        screen-by-screen, or removing the Silk UI reference outright) for
        upgrading a Silk Web UI Traditional Web App to the OutSystems 11 Web
        UI Framework, so they can pick the path that best fits their team's
        workflow and app complexity.
  - name: Mobile UI framework selection
    id: mobile-ui-framework-selection
    subtopics:
    - name: Choose UI framework
      id: choose-ui-framework-mobile
      description: Wants to choose between the Mobile UI and OutSystems UI
        frameworks for a mobile app, based on project type (consumer vs.
        enterprise), performance needs, design requirements, and long-term
        strategy, in ODC Studio, so they can pick the framework best suited
        to their app's requirements.
    - name: Compare UI frameworks
      id: compare-ui-frameworks-mobile
      description: Wants to compare the technical differences between the
        Mobile UI and OutSystems UI frameworks, including target platform,
        performance, customization, widget availability, and icon libraries,
        in ODC Studio, so they can evaluate which framework fits their app's
        technical requirements.
- name: Creating UI
  guid: a6b567df-2deb-4736-bd6d-cf144a9c6262
  topics:
  - name: Visibility conditions of inputs in App Editor
    id: visibility-conditions-app-editor
    subtopics:
    - name: Removing visibility conditions in App Editor
      id: remove-visibility-cond-app-editor
    - name: Adding visibility conditions in App Editor
      id: add-visibility-cond-app-editor
    - name: Modify visibility conditions in App Editor
      id: edit-visibility-cond-app-editor
    - name: Identify existing visibility conditions in App Editor
      id: id-visibility-cond-app-editor
  - name: Business validations of user input in App Editor
    id: biz-val-app-editor
    subtopics:
    - name: Removing validations in App Editor
      id: remove-biz-val-app-editor
    - name: Adding validations in App Editor
      id: add-biz-val-app-editor
    - name: Refine/Edit validations in App Editor
      id: edit-biz-val-app-editor
    - name: Identify existing validations in App Editor
      id: id-auto-gen-val
  - name: Adding UI Elements to a Screen
    id: add-ui-elements
    subtopics:
    - name: Use Separator pattern
      id: use-separator-pattern
      description: Wants to add the Separator UI Pattern to a Traditional Web
        App screen, recover the dependency if the widget does not display in
        the Toolbox, drag it into Main Content, and configure its optional
        properties in Service Studio, so that content is visually separated
        in O11.
    - name: Style Card Sectioned
      id: style-card-sectioned
      description: Wants to replace a List Item inside a Gallery/List widget
        with the Card Sectioned widget to organize content into its
        placeholders, in Service Studio for OutSystems 11, so that gallery
        items follow a structured and consistent content layout.
    - name: Fix gallery image sizing
      id: fix-gallery-image-sizing
      description: Wants to fix inconsistent image sizes in a Gallery/List
        widget by applying an image-sizing style class and setting explicit
        dimensions on the Image widget, in Service Studio for OutSystems 11,
        so that gallery items display with a clean, consistent visual layout.
    - name: Add menu link
      id: add-menu-link
      description: Wants to add a screen to the application menu by dragging it into the
        menu bar and setting its Text property, so that users can open that
        screen from the app toolbar in a Traditional Web App.
    - name: Mobile CRUD screens
      id: crud-screens-mobile
      description: Wants to build List and Form screens for an entity and wire
        up Create, Update, and Delete logic, including a swipe action to
        delete list items, in Service Studio for OutSystems 11 Mobile Apps,
        so that end users can list, add, edit, and delete records from the
        app.
    - name: Reactive CRUD screens
      id: crud-screens-reactive-web
      description: Wants to build a list screen and a form screen for an entity,
        and wire up Create, Update, and Delete logic using server actions and
        entity actions, so that end users can list, add, edit, and delete
        records, in Service Studio for OutSystems 11 Reactive Web Apps.
    - name: Master Detail reference
      id: master-detail-widget-reference
      description: Wants to look up the properties of the Master Detail UI Pattern in
        Service Studio, so that they can correctly configure the widget's layout
        and behavior in mobile apps and reactive web apps (O11).
    - name: Use Master Detail pattern
      id: use-master-detail-pattern
      description: Wants to add the Master Detail UI Pattern to a screen, bind
        the LeftContent list to an aggregate, and use a client action to
        filter and fetch the RightContent details for the selected item,
        including adding the widget via Toolbox or Search in other modules
        if the dependency isn't added, in Service Studio, so that a master
        list and its corresponding item details display together in mobile
        apps and reactive web apps (O11).
    - name: Use Stacked Cards pattern
      id: use-stacked-cards-pattern
      description: Wants to add the Stacked Cards UI Pattern to a screen, add the widget
        via Toolbox or Search in other modules if the dependency isn't added,
        populate the List and overlay placeholders, configure swipe actions for
        the overlay directions, and configure the pattern's optional properties,
        in Service Studio for OutSystems 11, so that users can swipe cards in
        multiple directions in mobile apps and reactive web apps.
    - name: Use Stacked Icon pattern
      id: use-stacked-icon-pattern
      description: Wants to add the Stacked Icon UI Pattern to a screen,
        configure the front and back icons, colors, and size, and recover the
        dependency if it is missing, in Service Studio for OutSystems 11, so
        that users can create a distinctive stacked icon in traditional web
        apps.
    - name: Stacked Icon reference
      id: stacked-icon-reference
      description: Wants to look up the properties of the Stacked Icon UI Pattern in
        Service Studio, so that they can correctly configure a stacked icon in
        traditional web apps for OutSystems 11.
    - name: Use Action Sheet pattern
      id: use-action-sheet-pattern
      description: Wants to add the Action Sheet UI Pattern to a screen, add
        the widget via Toolbox or Search in other modules if the dependency
        isn't added, add content to the button placeholders, and wire a
        local variable to the IsOpen property with an OnClick action to
        open the menu and an OnClose action to close it, in Service Studio,
        so that a menu slides from the bottom of the screen when
        triggered, in mobile apps and reactive web apps (O11).
    - name: Action Sheet reference
      id: action-sheet-reference
      description: Wants to look up the properties of the Action Sheet UI
        Pattern in Service Studio, including IsOpen and ExtendedClass, so
        that they can correctly configure the pattern's open/close state
        and custom styling, in mobile apps and reactive web apps (O11).
    - name: Use Wizard pattern
      id: use-wizard-pattern
      description: Wants to add the Wizard UI Pattern to a screen and build
        multi-step navigation, by adding the Wizard and Wizard Item widgets,
        creating a CurrentStep Input Parameter, setting each Wizard Item's
        Status via an expression that checks CurrentStep, using If widgets
        to show step-specific content, and wiring Previous/Next buttons with
        Assign actions and Enclose in If conditions to move between steps,
        in Service Studio, so that the wizard displays the active, past,
        and next steps correctly, in mobile apps and reactive web apps
        (O11).
    - name: Add switch widget
      id: add-switch-widget
      description: Wants to add a Switch widget to a screen and bind a
        boolean variable to its Variable property, in Service Studio, so
        that end users can provide boolean input in a form for mobile apps
        and reactive web apps (O11).
    - name: Add check box widget
      id: add-check-box-widget
      description: Wants to add a Check Box widget to a screen and bind a
        boolean variable to its Variable property, in Service Studio, so
        that end users can provide boolean input in a form for traditional
        web apps (O11).
    - name: Use Bottom Sheet pattern
      id: use-bottom-sheet-pattern
      description: Wants to add the Bottom Sheet UI Pattern to a screen, add
        the widget via Toolbox or Search in other modules if the dependency
        isn't added, add content to the TopBar and Content placeholders,
        and wire Open and Close buttons using the BottomSheetOpen and
        BottomSheetClose client actions with the WidgetId property set to
        BottomSheet.Id, in Service Studio, so that additional information
        displays and dismisses from the bottom of the screen in mobile
        apps (O11).
    - name: Use Floating Actions
      id: use-floating-actions
      description: Wants to add the Floating Actions UI Pattern to a screen,
        add the widget via Toolbox or Search in other modules if the
        dependency isn't added, add content to the Button and Items
        placeholders, and configure IsExpanded and IsHover in Service
        Studio, so that a floating action button opens additional actions
        on mobile and reactive web screens (O11).
    - name: Bottom Sheet reference
      id: bottom-sheet-reference
      description: Wants to look up the properties and events of the Bottom
        Sheet UI Pattern in Service Studio, including Shape, ShowHandler,
        ExtendedClass, and the OnToggle event, so that they can correctly
        configure the pattern's appearance and behavior, in mobile apps
        (O11).
    - name: Add icon block
      id: add-icon-block
      description: Wants to add an Icon block from Rich Widgets to a screen
        and select the icon to display via the Name property drop-down, in
        Service Studio, so that end users see an icon on the screen, in
        traditional web apps (O11).
    - name: Add dropdown widget
      id: add-dropdown-widget
      description: Wants to add the Dropdown Widget to a screen and configure
        its List, Options Content, Options Text, and Variable properties, in
        Service Studio, so that end users can choose one value from a list of
        values, in mobile apps and reactive web apps (O11).
    - name: Add combo box widget
      id: add-combo-box-widget
      description: Wants to add the Combo Box Widget to a screen and configure
        its Source Entity, Source Attribute, and Variable properties, in
        Service Studio, so that end users can choose one value from a list of
        values bound to an Entity, in traditional web apps (O11).
    - name: Use Dropdown pattern
      id: use-dropdown-pattern
      description: Wants to use the Dropdown UI Pattern in a Traditional Web
        App by adding the widget, binding a List Records widget to a data
        source, creating a Screen Action for each option, and refreshing the
        selected value after Ajax Submit, so that users can choose an option
        from a dynamic list and see feedback.
    - name: Use Dropdown Select pattern
      id: use-dropdown-select-pattern
      description: Wants to use the Dropdown Select UI Pattern in a Traditional
        Web App by adding the widget, linking it to a Combo Box or List Box
        widget, and configuring its WidgetId and optional properties, so that
        users can use an enhanced combo box or multi-select dropdown in forms.
    - name: Dropdown Server reference
      id: dropdown-server-reference
      description: Wants to look up the properties, events, and JavaScript API
        (OutSystems.OSUI.Patterns.DropdownAPI and DropdownServerSideItemAPI)
        of the Dropdown Server Side UI pattern in Service Studio, so that
        they can correctly configure and programmatically control the
        pattern's behavior in mobile apps and reactive web apps (O11).
    - name: Use Dropdown Server Side
      id: use-dropdown-server-side
      description: Wants to add the Dropdown Server Side UI pattern to a
        screen, bind its list source to an aggregate, customize item content
        with expressions and images, and save the selected option in a local
        variable via the OnSelected event, in Service Studio, so that they
        can build a customizable, data-driven dropdown for mobile apps and
        reactive web apps (O11).
    - name: Use User Avatar pattern
      id: use-user-avatar-pattern
      description: Wants to add the User Avatar UI Pattern to a screen, bind the Name
        property to an aggregate via the Expression Editor to display user
        initials, add the widget via Toolbox or Search in other modules if the
        dependency isn't added, and configure its display and behavior
        properties, so that a circular badge displays a user's initials or image
        in mobile apps and reactive web apps.
    - name: User Avatar reference
      id: user-avatar-reference
      description: Wants to look up the properties of the User Avatar UI Pattern in Service
        Studio, so that they can correctly configure the badge's appearance and
        content, in mobile apps and reactive web apps (O11).
    - name: Use Chat Message pattern
      id: use-chat-message-pattern
      description: Wants to add the Chat Message UI Pattern to a screen, add
        the widget via Toolbox or Search in other modules if the dependency
        isn't added, add content such as an avatar and text to the Image and
        Content placeholders (or bind a List to an entity to display a
        message per record), and customize its look and feel via optional
        properties, in Service Studio, so that conversational posts display
        in notifications or chat screens, in mobile apps and reactive web
        apps (O11).
    - name: Chat Message reference
      id: chat-message-reference
      description: Wants to look up the properties of the Chat Message UI
        Pattern in Service Studio, including DisplayOnRight, Time,
        MessageStatus, and ExtendedClass, so that they can correctly
        configure the pattern's alignment, timestamp, status, and custom
        styling, in mobile apps and reactive web apps (O11).
    - name: Add Sign In button
      id: add-sign-in-button
      description: Wants to add a second Sign In button to the Login Form UI
        Pattern by dragging a Columns 2 structure into the layout-login-button
        container and setting the new button's Label, in Service Studio, so
        that the login screen offers an additional sign-in action, in a
        Traditional Web App (O11).
    - name: Modal animation speed
      id: modal-animation-speed
      description: Wants to change the animation speed of the Modal UI Pattern
        by overriding the .modal .animate CSS class's animation-duration
        property in the theme, in Service Studio, so that the modal opens
        and closes faster or slower, in a Traditional Web App (O11).
    - name: Modal pattern reference
      id: modal-pattern-reference
      description: Wants to look up the properties of the Modal UI Pattern in Service
        Studio, so that they can correctly configure modal behavior and styling
        in a Traditional Web App (O11).
    - name: Use Modal pattern
      id: use-modal-pattern
      description: Wants to add the Modal UI Pattern to a screen, place
        content in the Title, Icon, Content, and Footer placeholders, and
        use the ToggleModal action with the WidgetId property to open and
        close the modal, in Service Studio, so that the app can display a
        focused interaction in a Traditional Web App (O11).
    - name: Use Panel pattern
      id: use-panel-pattern
      description: Wants to add the Panel UI Pattern to a screen, add the widget via
        Toolbox or Search in other modules if the dependency isn't added, fill
        the Title, Actions, Content, and Footer placeholders, and place grouped
        content in a small, structured block, so that users can present related
        content in a compact panel in a Traditional Web App (O11).
    - name: Use Fieldset pattern
      id: use-fieldset-pattern
      description: Wants to add the Fieldset UI Pattern to a Traditional Web
        App screen, set its Title, add related content to the placeholder,
        and optionally apply custom classes with ExtendedClass, so that
        related information is grouped and labeled clearly in Service Studio
        for O11.
    - name: Panel reference
      id: panel-reference
      description: Wants to look up the properties of the Panel UI Pattern in
        Service Studio, including ExtendedClass, so that they can correctly
        configure custom style classes for the pattern in a Traditional Web App
        (O11).
    - name: Adding a Block
      id: adding-a-block
    - name: Block widget reference
      id: block-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Block widget in Service Studio, including Name, Source Block, and Id, so
        that they can correctly display a Block with input parameters on a screen
        in a Traditional Web App or Mobile App (O11).
    - name: Block element reference
      id: block-element-reference
      description: Wants to look up the properties and events of the Block element,
        including its identification, visibility, and lifecycle event handlers,
        in Service Studio, so that they can correctly configure a Block's
        behavior and lifecycle in a Traditional Web App (O11).
    - name: Comment widget reference
      id: comment-widget-reference
      description: Wants to look up the properties of the Comment Widget in
        Service Studio, including Text and Is Reminder, so that they can
        correctly add a development-time screen annotation that appears as a
        TrueChange reminder in a Traditional Web App (O11).
    - name: Add panel shadow
      id: add-panel-shadow
      description: Wants to add a box-shadow effect to the Panel UI Pattern by
        setting the ExtendedClass parameter to shadow-l, in Service Studio, so
        that the panel displays a shadow effect in a Traditional Web App (O11).
    - name: Expression widget reference
      id: expression-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Expression widget in Service Studio, including Value, Escape Content, and
        Id, so that they can correctly display a literal text or an evaluated
        expression on a screen in a Traditional Web App (O11).
    - name: Range Slider reference
      id: range-slider-reference
      description: Wants to look up the events and CSS selectors for the
        Range Slider UI Pattern, including the OnChange event and
        .noUi-handle/.noUi-connect classes, so that they can correctly
        customize the handle and interval color, in a Traditional Web App
        (O11).
    - name: Use Range Slider Interval
      id: use-range-slider-interval-pattern
      description: Wants to add the Range Slider Interval UI Pattern to a
        screen by dragging the widget into the Main Content area, setting
        the MinValue, MaxValue, StartingValueFrom, and StartingValueTo
        properties, and creating an OnValueChange client action to read the
        IntervalStart and IntervalEnd values, in Service Studio, so that
        users can select a value within a chosen range, in mobile apps and
        reactive web apps (O11).
    - name: Slider Interval reference
      id: range-slider-interval-reference
      description: Wants to look up the configuration properties and events of the Range
        Slider Interval UI Pattern in Service Studio, so that they can correctly
        configure and customize the pattern's behavior, in mobile apps and
        reactive web apps (O11).
    - name: Light Box Image reference
      id: lightbox-image-reference
      description: Wants to look up the CSS selectors and classes for the
        Light Box Image UI Pattern, including .pswp__bg,
        .pswp__ui--fit .pswp__top-bar/.pswp__caption,
        .lightbox-content-thumbnail img, and .pswp__item img, so that they
        can correctly customize the overlay opacity and add rounded
        corners to images, in a Traditional Web App (O11).
    - name: Use Lightbox Image pattern
      id: use-lightbox-image-pattern
      description: Wants to add the Lightbox Image UI Pattern to a screen by
        dragging the widget from the Toolbox into the Main Content area,
        adding an Image placeholder per thumbnail and selecting or
        importing each image, in Service Studio, so that thumbnail images
        open in full screen mode for a gallery, in mobile apps and
        reactive web apps (O11).
    - name: Lightbox Image properties
      id: lightbox-image-properties
      description: Wants to look up the properties of the Lightbox Image UI
        Pattern in Service Studio, including Title, Group, ImageURL,
        ImageZoom, and ExtendedClass, so that they can correctly configure
        an image's title, gallery grouping, and full-screen zoom behavior,
        in mobile apps and reactive web apps (O11).
    - name: Use Section Group pattern
      id: use-section-group-pattern
      description: Wants to add the Section Group UI Pattern to a screen, add
        the widget via Toolbox or Search in other modules if the dependency
        isn't added, add content to the Title and Content placeholders, and
        set the HasStickyTitles and TopPosition properties, in Service
        Studio, so that section titles stay in view while scrolling
        through content, in mobile apps and reactive web apps (O11).
    - name: Section Group reference
      id: section-group-reference
      description: Wants to look up the properties of the Section Group UI
        Pattern in Service Studio, including HasStickyTitles, TopPosition,
        and ExtendedClass, so that they can correctly configure sticky
        header behavior and custom styling, in mobile apps and reactive
        web apps (O11).
    - name: Use Balloon pattern
      id: use-balloon-pattern
      description: Wants to add the Balloon UI Pattern to a Traditional Web
        App, bind it to a UI element, enter pop-up content, and configure
        its position, trigger, and optional styling so that additional
        information appears next to the target element.
    - name: Use Tooltip pattern
      id: use-tooltip-pattern
      description: Wants to add the Tooltip UI Pattern to a screen, add the
        widget via Toolbox or Search in other modules if the dependency
        isn't added, add content to the Content and Tooltip placeholders,
        wire an On Click event, and set the Position property, in Service
        Studio, so that informative text displays when a user hovers over,
        clicks, or taps an element, in mobile apps and reactive web apps
        (O11).
    - name: Tooltip properties reference
      id: tooltip-reference
      description: Wants to look up the properties of the Tooltip UI Pattern
        or widget in Service Studio or ODC Studio, including Position,
        StartsOpen, Trigger, and ExtendedClass, so that they can correctly
        configure tooltip appearance and behavior in mobile apps and
        reactive web apps.
    - name: Use Input with Icon pattern
      id: use-input-with-icon-pattern
      description: Wants to add the Input with Icon UI Pattern to a screen
        via Toolbox or Search in other modules if the dependency isn't
        added, set a local Variable for the Input widget, configure the
        Prompt text, and select an Icon, in Service Studio, so that end
        users get a visual hint when entering data, in mobile apps and
        reactive web apps (O11).
    - name: Input with Icon reference
      id: input-with-icon-reference
      description: Wants to look up the properties of the Input with Icon UI
        Pattern in Service Studio, including AlignIconRight and
        ExtendedClass, so that they can correctly configure icon placement
        and custom styling, in mobile apps and reactive web apps (O11).
    - name: File Upload reference
      id: file-upload-reference
      description: Wants to look up the CSS selectors and classes for the
        File Upload UI Pattern, including .file-upload.active, so that they
        can correctly customize the icon-to-delete visibility state, in a
        Traditional Web App (O11).
    - name: Separator reference
      id: separator-reference
      description: Wants to look up the properties of the Separator UI Pattern
        in Service Studio, so they can configure line color, spacing,
        orientation, and custom classes in mobile apps and reactive web apps
        (O11).
    - name: Use Sidebar pattern
      id: use-sidebar-pattern
      description: Wants to add and configure the Sidebar UI Pattern in a
        Traditional Web App, including finding the widget when the
        dependency is missing, placing it in the screen, wiring
        ToggleSidebar to a button, and setting WidgetId, HasOverlay, and
        ExtendedClass, so that additional information appears beside the
        main content.
    - name: Build calculator screen
      id: build-calculator-screen
      description: Wants to build a simple Reactive Web calculator screen with a screen
        input parameter, Text, Expression, Container, Button, Link, and If
        widgets, so that users can display a number, increment it by one,
        multiply it by two, and reset it only when the value is not zero.
    - name: Floating Actions reference
      id: floating-actions-reference
      description: Wants to look up the properties, events, and CSS selectors
        for the Floating Actions UI Pattern, including the OnToggle event and
        the .floating-actions.is--open class, so that they can correctly
        customize its layout, open/close behavior, and position (for example,
        moving it to the opposite side of the screen), in a Traditional Web
        App (O11).
    - name: Navigation Bar reference
      id: navigation-bar-reference
      description: Wants to look up the CSS selectors and classes for the
        Navigation Bar UI Pattern, including .navigation-bar.is--sticky,
        .navigation-bar-item.is--active/is--open/is--closed, and
        .navigation-bar-item-content active/open/closed/expanded/collapsed
        states, plus how to override the active-color CSS variable on items
        and sub-items, so that they can correctly customize its layout and
        appearance, in a Traditional Web App (O11).
    - name: Use Navigation Bar with ListRecords
      id: navigation-bar-listrecords
      description: Wants to combine the Navigation Bar UI Pattern with a
        ListRecords widget, by setting Line Separator to None and using
        expressions in the NavigationBarItem/NavigationBarSubItem
        placeholders, in Service Studio, so that navigation items are
        dynamically generated from database content, in a Traditional Web
        App (O11).
    - name: List Records layout
      id: list-records-layout
      description: Wants to display records side-by-side instead of one per line by
        setting the Line Separator property of the List Records widget to None, in
        Service Studio for O11, so that records appear in a multi-column layout on
        the screen.
    - name: Integrate jQuery plugin
      id: integrate-jquery-plugin
      description: Wants to integrate a third-party jQuery plugin into an OutSystems
        Traditional Web app by creating a web block that encapsulates the plugin's
        JavaScript, CSS, and resources, adding optional parameters to control the
        plugin, and using an un-escaped Expression to activate it, in Service Studio
        for O11, so that the plugin can be reused across multiple screens without
        duplicating resources.
    - name: Image widget reference
      id: image-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Image widget in Service Studio, including Type, Image, URL, Filename,
        Cache, Attribute, and Entity Identifier, so that they can correctly
        display an image from a static resource, external URL, or platform
        database in a Traditional Web App (O11).
    - name: Placeholder widget reference
      id: placeholder-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Placeholder widget in Service Studio, including Name, Align, Style
        Classes, Is Ajax Refreshed, and Id, so that they can correctly reserve
        and configure space in a Block for content in a Traditional Web App or
        Mobile App (O11).
    - name: Image resource reference
      id: image-resource-reference
      description: Wants to look up the properties and runtime properties of the
        Image resource element in Service Studio, including Width, Height,
        Runtime Path, Original Filename, Original Format, and Public, so that
        they can correctly configure an image file resource in a Traditional
        Web App or Mobile App (O11).
    - name: Resource reference
      id: resource-reference
      description: Wants to look up the properties and runtime properties of the
        Resource element in Service Studio, including Name, Public, Deploy
        Action, Target Directory, Runtime Path, Resource Content, URL, and
        Content, so that they can correctly configure a resource file in a
        Traditional Web App or Mobile App (O11).
    - name: Icon widget reference
      id: icon-widget-ref
      description: Wants to look up the properties, events, and runtime properties
        of the Icon widget in ODC Studio, so that they can correctly configure icon
        display — including icon selection, size, visibility, CSS classes, HTML
        attributes, and event handlers — in a Reactive Web App.
    - name: Check Box widget reference
      id: check-box-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Check Box widget in Service Studio, so that they can correctly configure a
        boolean-backed check box field with validation in a Traditional Web App
        (O11).
    - name: Radio Button widget reference
      id: radio-button-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Radio Button widget in Service Studio, including Name, Variable,
        Validation Parent, and Value, so that they can correctly configure a group
        of mutually exclusive options bound to the same variable in a Traditional
        Web App (O11).
    - name: Input Password widget reference
      id: input-password-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Input Password widget in Service Studio, including Password Type, Max.
        Length, and Extended Properties, so that they can correctly configure a
        masked text input field with validation in a Traditional Web App (O11).
    - name: Input Widget reference
      id: input-widget-reference
      description: Wants to look up the properties and runtime properties of the
        Input Widget in Service Studio, including Name, Variable, Mandatory,
        Max. Length, and Type, so that they can correctly configure a validated
        text input field in a Traditional Web App (O11).
    - name: If Widget reference
      id: if-widget-reference
      description: Wants to look up the properties and runtime properties of
        the If Widget in Service Studio, including Condition, Animate, and Id,
        so that they can correctly configure conditional content rendering on
        a screen in a Traditional Web App or Mobile App (O11).
    - name: Badge reference
      id: badge-reference
      description: Wants to look up the configurable properties of the Badge UI Pattern in
        Service Studio, so that they can correctly configure a numerical
        notification badge in a Mobile App or Reactive Web App for OutSystems
        11.
    - name: Use Badge pattern
      id: use-badge-pattern
      description: Wants to add the Badge UI Pattern to a screen and bind its
        Number property to an aggregate count, including adding the widget
        via Toolbox or Search in other modules if the dependency isn't added,
        in Service Studio, so that numerical notifications like unread counts
        display in mobile apps and reactive web apps (O11).
    - name: Use Blank Slate pattern
      id: use-blank-slate-pattern
      description: Wants to add the Blank Slate UI Pattern to a screen,
        adding the OutSystemsUI dependency via Search in other modules if
        the widget doesn't display in the Toolbox, and customizing its
        icon, content, and action placeholders, including the FullHeight
        and ExtendedClass properties, in Service Studio, so that
        empty-state messages display when there's no data or a task
        needs completing, in mobile apps and reactive web apps (O11).
    - name: Counter reference
      id: counter-reference
      description: Wants to look up the properties of the Counter UI Pattern in
        Service Studio, including BackgroundColor, IsVertical, Height, and
        ExtendedClass, so that they can correctly configure a counter's
        appearance and orientation, in a Mobile App or Reactive Web App for
        OutSystems 11.
    - name: Use Counter pattern
      id: use-counter-pattern
      description: Wants to add the Counter UI Pattern to a screen and bind its
        content to an aggregate's Count property, including creating the
        aggregate via Fetch Data from Database and adding the widget via
        Toolbox or Search in other modules if the dependency isn't added, in
        Service Studio, so that a dynamic numerical count, like registered
        users, displays in mobile apps and reactive web apps (O11).
    - name: Icon Badge reference
      id: icon-badge-reference
      description: Wants to look up the properties of the Icon Badge UI Pattern
        in Service Studio, including Number, Color, IsLight, and ExtendedClass,
        so that they can correctly configure a numerical notification badge in
        a Mobile App or Reactive Web App for OutSystems 11.
    - name: Add icon widget
      id: add-icon-widget-odc
      description: Wants to add an Icon widget to a screen and select the icon and weight,
        adjusting its size, color, and styling, in ODC Studio, so that visual
        cues are included in the app's UI.
    - name: Add icon (O11)
      id: add-icon-widget-o11
      description: Wants to add an Icon widget from the widgets toolbox to a
        screen, select the icon from the built-in icon collection, and
        optionally associate an OnClick action, in Service Studio, so that
        end users can visually identify functionality or information in
        reactive web apps and mobile apps (OutSystems 11).
    - name: Use Icon Badge pattern
      id: use-icon-badge-pattern
      description: Wants to add the Icon Badge UI Pattern to a screen and bind
        its Number property to an aggregate count, including adding the widget
        via Toolbox or Search in other modules if the dependency isn't added,
        and recovering the dependency in Service Studio if the widget does not
        display, so that numerical notifications like unread message counts
        display correctly.
    - name: Icon Badge layout reference
      id: icon-badge-layout-reference
      description: Wants to look up the layout and CSS classes of the Icon
        Badge UI Pattern, in Service Studio, so that they can correctly
        identify the pattern's structure for customization, in a Traditional
        Web App (O11).
    - name: Use Alert pattern
      id: use-alert-pattern
      description: Wants to add the Alert UI Pattern to a screen, add the OutSystemsUI
        dependency if the widget doesn't display in the Toolbox, and set the
        MessageText placeholder and the AlertType property to the appropriate
        alert type, in Service Studio, so that important messages, errors, or
        warnings display to users in mobile apps and reactive web apps (O11).
    - name: Alert reference
      id: alert-reference
      description: Wants to look up the CSS selectors and classes for the Alert
        UI Pattern, including alert-error, alert-info, alert-success,
        alert-warning, and alert-close.is--hidden, so that they can correctly
        customize the Alert Pattern's appearance based on its type and the
        ShowCloseButton parameter, in a Traditional Web App (O11).
    - name: Use Notification pattern
      id: use-notification-pattern
      description: Wants to add the Notification UI Pattern to a screen, add
        the OutSystemsUI dependency if the widget doesn't display in the
        Toolbox, add content to its placeholder, and wire Open/Close buttons
        to the NotificationOpen and NotificationClose client actions, in
        Service Studio, so that contextual short messages like app crashes,
        updates, or reminders display to users in mobile apps and reactive
        web apps (O11).
    - name: Notification reference
      id: notification-reference
      description: Wants to look up the properties and events of the Notification UI
        Pattern in Service Studio, so that they can correctly configure and
        control the pattern's appearance and behavior, in mobile apps and
        reactive web apps (O11).
    - name: Use Card pattern
      id: use-card-pattern
      description: Wants to add the Card UI Pattern to a screen, add the
        OutSystemsUI dependency via Search in other modules if the widget
        doesn't display in the Toolbox, and add content to its placeholder,
        in Service Studio, so that a small block of information is grouped
        and highlighted on-screen in mobile apps and reactive web apps
        (O11).
    - name: Card reference
      id: card-reference
      description: Wants to look up the properties of the Card UI Pattern in
        Service Studio, including UsePadding and ExtendedClass, so that they
        can correctly configure the pattern's padding and custom styling, in
        mobile apps and reactive web apps (O11).
    - name: Use Card Sectioned
      id: use-card-sectioned
      description: Wants to add the Card Sectioned UI Pattern to a Traditional
        Web App screen, add the OutSystemsUI dependency if the widget does not
        display in the Toolbox, drag it into Main Content, and populate its
        placeholders, so that users can group short pieces of information into
        a noticeable block on screen.
    - name: Card Sectioned settings
      id: card-sectioned-settings
      description: Wants to configure the Card Sectioned widget settings,
        including UsePadding, IsVertical, ImagePadding, and ExtendedClass, so
        that the card layout, padding, image spacing, and custom styling match
        the screen design.
    - name: Use Card Item pattern
      id: use-card-item-pattern
      description: Wants to add the Card Item UI Pattern to a screen, add
        the OutSystemsUI dependency via Search in other modules if the
        widget doesn't display in the Toolbox, and add content to each of
        its Left, Title, Content, and Right placeholders, in Service
        Studio, so that items with multiple content types (image/icon,
        title, description, and an action) are listed together in mobile
        apps and reactive web apps (O11).
    - name: Card Item reference
      id: card-item-reference
      description: Wants to look up the properties of the Card Item UI
        Pattern in Service Studio, including ExtendedClass, so that they
        can correctly configure the pattern's custom styling, in mobile
        apps and reactive web apps (O11).
    - name: Use List Item Content pattern
      id: use-list-item-content-pattern
      description: Wants to add the List Item Content UI Pattern to a
        screen, add the OutSystemsUI dependency via Search in other
        modules if the widget doesn't display in the Toolbox, and add
        content to its placeholders, in Service Studio, so that critical
        content such as icons, text, and actions organizes in a readable
        way inside a list, in mobile apps and reactive web apps (O11).
    - name: List Item Content reference
      id: list-item-content-reference
      description: Wants to look up the properties of the List Item
        Content UI Pattern in Service Studio, including ExtendedClass, so
        that they can correctly configure the pattern's custom styling,
        in mobile apps and reactive web apps (O11).
    - name: Use Card Background pattern
      id: use-card-background-pattern
      description: Wants to add the Card Background UI Pattern to a screen,
        add the OutSystemsUI dependency if the widget doesn't display in the
        Toolbox, and configure its content, background image, color, and
        height properties, in Service Studio, so that a small block of
        information is grouped and highlighted with a background image.
    - name: Use Carousel pattern
      id: use-carousel-pattern
      description: Wants to add the Carousel UI Pattern to a screen, add the OutSystemsUI
        dependency if the widget doesn't display in the Toolbox, place content
        or a List widget inside the CarouselItems placeholder, and configure the
        carousel's optional settings, so that horizontal slides display.
    - name: Use Tag pattern
      id: use-tag-pattern
      description: Wants to add the Tag UI Pattern to a screen, add the
        OutSystemsUI dependency if the widget doesn't display in the
        Toolbox, add content to its placeholders, and configure its Color,
        Shape, Size, IsLight, and ExtendedClass properties, in Service
        Studio, so that small texts display in a colored tag format to show
        statuses, labels, or categories.
    - name: Tag reference
      id: tag-reference
      description: Wants to look up the properties of the Tag UI Pattern in
        Service Studio, including Color, Shape, Size, IsLight, and
        ExtendedClass, so that they can correctly configure a Tag's
        appearance in mobile apps and reactive web apps (O11).
    - name: Use Animated Label pattern
      id: use-animated-label-pattern
      description: Wants to add the Animated Label UI Pattern to a screen,
        add the OutSystemsUI dependency if the widget doesn't display in
        the Toolbox, enter the label text, and set a name and type for the
        Input variable, in Service Studio, so that an animated label
        displays for user input.
    - name: Animate Alert pattern
      id: animate-alert-pattern
      description: Wants to add an enter animation to the Alert UI Pattern, using
        a container bound to a Boolean local variable and the Animate pattern
        with the EnterTop animation, in Service Studio, so that the alert
        transitions in visually when triggered, in a Traditional Web App (O11).
    - name: Animate reference
      id: animate-reference
      description: Wants to look up the CSS selectors and classes for the
        Animate UI Pattern, including animate-wrapper.is--visible/is--hidden
        and the enter/leave direction, fade, and scale classes, plus the
        animate-slow and animate-fast timing modifiers, so that they can
        correctly customize enter and leave animations for elements in a
        Traditional Web App (O11).
    - name: Trigger Animate pattern
      id: trigger-animate-pattern
      description: Wants to trigger the Animate UI Pattern programmatically,
        using the ToggleAnimate server action to show or hide an element, or
        the LoadOnVisible pattern to animate elements as they scroll into
        view, in Service Studio, so that UI elements animate dynamically in
        a Traditional Web App (O11).
    - name: Use Animate pattern
      id: use-animate-pattern-o11
      description: Wants to add the Animate UI Pattern to a screen, add the
        OutSystemsUI dependency via Search in other modules if the widget
        doesn't display in the Toolbox, add content to its Content
        placeholder, and configure the AnimationType, Delay, and Speed
        properties, in Service Studio, so that elements are emphasized with
        an entrance animation as they appear on screen, in mobile apps and
        reactive web apps (O11).
    - name: Use Animate web pattern
      id: use-animate-web-pattern-o11
      description: Wants to add the Animate UI Pattern to a Traditional Web
        App screen, add the OutSystemsUI dependency if the widget does not
        display in the Toolbox, drag the widget into Main Content, and
        configure EnterAnimation, LeaveAnimation, Speed, Delay, and
        ExtendedClass in Service Studio, so that elements animate as they
        appear on screen in a Traditional Web App (O11).
    - name: Animate properties
      id: animate-properties-reference
      description: Wants to look up the properties of the Animate UI Pattern
        in Service Studio, including AnimationType, Delay, and Speed, so
        that they can correctly configure how and when an element's
        entrance animation occurs, in mobile apps and reactive web apps
        (O11).
    - name: Adaptive UI patterns
      id: adaptive-ui-patterns
      description: Wants to understand what adaptive UI patterns are and why to use them,
        so that they can choose the appropriate pattern to ensure their app
        displays correctly across different device sizes, resolutions, and
        aspect ratios for mobile apps and reactive web apps.
    - name: Number patterns overview
      id: number-patterns-overview
      description: Wants to know which number-based UI pattern to use for displaying
        numerical data, so that they can choose the appropriate pattern for
        showing counts, percentages, ratings, or progress in a Mobile App or
        Reactive Web App for OutSystems 11.
    - name: Content patterns overview
      id: content-patterns-overview
      description: Wants to know which content UI pattern to use for displaying
        warnings, errors, tooltips, notifications, or organizing information
        into sections and lists, so that they can choose the appropriate
        pattern for a Mobile App or Reactive Web App for OutSystems 11.
    - name: Use Columns pattern
      id: use-columns-pattern
      description: Wants to add the Columns UI Pattern to a screen and configure
        its responsive layout properties — including GutterSize, TabletBehavior,
        and PhoneBehavior — in ODC Studio, so that content is organized into an
        adaptive multi-column layout across different device types.
    - name: Use Columns pattern (O11)
      id: use-columns-pattern-o11
      description: Wants to add the Columns UI Pattern to a screen and configure its
        responsive layout properties, in Service Studio for OutSystems 11, so
        that content is organized into an adaptive multi-column layout across
        different device types, in mobile apps and reactive web apps.
    - name: Use Display on Device
      id: use-display-on-device
      description: Wants to add the Display on Device UI pattern to a screen and
        configure its device-specific content placeholders — OnDesktop, OnTablet,
        and OnPhone — and Behavior property in Service Studio or ODC Studio, so
        that different content is displayed on desktop computers, tablets, and
        phones.
    - name: Use Move on Device
      id: use-move-on-device
      description: Wants to add the Move on Device UI pattern to a screen and
        configure its device-specific target containers — PhoneWidgetId and
        TabletWidgetId — in Service Studio for OutSystems 11, so that content
        moves to the appropriate container on phones and tablets.
    - name: Use Gallery pattern
      id: use-gallery-pattern
      description: Wants to add the Gallery UI Pattern to a screen and configure
        its responsive layout properties — including RowItemsDesktop, RowItemsTablet,
        RowItemsPhone, ItemsGap, and ExtendedClass — in ODC Studio, so that groups
        of images or content are displayed in a browsable, sequential grid across
        different device types.
    - name: Use Gallery pattern (O11)
      id: use-gallery-pattern-o11
      description: Wants to add the Gallery UI Pattern to a screen and configure
        its responsive layout properties — including RowItemsDesktop, RowItemsTablet,
        RowItemsPhone, ItemsGap, and ExtendedClass — in Service Studio for
        OutSystems 11, so that groups of images or content are displayed in a
        browsable, sequential grid across different device types, in mobile
        apps and reactive web apps.
    - name: Use Floating Content
      id: use-floating-content
      description: Wants to add the Floating Content UI Pattern to a screen and
        configure its properties, including Position, UseFullHeight, UseFullWidth,
        UseMargin, and ExtendedClass, in ODC Studio, so that content overlays on
        top of other screen elements like a map, in mobile apps and reactive web
        apps.
    - name: Use Floating Content (O11)
      id: use-floating-content-o11
      description: Wants to add the Floating Content UI Pattern to a screen,
        drag it into the Main Content area, add content to its placeholder,
        and set the mandatory Position property (plus optional UseFullHeight,
        UseFullWidth, UseMargin, and ExtendedClass properties), including
        adding the widget via Toolbox or Search in other modules if the
        dependency isn't added, in Service Studio for OutSystems 11, so that
        content overlays on top of other screen elements like a map, in
        mobile apps and reactive web apps.
    - name: Floating Content reference
      id: floating-content-reference
      description: Wants to look up the properties of the Floating Content UI Pattern in
        Service Studio, so that they can correctly configure the pattern's
        display and layout, in mobile apps and reactive web apps (O11).
    - name: Use Center Content
      id: use-center-content
      description: Wants to add the Center Content UI Pattern to a screen and
        configure its Top, Center, and Bottom placeholders, including adding the
        widget via Toolbox or Search in other modules if the dependency isn't
        added, and setting the ExtendedClass property, in Service Studio for
        OutSystems 11, so that content is vertically centered within its
        container, in mobile apps and reactive web apps.
    - name: Use Margin Container
      id: use-margin-container
      description: Wants to add the Margin Container UI Pattern to a screen to
        add symmetrical padding around a container, including adding the
        widget via Toolbox or Search in other modules if the dependency isn't
        added, and setting the ExtendedClass property, in Service Studio for
        OutSystems 11, so that content has consistent spacing in mobile apps
        and reactive web apps.
    - name: Margin Container reference
      id: margin-container-reference
      description: Wants to look up the properties of the Margin Container UI
        Pattern in Service Studio, including ExtendedClass, so that they can
        correctly configure custom CSS styling for a Mobile App or Reactive
        Web App in OutSystems 11.
    - name: Display binary image
      id: display-binary-image
      description: Wants to configure an Image widget's Type as Binary Data and
        bind its Image Content property to a binary attribute retrieved from an
        Aggregate, including setting a default image fallback and style properties,
        so that profile pictures show correctly when available and a placeholder
        appears when none is uploaded.
    - name: Use Align Center pattern
      id: use-align-center-pattern
      description: Wants to add the Align Center UI Pattern to a screen and configure
        its IsHorizontal and ExtendedClass properties, in ODC Studio, so that content
        is centered horizontally or vertically on the screen, in mobile apps and
        reactive web apps.
    - name: Use Align Center pattern (O11)
      id: use-align-center-pattern-o11
      description: Wants to add the Align Center UI Pattern to a screen and configure
        its IsHorizontal and ExtendedClass properties, in Service Studio for
        OutSystems 11, so that content is centered horizontally or vertically on
        the screen, in mobile apps and reactive web apps.
    - name: Align Center reference
      id: align-center-reference
      description: Wants to look up the CSS selectors and classes for the Align
        Center UI Pattern, including center-align.flex-direction-row and
        center-align.flex-direction-column, and see an example of building
        custom vertically aligned content with image and text widgets, so that
        they can correctly customize the pattern's layout and appearance in a
        Traditional Web App (O11).
    - name: Fieldset reference
      id: fieldset-reference
      description: Wants to look up the CSS selectors and classes for the
        Fieldset UI Pattern, including .fieldset and .fieldset-legend, and
        see two example style overrides, so that they can correctly customize
        the pattern's appearance in a Traditional Web App (O11).
    - name: Flip Content reference
      id: flip-content-reference
      description: Wants to look up the CSS selectors and classes for the
        Flip Content UI Pattern, including .flip-content.is--hover,
        .flip-content.is--click, and .flip-content.is--click.is--flipped, so
        that they can correctly customize the pattern's hover, click, and
        flipped states in a Traditional Web App (O11).
    - name: Flip Content events
      id: flip-content-events
      description: Wants to interact with other patterns or trigger actions
        based on the Flip Content pattern's flipped state, by creating a
        screen action that assigns the Flipped local variable to the
        isFlipped event variable and triggers an Ajax Refresh, in Service
        Studio, so that other UI elements update dynamically when a Flip
        Content pattern flips, in a Traditional Web App (O11).
    - name: Toggle Flip Content
      id: toggle-flip-content
      description: Wants to flip the Flip Content pattern programmatically
        using a button or container, by setting the pattern's Trigger
        property to Manual and calling the ToggleFlipContent server action
        with the pattern's WidgetId, in Service Studio, so that the pattern
        can be triggered from custom UI elements in a Traditional Web App
        (O11).
    - name: Use Inline SVG pattern
      id: use-inline-svg-pattern
      description: Wants to add the Inline SVG UI Pattern to a screen and configure
        its SVGCode and ExtendedClass properties, so that they can
        customize SVG fill and stroke properties and animate SVG paths,
        in mobile apps and reactive web apps.
    - name: Use Range Slider
      id: use-range-slider-pattern
      description: Wants to add the Range Slider UI Pattern to a screen,
        including adding the dependency when the widget does not appear in
        the Toolbox, setting the MinValue, MaxValue, and StartingValue
        properties, wiring the OnValueChange event to a local variable
        through an Assign action, and configuring optional properties, so
        that users can select a single value within a defined range in
        mobile apps and reactive web apps.
    - name: Range Slider properties
      id: range-slider-properties
      description: Wants to look up the Range Slider UI Pattern's properties and events in
        Service Studio for OutSystems 11, so that they can correctly configure
        the Range Slider in mobile apps and reactive web apps.
    - name: Use Month Picker
      id: use-month-picker
      description: Wants to add the Month Picker UI Pattern to a screen, including adding
        the dependency when the widget does not appear in the Toolbox,
        creating local variables for the input and selected month, wiring the
        OnSelected event, and configuring its properties in Service Studio for
        OutSystems 11, so that users can select a month and year in mobile
        apps and reactive web apps.
    - name: Month Picker reference
      id: month-picker-reference
      description: Wants to look up the Month Picker UI Pattern's configurable properties
        and events in Service Studio for OutSystems 11, so that they can
        correctly configure month selection behavior and styling in mobile apps
        and reactive web apps.
    - name: Use Time Picker pattern
      id: use-time-picker-pattern
      description: Wants to add the Time Picker UI Pattern to a screen, including adding
        the dependency when the widget does not appear in the Toolbox,
        creating local variables for the input and selected time, wiring the
        OnSelected event, and configuring the pattern's properties in Service
        Studio for OutSystems 11, so that users can select a time in mobile
        apps and reactive web apps.
    - name: Time Picker reference
      id: time-picker-reference
      description: Wants to look up the Time Picker UI Pattern's configurable properties
        and events in Service Studio for OutSystems 11, so that they can
        correctly configure time selection behavior and styling in mobile apps
        and reactive web apps.
    - name: Use Date Picker
      id: use-date-picker
      description: Wants to add the Date Picker UI Pattern to a screen, including adding
        the dependency when the widget does not appear in the Toolbox,
        creating the input and local variable, wiring the OnClose and OnSelect
        events, and configuring its properties in Service Studio for
        OutSystems 11, so that users can select a single date or a date range
        in Traditional Web Apps.
    - name: Use Date Picker Range
      id: use-date-picker-range
      description: Wants to add the Date Picker Range UI Pattern to a screen and configure
        its properties, in Service Studio for OutSystems 11, so that users can
        select a date range from a calendar, in mobile apps and reactive web
        apps.
    - name: Date Picker reference
      id: date-picker-reference
      description: Wants to look up the Date Picker UI Pattern's configurable properties
        and advanced format options in Service Studio for OutSystems 11, so
        that they can correctly configure date selection behavior, disabled
        dates, default dates, and styling in Traditional Web Apps.
    - name: Use Search Balloon pattern
      id: use-search-balloon-pattern
      description: Wants to add the Search Balloon UI Pattern to a Traditional Web App, wire its search input and results list, configure the preparation action, filter condition, and refresh logic, so that matching records update as the user types.
    - name: Use Dropdown Search
      id: use-dropdown-search
      description: Wants to add the Dropdown Search UI Pattern to a screen and configure
        its properties, in Service Studio for OutSystems 11, so that users can
        select an option from a searchable dropdown list, in mobile apps and
        reactive web apps.
    - name: Multi-select filter list
      id: multi-select-filter-list
      description: Wants to configure a Dropdown Search widget with AllowMultipleSelection
        enabled and store the selected values as a comma-separated screen variable
        used in an aggregate's filter condition, in Service Studio for OutSystems
        11, so that a list can be filtered by multiple categories at once.
    - name: Dropdown Search reference
      id: dropdown-search-ref
      description: Wants to look up the properties and events of the Dropdown
        Search widget in Service Studio, including OptionsList, StartingSelection,
        Prompt, and OptionalConfigs, so that they can correctly configure a
        searchable dropdown selector in OutSystems 11 mobile apps and reactive
        web apps.
    - name: Use Dropdown Tags pattern
      id: use-dropdown-tags-pattern
      description: Wants to add the Dropdown Tags UI Pattern to a screen, bind
        it to an aggregate, and configure the OnChanged event to display the
        number of selected options, in Service Studio, so that end users can
        select multiple values from a searchable dropdown, in mobile apps and
        reactive web apps (O11).
    - name: Dropdown Tags reference
      id: dropdown-tags-reference
      description: Wants to look up the properties and events of the Dropdown
        Tags widget in Service Studio, including OptionsList, StartingSelection,
        Prompt, and OptionalConfigs, so that they can correctly configure a
        multi-select searchable dropdown in mobile apps and reactive web apps
        (O11).
    - name: Dropdown reference
      id: dropdown-reference
      description: Wants to look up the CSS selectors and classes for the
        Dropdown UI Pattern, including dropdown.is--hidden and
        dropdown.is--visible, so that they can correctly customize the
        pattern's open/closed state appearance in a Traditional Web App (O11).
    - name: Dropdown Select reference
      id: dropdown-select-reference
      description: Wants to look up the CSS selectors and classes for the
        Dropdown Select UI Pattern, including choices__list--dropdown.is-active,
        and see examples of customizing the border color and removable tag
        background using CSS variables, so that they can correctly customize
        the pattern's appearance in a Traditional Web App (O11).
    - name: Dropdown link list
      id: dropdown-link-list
      description: Wants to build a list of links inside the Dropdown UI
        Pattern by dragging a ListRecords widget into the DropdownList
        placeholder, setting its Line Separator property to None, and
        connecting a link to a destination for each record, in Service
        Studio, so that a dropdown displays a dynamic list of navigable
        links in a Traditional Web App (O11).
    - name: Style dropdown icons
      id: style-dropdown-icons
      description: Wants to override the default circular icon style of the
        Dropdown Search widget with custom CSS (border-radius, height, width)
        in Service Studio for OutSystems 11, so that flag or other custom
        icons display without the default circular mask, in mobile apps and
        reactive web apps.
    - name: Hide List Box scrollbar
      id: hide-list-box-scrollbar
      description: 'Wants to hide the vertical scroll bar of a List Box widget
        by adding overflow-y: hidden and background-image: none CSS style
        properties, in Service Studio for OutSystems 11, so that all list
        values remain visible without a visible scroll bar.'
    - name: List scroll blanks
      id: list-scroll-blanks
      description: 'Wants to prevent blank spaces from appearing while scrolling
        a list on low-end devices, by creating a placeholder image for list
        items without data and applying it via a custom CSS class added to
        the List widget''s Style Classes property, in Service Studio, so that
        scrolling performance and experience improve, in traditional web apps,
        mobile apps, and reactive web apps for OutSystems 11.'
    - name: Use Bottom Bar Item
      id: use-bottom-bar-item
      description: Wants to add the Bottom Bar Item UI Pattern to a screen and
        configure its Icon and Text placeholders across up to four items,
        including a floating action button, in Service Studio for OutSystems
        11, so that users can access a bottom navigation drawer and quickly
        switch between core app features, in mobile apps and reactive web
        apps.
    - name: Use Overflow Menu pattern
      id: use-overflow-menu-pattern
      description: Wants to add the Overflow Menu UI Pattern to a screen and configure its
        properties, in Service Studio for OutSystems 11, so that additional
        options are shown for a row or element when there's a space constraint
        on the UI, in mobile apps and reactive web apps.
    - name: Use Submenu pattern
      id: use-submenu-pattern
      description: Wants to add the Submenu UI Pattern to a screen and populate
        its Menu and Items placeholders with content and links, including
        adding the widget via Toolbox or Search in other modules when the
        dependency is missing, in Service Studio for OutSystems 11, so that
        users get a menu nested within another menu, in mobile apps and
        reactive web apps.
    - name: Use Mouse Events pattern
      id: use-mouse-events-pattern
      description: Wants to add the Mouse Events UI Pattern to a screen and
        configure its WidgetId, PreventDefaults, and Handler properties,
        including adding the widget via Toolbox or Search in other modules
        if the dependency isn't added, in Service Studio for OutSystems 11,
        so that they can capture high-precision mouse drag events, such as
        drag distance, in mobile apps and reactive web apps.
    - name: Mouse Events reference
      id: mouse-events-reference
      description: Wants to look up the properties of the Mouse Events UI
        Pattern in Service Studio, including WidgetId and PreventDefaults,
        so that they can correctly configure high-precision mouse event
        handling in OutSystems 11 mobile apps and reactive web apps.
    - name: Use Touch Events pattern
      id: use-touch-events-pattern
      description: Wants to add the Touch Events UI Pattern to a screen and
        configure it to handle touch events on a specific widget, including
        calling event.preventDefault() for custom touchmove behavior or
        scroll-linked header hiding, in Service Studio for OutSystems 11,
        so that mobile apps and reactive web apps respond correctly to touch
        interactions.
    - name: Validate Upload widget file
      id: validate-upload-widget-file
      description: Wants to add custom validation logic for the Upload widget using If
        conditions on its file name, content, and type information, in Service
        Studio for OutSystems 11, so that end users get an error message when no
        file is selected, an empty file is chosen, or the file type doesn't
        match what's expected, in Traditional Web Apps and Reactive Web Apps.
    - name: Use Upload widget
      id: use-upload-widget-o11
      description: Wants to configure the Upload widget's File Content and File
        Name properties by binding them to aggregate data, so that users can
        upload a file (such as a photo) and save it to the database from a
        Traditional Web App or Reactive Web App form.
    - name: Use Download widget
      id: use-download-widget-o11
      description: Wants to configure the Download widget's File Content and
        File Name properties in Service Studio, so that the browser downloads
        binary content, such as an image, directly from an action, in a
        Traditional Web App or Reactive Web App.
    - name: Use Pagination pattern
      id: use-pagination-pattern
      description: 'Wants to add the Pagination UI Pattern to a screen and configure
        its StartIndex, MaxRecords, TotalCount, ShowGoToPage, and ExtendedClass
        properties, in Service Studio for OutSystems 11, so that users can
        navigate through long listings such as e-commerce category pages, search
        engines, and article archives, in mobile apps and reactive web apps.'
    - name: Pagination reference
      id: pagination-reference
      description: Wants to look up the properties of the Pagination UI Pattern
        in Service Studio, including StartIndex, MaxRecords, TotalCount, ShowGoToPage,
        and ExtendedClass, so that they can correctly configure pagination
        behavior and appearance in OutSystems 11 mobile apps and reactive web apps.
    - name: Use Section Index pattern
      id: use-section-index-pattern
      description: Wants to add the Section Index UI Pattern to a screen, including
        adding Section Index Items and Section widgets, entering titles and content,
        and linking each Section Index Item to its Section via the ScrollToWidgetId
        property, in Service Studio for OutSystems 11, so that users can quickly
        navigate to different content sections within a page, in mobile apps and
        reactive web apps.
    - name: Section Index reference
      id: section-index-reference
      description: Wants to look up the properties of the Section Index and Section
        Index Item widgets in Service Studio, including SmoothScrolling, IsFixed,
        ScrollToWidgetId, and ExtendedClass, so that they can correctly configure
        the pattern's scrolling behavior and appearance, in OutSystems 11 mobile
        apps and reactive web apps.
    - name: Use Timeline Item pattern
      id: use-timeline-item-pattern
      description: Wants to add the Timeline Item UI Pattern to a screen and configure
        its IsActive, Color, and ExtendedClass properties, in Service Studio for
        OutSystems 11, so that related events display in chronological order, in
        mobile apps and reactive web apps.
    - name: Use Timeline pattern
      id: use-timeline-pattern
      description: Wants to add the Timeline UI Pattern to a Traditional Web App in
        Service Studio, place Timeline Items on a screen, fill the Icon, Date, and
        Content placeholders, and configure the Timeline and Timeline Item
        properties, so that related events display in chronological order.
    - name: Use Progress Bar pattern
      id: use-progress-bar-pattern
      description: Wants to add the Progress Bar UI Pattern to a screen and configure
        its Progress property using a local variable and increment button, in
        Service Studio for OutSystems 11, so that they can display percentage
        values and show the current progress of a task flow, in mobile apps and
        reactive web apps.
    - name: Progress Bar reference
      id: progress-bar-reference
      description: Wants to look up the properties of the Progress Bar UI Pattern
        in Service Studio, including Progress, ProgressColor, TrailColor, Thickness,
        and OptionalConfigs (Shape, AnimateInitialProgress), so that they can
        correctly configure a progress bar's appearance and behavior in OutSystems
        11 mobile apps and reactive web apps.
    - name: Progress Bar CSS reference
      id: progress-bar-css-reference
      description: Wants to look up the CSS selectors and classes for the
        Progress Bar UI Pattern, including .progress-container.flex-direction-column
        and .progress-container.flex-direction-row, so that they can correctly
        customize the pattern's layout, in a Traditional Web App (O11).
    - name: Customize Progress Bar CSS
      id: customize-progress-bar-css
      description: Wants to customize the Progress Bar UI Pattern's appearance
        with custom CSS, including dynamically changing its color based on value
        via the Color parameter, overriding the .progress class's padding and
        border, and setting background-color to transparent to remove the
        background, in Service Studio, so that the progress bar's visual style
        matches app requirements, in a Traditional Web App (O11).
    - name: Progress Circle CSS reference
      id: progress-circle-css-reference
      description: Wants to look up the CSS selectors and classes for the
        Progress Circle UI Pattern, including .progress-circle .progress-circle-content
        and .progress-circle .progress-semi-circle-content, so that they can
        correctly identify the pattern's structure when the IsSemiCircle
        parameter is False or True, in a Traditional Web App (O11).
    - name: Customize Progress Circle CSS
      id: customize-progress-circle-css
      description: Wants to customize the Progress Circle UI Pattern's appearance
        with custom CSS, including dynamically changing its color based on value
        via the ProgressColor parameter, removing the round corners by setting
        stroke-linecap to square, and changing the trail thickness via the
        AdvancedFormat property, in Service Studio, so that the progress
        circle's visual style matches app requirements, in a Traditional Web
        App (O11).
    - name: Use Progress Circle
      id: use-progress-circle-traditional-web
      description: Wants to add the Progress Circle UI Pattern to a Traditional Web App
        screen and configure its Progress property and content placeholder so that
        they can display the current progress of an operation flow as a percentage
        value, in Service Studio for OutSystems 11.
    - name: Progress Circle reference
      id: progress-circle-reference
      description: Wants to look up the configurable properties of the Progress Circle UI
        Pattern in Service Studio so that they can correctly configure a Progress
        Circle's appearance and behavior.
    - name: Use Progress Circle pattern
      id: use-progress-circle-mobile-reactive-web
      description: Wants to add the Progress Circle UI Pattern to a screen and
        configure its Progress property using a local variable and increment
        button, in Service Studio for OutSystems 11, so that they can display
        the current progress of an operation flow as a fraction, in mobile
        apps and reactive web apps.
    - name: Use Rating pattern
      id: use-rating-pattern
      description: Wants to add the Rating UI Pattern to a screen and configure its
        RatingValue property, in Service Studio for OutSystems 11, so that users
        can rate items such as products or services on a customizable icon
        scale, in mobile apps and reactive web apps.
    - name: Rating pattern reference
      id: rating-pattern-reference
      description: Wants to look up the properties and events of the Rating UI
        Pattern in Service Studio, including RatingScale, IsEdit, Size,
        ExtendedClass, and OnSelect, so that they can correctly configure a
        rating pattern's scale, editability, and appearance in OutSystems 11
        mobile apps and reactive web apps.
    - name: Use Button Loading pattern
      id: use-button-loading-pattern
      description: Wants to add the Button Loading UI Pattern to a screen and
        configure its IsLoading property using a Boolean local variable, in
        Service Studio for OutSystems 11, so that a button shows a loading
        spinner and is disabled from being clicked while a non-immediate
        action is executing, in mobile apps and reactive web apps.
    - name: Use Load on Visible pattern
      id: use-load-on-visible-pattern
      description: Wants to add the Load on Visible UI Pattern to a Traditional
        Web App screen and place content in its placeholder so that content
        loads only when it becomes visible and page performance improves.
    - name: Use Button Group pattern
      id: use-button-group-pattern
      description: Wants to add the Button Group UI Pattern to a Traditional
        Web App screen and configure its radio-button items, labels, and
        IsJustified property, so that users can choose from a small set of
        options with grouped buttons.
    - name: Button Group reference
      id: button-group-reference
      description: Wants to look up the Button Group UI Pattern's IsJustified
        property in Service Studio, so that they can control how the button
        group items are distributed in a Traditional Web App.
    - name: Button Loading reference
      id: button-loading-reference
      description: Wants to look up the properties of the Button Loading UI
        Pattern in Service Studio, including IsLoading, ShowLabelOnLoading,
        and ExtendedClass, so that they can correctly configure a button's
        loading spinner behavior and appearance in OutSystems 11 mobile apps
        and reactive web apps.
    - name: Use Toggle Button pattern
      id: use-toggle-button-pattern
      description: Wants to add the Toggle Button UI Pattern to a Traditional
        Web App screen, bind it to a Boolean local variable, and use OnChange
        with Ajax Refresh to update a related button's Enabled state, in
        Service Studio, so that users can switch between two states before
        submitting.
    - name: Use Video pattern
      id: use-video-pattern
      description: Wants to add a video to a screen using the OutSystems UI
        Video Pattern, configuring it with a URL-hosted video file or an
        uploaded Resource, in Service Studio, so that videos play within
        the app's UI.
    - name: Video pattern reference
      id: video-pattern-reference
      description: Wants to look up the properties of the Video UI Pattern
        in Service Studio, including VideoURL, PosterURL, Width, Height,
        Muted, Loop, Controls, Autoplay, and ExtendedClass, so that they
        can configure a video widget correctly in mobile apps and reactive
        web apps.
    - name: Embed streaming video
      id: embed-streaming-video
      description: Wants to embed a video from a streaming platform like
        YouTube or Vimeo into an app screen by copying the platform's embed
        iframe code and pasting the source URL into an Iframe block's
        SourceURL property, in Service Studio, so that streamed video
        content displays within the app.
    - name: Add autocomplete search
      id: add-autocomplete-search
      description: Wants to add a server-side filtered autocomplete search field
        to an input widget, by adding the Input_AutoComplete and
        Input_AutoComplete_ShowList RichWidgets, wiring the On Notify event to a
        Screen Action containing an Aggregate and filter, in Service Studio for
        OutSystems 11, so that a long list of results is filtered on the server
        and suggestions appear below the field as the user types in a Traditional
        Web App.
    - name: Charts extensibility
      id: charts-extensibility
      description: Wants to understand the Charts extensibility surface in ODC
        Studio, including the available chart widgets, add-ons, and API actions,
        so that they can choose the right chart component and customization
        entry point.
    - name: Configure Line Chart
      id: configure-line-chart
      description: Wants to add the Line Chart widget to a screen and configure
        its DataPointList (Label, Value, SeriesName) and Spline property,
        then customize series and markers via SeriesStyling, in Service
        Studio for OutSystems 11, so that a line chart with a smooth spline
        line and custom markers displays correctly in traditional web apps,
        mobile apps, and reactive web apps.
    - name: Configure Area Chart
      id: configure-area-chart
      description: Wants to add the Area Chart widget to a screen and configure
        its DataPointList, SeriesName, and StackingType properties in Service
        Studio for ODC, so that a stacked area chart displays correctly in
        mobile apps and reactive web apps.
    - name: Configure radar chart
      id: configure-radar-chart
      description: Wants to add the Radar Chart widget to a screen and
        configure its DataPointList, SeriesName, SeriesStyling, and SeriesType
        properties in ODC Studio, so that the radar chart displays multiple
        series and area styling correctly in mobile apps and reactive web apps.
    - name: Configure chart data points
      id: configure-chart-data-points
      description: Wants to populate a chart's DataPointList with fixed or
        variable data, map Label and Value fields, and set SeriesName for
        multiple series in ODC Studio, so that chart widgets display data
        correctly in mobile apps and reactive web apps.
    - name: Set donut inner size
      id: configure-donut-inner-size
      description: Wants to set the Donut Chart InnerSize property in ODC Studio,
        so that the donut chart displays with the intended hole size.
    - name: Charts API v2 reference
      id: charts-api-v2-reference
      description: Wants to look up the available Charts API v2 widgets,
        add-ons, the HighchartsModules element, and the client actions used
        to extend chart configuration in Service Studio for OutSystems 11,
        so that they can choose the right chart component and customize it
        correctly in traditional web apps, mobile apps, and reactive web
        apps.
    - name: Combo Box widget reference
      id: combo-box-widget-reference
      description: Wants to look up the properties and runtime properties of the Combo Box
        widget in Service Studio, including its data binding, display, and
        behavior configuration options, so that they can correctly configure a
        value-selection dropdown field bound to an Entity, Structure, or special
        values list in a Traditional Web App (O11).
    - name: Order combo box list
      id: order-combo-box-list
      description: Wants to control the order of items displayed in a Combo Box
        widget by sorting the Source Record List, for example via an Aggregate's
        Sort property, in Service Studio, so that a combo box displays entries
        in a specific order, in traditional web apps, mobile apps, and reactive
        web apps for OutSystems 11.
    - name: Set combo box default
      id: set-combo-box-default
      description: Wants to set the default selected value of a Combo Box widget
        by initializing the destination Variable with the desired Source
        Identifier Attribute value, for example using IntegerToIdentifier, in
        Service Studio, so that the combo box pre-selects a specific record
        when the screen loads, in traditional web apps, mobile apps, and
        reactive web apps for OutSystems 11.
    - name: Ajax Refresh reference
      id: ajax-refresh-reference
      description: Wants to look up the properties and key usage rules of the
        Ajax Refresh element (Widget or Web Block, Row Number, Animation
        Effect, one-element-per-node limit, Ajax Submit requirement, Name
        requirement, Web Block Preparation re-execution, refresh order) in
        Service Studio, so that they can correctly configure partial screen
        refreshes without a full page reload, in OutSystems 11 traditional
        web apps, mobile apps, and reactive web apps.
    - name: Iframe responsive width
      id: iframe-responsive-width
      description: Wants to change the width of an Iframe based on the device
        type by setting the Width property to a conditional expression using
        IsDesktop(), in Service Studio for O11 Traditional Web Apps, so that
        the Iframe displays at a fixed width on desktop while remaining
        full-width on mobile to avoid overflow.
    - name: Use Bullets pattern
      id: use-bullets-pattern
      description: Wants to add the Bullets UI Pattern to a screen, by
        dragging the widget from the Toolbox into the Main Content area,
        adding the OutSystemsUI dependency via Search in other modules if it
        does not display, adding content to the Bullet Item placeholders, and
        nesting a Bullets widget inside a Bullet Item to create sub-bullets,
        in Service Studio, so that users can organize content into bullet
        points in Traditional Web Apps (O11).
    - name: Use Accordion pattern
      id: use-accordion-pattern
      description: Wants to add the Accordion UI Pattern to a screen, by
        dragging the widget from the Toolbox into the Main Content area
        (adding the OutSystemsUI dependency via Search in other modules if
        it doesn't display), adding content to the AccordionItem
        placeholders, and setting properties like MultipleItems, in Service
        Studio, so that users can expand and collapse content sections, in
        mobile apps and reactive web apps (O11).
    - name: Accordion reference
      id: accordion-reference
      description: Wants to look up the properties of the Accordion and
        Accordion Item widgets in Service Studio, including MultipleItems,
        ExtendedClass, StartsExpanded, Icon, IconPosition, IsDisabled, and
        the OnToggle event, so that they can correctly configure the
        pattern's expand/collapse behavior and appearance, in mobile apps
        and reactive web apps (O11).
    - name: Use Tabs pattern
      id: use-tabs-pattern-o11
      description: Wants to add the Tabs UI Pattern to a screen to divide
        content into meaningful sections, by dragging the widget from the
        Toolbox into the Main Content area (adding the OutSystemsUI
        dependency via Search in other modules if it doesn't display),
        adding content to the Header Item and Content Item placeholders,
        and setting properties like TabsOrientation and StartingTab, in
        Service Studio, so that users can switch between sections within
        the same context, in mobile apps and reactive web apps (O11).
    - name: Tabs reference
      id: tabs-reference-o11
      description: Wants to look up the properties and events of the Tabs,
        Tabs Content Item, and Tabs Header Item widgets in Service Studio,
        including TabsOrientation, StartingTab, Height,
        TabsVerticalPosition, OptionalConfigs.ContentAutoHeight,
        OptionalConfigs.JustifyHeaders, ExtendedClass, and the OnTabChange
        event, so that they can correctly configure the pattern's layout,
        behavior, and styling, in mobile apps and reactive web apps (O11).
    - name: Use Tabs pattern
      id: use-tabs-pattern-traditional-web
      description: Wants to add the Tabs UI Pattern to a Traditional Web App
        screen by dragging the widget from the Toolbox into the Main Content
        area, adding the OutSystemsUI dependency via Search in other modules
        if it doesn't display, filling the Header Item and Content Item
        placeholders, and setting properties like ActiveTab and Orientation in
        Service Studio, so that users can switch between sections within the
        same context in OutSystems 11 traditional web apps.
    - name: Tabs reference
      id: tabs-reference-traditional-web
      description: Wants to look up the properties of the Tabs, Tabs Header
        Item, and Tabs Content Item widgets in Service Studio, including
        ActiveTab, Orientation, IsJustified, IsRight, ExtendedClass, and
        DataTab, so that they can correctly configure the pattern's layout,
        behavior, styling, and tab-item pairing in OutSystems 11 traditional
        web apps.
    - name: Use Section pattern
      id: use-section-pattern
      description: Wants to add the Section UI Pattern to a screen, by
        dragging the widget from the Toolbox into the Main Content area
        (adding the OutSystemsUI dependency via Search in other modules if
        it doesn't display), adding content to the Title and Content
        placeholders, and setting properties like UsePadding and
        ExtendedClass, in Service Studio, so that they can organize
        on-screen content into different sections, in mobile apps and
        reactive web apps (O11).
    - name: Section reference
      id: section-reference
      description: Wants to look up the properties of the Section UI
        Pattern in Service Studio, including UsePadding and ExtendedClass,
        so that they can correctly configure padding and custom CSS
        styling for the pattern, in mobile apps and reactive web apps
        (O11).
    - name: Use Flip Content pattern
      id: use-flip-content-pattern
      description: Wants to add the Flip Content UI Pattern to a screen, drag
        the widget into the Main Content area, and add content to the
        CardFront and CardBack placeholders, including adding the widget via
        Toolbox or Search in other modules if the dependency isn't added, in
        Service Studio, so that content flips between a front and back
        state, in mobile apps and reactive web apps.
    - name: Flip Content widget reference
      id: flip-content-widget-reference
      description: Wants to look up the properties and events of the Flip
        Content UI Pattern, including StartsFlipped, FlipOnClick,
        ExtendedClass, and the OnFlip event, in Service Studio, so that they
        can correctly configure the pattern's initial flipped state, trigger
        behavior, and react to flip changes, in mobile apps and reactive web
        apps.
    - name: Enable grid column edit
      id: enable-grid-column-edit
      description: Wants to enable and configure column editing in the
        OutSystems Data Grid component, by setting the AllowColumnEdit
        property to True and adding Column widgets (such as Text, Number,
        Currency, and Checkbox Columns) with Header and Binding properties
        configured, in Service Studio, so that end users can edit cell
        values directly within the Grid, in Reactive Web Apps (O11).
    - name: Save Data Grid changes
      id: save-data-grid-changes
      description: Wants to save edited cell data from the OutSystems Data
        Grid component to the database, by adding a Save button that runs
        a client action calling GetChangedLines, deserializing the JSON
        result with JSON Deserialize, passing it to a Server Action that
        updates the database, and calling MarkChangesAsSaved to clear
        change indicators, in Service Studio, in Reactive Web Apps (O11).
    - name: Data Grid overview
      id: data-grid-overview
      description: Wants to understand what the OutSystems Data Grid
        component is and its key features (data selection and editing,
        sorting, grouping, and virtual scrolling), and where to download it
        for Traditional Web and Reactive Web apps, so they can decide
        whether it fits their data manipulation needs, in OutSystems 11.
    - name: Data Grid properties
      id: data-grid-properties
      description: Wants to look up the OutSystems Data Grid properties in Service Studio,
        so they can configure the Grid correctly in Reactive Web Apps (O11).
    - name: Display grid data
      id: display-grid-data
      description: Wants to display database data in the OutSystems Data Grid in a Reactive
        Web App by creating a Data Action, using ArrangeData to serialize the
        aggregate result to JSON, assigning ArrangeData.DataJSON to the action
        output, and binding the Grid's Data and IsDataFetched properties, so
        users can review query results in a grid, in OutSystems 11.
    - name: Map component overview
      id: map-component-overview
      description: Wants to understand what the OutSystems Map component
        does, which app types support it, and how it supports single or
        multiple locations, customization, location search, and route
        planning, when they need to build a map-enabled screen.
    - name: Static maps usage
      id: static-map-usage
      description: Wants to use a static map to show a simple,
        non-interactive map when a dynamic map is not possible or not
        needed, such as in an email, and accept that users cannot
        interact with it or add markers at runtime.
    - name: Add marker on map click
      id: add-marker-map-click
      description: Wants to add a marker to the OutSystems Map component and
        capture its coordinates, by adding a Map Event block with EventName
        set to Click, creating Latitude and Longitude local variables, and
        binding a Marker block's Position property to them via a handler,
        in Service Studio, so that end users can drop a marker at the
        location they click on the map, in mobile apps and reactive web
        apps (O11).
  - name: Group inputs with forms
    id: group-inputs-with-forms
    description: Wants to understand what a Form widget is and how it
      groups input widgets to collect, submit, and validate user data in
      real time, in OutSystems 11 traditional web apps, mobile apps, and
      reactive web apps, so that they can decide when to use a form on a
      screen.
    subtopics:
    - name: Automatic form validation
      id: automatic-form-validation
      description: Wants to identify which input validations the Form widget
        performs automatically in ODC, such as mandatory fields and data
        type checks, and which validation still requires custom logic, so
        they can recognize built-in validation behavior.
    - name: Check form validity
      id: check-form-validity
      description: Wants to check a Form's Valid property after custom
        validations run so execution branches correctly and invalid data is
        not stored in the database in ODC.
    - name: Invalid widget feedback
      id: invalid-widget-feedback
      description: Wants to understand how an input widget behaves when its
        Valid property is set to False, including that it keeps its normal
        appearance, applies invalid styling, and shows the validation
        message, so they can recognize built-in validation feedback in an
        OutSystems 11 screen.
  - name: Navigation patterns overview
    id: navigation-patterns-overview
    description: Wants to know which navigation UI pattern to use for grouping
      related content and helping users move around an app, such as Bottom
      Bar Item, Breadcrumbs, Pagination, Section Index, Submenu, Timeline
      Item, or Wizard, so that they can choose the appropriate pattern for a
      Mobile App or Reactive Web App for OutSystems 11.
    subtopics:
    - name: Use Breadcrumbs pattern
      id: use-breadcrumbs-pattern
      description: Wants to add the Breadcrumbs UI Pattern to a screen,
        add the OutSystemsUI dependency if the widget does not display in
        the Toolbox, configure Breadcrumb Item widgets with titles, icons,
        and links, and create a breadcrumb trail in Service Studio for
        O11, so that users can navigate through a mobile, reactive, or
        Traditional Web App with location context.
    - name: Use web navigation bar
      id: use-web-navigation-bar
      description: Wants to add the Navigation Bar UI Pattern to a
        Traditional Web App screen or web block, add the widget via the
        Toolbox or Search in other modules if the dependency is missing,
        add content to the Navigation Bar Item and Navigation Bar
        SubItem placeholders, configure optional properties, and
        publish the module so that users can navigate through main
        sections and subsections.
  - name: UI elements (Blocks, widgets, OS UI patterns, accelerators)
    id: ui-elements-accelerators
    subtopics:
    - name: Use UI accelerator
      id: use-ui-accelerator
      description: Wants to add a pre-built OutSystems UI Accelerator to an empty Screen by
        dragging it from the Toolbox, in Service Studio for OutSystems 11, so
        that a complex, fully functional Screen layout scaffolds quickly with
        sample data instead of building each widget from scratch.
    - name: Use Search pattern
      id: use-search-pattern
      description: Wants to add the Search UI Pattern to a screen and configure
        its input variable in Service Studio, so that users can search for
        content by entering queries.
    - name: Use web gallery
      id: use-web-gallery-o11
      description: Wants to add the Gallery UI Pattern to a Traditional Web App
        screen, add the widget if it is missing from the Toolbox, drag it into
        the Main Content area, add List Records or images as content, and
        configure ItemsDesktop, ItemsTablet, and ItemsPhone, so that users can
        browse grouped content in a responsive grid.
  - name: Adding a Widget or OS UI pattern
    id: add-widget-ui-pattern
    subtopics:
    - name: Use Character Count
      id: use-character-count-pattern
      description: Wants to add the Character Count UI Pattern to a
        Traditional Web App screen, bind it to an Input widget, set the
        Limit and IsDescending properties, and recover the widget if it
        does not appear in the Toolbox because unused references were
        removed, so that the screen shows how many characters a user has
        entered or has remaining.
  - name: Build screen layouts
    id: build-screen-layouts
    description: Wants to use Text, Container, Button, and Link widgets to set screen
      titles, add navigation buttons for related screens, and arrange the
      detail screen into side-by-side containers, so that the app screens
      present the intended layout and navigation in a Traditional Web App.
    subtopics:
    - name: Conditionally show link
      id: conditionally-show-link
      description: Wants to show a Reset link only when a condition is true by enclosing it
        in an If widget, so that the link appears only when the condition is
        true.
  - name: Layouts
    id: layouts
    subtopics:
    - name: Customize layout block
      id: customize-layout-block
      description: Wants to customize a reactive app layout block by applying a CSS
        class through ExtendedClass for visual changes or duplicating the
        block to make structural edits, so the app can change the page shell
        without affecting other apps.
    - name: Label layout navigation
      id: label-layout-navigation
      description: Wants to add a unique aria-label to a layout nav element,
        either directly or through a MenuName input parameter, so screen
        readers can distinguish navigation regions in a reactive app layout.
    - name: Use layout login
      id: use-layout-login
      description: Wants to locate and reuse the Layout Login web block in a Traditional
        Web App by customizing its placeholders and widgets and adjusting its
        optional device and accessibility configuration properties, so that
        login screens use the built-in layout.
    - name: Use layout popup
      id: use-layout-popup
      description: Wants to locate and reuse the Layout Popup web block in a Traditional
        Web App by customizing its placeholders and widgets and adjusting its
        optional DeviceConfiguration and AccessibilityConfiguration properties,
        so that screens can display reusable off-canvas content in the built-in
        popup layout.
    - name: Use layout side menu
      id: use-layout-side-menu
      description: Wants to locate and reuse the Layout Side Menu web block in a
        Traditional Web App by customizing its placeholders and widgets and
        adjusting its DeviceConfiguration, AccessibilityConfiguration, and
        ExtendedClass properties, so that screens use the built-in side menu
        layout.
    - name: Use layout top menu
      id: use-layout-top-menu
      description: Wants to locate and reuse the Layout Top Menu web block in a
        Traditional Web App by customizing its placeholders and widgets and
        adjusting its DeviceConfiguration, AccessibilityConfiguration, and
        ExtendedClass properties, so that screens use the built-in top menu
        layout.
  - name: UI performance best practices
    id: ui-performance-best-practices
    description: Wants to apply best practices for optimizing OutSystems 11
      screen performance and load times, in Traditional Web, mobile, and
      Reactive Web apps.
    subtopics:
    - name: Avoid preparation data in screen actions
      id: avoid-preparation-data-in-screen-actions
      description: Wants to avoid using preparation data in screen actions in
        OutSystems 11, to reduce Viewstate size and network payload, so that
        Traditional Web, mobile, and Reactive Web apps respond quickly.
    - name: Use AJAX in moderation
      id: use-ajax-in-moderation
      description: Wants to use AJAX in moderation in OutSystems 11 screens, to
        reduce Viewstate size and network payload, so that Traditional Web,
        mobile, and Reactive Web apps respond quickly.
    - name: Split large screens
      id: split-large-screens
      description: Wants to split large screens in OutSystems 11, to reduce
        Viewstate size and network payload, so that Traditional Web, mobile,
        and Reactive Web apps respond quickly.
    - name: Minimize screen parameters
      id: minimize-screen-parameters
      description: Wants to minimize screen parameters in OutSystems 11, to
        reduce Viewstate size and network payload, so that Traditional Web,
        mobile, and Reactive Web apps respond quickly.
    - name: Cache screens and blocks
      id: cache-screens-and-blocks
      description: Wants to cache screens and blocks in OutSystems 11, to reduce
        Viewstate size and network payload, so that Traditional Web, mobile,
        and Reactive Web apps respond quickly.
    - name: Optimize JavaScript loading
      id: optimize-javascript-loading
      description: Wants to optimize how JavaScript is included and loaded on
        OutSystems 11 screens, by placing JavaScript in files instead of
        screen expressions, deferring JavaScript that isn't needed at load
        time to the end of the page, and using minified JavaScript in
        production, so that screen size and page load time are reduced.
    - name: Use CSS sprites
      id: use-css-sprites
      description: Wants to reduce the number of server requests for images
        on high-traffic, public-facing OutSystems 11 screens by combining
        frequently used images into a single CSS sprite using
        background-image and background-position, so that screen load times
        improve.
    - name: Simplify splash screen
      id: simplify-splash-screen
      description: Wants to keep a mobile app splash screen simple by
        minimizing server requests, complex UI logic, and Blocks, so that the
        app loads faster and users do not see a blank screen.
  - name: Creating UI interactions
    id: creating-ui-interactions
    subtopics:
    - name: When to use JS
      id: when-to-use-javascript
      description: Wants to understand when and through which mechanism to
        add custom JavaScript to an app, using extended properties,
        unescaped expressions, or platform APIs in Traditional Web Apps, or
        the JavaScript element inside client actions in Reactive Web Apps
        and Mobile Apps, in OutSystems 11, so that they can decide whether
        JavaScript is the right solution and find the detailed instructions
        for their app type.
    - name: JS best practices
      id: javascript-best-practices
      description: Wants to know the recommendations to follow before adding
        custom JavaScript to an app, such as preferring built-in OutSystems
        features, keeping code focused and non-duplicated, testing across
        supported browsers and devices, and avoiding direct DOM manipulation
        that can interfere with the framework's rendering lifecycle, in
        OutSystems 11, so that their JavaScript stays maintainable and
        doesn't conflict with Reactive Web and Mobile app rendering.
    - name: Implement form validation
      id: implement-form-validation
      description: Wants to implement custom business-rule validation on a
        screen's Save action, using an If condition to check a field's value
        and an Assign to set the input's Valid property to False and its
        ValidationMessage, so that
        invalid data (such as a past due date or a malformed phone number) is
        rejected before it's saved to the database.
    - name: Navigating between screens
      id: navigate-screens
    - name: Go To Destination
      id: go-to-destination
      description: Wants to look up the purpose and properties (Destination)
        of the Go To Destination node in Service Studio, so that they can
        navigate to a specific screen, in OutSystems 11 traditional web apps,
        mobile apps, and reactive web apps.
    - name: External Site reference
      id: external-site-reference
      description: Wants to look up the purpose and configurable properties
        (Name, Description, URL, Is Frequent Destination) of the External
        Site logic element in Service Studio, including how to supply the
        URL dynamically via its required URL input parameter and why only
        that input, not the base URL, should be encoded with EncodeURL(),
        in OutSystems 11, so that they can navigate users to an external
        site correctly and securely.
    - name: Resolve unused connector
      id: resolve-unused-connector
      description: 'Wants to resolve the "Connector from <screen> to <screen>
        in never used in <flow>" warning in Service Studio, caused by having
        a connector between two screens that hasn''t been instantiated, by
        adding a Link or Button on the first screen to the second screen so
        the connector is used, so that the module compiles without the
        warning, in OutSystems 11 (O11).'
    - name: Override back navigation
      id: override-back-navigation
      description: Wants to override the default back-navigation behavior by
        registering and unregistering a custom back handler callback via
        $public.Navigation's registerBackNavigationHandler and
        unregisterBackNavigationHandler JavaScript API functions, in ODC mobile
        apps and reactive web apps, so that pressing the device back button (for
        example, Android) triggers custom logic instead of navigating to the
        previous screen.
    - name: System back navigation
      id: system-back-navigation
      description: Wants to route system back navigation through the native
        shell so that Android back gestures and iOS back-swipes use WebView
        history, exit the app at the navigation root, and keep screen-to-screen
        navigation in app logic.
    - name: Intercept navigation events
      id: intercept-navigation-events
      description: Wants to intercept and conditionally cancel in-app navigation
        events by registering a synchronous callback via $public.Navigation's
        registerNavigationHandler JavaScript API, in ODC mobile apps and reactive
        web apps, so that navigation only proceeds when custom business logic
        approves it.
    - name: Navigate with animations
      id: navigate-with-animations
      description: Wants to programmatically navigate between screens using
        $public.Navigation's navigateBack, navigateForward, and navigateTo
        JavaScript API functions with optional transition animations, in ODC
        mobile apps and reactive web apps, so that users experience smoother
        in-app transitions between screens.
    - name: Detect history-based navigation
      id: detect-history-based-navigation
      description: Wants to check whether the current screen was loaded from
        browser history using $public.Navigation's navigatedFromHistory
        JavaScript API, in ODC mobile apps and reactive web apps, so that the
        app restores UI state and avoids re-running initialization logic when
        users return via browser history.
    - name: Changing data with UI
      id: changing-data-with-ui
    - name: UI interactions in apps
      id: ui-interactions-in-apps
    - name: Create popup widget
      id: create-popup-widget
      description: Wants to create and show a popup to display information or
        collect user input, by adding a Popup widget, toggling its Show Popup
        property with a boolean variable, and using Client Actions to set that
        variable via Close Popup and Show Popup buttons, in Service Studio for
        OutSystems 11 Reactive Web and Mobile Apps, so that users stay on the
        same page while interacting with the popup.
    - name: Create Popup Editor
      id: create-popup-editor
      description: Wants to create and show a popup screen in Traditional Web
        Apps, by adding a Link widget to trigger navigation, creating a new
        screen from the LayoutPopup template, and configuring the Popup Editor
        widget's LinkOnButtonWidgetId property to open it, in Service Studio
        for OutSystems 11, so that the popup screen displays without a full
        page navigation.
    - name: Popup link confirmation
      id: popup-link-confirmation
      description: Wants to show a Confirmation Message on a Link widget that navigates to
        a popup screen destination, by routing through an intermediate hidden
        Link and a Server Action that triggers RichWidgets\Widget_Click, in
        Service Studio for OutSystems 11 Traditional Web Apps, so that the
        confirmation dialog appears, which OutSystems doesn't show when
        navigating directly to a popup screen.
    - name: Resize popup window
      id: resize-popup-window
      description: Wants to dynamically resize a Popup_Editor window at runtime by
        setting the AutoResize property to enable auto-growth and using a JavaScript
        snippet with the RunJavaScript action to enable auto-shrink, in Service Studio
        for O11 Traditional Web Apps, so that the pop-up window adjusts its size
        automatically when its contents change.
    - name: Order workflow patterns
      id: order-workflow-patterns
      description: Wants to implement a status-driven order workflow across multiple
        screens using the Wizard and Popup UI patterns in Service Studio, for
        OutSystems 11 Traditional Web Apps, so that orders are processed and
        displayed through a guided multi-step, multi-screen UI.
    - name: Save active tab state
      id: save-active-tab-state
      description: Wants to save and restore the active tab between page visits in
        a SilkUIFramework Tabs widget, by capturing the clicked tab number via a
        JavaScript onclick handler, persisting it in a session variable, and passing
        it back to the ActiveTab parameter, in Service Studio for O11 Traditional
        Web Apps, so that users return to the same tab after navigating away and
        back.
    - name: Prevent tab cache reload
      id: prevent-tab-cache-reload
      description: Wants to prevent the browser from showing a cached version of
        a screen with tabs after a reload or back-navigation, by adding a Cache-Control
        'NO-STORE' response header via the HttpRequestHandler AddHeader action in
        the screen's Preparation, in Service Studio for O11 Traditional Web Apps,
        so that the restored active tab reflects current state instead of a stale
        cached page.
    - name: Custom validation location
      id: custom-validation-message-location
      description: Wants to display input validation messages in a custom location
        by hiding the default .ValidationMessage CSS class and binding
        <input_name>.ValidationMessage to an Expression widget, in Service Studio
        for O11 Traditional Web Apps and Mobile Apps, so that validation feedback
        appears where business or design requirements demand rather than next to
        the default input position.
    - name: Show feedback message
      id: show-feedback-message
      description: Wants to display and manage feedback messages in an ODC app using the
        FeedbackMessage JavaScript API's showFeedbackMessage and
        closeFeedbackMessage functions, including configuring the message's
        type, styling, and dismissal behavior, in ODC Studio, so that they can
        show styled, auto-dismissing or persistent feedback messages to users in
        Mobile Apps and Reactive Web Apps.
    - name: Identify current screen
      id: identify-current-screen
      description: Wants to identify the currently running application and screen
        (application key/name, screen key/name, ready state) at runtime, by calling
        the ApplicationContext.getCurrentContext() JavaScript API, in ODC Mobile
        Apps and Reactive Web Apps, so that client-side logic can behave contextually
        based on the active screen or app.
    - name: Handle app load status
      id: handle-app-load-status
      description: Wants to detect whether the app is currently being upgraded and
        react to application load events (upgrade progress and load completion), by
        using the ApplicationLifecycle JavaScript API's isUpgradingVersion and
        listen functions, in ODC Mobile Apps and Reactive Web Apps, so that they can
        customize the loading or splash screen experience shown during app
        initialization and upgrades.
    - name: Discover JS API modules
      id: discover-js-api-modules
      description: Wants to discover which JavaScript API modules ODC provides and call
        them through the $public object, in ODC Studio, so that custom app
        behavior can be built in JavaScript for mobile apps and reactive web
        apps.
    - name: Manipulate screen DOM
      id: manipulate-screen-dom
      description: Wants to apply custom CSS classes to the current screen's root DOM
        element and to the document body based on device orientation and type,
        using the View JavaScript API's getCurrentScreenRootElement and
        registerDeviceClassGetter functions, in ODC Mobile Apps and Reactive Web
        Apps, so that DOM-based styling and animations can react to the active
        screen and device characteristics.
    - name: Detect render completion
      id: detect-render-completion
      description: Wants to know when the current screen/block has finished
        rendering, using the View JavaScript API's render() promise, in ODC
        Mobile Apps and Reactive Web Apps, so that custom logic can run after
        rendering completes.
    - name: Detect view cache restoration
      id: detect-view-cache-restoration
      description: Wants to know whether the current view was restored from
        cache, using the View JavaScript API's
        wasCurrentViewRestoredFromCache() function, in ODC Mobile Apps and
        Reactive Web Apps, so that behavior can adapt when a view is restored
        from cache.
    - name: Validate widget state
      id: validate-widget-state
      description: Wants to check whether a widget is valid and set it as valid or
        invalid with a custom message, using the Validation JavaScript API's
        isWidgetValid, setWidgetAsInvalid, and setWidgetAsValid functions, in
        ODC Studio, so that widgets inside iterators (for example, inside a For
        Each node) can be validated individually, in ODC Mobile Apps and
        Reactive Web Apps.
    - name: Define JS code blocks
      id: define-js-code-block-o11
      description: Wants to define JavaScript code blocks at the Module, Web
        Screen, or Web Block level in Service Studio, so that OutSystems
        compiles them into separate .js files added via <script> tags in the
        page's <head> element and the code becomes reusable across Web
        Screens and Web Blocks, in Traditional Web Apps (O11).
    - name: Run JS code methods
      id: run-js-code-methods
      description: Wants to know the ways to run JavaScript code in a
        Traditional Web App, using an Extended Property event, an unescaped
        Expression, or the RunJavaScript Server Action of the
        HTTPRequestHandler API, in Service Studio for O11, so that they can
        trigger custom JavaScript behavior in response to events or during
        screen rendering.
    - name: Set dynamic input value
      id: set-dynamic-input-value
      description: Wants to automatically update one input's value when another
        input changes, using the On Change event, an Assign node, and an Ajax
        Refresh, in Service Studio for OutSystems 11 Traditional Web Apps, so
        that dependent form fields (for example, a date range) update without
        requiring the user to manually re-enter data.
    - name: Multiline table input
      id: multiline-table-input
      description: Wants to allow multiline input in an Editable Table's Text
        Area by using JavaScript to intercept the Enter keypress and insert
        a new line instead of triggering the table's auto-save, in Service
        Studio for OutSystems 11 Traditional Web Apps, so that users can
        enter multi-line text without the row saving prematurely.
    - name: Remove add record
      id: remove-add-record-editable-table
      description: Wants to disable the ability to add new records in an
        Editable Table by modifying the On Row Save server action (changing
        CreateOrUpdate to Update and removing row-creation logic), hiding the
        Add Record icon with a CSS snippet, and blocking the auto-new-row TAB
        behavior with a JavaScript snippet, in Service Studio for OutSystems
        11 Traditional Web Apps, so that end users can only edit existing
        records.
    - name: Remove delete record
      id: remove-delete-record-editable-table
      description: Wants to disable the ability to delete records in an
        Editable Table by leaving the On Row Delete event empty and hiding
        the Delete Record icon/link with a CSS snippet, in Service Studio for
        OutSystems 11 Traditional Web Apps, so that end users can only edit
        existing records without deleting them.
    - name: Fix ignored input
      id: fix-ignored-input
      description: Wants to fix Service Studio warnings caused by user input
        that never reaches the server, either because a screen or block has
        no widget submitting its inputs (no Submit/Ajax Submit button or
        Ajax-triggering widget with a mapped screen action) or because an
        Editable Table has no screen action on its On Row Save or On Row
        Delete event, in OutSystems 11, by adding the missing submit
        button/screen action or event action, so that end user input is
        saved instead of silently ignored.
    - name: JavaScript API keywords
      id: javascript-api-keywords
      description: Wants to use the JavaScript API keywords inside JavaScript nodes in
        Reactive Web Apps, so they can pass values into JavaScript, call other
        client actions, check the current end-user role, and use platform APIs
        from client-side logic.
    - name: JavaScript flow element
      id: javascript-flow-element
      description: Wants to know the configurable properties of the JavaScript flow element
        available in Service Studio, so that they can add custom JavaScript code
        to a screen, screen action, or module, in OutSystems 11 Traditional Web
        Apps, Mobile Apps, and Reactive Web Apps.
  - name: Using external JavaScript
    id: using-external-javascript
    subtopics:
    - name: Add external JS library
      id: add-external-js-library
      description: Wants to add JavaScript code from an external library or a
        .js file to an app by importing or creating a script under the Scripts
        folder and setting it as the Required Scripts property on a screen or
        block, in Service Studio for OutSystems 11 Mobile Apps and Reactive Web
        Apps, so that the script's functions and objects become available in
        JavaScript elements on that screen or block.
    - name: Script loading order
      id: script-loading-order
      description: Wants to understand when and in what order required scripts
        load and execute (module-level scripts first, then block-level, then
        screen-level), including that module scripts load before the On
        Application Ready system event and block/screen scripts load before
        their element renders, in OutSystems 11 Mobile Apps and Reactive Web
        Apps, so that they can predict when JavaScript functions and objects
        become available at runtime.
    - name: Add external JS file O11
      id: add-external-js-file-o11
      description: Wants to reference a JavaScript file hosted outside their
        OutSystems infrastructure by adding the AddJavaScriptTag Server
        Action of the HTTPRequestHandler API to a screen's Preparation with
        the file's URL, in Service Studio for O11 Traditional Web Apps, so
        that the external script runs on that screen without hosting the
        file locally.
  - name: Securing UI
    id: securing-ui
    subtopics:
    - name: Secure UI element
      id: secure-ui-element
    - name: Secure screen
      id: secure-screen
    - name: Prevent content copying
      id: prevent-content-copying
      description: Wants to prevent users from copying or capturing page text
        and images, by adding CSS to disable text selection and hide content
        when printing, and JavaScript to block the right-click context menu
        and the print screen key, in OutSystems 11 traditional web apps,
        mobile apps, and reactive web apps, so that page content is harder
        to copy, print, or screenshot.
  - name: Basic front-end architecture
    id: basic-frontend-arch
    subtopics:
    - name: Elements of UI in OutSystems (UI Flow, screen, UI element)
      id: ui-elements-screen-blocks
    - name: Web application overview
      id: web-application-overview
      description: Wants to understand how a web application in OutSystems is
        put together, including screens, blocks, widgets, forms, themes,
        entities, aggregates, events, debugging tools, and security settings,
        so that they can orient themselves before building or reviewing a web
        app.
  - name: Creating Screens
    id: creating-screens
    subtopics:
    - name: Create screen from scratch
      id: create-screen-scratch
    - name: Create screen using scaffolding
      id: screen-scaffolding
    - name: Create screen from screen template
      id: screen-template
    - name: Create screen templates
      id: create-screen-templates
      description: Wants to create custom Screen Templates in a Custom Screen
        Templates Module, add metadata and a preview image, choose whether to
        bootstrap the template with content or start empty, and publish the
        module so other developers can create reusable screens with compatible
        themes, generic prompts, shared logic, and data replacement support.
    - name: Set screen access
      id: set-screen-access
      description: Wants to configure a screen's Accessible by property to Everyone in ODC Studio so the screen is available to all users during initial app setup.
    - name: Screen properties reference
      id: screen-properties-reference
      description: Wants to look up the configuration properties available for a
        screen in OutSystems 11 Service Studio, including which are mandatory
        and their default values, so they can correctly configure and
        understand the screen's behavior.
    - name: Entry point URL
      id: entry-point-url
      description: 'Wants to understand what an Entry element is in a Traditional
        Web App UI Flow, including how its Name property forms the page''s URL
        (<entry_name>.aspx) and how the Is Default property designates the
        module''s index page, so that they can configure the correct entry
        point for the web app.'
- name: Deployment Management
  guid: 2d8be9e7-a1a1-4c3b-943e-93ad568b32b0
  topics:
  - name: Manual deployment without an infrastructure management console
    id: manual-deploy-no-console
    subtopics:
    - name: Publish a module in SC
      id: publish-a-module-in-sc
    - name: Create a solution in SC
      id: create-a-solution-in-sc
    - name: Publish a solution in SC
      id: publish-a-solution-in-sc
    - name: Publish an app in SC
      id: publish-an-app-o11
      description: Wants to publish an app from Service Studio or Service
        Center to the development environment, understanding that this
        compiles the code, assigns an incremental version number, and
        optionally attaches a message that teammates can review later in
        Service Center's Versions tab or query via the Espace_CommitMessage
        entity, so that they can share app changes and track what was
        published, in OutSystems 11.
    - name: Publish an extension in SC
      id: publish-extension-sc
    - name: Open other version
      id: open-other-version
      description: Wants to switch to a previous published version of a
        module from Service Studio's Module menu using Open Other Version,
        reviewing the list of versions along with the publish date, time,
        and publisher, so that they can revert to or inspect an earlier
        version, in OutSystems 11.
    - name: Publish modules and track versions
      id: publish-modules-version-history
      description: Wants to publish modules and review the version history
        created by each publish in Service Center, so that they can trace
        releases.
    - name: Tag milestone versions
      id: tag-milestone-versions
      description: Wants to mark specific published versions as milestones,
        so that they can identify important release checkpoints.
    - name: Roll back to a previous tagged version
      id: rollback-to-previous-tagged-version
      description: Wants to restore an app to a previously tagged version,
        so that they can recover from a bad release.
  - name: Package version errors
    id: package-version-errors
    subtopics:
    - name: Invalid package version
      id: invalid-package-version
      description: 'Wants to fix the OS-PKGS-40003 "The package version ''<version>'' is not in the correct format (MAJOR.MINOR.PATCH)." error that blocks package installation by updating the package version to MAJOR.MINOR.PATCH format, so that the package installs successfully.'
  - name: Package repository errors
    id: package-repository-errors
    subtopics:
    - name: Fix invalid package data
      id: fix-invalid-package-data
      description: 'Wants to fix the OS-PKGS-40004 "Requested package <package> not found in the package repository." error by retrying the package installation and creating a support case if the problem persists, so that the package installs successfully.'
  - name: Basic deployment concepts
    id: basic-deployment-concepts
    subtopics:
    - name: Dependencies
      id: dependencies
    - name: Solutions/Apps/Modules/Extensions
      id: solutions-apps-modules
    - name: Environments/Stages
      id: environments-stages
    - name: Get default stage domain
      id: get-default-domain
      description: Wants to get the default domain configured for the current ODC
        stage by calling GetDefaultDomain, so that they can build URLs against
        the active stage domain when no custom domain is set.
    - name: Understand ODC domains
      id: understand-odc-domains
      description: Wants to understand ODC domain types, the default domain,
        and how built-in and custom domains affect access and future domain
        changes, so they can plan domain-related decisions safely.
    - name: Deployment options
      id: deployment-options
    - name: Deployment error concept
      id: deployment-error-concept
      description: Wants to understand what a deployment error is in OutSystems
        11, an issue that must be fixed in Service Studio before a module can be
        published to the server, so that they know why every reported error
        needs resolving prior to deployment.
    - name: Warnings overview
      id: warnings-overview
      description: Wants to understand what a warning is in OutSystems 11 and
        that warnings do not block module deployment, so they can decide which
        warnings need investigation and follow the specific guidance that
        applies.
    - name: 1-Click Publish steps
      id: 1-click-publish-steps
      description: Wants to understand what happens when they use 1-Click
        Publish to publish an app or library in OutSystems Developer Cloud
        (ODC), including compiling code, building the container image,
        generating database sync scripts, and deploying to the Kubernetes
        cluster, so that they can anticipate the build and deployment work
        triggered each time they publish a new revision in the Development
        stage.
    - name: ODC publish message
      id: odc-publish-message
      description: Wants to add an optional message describing their changes when 1-Click
        Publishing an app in ODC Studio, understanding the message has a maximum
        length and becomes a permanent, read-only record reviewable via App >
        View revisions, so that their team can track the intent behind each
        revision.
    - name: IPP protection concept
      id: ipp-protection-concept
      description: Wants to understand what Intellectual Property Protection
        (IPP) is in OutSystems 11, how the protected and unprotected states
        control whether applications can be deployed to environments in other
        infrastructures, and how to request their infrastructure's IPP be
        changed, so that they know why cross-infrastructure deployment may be
        blocked and what to do about it.
    - name: Check IPP errors
      id: check-ipp-errors
      description: Wants to identify modules in an OutSystems 11
        infrastructure that will return an IPP error when republished, by
        running the ActivationCodeCheck app, reviewing the flagged modules,
        and exporting the results, so that they can fix the affected modules
        before republishing them.
    - name: Unexpected app rename
      id: unexpected-app-rename
      description: Wants to understand why an app or module's name changes
        unexpectedly in the target environment after staging, caused by
        LifeTime maintaining a centralized copy of app details that
        reflects the last-renamed source environment, by checking the name
        displayed in LifeTime before staging, so that they can confirm the
        expected app name before promoting it in OutSystems 11.
    - name: Synchronize app definition
      id: sync-app-definition
      description: Wants to display an app's name, icon, and color from a
        particular environment in LifeTime by using Synchronize App Definition,
        so that the app details match that environment instead of the most
        recently modified version.
    - name: Use IPP portal
      id: use-ipp-portal
      description: Wants to deploy an OutSystems 11 application to an
        infrastructure it doesn't belong to by using the IPP portal to
        change the application's activation code, so that they can publish
        it in infrastructures outside its original IPP protection.
    - name: Transfer PE apps
      id: transfer-pe-apps-enterprise
      description: Wants to transfer applications from an OutSystems 11
        Personal or Trial Edition environment to a paid Enterprise
        subscription environment, by creating a Solution in Service Center,
        removing IP protection via the Intellectual Property Protection
        tool, and uploading/publishing it in the new environment, so that
        they can migrate their apps after upgrading their license,
        understanding this is intended as a one-time migration and app data
        itself isn't transferred.
    - name: Portfolio deploy scope
      id: portfolio-deploy-scope
      description: Wants to understand how deployment, promotion, and undeployment
        of assets are scoped to a single portfolio's stages and pipeline in a
        multi-portfolio OutSystems Developer Cloud (ODC) organization,
        including separate revisions, versions, and deployment history per
        portfolio, so that they deploy and undeploy assets in the correct
        portfolio without affecting others.
    - name: ODC container deployment
      id: odc-container-deployment
      description: Wants to understand how OutSystems Developer Cloud packages
        web apps and libraries into loosely coupled containers, with only
        weak references allowed between apps and strong references created
        when consuming library versions, so they understand how ODC's
        packaging model differs from OutSystems 11 modules.
    - name: ODC deployment terminology
      id: odc-deployment-terminology
      description: Wants to understand key OutSystems Developer Cloud
        deployment terminology, including organization, portfolio, stage,
        app, and revision, and how selecting a revision and running impact
        analysis fit into deploying an app to a stage in the ODC Portal, so
        they can follow ODC deployment documentation and workflows.
    - name: Rename org or stage
      id: rename-org-or-stage
      description: Wants to change the display name of their ODC organization
        or a pipeline stage from the Organization page in the ODC Portal, so
        that the name shown throughout the portal and ODC Studio matches
        updated naming conventions without affecting apps, users, domains, or
        configurations.
    - name: Plan domains early
      id: plan-domains-early
      description: Wants to deliberately choose their ODC organization
        subdomain and each stage's domain when provisioning a new tenant,
        before building apps, generating native mobile builds, or
        configuring external identity providers, so that they minimize the
        need for a disruptive domain change later.
    - name: Change org subdomain
      id: change-org-subdomain
      description: Wants to change their ODC organization's built-in subdomain
        (<subdomain>.outsystems.dev) from the ODC Portal's Organization page,
        understanding that the new subdomain isn't set as default until they
        explicitly switch it, so they can rebrand or correct their
        organization's address without disrupting active users.
    - name: Change stage subdomain
      id: change-stage-subdomain
      description: Wants to change a stage's built-in subdomain
        (<subdomain>.outsystems.app) from the ODC Portal's Domains page,
        understanding that both the new and previous subdomains stay active
        until they explicitly set the new one as default, so they can rename
        a stage's address after a rebrand or SDLC process change without
        breaking existing traffic.
    - name: Finalize subdomain switch
      id: finalize-subdomain-switch
      description: Wants to complete a built-in subdomain change in the ODC
        Portal by setting the new organization or stage subdomain as default
        and then deleting the previous one, understanding that deletion is
        immediate, permanent, and only possible for a non-default subdomain,
        so that they safely retire the old address once all dependent
        configurations have been updated.
    - name: Reauthenticate users after domain change
      id: reauthenticate-users-domain-change
      description: Wants to manage member or end-user reauthentication after
        an organization or stage domain change in ODC, understanding that
        both the previous and new domain stay active until the previous one
        is deleted, so that they avoid access disruptions when retiring the
        old domain.
    - name: Update OIDC redirect URIs after domain change
      id: update-oidc-redirect-uris-domain-change
      description: Wants to update OIDC redirect URIs after an organization
        or stage domain change in ODC, understanding that both the previous
        and new domain stay active until the previous one is deleted, so
        that sign-in flows keep working when they retire the old domain.
    - name: Rebuild native mobile apps after domain change
      id: rebuild-native-mobile-apps-domain-change
      description: Wants to rebuild native mobile apps after an organization
        or stage domain change in ODC, understanding that both the previous
        and new domain stay active until the previous one is deleted, so
        that distributed mobile apps keep pointing to the active domain
        after the old one is removed.
    - name: Reconnect private gateway connectors after domain change
      id: reconnect-private-gateway-connectors-domain-change
      description: Wants to reconnect private gateway connectors after an
        organization or stage domain change in ODC, understanding that both
        the previous and new domain stay active until the previous one is
        deleted, so that private connectivity keeps working when they retire
        the old domain.
    - name: Reconnect self-hosted configurator after domain change
      id: reconnect-self-hosted-configurator-domain-change
      description: Wants to reconnect the self-hosted configurator after an
        organization or stage domain change in ODC, understanding that both
        the previous and new domain stay active until the previous one is
        deleted, so that self-hosted setup keeps working when they retire
        the old domain.
    - name: Reorder pipeline stages
      id: reorder-pipeline-stages
      description: Wants to change the sequence of non-production stages in
        their ODC organization or portfolio pipeline by dragging them into a
        new order in the ODC Portal, understanding that the Development and
        Production stage positions are locked, so that assets move through
        deployment in the desired sequence.
    - name: Stable stage identifiers
      id: stable-stage-identifiers
      description: Wants to identify ODC pipeline stages reliably in scripts
        or integrations that call the ODC APIs for deployment automation,
        using each stage's stable key rather than its order value, since the
        order value changes whenever stages are reordered and would
        otherwise point at a different stage.
  - name: Manual deployment with an infrastructure management console
    id: manual-deploy-mgmt-console
    subtopics:
    - name: Tag a version
      id: tag-a-version
    - name: Dealing with dependencies during deployments
      id: deploy-dependency-management
    - name: Deployment plan
      id: deployment-plan
    - name: Deploy apps in LT/Portal
      id: deploy-apps-in-lt-portal
    - name: Deploy single module
      id: deploy-single-module
      description: Wants to deploy a single module from a Traditional Web app to
        another stage in LifeTime by choosing Deploy Custom, selecting the
        module, validating the deployment, and continuing the plan, so that
        they update only the changed module instead of the whole application.
    - name: Block app deployment
      id: block-app-deployment
      description: Wants to block an app from being deployed to specific
        environments in LifeTime, understand how blocked apps behave in
        deployment plans and direct deployments, and unblock the app when
        needed, so that deployment restrictions stay enforced.
    - name: Block newly created apps
      id: block-newly-created-apps
      description: Wants to turn off the LifeTime setting that lets newly
        created applications be deployed to all environments, choosing the
        self-managed or cloud location for the setting and recording the
        required reason for change, so that new apps are blocked by
        default.
    - name: Deployment flow
      id: deployment-flow
      description: Wants to understand how a LifeTime deployment runs after
        the plan is defined, including the progress screen, who can follow
        the execution, when it can be aborted, and the backend steps that
        retrieve applications, analyze impact, compile code, generate SQL
        scripts, distribute code, update the database, hot deploy versions,
        and synchronize environments, so that they can explain or monitor
        the deployment.
    - name: Republish vs redeploy
      id: republish-vs-redeploy
      description: Wants to understand the difference between republishing an
        app in OutSystems 11, which recompiles and redistributes its code
        without changing its version when LifeTime's deployment plan flags
        dependent apps with outdated dependencies, and redeploying an app,
        which only redistributes the already-compiled code via Service
        Center's Redeploy Published Version option without recompiling, so
        that they choose the correct action to keep dependent apps and
        frontends synchronized.
    - name: Prevent entity data loss
      id: prevent-entity-data-loss
      description: Wants to prevent entity data loss after deployment when
        recreating an entity changes its Identifier across environments, by
        rolling back Production and staging the entity from Production to
        Development before redeploying, or by repopulating the data when
        rollback is not feasible, so that the original records remain
        available.
  - name: Advanced manual deployments
    id: manual-advanced-deploy
    subtopics:
    - name: Selective Deployment Using Deployment Zones (self-managed)
      id: selective-deploy-zones
    - name: App zone behavior
      id: app-zone-behavior
      description: Wants to understand how application-level deployment zones work in OutSystems 11 so that existing mixed-mode apps keep their module-level settings after upgrade, new apps use application-wide zone configuration only, and changing an application's zone removes module-level zone settings.
    - name: Configure app zone
      id: configure-app-zone
      description: Wants to configure an OutSystems application to use a specific deployment zone in Service Center by selecting the zone in the application's Operation tab and saving the change, so that the platform deploys the app to the chosen zone.
    - name: Unify deployment zones
      id: unify-deployment-zones
      description: Wants to unify modules under one deployment zone, so that
        they can follow the Service Center procedure for deployment-zone
        changes.
    - name: Split application into separate apps
      id: split-application-into-separate-apps
      description: Wants to split an application into separate apps when
        modules must stay in different deployment zones, so that they can
        follow the Service Studio procedure for deployment-zone changes.
    - name: Deployments with no downtime
      id: zero-downtime-deployments
  - name: Automated deployments foundations
    id: automated-deploy-basics
    subtopics:
    - name: Automating deployments with API
      id: deployments-api-automation
    - name: Service accounts for automatic deployments
      id: service-accounts-deploy
    - name: CI/CD foundations
      id: ci-cd-foundations
    - name: DevOps toolset
      id: devops-toolset
      description: Wants to understand which OutSystems and third-party tools support
        DevOps work across the application lifecycle, so that they can choose the
        right toolset for delivery and operations.
  - name: CI/CD pipelines
    id: ci-cd-pipelines
    subtopics:
    - name: Integrate with external orchestrators for CD
      id: external-orchestrator-cd
    - name: ODC CI/CD pipeline model
      id: odc-cicd-pipeline-model
      description: Wants to understand that ODC apps are built once in the
        Development stage and deployed unchanged to later pipeline stages
        without recompilation, and that only Development supports
        publishing while other stages are only updated via a new
        deployment, so they understand ODC's CI/CD deployment flow.
    - name: Build O11 CD pipeline
      id: cd-pipeline-stages
      description: Wants to build an automated continuous delivery pipeline for
        O11 applications by using the outsystems-pipeline accelerator with a
        CI/CD platform (Jenkins or Azure DevOps) so that release candidates
        progress automatically through Development, Regression, Acceptance,
        and Pre-production/Production stages with governance and impact
        analysis from LifeTime.
  - name: Troubleshoot 1-Click Publish
    id: troubleshoot-1-click-publish
    subtopics:
    - name: 1-Click Publish errors
      id: 1-click-publish-errors
      description: Wants to locate and resolve ODC build errors from the 1-Click
        Publish service by finding the error code in the reference, understanding
        its cause and impact, and following the recommended fix action, so that
        they can resolve build failures when publishing mobile or reactive web
        apps.
  - name: Troubleshoot LifeTime deployments
    id: troubleshoot-lifetime-deployments
    subtopics:
    - name: Diagnose LifeTime deploy
      id: diagnose-lifetime-deploy
      description: Wants to diagnose why deploying an application through
        LifeTime from a source to a target environment fails or gets stuck,
        by checking network connectivity between LifeTime and the
        environments, confirming OutSystems services are running in both
        environments, and reviewing LifeTime's Business Process Technology
        processes for suspended or errored instances, in an on-premises
        OutSystems 11 infrastructure, so that they can identify and resolve
        the cause of the failed deployment.
    - name: LifeTime process errors
      id: lifetime-process-errors
      description: Wants to fix suspended process instances or active
        instances with errors on the LifeTime environment's Service Center
        Monitoring > Processes page, which indicate a problem with
        LifeTime's Business Process Technology deployment synchronization,
        by opening a support case with OutSystems, so that stuck
        deployments between environments can be resolved.
    - name: LifeTime sync stuck
      id: lifetime-sync-stuck
      description: Wants to diagnose why environment synchronization in
        LifeTime is stuck, by verifying LifeTime and Platform Server are on
        the latest version, testing connectivity between LifeTime and the
        synchronizing environment, confirming server timezones match, and
        checking for suspended or errored Business Process Technology
        process instances in Service Center, in an OutSystems 11
        infrastructure, so that they can identify and resolve the cause of
        the stuck synchronization.
  - name: Troubleshoot self-managed publish errors
    id: troubleshoot-self-managed-publish-errors
    subtopics:
    - name: File access publish error
      id: file-access-publish-error
      description: 'Wants to resolve the "The process cannot access the file
        because it is being used by another process" error during publishing
        in OutSystems 11 (Service Studio, Service Center, or LifeTime), by
        identifying whether antivirus software or another third-party process
        is locking Platform Server files, excluding the required folders from
        antivirus scanning, or using tools like Process Explorer/Process
        Monitor to find and stop the conflicting process, so that publication
        completes successfully.'
    - name: Fix invoke ping error
      id: fix-invoke-ping-error
      description: 'Wants to resolve a "Could not invoke ping" deployment error
        by identifying the HTTP status code returned (5xx or 4xx) on the
        module''s ping request and applying the corresponding fix (e.g.,
        IIS/application pool checks, gateway timeout, network/DNS
        configuration), in OutSystems 11, so that the module''s ping request
        succeeds and deployment completes.'
    - name: Fix ping content error
      id: fix-ping-content-error
      description: 'Wants to resolve an "Unexpected content found in ping"
        deployment error by determining whether the _ping.html content was
        altered by a code-injection tool or by a redirection caused by SEO
        rules, and applying the corresponding fix (disabling the injecting
        tool or removing the offending SEO/pathrule/siterule), in OutSystems
        11, so that deployment completes successfully.'
    - name: Slow deploy new attribute
      id: slow-deploy-new-attribute
      description: Wants to fix an application deployment that takes a long
        time or times out during the update database phase after adding a
        new Attribute to an existing Entity with millions of records, in an
        affected version of OutSystems 11 Platform Server, by upgrading to a
        version that resolves the issue or increasing the Configuration
        Tool's Default Query Timeout, so that the deployment completes
        successfully.
    - name: Invalid argument error
      id: invalid-argument-error
      description: 'Wants to fix the "Invalid Argument Value" error in Service
        Studio, caused by referencing a non-existent Web Service name in an
        action''s argument, by checking whether the Web Service is consumed
        by the module or correcting the argument name, in OutSystems 11, so
        that the module compiles and publishes successfully.'
    - name: Invalid argument warning
      id: invalid-argument-warning
      description: 'Wants to understand the cause of the "Unable to validate
        if the argument of the action refers a web service consumed in your
        module because the name supplied is not a literal" warning in
        Service Studio, triggered by passing a non-literal (variable) value
        to an action parameter that expects a consumed Web Service name, so
        that they understand why Service Studio can''t validate the web
        service reference at design time, in OutSystems 11.'
    - name: Invalid Blocks error
      id: invalid-blocks-error
      description: 'Wants to resolve the "Invalid Blocks" "Block loop detected"
        error in OutSystems 11, by reading the web block include sequence
        reported in the TrueChange tab or the 1-Click Publish compile step to
        identify which web blocks include each other in a loop, so they can
        break the loop and get the module to compile and publish
        successfully.'
    - name: Invalid client function error
      id: invalid-client-function-error
      description: 'Wants to resolve the "Invalid Client Function" error in
        OutSystems 11, caused by a Client Action marked as a function calling
        a Server Action, Local Storage Aggregate, or asynchronous JavaScript
        node, or by calling a non-function action inside another function, by
        changing the Function property of the relevant Client Action to Yes
        or No as appropriate, so that the module compiles and publishes
        successfully.'
    - name: Invalid Combo Box error
      id: invalid-combo-box-error
      description: 'Wants to resolve the "Invalid Combo Box Widget" error in
        Service Studio, caused by missing or inconsistent Source Entity/Struct,
        Source Record List, Special List, Variable, or Source Attribute
        properties on a Combo Box widget, by editing the widget to set the
        correct combination of properties matching the specific error
        message, in OutSystems 11, so that the module compiles and publishes
        successfully.'
    - name: Fix Empty Widget error
      id: fix-empty-widget-error
      description: 'Wants to resolve the "Empty Widget" error in Service
        Studio, caused by a Dropdown widget that has no content to display
        its values, by placing a widget such as an Expression inside the
        Dropdown, or dragging an Entity or Static Entity Identifier variable
        into it to auto-configure the Dropdown, in OutSystems 11, so that
        the module compiles and publishes successfully.'
    - name: Invalid widget warning
      id: invalid-widget-warning
      description: 'Wants to resolve the "Screen ''<screen name>'' contained
        a corrupted <widget kind> Widget that was re-created. Please check
        its properties" warning in Service Studio, caused by Service Studio
        automatically detecting and recreating a corrupted widget, by
        reviewing and correcting the properties of the recreated widget, in
        OutSystems 11, so that the screen behaves as expected.'
    - name: Invalid Data Action error
      id: invalid-data-action-error
      description: 'Wants to resolve the "Invalid Data Action" error in
        OutSystems 11, caused by a Data Action that doesn''t set a value for
        an Output Parameter or that has no Output Parameters at all, by
        assigning a correctly typed value to the existing Output Parameter
        or creating at least one, so that the module compiles and publishes
        successfully.'
    - name: Duplicate page name error
      id: duplicate-page-name-error
      description: 'Wants to resolve the "Duplicated Page Name" error in
        Service Studio, caused by a Screen''s Page Name clashing with the
        name of another Screen, Block, UI Flow, or the module itself, by
        changing the Page Name to a unique value among those elements, in
        OutSystems 11, so that the module compiles and publishes
        successfully.'
    - name: Incompatible version error
      id: incompatible-version-error
      description: 'Wants to resolve the "This version of Service Studio is
        not compatible with the selected Platform Server" error when
        publishing a module in OutSystems 11, by checking with the Service
        Center administrator which Platform Server version is in use and
        what needs to be upgraded, so that the module publishes
        successfully.'
    - name: Fix Incomplete Upgrade error
      id: fix-incomplete-upgrade-error
      description: 'Wants to resolve the "Incomplete Upgrade" error when
        publishing a module in Service Studio, caused by the module
        consuming other modules that weren''t upgraded yet to the new
        Platform Server version after a server upgrade, by publishing the
        consumed modules first, in OutSystems 11, so that the module
        publishes successfully.'
    - name: Fix upgrade warning
      id: fix-upgrade-warning
      description: 'Wants to resolve the "Upgrade Information Warning" (Producer,
        Consumer, or User Provider "<module>" was published with a different
        Platform Server version) in Service Studio or Service Center, caused
        by a referenced producer, consumer, or user provider module not being
        republished since the last Platform Server upgrade, by republishing
        the affected module (via Service Center notification or 1-Click
        Publish), in OutSystems 11, so that the module avoids runtime errors
        from version mismatches.'
    - name: Fix upgrade disabled
      id: fix-upgrade-disabled
      description: 'Wants to resolve the "Upgrade Disabled: Extension
        recompilation on upgrade is disabled" error during publishing on
        Platform Server 11.14.0 or later, by upgrading and recompiling the
        extension locally with Integration Studio and republishing it
        through Service Center, in OutSystems 11, so that the publish
        completes without automatic server-side recompilation.'
    - name: Fix recompilation warning
      id: fix-recompilation-warning
      description: 'Wants to resolve the "Continuing with extension
        recompilation enabled may result in security flaws" publish warning
        in Service Center, caused by automatic upgrade and recompilation of
        extensions being enabled on Platform Server 11.14.0 or later, by
        disabling the setting and using the secure upgrade/recompilation
        process (or knowingly accepting the risk and keeping it enabled
        temporarily), in OutSystems 11, so that the environment isn''t
        exposed to unattended and unwanted code modifications.'
    - name: Invalid Reference error
      id: invalid-reference-error
      description: 'Wants to resolve the "Invalid Reference" error in Service
        Studio, caused either by an Action reference to a Multi-tenant
        entity parameter where the ''Show Tenant Identifier'' property
        differs from the producer module (fixed by removing the reference
        or synchronizing the property value), or by a Block or Screen
        navigating to a Screen outside the application (fixed by removing
        the navigation, using an external link, or moving the module into
        the application), in OutSystems 11, so that the module compiles
        and publishes successfully.'
    - name: Invalid module error
      id: invalid-module-error
      description: 'Wants to resolve the "Invalid module" error in Service
        Studio, caused by a multi-tenant module being used as a User
        Provider for other modules, by setting the module to single-tenant
        or providing an alternative module as the User Provider, in
        OutSystems 11, so that the module compiles and publishes
        successfully.'
    - name: Fix invalid screen error
      id: fix-invalid-screen-error
      description: 'Wants to fix the "Invalid Screen" error family in Service
        Studio for OutSystems 11, caused by setting the Cache in Minutes
        property on a screen or block with a Binary Data, Record, or List
        input parameter, binding the same variable to multiple input widgets,
        or having a corrupted widget definition, by unsetting Cache in
        Minutes, binding each variable to only one widget, or deleting and
        re-creating the corrupted widget, so that the screen or block
        compiles and publishes successfully.'
    - name: Fix invalid property
      id: fix-invalid-property
      description: 'Wants to fix the "Invalid Property" error in Service
        Studio for OutSystems 11, caused by defining the same property (for
        example, an extended property like Title) more than once on the same
        element, by editing the element to remove the duplicate entry, so
        that the module compiles and publishes successfully.'
    - name: Fix invalid property value
      id: fix-invalid-property-value
      description: 'Wants to fix the "Invalid Property Value" error family in
        Service Studio for OutSystems 11, caused by leaving a mandatory widget
        property unset, setting the Default Value property to an expression
        instead of a literal, or setting a Record widget''s Source Record to
        the content of another widget, by setting the required property,
        using a literal or built-in default function, or referencing a local
        variable, query, or preparation-evaluated list instead, so that the
        module compiles and publishes successfully.'
    - name: Fix invalid public action
      id: fix-invalid-public-action
      description: 'Wants to fix the "Invalid Public Action" error in Service
        Studio for OutSystems 11, caused by a public action having a Record
        (Entity/Structure) parameter that isn''t declared as public or is
        defined by a module reference, by making the underlying
        Entity/Structure public or redefining the parameter using an
        Entity/Structure from the current module, so that the module
        compiles and publishes successfully.'
    - name: Fix invalid public block
      id: fix-invalid-public-block
      description: 'Wants to resolve the "Invalid Public Block" error in
        Service Studio, caused by a public Block that uses a Destination
        node to navigate to a Screen or includes another Block that does
        so, by removing the navigation or setting the Block''s Public
        property to No, in OutSystems 11, so that the module compiles and
        publishes successfully.'
    - name: Public screen block error
      id: public-screen-block-error
      description: 'Wants to resolve the "Invalid Public Screen Block" error
        family in Service Studio, caused by a public Screen Block whose
        Record-type input parameter references a non-public Entity/Structure,
        references an Entity/Structure from another module, or is used in a
        link Destination property with a Binary Data, Record, or List input
        parameter, by making the referenced Entity/Structure public, using
        only in-module Entities/Structures, or adjusting/removing the
        Destination argument or the block''s Public property, in OutSystems
        11, so that the module compiles and publishes successfully.'
    - name: Incompatible consumer warning
      id: incompatible-consumer-warning
      description: 'Wants to resolve the "Consumer module has incompatible
        dependencies" warning in OutSystems 11, caused by changing a
        producer module''s exposed signature in a way that breaks a
        consumer module, by refreshing the dependency in the consumer
        module in Service Studio, adapting the logic, and republishing it,
        so that runtime errors are avoided.'
    - name: Outdated consumer
      id: outdated-consumer-warning
      description: 'Wants to resolve the "Consumer module is outdated" warning
        in OutSystems 11, caused by a compatible change to a producer
        module''s exposed functionality that leaves the consumer running an
        older version (applicable only to strong dependencies), by
        republishing the consumer module without needing to refresh its
        producer dependencies, so that it uses the latest producer
        implementation.'
    - name: Inconsistent dependency
      id: inconsistent-dependency-warning
      description: 'Wants to resolve the "Producer is inconsistent" warning
        in OutSystems 11, caused by a Producer (module or Extension) that
        isn''t consistent in the Platform Server where it was deployed, by
        1-Click Publishing the producer module and notifying the Service
        Center administrator, so that runtime errors are prevented.'
    - name: Outdated producer warning
      id: outdated-producer-warning
      description: 'Wants to resolve the "Producer ''<producer>'' uses
        outdated references. Please republish ''<producer>'' to prevent
        runtime errors" warning in OutSystems 11, caused by a referenced
        producer module itself having outdated references to other
        producers that changed since it was last published, by
        republishing the referenced producer module and then republishing
        the consuming module, so that runtime errors are prevented.'
    - name: Fix invalid theme warning
      id: fix-invalid-theme-warning
      description: 'Wants to resolve the "Invalid Theme" warning family in
        Service Studio, caused by using a Web Block as a theme''s Layout,
        Info Balloon, Pop-up Editor, Email, or Menu without the required
        placeholders (''Title'' and ''MainContent'' for Layout;
        ''MainContent'' for Info Balloon/Pop-up Editor/Email; or the
        Menu-specific requirements), by editing the Web Block to add the
        missing placeholders or comply with Menu requirements, or choosing
        a different compliant Web Block, in OutSystems 11, so that the
        theme compiles and publishes successfully.'
    - name: Module name changed warning
      id: module-name-changed-warning
      description: 'Wants to resolve the "Module <name> was renamed to
        <name>. Ensure that external links to this application are
        compliant" warning in OutSystems 11, caused by renaming a module,
        by updating the external links related to the application (for
        example, External Sites that other modules have to this module),
        so that the application remains compliant.'
    - name: Fix missing entity warning
      id: fix-missing-entity-warning
      description: 'Wants to fix the "Foreign Entity/Referenced Entity was
        not found in Platform Server" warning family in OutSystems 11,
        caused by using a Foreign Entity or Entity Reference that doesn''t
        exist yet in the connected server (for example, before deploying
        to a production server), by publishing the extension or Producer
        module that exposes the entity to that Platform Server, so that
        the module publishes without the warning.'
    - name: Missing module warning
      id: missing-module-warning
      description: 'Wants to resolve the "Module <module name> uses module
        <producer name> that was not found. Runtime errors may occur."
        warning in OutSystems 11, caused by a module referencing a producer
        module that can''t be found on the connected Platform Server, by
        checking with the producer module''s owner or the Platform Server
        administrator to determine the cause, so that runtime errors are
        avoided.'
    - name: Missing provider warning
      id: missing-provider-warning
      description: 'Wants to resolve the "Could not find the <provider> User
        Provider module in this server. Application runtime errors might
        occur" warning in OutSystems 11, caused by a module''s configured
        User Provider not existing on the Platform Server it''s published
        to, by checking with the Platform Server administrator or the User
        Provider''s owner to determine the cause, or by changing the
        module''s User Provider property, so that runtime errors are
        avoided.'
    - name: Fix reference warning
      id: modified-or-missing-reference-warning
      description: 'Wants to resolve the "''<element>'' <type> is incompatible
        with ''<Producer>'' <module | extension> definition" or "''<element>''
        <type> was not found in the ''<Producer>'' <module | extension>
        definition" warning in OutSystems 11, caused by a module referencing
        an element that was modified or removed in the Producer module or
        Extension, by refreshing the modified reference or removing the
        missing reference in Service Studio, so that runtime errors are
        avoided.'
    - name: Rename staging conflict
      id: rename-staging-conflict
      description: 'Wants to resolve the "Name Conflict: Cannot publish
        because another module with the same name already exists" staging
        error in LifeTime, caused by renaming an app or module in the source
        environment and then creating a new app or module that reuses the
        old name before staging the renamed one to the target environment,
        by staging the renamed app or module to the target environment
        first, in OutSystems 11, so that the staging plan completes
        successfully.'
    - name: Slow farm publishing
      id: slow-farm-publishing
      description: Wants to fix slow publishing, staging, or deployment in self-managed
        OutSystems 11 farm installations running an affected Platform Server
        version where the deployment zone address is set to the load balancer's
        hostname, by confirming the symptom pattern (slow Deployment phase,
        SLOWEXTENSION RefreshISAPIFilters entries, RefreshPathRules connection
        errors) and applying the Compiler.UseFrontEndAddressRefreshPaths
        database workaround or upgrading to a version that resolves the issue,
        so that deployments complete in normal time.
  - name: Troubleshoot SEO-friendly URLs
    id: troubleshoot-seo-friendly-urls
    subtopics:
    - name: Fix SEO redirects
      id: fix-seo-redirects
      description: 'Wants to fix SEO-friendly URL redirect rules that don''t
        work in a small number of OutSystems Cloud environments, by checking
        the React version their app is running via the browser console and
        contacting OutSystems Support if it''s lower than 16, in Reactive Web
        Apps, so that redirects work correctly.'
    - name: Fix blocked external URL
      id: fix-blocked-external-url
      description: 'Wants to fix a blank page and "Content Security Policy
        blocked" error that stops users reaching a login or home page, caused
        by using an External URL node in a Screen''s SecurityException logic
        with an active site rule for a domain, by replacing it with a
        JavaScript redirect (window.location.href) instead, in OutSystems 11
        Reactive Web Apps.'
    - name: Avoid duplicate redirects
      id: avoid-duplicate-redirects
      description: 'Wants to avoid duplicate redirects and unpredictable URL
        routing caused by setting a Screen Page Name that matches an app or
        module name already targeted by an environment Site Rule, by
        checking existing Page Names, app/module names, and Site Rules
        before choosing a Page Name, in OutSystems 11 Reactive Web Apps, so
        that SEO-friendly URLs route to the intended destination.'
    - name: Invalid page name error
      id: invalid-page-name-error
      description: 'Wants to resolve the "Invalid Page Name Error" in Service
        Studio, caused by a Screen''s Page Name containing characters other
        than letters, digits, dash, tilde, or slash, by removing the invalid
        characters, in OutSystems 11, so that SEO-friendly URLs work
        correctly.'
    - name: Fix parameter order
      id: fix-parameter-order
      description: 'Wants to fix the "Invalid Parameter Order" error in
        Service Studio, caused by having optional Input Parameters placed
        before mandatory ones on a Screen with custom (Path) URLs, by
        reordering the Input Parameters or changing their Is Mandatory
        property, in OutSystems 11, so that SEO-friendly URLs work
        correctly.'
  - name: Portfolio API management
    id: portfolio-api-management
    subtopics:
    - name: List portfolios via API
      id: list-portfolios-via-api
      description: Wants to list ODC portfolios programmatically by using the
        Portfolio API v2 REST endpoints, so they can discover available
        portfolios before automating portfolio administration.
    - name: Rename portfolio via API
      id: rename-portfolio-via-api
      description: Wants to rename an ODC portfolio programmatically by using
        the Portfolio API v2 REST endpoints, so they can keep portfolio names
        aligned with organizational changes.
    - name: Reorder portfolios via API
      id: reorder-portfolios-via-api
      description: Wants to reorder ODC portfolios programmatically by using
        the Portfolio API v2 REST endpoints, so they can keep portfolio
        organization consistent across environments.
    - name: List environments via Portfolio API
      id: list-environments-via-portfolio-api
      description: Wants to list ODC environments programmatically by using the
        Portfolio API v2 REST endpoints, so they can discover available
        environments before automating environment administration.
    - name: List deployed assets via Portfolio API
      id: list-deployed-assets-via-portfolio-api
      description: Wants to list deployed assets programmatically by using the
        Portfolio API v2 REST endpoints, so they can discover deployed assets
        across portfolios and environments before automating asset
        administration.
    - name: List libraries via Portfolio API
      id: list-libraries-via-portfolio-api
      description: Wants to list libraries programmatically by using the
        Portfolio API v2 REST endpoints, so they can discover available
        libraries before automating library administration.
    - name: Migrate Portfolio API
      id: migrate-portfolio-api-v1-v2
      description: Wants to migrate an existing Portfolio API v1 integration to v2 in ODC
        before v1 is sunset, by updating endpoint URL paths, replacing removed
        or renamed Environment fields with their v2 equivalents, handling the
        removed EnvironmentPurpose enum value, and accounting for deployment
        URLs built from the default domain, so that their integration keeps
        working after v1 is removed.
  - name: ODC public REST APIs
    id: odc-public-rest-apis
    subtopics:
    - name: Choose REST API domain
      id: choose-rest-api-domain
      description: Wants to identify which ODC public REST API domain addresses a given
        automation task, based on each domain's high-level purpose, so they can
        locate the correct API reference for implementation.
    - name: ODC API rate limits
      id: odc-api-rate-limits
      description: Wants to know the domain-wide and specific endpoint request-per-minute
        limits for each ODC public REST API domain, so they can design
        integrations that stay within OutSystems Developer Cloud's rate limits.
    - name: Handle rate limit errors
      id: handle-rate-limit-errors
      description: Wants to know what response the ODC public REST APIs
        return when a client exceeds an API rate limit, so they can detect
        and handle the 429 Too Many Requests response in their integration
        code.
    - name: Paged request limit
      id: paged-request-limit
      description: Wants to know the valid range for the limit value in a
        paged request, so they can avoid the OS-FRGE-AST-40003 error and
        fetch assets successfully.
    - name: Paged request offset
      id: paged-request-offset
      description: Wants to know the valid range for the offset value in a
        paged request, so they can avoid the OS-FRGE-AST-40004 error and
        fetch assets successfully.
    - name: API terminology mapping
      id: api-terminology-mapping
      description: Wants to map ODC Portal terminology (stage, end-user role) to
        their corresponding ODC REST API resource names (environment,
        application role), so they can correctly interpret API reference
        documentation without confusion between the two naming conventions.
    - name: Call API with token
      id: call-api-with-token
      description: Wants to call an ODC public REST API with an access token after
        the client credentials and token are already in place, by consuming the
        API in ODC Studio, setting the base URL, adding the token to the
        Authorization header, and invoking the method from app logic, so that
        the request authenticates successfully before the token expires.
    - name: Upload external library
      id: upload-external-library
      description: Wants to upload an external library to ODC by obtaining an upload URL,
        uploading the ZIP file to the provided storage location, creating the
        generation operation with the download URL, and checking the operation
        status, so that the library can be published for ODC assets to consume.
    - name: Create asset via API
      id: create-asset-via-api
      description: Wants to create a new ODC asset through the Asset Repository API,
        including choosing a valid asset type and using a subtype only for
        LowCodeLibraries, so that the create request succeeds.
- name: DevOps and ALM
  guid: a8f0bc19-a180-4f83-b28e-8c786568516c
  topics:
  - name: OutSystems DevOps loop overview
    id: devops-loop-overview
    description: Wants to decide whether OutSystems' built-in DevOps and ALM capabilities
      can replace a custom toolchain for their application lifecycle, so they
      can determine whether an out-of-the-box DevOps model meets their
      organization's needs before evaluating the individual lifecycle stages
      (plan, develop, test, deploy, release, secure, and monitor) in more depth.
  - name: DORA metrics
    id: dora-metrics
    description: Wants to identify the four DORA metrics, deployment frequency,
      lead time for changes, time to restore service, and change failure rate,
      so they can recognize the correct measure set for DevOps delivery
      effectiveness in an assessment.
  - name: DevOps reference model
    id: devops-reference-model
    description: Wants to understand how the DevOps reference model maps OutSystems
      capabilities across delivery, governance, and operations, so that they can
      plan a DevOps approach for the platform.
- name: Develop for testability
  guid: 71fc8278-36e0-4a93-a84d-79eb8da3135d
  topics:
  - name: Fundamentals of Domain Driven Design for testability
    id: domain-driven-design-test
    subtopics:
    - name: Code isolation
      id: code-isolation
    - name: DDD and testability
      id: ddd-and-testability
    - name: Mapping tests to the Architecture Canvas
      id: map-tests-architecture
  - name: Test data
    id: test-data
    subtopics:
    - name: Setup & teardown strategies for automation
      id: setup-teardown-automation
  - name: Develop UI for testability
    id: testable-ui-development
    subtopics:
    - name: UI development for testability
      id: testable-ui-dev
- name: Developing Plugins
  guid: 241c1f68-e8b9-4b5d-9e03-c1c014240027
  topics:
  - name: Wrap a Cordova plugin as an OS Plugin
    id: wrap-cordova-plugin
  - name: Create LifeTime plugin
    id: create-lifetime-plugin
    description: Wants to create a custom LifeTime plugin for O11 by installing the
      LifeTime SDK in a development environment, exporting sample data,
      developing the plugin in Service Studio using the plugin template, and
      publishing it to the LifeTime environment via Service Center, so that they
      can extend LifeTime with functionality tailored to their organization's
      governance or automation needs.
  - name: Customize authentication plugin
    id: customize-authentication-plugin
    description: Wants to customize or implement an authentication plugin
      for OutSystems 11, using the provided external authentication provider
      model as a base or extending the built-in Active Directory and LDAP
      options, so that they can support the authentication flow their
      organization needs.
- name: Getting started with app development
  guid: 08c8e683-27ea-4656-8559-e145b5e05086
  topics:
  - name: Front-end design journey
    id: front-end-development-journey
    description: Wants to understand how an app's front-end development moves
      through the UX Concept, UI design, and Front-End Development stages,
      from wireframes and Hi-Fi mockups to a live style guide, so they know
      how business requirements get translated into application screens.
    subtopics:
    - name: Design mobile apps
      id: design-mobile-apps
      description: Wants to design a mobile app by accounting for device
        form factor, input and output methods, processing power, and usage
        context, so that the app works well on mobile devices and matches
        user expectations.
  - name: Using Service Studio
    id: using-service-studio
    subtopics:
    - name: Log in to an environment
      id: log-in-to-an-environment
    - name: Service Studio overview
      id: service-studio-overview
      description: Wants to navigate Service Studio as a newcomer by understanding
        what each major area of the interface is for and where to perform common
        setup and app-building actions, so they can move through the tool without
        getting blocked by layout or navigation friction.
    - name: Download and set up
      id: download-and-set-up
    - name: Adopt visual paradigm
      id: adopt-visual-paradigm
      description: Wants to understand why the OutSystems visual, model-driven paradigm
        differs from traditional hand-coding, for example through declarative
        logic, drag-and-drop UI composition, and automatic data and logic
        wiring, so they don't force imperative coding habits into Service Studio
        and get blocked early when building their first app.
    - name: Compare platform stacks
      id: compare-platform-stacks
      description: Wants to compare the .NET and OutSystems development stacks,
        including Visual Studio workloads, Service Studio, Integration Studio,
        and the IIS-based runtime servers, so they can map each platform's
        setup and execution path.
    - name: Enable enhanced logging
      id: enable-service-studio-logging
      description: 'Wants to enable enhanced/verbose logging in Service Studio,
        via Settings.xml switches or ServiceStudio.exe.config, when the
        diagnostics report doesn''t contain enough detail, by turning on
        diagnostic switches and specifying a log output path, so they can
        capture more detailed logs before sharing them with Support.'
    - name: Fix Studio permissions
      id: fix-studio-permissions
      description: Wants to fix Service Studio or ODC Studio failing to launch because it
        can't create or read its local configuration folder, by resetting folder
        ownership and permissions via Terminal or Finder on macOS, or the
        Security tab in Windows File Explorer, so that Studio can launch
        normally.
  - name: Element shortcuts
    id: element-shortcuts
    subtopics:
    - name: Create elements with shortcuts
      id: create-elements-with-shortcuts
      description: Wants to create common Service Studio elements faster with
        drag-and-drop shortcuts, so they can build the app without manually
        inserting each element.
    - name: Reuse elements with Extract to Action
      id: reuse-elements-with-extract-to-action
      description: Wants to reuse an existing Service Studio element by
        extracting it to an action, so they can avoid duplicating logic and
        keep the app easier to maintain.
    - name: Convert existing elements
      id: convert-existing-elements
      description: Wants to convert existing Service Studio elements with
        conversion commands, so that they can adapt the app structure without
        rebuilding it from scratch.
  - name: Service Studio workspace
    id: service-studio-workspace
    subtopics:
    - name: Workspace navigation
      id: service-studio-workspace-navigation
      description: Wants to navigate the Service Studio workspace with
        shortcuts, so they can move around the IDE faster.
    - name: Open files and modules
      id: service-studio-open-files-and-modules
      description: Wants to manage open Service Studio files and modules from
        the workspace, so they can switch between items without losing
        context.
    - name: Inspect screens in the browser
      id: service-studio-inspect-screens-in-browser
      description: Wants to inspect a Service Studio screen in the browser,
        so they can preview how the app renders.
    - name: Create entity diagrams
      id: service-studio-create-entity-diagrams
      description: Wants to create entity diagrams in Service Studio, so they
        can visualize the data model.
    - name: Guess data types
      id: service-studio-guess-data-types
      description: Wants to guess data types in Service Studio, so they can
        configure elements faster without checking each type manually.
    - name: Manage dependencies
      id: service-studio-manage-dependencies
      description: Wants to add or remove dependencies in Service Studio, so
        they can control which modules the app can use.
    - name: Tree helpers
      id: service-studio-tree-helpers
      description: Wants to use Service Studio tree helpers, so they can
        organize elements faster.
    - name: Styling helpers
      id: service-studio-styling-helpers
      description: Wants to use Service Studio styling helpers, so they can
        adjust element appearance faster.
  - name: Using ODC Portal and Studio
    id: using-odc-portal-and-studio
    subtopics:
    - name: ODC Portal overview
      id: odc-portal-overview
      description: Wants to navigate the ODC Portal as a newcomer by understanding
        what the top bar, Navigation menu, and app-creation options are for, so
        they can move through the console without getting blocked by layout or
        navigation friction.
    - name: ODC Studio overview
      id: odc-studio-overview
      description: Wants to navigate the ODC Studio IDE as a newcomer by understanding what
        each major area of the interface is for, so they can move through the
        tool without getting blocked by layout or navigation friction.
    - name: Troubleshoot reminder comments
      id: troubleshoot-reminder-comments
      description: Wants to troubleshoot reminder comments in ODC Studio by understanding
        what turns a comment into a reminder and why removing the text alone
        does not clear the flag, so that they can resolve or unmark the reminder
        correctly.
    - name: ODC Studio commands
      id: odc-studio-commands
      description: 'Wants to run ODC Studio command-line switches from the installation
        directory by prefixing each command with `.\odcstudio.exe`, including
        `-create`, so that they can execute supported CLI tasks from Windows
        Command Prompt or PowerShell.'
    - name: Compare locally saved apps
      id: compare-locally-saved-apps
      description: 'Wants to compare two locally saved apps with `-diff`, so
        that they can review differences from the command line.'
    - name: Merge locally saved apps
      id: merge-locally-saved-apps
      description: 'Wants to merge two locally saved apps with `-merge`, so
        that they can attempt an automatic merge from the command line.'
    - name: Open remote apps
      id: open-remote-apps
      description: 'Wants to open a remote app with `-openModuleFromUrl`, so
        that they can manage apps from the command line when working with
        remote locations.'
    - name: Refresh local apps
      id: refresh-local-apps
      description: 'Wants to refresh a local app with `-refresh`, so that they
        can manage apps from the command line when working with local
        app updates.'
    - name: Recover local apps
      id: recover-local-apps
      description: 'Wants to recover a local app with `-recover`, so that they
        can manage apps from the command line when working with corrupted
        `.oml` files.'
    - name: Get diagnostics report
      id: diagnostics-report
      description: Wants to obtain and send a diagnostics report to OutSystems
        Support after an unexpected error or a non-error behavior issue, by
        expanding the Unexpected Error window or using the Give Us Feedback
        option, opening the report in a text editor, and saving it as a text
        file, so that Support has the information needed to investigate.
  - name: Development artifacts
    id: development-artifacts
    subtopics:
    - name: Project structure
      id: project-structure
      description: 'Wants to compare .NET solutions, projects, and libraries with OutSystems applications and modules, and use Service Studio''s Applications and Modules views when working with development artifacts.'
    - name: Programming style
      id: programming-style
      description: Wants to compare .NET object-oriented classes and
        OutSystems component-oriented visual elements and structured text, so
        they understand how the platform expresses logic and UI resources.
    - name: Resource storage
      id: resource-storage
      description: Wants to understand how OutSystems stores application
        resources in OML files locally and on the server, so they know what
        gets saved, published, and tested where.
  - name: Publish and share apps
    id: publish-and-share-apps
    subtopics:
    - name: Try your app and share with end-users
      id: test-share-app-users
    - name: Publish an app
      id: publish-an-app-odc
      description: Wants to publish an app from ODC Studio to a stage in ODC,
        so that the latest changes are available for testing or use.
  - name: Creating apps
    id: creating-apps
    subtopics:
    - name: Choosing the right app type
      id: choose-app-type
    - name: Creating app from scratch
      id: creating-app-from-scratch
    - name: Traditional web app
      id: traditional-web-app
      description: Wants to create a traditional web application with a UI module and a blank Core module, connect the modules with a public server action, and show that action result on an anonymous home screen, so they can publish and test the app in a browser.
    - name: Application templates overview
      id: application-templates-overview
      description: Wants to understand what application templates are, what types are
        available (built-in, custom, and Forge), and what predefined elements they
        provide, when starting to create an OutSystems app in Service Studio, so that
        they can choose the right template for their project.
    - name: Create use-case app
      id: create-use-case-app
      description: Wants to create a first app for a supported business use case by
        following the matching Service Studio tutorial and development
        accelerator, then customize it with their own data and use case-specific
        screens, so that they can get a working starter app for a real business
        scenario.
    - name: Patient portal overview
      id: patient-portal-overview
      description: Wants to understand the patient self-service portal they will
        build in the guided path, so they know the app purpose and scope before
        starting the course.
    - name: Create custom app template
      id: create-custom-app-template
      description: Wants to create a reusable custom app template in ODC Studio by
        naming the app with the Template_ prefix and adding a description and
        icon, then publishing it, so that the template becomes available as a
        base for new apps with consistent look, feel, and logic.
    - name: Template color inheritance
      id: template-color-inheritance
      description: Wants to understand that an app created from a custom app
        template inherits the template's primary color from its CSS
        declarations even if a different color is selected during app
        creation, so they can predict and control the new app's initial theme
        color.
  - name: Using external OutSystems components
    id: external-outsystems-comp
    subtopics:
    - name: Adding dependencies to a module
      id: add-module-dependency
    - name: Resolve dependency error
      id: resolve-dependency-error
      description: Wants to resolve an ODC app or library dependency error after the
        referenced app or library is removed from the organization, so that the
        dependency becomes valid again and the app or library keeps working.
    - name: Dependency management
      id: dependency-management-overview
      description: Wants to compare how .NET and OutSystems expose dependency references, tooling, and package sources, so they can locate, add, and use external components in either platform when moving between Visual Studio and Service Studio.
    - name: Add language extensions
      id: add-language-extensions
      description: Wants to extend OutSystems with Integration Studio and Visual Studio
        so they can add C# code and custom components that the visual language
        does not provide.
    - name: Installing Forge component
      id: install-forge-component
    - name: ODC solutions overview
      id: odc-solutions-overview
      description: Wants to understand what an ODC solution is, how it bundles components,
        and how components differ from dependencies, so that they can tell what
        belongs in the solution and what stays separate.
    - name: Install ODC solution
      id: install-odc-solution
      description: Wants to install an ODC solution from Forge, including selecting a
        portfolio, accepting the required terms, and tracking installation progress,
        so that they can get the solution into their organization successfully.
    - name: Manage ODC solution
      id: manage-odc-solution
      description: Wants to manage an installed ODC solution after installation, including independent deployment, customization, and locating its components in the Solutions console and asset lists, so that they can work with it like standard assets.
    - name: Forge overview
      id: forge-overview
      description: Wants to understand what Forge is, what types of reusable assets (apps, libraries, agentic apps) are available, and how the community-driven sharing model works in OutSystems.
    - name: Forge project types
      id: forge-project-types
      description: Wants to understand the different types of Forge projects, so that they
        know what kind of project to share or look for in the OutSystems 11
        Forge.
    - name: Forge licensing model
      id: forge-licensing-model
      description: Wants to understand that individual Forge projects (not
        Business Solutions) follow a BSD open-source licensing model by
        default, but contributors can share under other licenses, and that
        verifying and complying with a project's license is the user's own
        responsibility, so that they use OutSystems 11 Forge projects
        appropriately.
    - name: Forge project classifications
      id: forge-project-classifications
      description: Wants to understand the different Forge project classifications and what
        each means for reliability, maintenance, and support, when evaluating a
        Forge component or app, so that they can decide how much to rely on it.
    - name: Forge project support
      id: forge-project-support
      description: Wants to get support for a Forge project, by contacting
        the appropriate support channel based on the project's
        classification, so that they can resolve issues with a project they
        use.
    - name: Forge project removal
      id: forge-project-removal
      description: Wants to request removal of a Forge project through the
        project owner, following the appropriate process based on the
        project's classification, so that they can address issues with a
        project they use.
    - name: Deactivate own Forge component
      id: deactivate-forge-component
      description: Wants to request removal of their own Forge component as
        the project owner, by using Edit project details and Deactivate
        Component in the Forge UI, or contacting the Community team by
        email for urgent cases, so that the component is deactivated
        pending Community team review.
    - name: Report Forge content
      id: report-forge-content
      description: Wants to understand how to report a Forge component or
        flag a component review/comment as inappropriate, notifying the
        moderation team and component owner, in OutSystems 11, so that
        content quality, security, and development issues get addressed.
  - name: Submitting assets to Forge
    id: submitting-assets-to-forge
    subtopics:
    - name: Export Forge asset
      id: export-forge-asset
      description: Wants to export a Forge asset as an application package or module when preparing it for sharing, by using the appropriate Service Studio or Service Center export option for the asset type, so that they can upload the right file to Forge.
    - name: Document Forge asset
      id: document-forge-asset
      description: Wants to document a Forge asset before sharing it by outlining the introduction, best practices, and installation and setup guidance, so that readers know what the asset does and how to install and use it.
    - name: Submit asset to Forge
      id: submit-asset-to-forge
      description: Wants to submit a new software asset (app or library) built in their
        organization to the Forge repository from ODC Portal, meeting the
        submission prerequisites and completing the submission wizard, so that
        the asset can be reviewed and made available for other users to install.
    - name: Submit new Forge version
      id: submit-new-forge-version
      description: Wants to resubmit an updated version of an asset already
        published to Forge, using the same asset name and updating the
        description, images, release notes, license, and documentation as needed,
        so that existing installers can update to the new version.
    - name: Upload Forge asset
      id: upload-forge-asset
      description: Wants to upload a prepared OutSystems asset to Forge by
        completing the upload flow, including pre-upload checks, asset and demo
        file selection, asset details, documentation, dependencies, and
        confirmation, so that the asset is published for others to download.
  - name: Troubleshoot Forge
    id: troubleshoot-forge
    subtopics:
    - name: Forge errors
      id: forge-errors
      description: Wants to locate and resolve ODC Forge errors by finding the
        error code in the reference, understanding its cause and impact, and
        following the recommended action, so that they can fix issues affecting
        mobile or reactive web apps.
  - name: Setting up your OutSystems environment
    id: setup-outsystems-env
    subtopics:
    - name: Restart cloud services
      id: restart-cloud-services
      description: Wants to resolve OutSystems Cloud environment health
        problems caused by synchronization or service errors, by retrying
        synchronization or using Service Center's Restart Services option,
        understanding that this performs an IIS reset, clears application
        pools, restarts the core OutSystems services, and balances
        front-end restarts across multiple servers, so that transient
        Cloud environment issues are resolved without opening a support
        case.
    - name: Schedule PE upgrade
      id: schedule-pe-upgrade
      description: 'Wants to check for and schedule an automatic platform
        version upgrade of an OutSystems 11 personal environment, by using
        the Environment tab''s "Check for new platform release" link and
        the Schedule upgrade button, so that they can control when their
        personal environment is upgraded.'
    - name: Cancel scheduled PE upgrade
      id: cancel-scheduled-pe-upgrade
      description: 'Wants to cancel an already scheduled automatic platform
        version upgrade of an OutSystems 11 personal environment, by using
        the Environment tab''s Cancel upgrade button, so that they can
        reschedule it for a different time.'
    - name: Wake sleeping PE
      id: wake-sleeping-pe-o11
      description: 'Wants to regain access to an OutSystems 11 personal
        environment that automatically fell asleep due to inactivity, by
        going to outsystems.com/home, logging in, and clicking Wake up, so
        they can resume using their apps without losing data.'
    - name: Restore deleted PE
      id: restore-deleted-pe
      description: 'Wants to recover access to an OutSystems 11 personal
        environment that was deleted after prolonged inactivity by clicking
        Fetch me a new environment, then republishing their apps in the
        new environment, so that they can keep developing after the old
        environment is gone.'
    - name: OutSystems overview
      id: outsystems-overview
    - name: Install Azure trial
      id: install-azure-trial
      description: Wants to install the OutSystems 11 trial on Microsoft Azure by
        completing the Azure Portal wizard (resource group, credentials, VM size),
        starting the deployed VM, and connecting Service Studio to the new environment,
        so that they have a working OutSystems trial up and running.
    - name: Back up trial apps
      id: back-up-trial-apps
      description: Wants to back up their trial apps by creating a Personal Environment
        if needed, downloading the .osp files, uploading them to Service Center, and
        continuing development in Service Studio after the trial ends, so that they
        can keep working on the apps they built during the trial.
    - name: Missing confirmation email
      id: missing-confirmation-email
      description: 'Wants to troubleshoot not receiving the ODC shared space signup
        confirmation email by checking the spam folder and whitelisting the sender
        address, so they know when to try signing up again if it''s still missing.'
    - name: Timezone sync requirements
      id: timezone-sync-requirements
      description: Wants to understand why all components of an OutSystems 11 environment
        (database, controller, and front-ends) must share the same timezone and be
        synchronized via NTP, so that they can configure their infrastructure correctly
        and avoid unexpected behavior with time-dependent features such as timers
        and BPT.
    - name: Cloud UTC time zone
      id: cloud-utc-timezone
      description: Wants to understand that OutSystems Cloud fixes all servers
        to the UTC time zone, a setting that can't be changed, so that they can
        design applications from early development to properly handle time
        zone differences, using techniques such as the unsupported Timezone
        Forge component.
    - name: Fix .NET Core upgrade
      id: fix-net-core-upgrade
      description: Wants to fix Server.API and Server.Identity failing to initialize
        after upgrading to Platform Server 11.12.2 or later in a self-managed environment,
        caused by a missing .NET Core 3.1 Runtime & Hosting Bundle for Windows, so
        that they can verify the cause and complete the platform upgrade successfully.
    - name: Fix controller duplication
      id: fix-controller-duplication
      description: Wants to fix the Environment Health screen showing a Deployment
        Controller or Front-End server listed twice under different names or IP addresses,
        caused by a mismatch between the server's registered name and the Deployment
        Controller Hostname in Configuration Tool, so that they can resolve the duplication
        and restore an accurate environment health display.
    - name: O11 Windows Event logs
      id: o11-windows-event-logs
      description: Wants to locate the log entries generated by OutSystems 11 platform
        components by opening Windows Event Viewer and navigating to the Application
        folder, so they can inspect service errors, warnings, and informational messages
        in a self-managed environment.
    - name: O11 platform log sources
      id: o11-platform-log-sources
      description: Wants to understand the types (error, warning, information), sources
        (which OutSystems 11 services), and contents of log entries written to Windows
        Event Viewer by the OutSystems platform, so they can correctly interpret log
        entries and identify which component generated a given issue.
    - name: Fix CT symlink downtime
      id: fix-ct-symlink-downtime
      description: Wants to diagnose and recover from application downtime caused by
        running Configuration Tool from different paths via a symbolic link in Platform
        Server 11.10.0, which overwrites configuration files and deletes DLLs, so
        that they can remove affected front-end servers from the load balancer, republish
        all modules, and avoid recurrence.
    - name: Fix site property upgrade
      id: fix-site-property-upgrade
      description: 'Wants to fix the "Value cannot be null. Parameter name: key" error
        that occurs when upgrading O11 Platform Server to 11.10.2 or higher on an
        environment originally installed on version 5 or lower, caused by legacy Site
        Property IDs in non-standard format that were not migrated in version 5, by
        running SQL queries to identify and delete the outdated entries, so that they
        can complete the Service Center installation and the platform upgrade successfully.'
    - name: Fix ODP.NET conflict
      id: fix-odp-net-conflict
      description: Wants to resolve Oracle Data Provider for .NET (ODP.NET) driver errors
        — such as "Unsupported column datatype" and TransformDatabaseToRuntimeValue access
        failures — caused by a conflicting Oracle.ManagedDataAccess.dll version in the
        GAC on the platform server, by removing or replacing the DLL using OraProvCfg.exe,
        so that Configuration Tool, Service Studio, and application runtime stop throwing
        Oracle driver errors.
    - name: Fix service permissions
      id: fix-service-permissions
      description: 'Wants to fix the "Failed to set permissions for OutSystems
        services" error when installing or upgrading to Platform Server
        11.12.0 or later in a self-managed environment, caused by Active
        Directory Group Policy blocking creation of the lower-privilege
        OSControllerUser/OSSchedulerUser (or NT Service) accounts, by adding
        those accounts to the "Create symbolic links" user rights assignment
        via Group Policy Management, so that the Deployment Controller and
        Scheduler Services can run with least-privilege accounts and
        installation completes.'
    - name: ODC shared trial
      id: odc-shared-trial
      description: Wants to understand what the ODC shared space trial is, how long
        it lasts (10 days), who shares it, and what limitations it has compared to
        a paid license (no Forge submissions, no user invites, no connections, limited
        apps, no official support), so they can decide whether it meets their learning
        or exploration needs.
    - name: Shared space permissions
      id: shared-space-permissions
      description: Wants to know what permissions a developer has in an ODC shared
        space, including access to the development stage, assets, logs, configurations,
        end-user management, and Forge, so they can plan what they can build and manage
        during the trial.
    - name: PE database space
      id: pe-database-space
      description: Wants to check how much database space (system data versus application
        data) their OutSystems 11 personal environment is using via the
        Environments tab's "View details" link, understanding that usage figures
        refresh periodically and knowing to open a Support Case with OutSystems
        if usage reaches the platform's storage limit, so that they can monitor
        space and get help unblocking the environment before it becomes
        unavailable.
    - name: PE 503 recovery
      id: pe-503-resource-limit
      description: 'Wants to diagnose an HTTP 503 "Service Unavailable" error
        appearing when accessing apps, using 1-Click Publish in Service
        Studio, or viewing Environment Health in Service Center/LifeTime,
        caused by OutSystems 11 automatically disabling their Personal
        Environment after exceeding memory, CPU, or resource limits, by
        freeing up resources via the Free Up Resources page or contacting
        Support if the problem persists, so that they can restore access to
        their Personal Environment.'
    - name: PE expiration policy
      id: pe-expiration-policy
      description: Wants to understand that the OutSystems Personal Edition is free
        indefinitely while actively used, stays active for a default period that
        renews with continued activity, enters expiration after prolonged
        inactivity with an advance warning email, and can be recreated afterward
        (with OML export/import as the only way to preserve apps), so they know
        how to avoid losing work and what to do if their PE expires.
    - name: PE resource limits
      id: pe-resource-limits
      description: Wants to know the concrete resource limits of the OutSystems Personal
        Edition compared to a commercial ODC tenant — including the number of
        development stages, API clients, users, and active components
        (apps/workflows/AI agents/timers) allowed, the database storage cap, and
        the daily execution/log caps — so they can plan what they can
        realistically build and run in a PE.
    - name: PE infrastructure limits
      id: pe-infrastructure-limits
      description: Wants to understand that an OutSystems 11 Personal Environment runs on
        shared infrastructure with limited CPU, memory, IO, and database
        storage, reduced caching and collaboration capabilities, and app-version
        retention limits, so they know the hosting constraints.
    - name: PE hibernation basics
      id: pe-hibernation-basics
      description: Wants to understand what PE hibernation is, when it triggers after a
        period of development inactivity, what activity prevents it, and its
        impact on published apps and cost, in OutSystems Personal Edition, so
        that they know what to expect from the sleep state and don't mistake it
        for data loss or a paid feature.
    - name: Wake up hibernated PE
      id: wake-hibernated-pe-odc
      description: Wants to reactivate a hibernated OutSystems Personal
        Edition, either manually from the ODC Portal's Manage Organization
        console or automatically by performing a developer action that
        requires the environment to be online, and know how long
        reactivation takes, so that they can resume development or restore
        access to published apps.
    - name: PE hibernation vs deletion
      id: pe-hibernation-vs-deletion
      description: Wants to understand the difference between a hibernated
        Personal Edition (asleep but fully preserved) and a deleted one
        (removed after extended inactivity per the PE lifecycle), so that
        they know hibernation doesn't mean data loss and can anticipate when
        their PE might eventually be deleted.
    - name: O11 error tracking
      id: o11-error-tracking-setup
      description: Wants to obtain error information in a self-managed O11 installation
        by using Service Center's Monitoring area, querying the OutSystems Log Database
        directly, and reading Windows Event Viewer alerts, so that they can track,
        monitor, and receive alerts about errors in the platform.
    - name: O11 perf counters
      id: o11-perf-counters
      description: Wants to use OutSystems Platform Server's Windows Performance Counters
        to monitor asynchronous job execution, application error rates, screen access,
        and web service access in a self-managed O11 environment, so that they can
        track platform runtime performance using Windows Performance Monitor.
    - name: O11 service error recovery
      id: o11-service-error-recovery
      description: Wants to diagnose and recover from errors in OutSystems 11 platform
        services — including Deployment Controller, Deployment Service, Scheduler
        Service, Log Service, IIS, and Message Queue — by reading error messages,
        checking Service Center Monitoring, and restarting services in a self-managed
        environment, so that they can restore normal platform operation.
    - name: Service Center analytics
      id: service-center-analytics
      description: Wants to use Service Center's built-in analytics reports to review
        application usage and diagnose issues for a specific date interval, so
        they can decide whether it meets their learning or exploration needs.
    - name: Monitoring area screens
      id: monitoring-area-screens
      description: Wants to understand which screen to check first in Service Center's
        Monitoring area for a given kind of issue, so they can locate the right
        diagnostic information without searching through every screen.
    - name: Native app shell logs
      id: native-app-shell-logs
      description: Wants to understand how native app shell logs appear in the Errors and
        General screens of Service Center's Monitoring area, including the
        device information fields they contain, so they can diagnose errors
        occurring on end users' mobile devices.
    - name: O11 platform services
      id: o11-platform-services
      description: Wants to understand the role of each OutSystems 11 platform service
        shown in the Environment Health screen, so they can interpret
        environment health status and know which service is responsible for
        which platform function.
    - name: Code generator service
      id: code-generator-service
      description: Wants to understand how the Code Generator Service receives XML from
        Service Studio and binary code from Integration Studio, compiles deployable
        code, generates SQL, backend logic, and interface assets, validates external
        dependencies, and runs database upgrade scripts before deployment, so they
        can trace what the service does during 1-Click Publish.
    - name: Deployment services
      id: deployment-services
      description: Wants to understand how the Deployment Controller Service coordinates
        compilation and deployment across front-end Deployment Services, retries
        failed deployments, uses the Cache Invalidation Service to notify front-end
        servers through publish/subscribe messaging, and monitors the process in
        Service Center, so they can follow how applications reach the application
        servers.
    - name: Configure HTTP proxy
      id: configure-http-proxy
      description: Wants to configure a HTTP proxy server so Service Studio
        can reach the Internet on a restricted network, by setting the proxy
        in the operating system (Windows or macOS) and, if the proxy
        requires it, enabling proxy authentication in Service Studio's
        Preferences dialog, so that Service Studio can connect to their
        OutSystems 11 environment.
    - name: Fix proxy auth 401
      id: fix-proxy-auth-401
      description: Wants to fix the 401 Unauthorized error (or missing app templates)
        when creating a new app in Service Studio, caused by a proxy between the
        developer's computer and Platform Server tampering with authorization tokens,
        by asking IT to confirm the proxy doesn't manipulate authentication headers,
        so that Service Studio can retrieve app templates in a self-managed OutSystems
        11 environment.
    - name: Fix IIS Basic Auth 401
      id: fix-iis-basic-auth-401
      description: Wants to fix the 401 Unauthorized error (or missing app templates)
        when creating a new app in Service Studio, caused by Basic Authentication
        being enabled on the module at the IIS level instead of the required Anonymous
        Authentication, by enabling Anonymous Authentication in IIS, so that Platform
        Server can correctly authenticate requests in a self-managed OutSystems 11
        environment.
    - name: O11 log table separation and retention
      id: o11-log-table-separation-retention
      description: Wants to understand how OutSystems 11 separates logs by type
        into dedicated database tables and rotates records through retention
        policies, so that they can inspect the correct log table and interpret
        why older records are no longer available.
    - name: O11 asynchronous log writing
      id: o11-async-log-writing
      description: Wants to understand how OutSystems 11 applications write log
        entries asynchronously through an internal logging API with bulk writes,
        so that they can reason about logging behavior without assuming user
        requests block on log persistence.
    - name: O11 separate log database
      id: o11-separate-log-database
      description: Wants to understand how OutSystems 11 log data can be stored
        in a separate database from application data, so that they can plan
        capacity and reduce logging impact on transactional workloads.
    - name: Deployment Controller vs Service Center log access
      id: o11-deployment-controller-vs-service-center-log-access
      description: Wants to understand why the Deployment Controller Service
        writes directly to log tables while Service Center reads through database
        views, so that they can troubleshoot with the correct data path and avoid
        misinterpreting each component's role.
    - name: View log entities Service Studio
      id: view-log-entities-ss
      description: Wants to consult OutSystems 11 log data directly in Service Studio
        by referencing the PlatformLogs Extension in the Manage Dependencies window
        and loading data from the Entities under Data > Entities > PlatformLogs, so
        that they can inspect log records without querying the database manually.
    - name: Top-level vs drill logs
      id: top-level-vs-drill-logs
      description: Wants to distinguish top-level logs (Screen, exposed Integration,
        Mobile Request, Cyclic Job) from drill logs (Error, General, consumed Integration,
        Extension) in the OutSystems 11 log database, and how they're linked via
        the request_key field, so that they can correctly interpret how a drill
        log relates to the top-level access request that generated it.
    - name: Remove Log Service
      id: remove-log-service
      description: Wants to remove the OutSystems Log Service and its Microsoft
        Message Queues from a self-managed OutSystems 11 environment after upgrading
        every module of every application to OutSystems 11, by running a PowerShell
        cleanup script and closing the associated firewall port, so that they can
        free up server memory no longer needed for pre-OutSystems 11 logging.
    - name: Configure environment purpose
      id: configure-environment-purpose
      description: Wants to set the Purpose of an OutSystems 11 environment in Service
        Center's Environment Configuration, understanding when it can be
        changed, its license restrictions, and how it affects available platform
        features, so that they configure the environment's behavior correctly
        for its role.
    - name: Configure debug mode
      id: configure-debug-mode
      description: Wants to enable or disable the Debug Mode of an OutSystems 11
        environment in Service Center's Environment Configuration, understanding
        that both environment and module Debug Mode must be enabled to debug a
        module in Service Studio and that changes take effect after
        republishing, so that they can control which modules can be debugged.
    - name: Configure application settings
      id: configure-application-settings
      description: Wants to configure application settings per environment in Service
        Center after deployment, so that each environment uses the correct
        settings without republishing or downtime.
    - name: Apply runtime settings
      id: apply-runtime-settings
      description: Wants to apply runtime settings in Service Center for
        applications, modules, extensions, or the whole factory by saving
        changes, applying them to the factory or a selected solution, and
        handling the resulting reload and pending-change warnings, so that
        configuration changes take effect without republishing code.
    - name: Republish compile-time changes
      id: republish-compile-time-changes
      description: Wants to republish modules after changing compile-time
        settings in Service Center or Service Studio, so that the updated
        configuration values are used when the modules are compiled.
    - name: O11 service log levels
      id: o11-increase-service-log-levels
      description: Wants to increase the logging verbosity of a self-managed OutSystems 11
        platform service on the .NET stack by editing the service's `.config`
        file or on the Java stack by editing the service's `.properties` file,
        and restart the service, so that they can collect the detailed logs
        Support needs for troubleshooting.
    - name: Increase O11 app logs
      id: o11-increase-app-log-levels
      description: Wants to increase the logging verbosity for a specific OutSystems 11
        application at runtime, using the Factory Configuration tool's OSTrace
        sample on the .NET stack or manually editing the application server's
        configuration on the Java stack, so that they can capture detailed
        traces for troubleshooting or Support.
- name: Hotfixing
  guid: d8111f11-34b0-4f33-8374-5d9611d507e2
  topics:
  - name: Solve issues identified in Production
    id: fix-issues-production
    subtopics:
    - name: Hotfix in QA
      id: hotfix-in-qa
    - name: Hotfixing strategies in OutSystems
      id: hotfix-strategy-os
    - name: Deploy Hotfix to Production
      id: deploy-hotfix-production
  - name: Solve issues directly in Production
    id: fix-production-issues
    subtopics:
    - name: Hotfix directly in Production
      id: hotfix-production
  - name: Propagate hotfix to other environments
    id: hotfix-propagation
    subtopics:
    - name: Portback hotfix
      id: portback-hotfix
    - name: Preparing hotfix portback
      id: preparing-hotfix-portback
  - name: Advanced hotfixing strategies
    id: hotfix-strategy-advanced
    subtopics:
    - name: Hotfix from development with feature toggles
      id: hotfix-dev-feature-toggle
  - name: Hotfixing with a CI/CD pipeline
    id: hotfix-ci-cd
    subtopics:
    - name: Hotfixing from dev with CI/CD pipeline
      id: hotfix-dev-ci-cd
- name: Integrate AI
  guid: a7056193-1048-45b9-ba65-37023127caab
  topics:
  - name: Chatbots
    id: chatbots
    subtopics:
    - name: Configure chatbot wizard
      id: configure-chatbot-wizard
      description: Wants to access Chatbot Configurator, sign in with an OutSystems
        account, and set up the AI provider connection for an OutSystems
        environment, so that they can manage the chatbot wizard.
    - name: Add QnA chatbots
      id: add-qna-chatbots
      description: Wants to add QnA chatbots in Chatbot Configurator after setting
        up the AI provider connection, so that the chatbot can answer questions in
        their OutSystems app.
    - name: Create chatbot webhook module
      id: create-chatbot-webhook-module
      description: Wants to create a chatbot webhook module from the Chatbot Webhook
        app template in Service Studio and publish it, so that they can add custom
        chatbot logic and richer responses.
    - name: Configure chatbot webhook module
      id: configure-chatbot-webhook-module
      description: Wants to configure a chatbot webhook module in Service Center
        and Azure by setting MicrosoftClientId and MicrosoftClientSecret, copying
        the Request URL, and updating the bot messaging endpoint, so that Azure
        sends chatbot messages to the module.
    - name: Chatbot block settings
      id: chatbot-block-settings
      description: Wants to identify the chatbot block properties and what each one
        configures, so they can configure the chatbot component.
    - name: Compare chatbot versions
      id: compare-chatbot-versions
      description: Wants to choose between the Reactive Web App and Traditional
        Web App chatbot components, so they can use the compatible version for
        their app type and account for the documented differences.
  - name: Azure AI resources
    id: azure-ai-resources
    description: Wants to know which Azure resources each OutSystems AI component
      and connector requires, so they can provision the right Azure services
      before configuring the integration.
  - name: Document Conversion
    id: document-conversion
    subtopics:
    - name: Convert file to Markdown
      id: convert-file-to-markdown
      description: Wants to convert a binary file stored or received in their app to
        Markdown using the OmniDoc2MD Forge component's
        ConvertDocumentToMarkdown action, including installing the component and
        configuring its parameters, so that document content is normalized for
        downstream processing.
    - name: Use Markdown in agent
      id: use-markdown-in-agent
      description: Wants to pass Markdown converted from a document into an AI
        agent's context in Agent Workbench, by assigning it inside the GetGroundingData
        or BuildMessages hooks, so that the agent's grounding data or prompt includes
        the document's normalized text content.
    - name: Add images to message
      id: add-images-to-message
      description: Wants to attach images extracted from a converted document, using
        OmniDoc2MD's extractedImagesMetadata and imagesZipFile outputs with LoadZIP
        and GetFiles, to the same user message that carries the Markdown, so that
        a multimodal AI model receives both text and image content together.
    - name: Attach docs to message
      id: attach-docs-to-message
      description: Wants to attach multiple binary documents (PDFs, images)
        directly as content items in an agent's user message, using a
        ForEach loop with ListAppend nodes to add a text label and an
        ImageBinary content item per document, so that a multimodal AI
        model can analyze several documents in a single call without prior
        conversion to Markdown.
  - name: Sentiment analysis
    id: sentiment-analisys
    subtopics:
    - name: Sentiment analysis
      id: sentiment-analysis
  - name: Machine learning
    id: machine-learning
  - name: Agentic development overview
    id: agentic-development-overview
    description: Wants to understand what agentic development is, how it uses
      AI capabilities (Mentor Web and Mentor Studio) to build and evolve apps
      through a describe-review-refine conversational workflow, and how AI
      agents, the OutSystems Model, and the compiler work together under the
      hood, so that they can decide whether it fits their use case.
  - name: Agentic dev vs AI apps
    id: agentic-vs-ai-powered-apps
    description: Wants to distinguish agentic development (AI helping the
      developer build apps via Mentor Web and Mentor Studio) from AI-powered
      apps (apps that call AI models at runtime via Agent Workbench and AI
      Agent Builder to serve end users), so that they choose the right course
      and tools for their goal.
  - name: Choose Mentor tool
    id: choose-mentor-tool
    description: Wants to decide whether to use Mentor Web or Mentor Studio
      based on app type (new web app vs existing apps, libraries, or agentic
      apps), complexity, and access point (ODC Portal vs ODC Studio), so that
      they pick the right tool for their development task.
  - name: What is an AI agent
    id: what-is-an-ai-agent
    description: Wants to understand what an AI agent is and what makes it
      unique (fully autonomous, able to make decisions and act without a new
      prompt for each task), including its five core components, AI model,
      actions, grounding data, system prompts, and memory (state
      persistence), so that they have a foundational understanding of AI
      agents before building or configuring one.
  - name: Agentic Apps
    id: agentic-apps
    subtopics:
    - name: Consume AI agent
      id: consume-ai-agent
      description: Wants to understand what a consumer app is and how to integrate it with an AI agent by passing UserInput and SessionId parameters, so that the app can send user requests to the agent and maintain conversation context across interactions.
    - name: Consumer app workflow
      id: consumer-app-workflow
      description: Wants to implement the end-to-end workflow for calling an AI agent from a consumer app — including session setup, capturing user input, invoking the agent's service action, handling the response, and persisting the session — so that the app delivers a coherent, context-aware conversational experience.
    - name: Debug agentic apps
      id: debug-agentic-apps
      description: Wants to debug an end-to-end flow that calls an AI agent by running separate debugging sessions for the consumer app and the agentic app in ODC Studio, so that they can inspect inputs, outputs, and errors across both apps.
    - name: Test agent messages
      id: test-agent-messages
      description: Wants to test System and User messages for an AI agent in ODC Studio's Test area before publishing, so that they can verify message effectiveness and avoid committing to an incorrect configuration.
    - name: Test agentic app
      id: test-agentic-app
      description: Wants to validate an agentic app's behavior after publishing by launching the auto-generated test app or an existing consumer app from ODC Studio, selecting which public service action to test, and iterating on changes in the browser, so that the app's logic and responses can be confirmed before deploying to production.
    - name: Test app prerequisites
      id: test-app-prerequisites
      description: Wants to know which service action parameters are required — name, type, and data type — for ODC Studio to successfully generate a test app for an agentic app, so that the setup is correct before clicking Test agent.
    - name: Data source contract
      id: data-source-contract
      description: Wants to implement a custom data source REST API following the AI
        Agent Builder contract, including HTTP parameters, input and output schemas,
        request/response formats, and error codes, so that a custom retrieval service
        can be integrated with AI Agent Builder search.
    - name: Add custom data source
      id: add-custom-data-source
      description: Wants to add a custom data source in the AI Agent Builder app by
        opening Configurations, selecting Custom connection, entering the source
        details, adding an authentication header, and saving the configuration, so
        that the app can connect to a custom database or search service.
    - name: Add Amazon Kendra data source
      id: add-amazon-kendra-data-source
      description: Wants to add an Amazon Kendra data source in the AI Agent Builder
        app by opening Configurations, selecting Amazon Kendra, entering the source
        details, and saving the connection, so that the app can use Kendra as a
        search data source.
    - name: Configure CallAgent
      id: configure-callagent
      description: Wants to add the CallAgent service action to their app in ODC
        Studio and configure its AgentId, ChatMessages, and TokenCount parameters,
        so the app can send requests to an AI agent and receive its response.
    - name: Agent Builder overview
      id: ai-agent-builder-overview
      description: Wants to understand what the AI Agent Builder is, its core
        terminology (agent, agent template, AI model, data source, instructions),
        and how information flows between models, data sources, and agents, so
        they can decide whether it fits their use case before building an agent.
    - name: Agent Builder access
      id: ai-agent-builder-access
      description: Wants to understand the AI Agent Builder homepage and the
        access it provides to sample apps, the playground, model and data source
        configurations, and components, so they can navigate the tool and find
        the right entry point for their task.
    - name: Agent Builder analytics
      id: agent-builder-analytics
      description: Wants to review AI Agent Builder analytics for agents, AI
        models, and data sources, export usage data, and use logs and traces
        to troubleshoot latency or request errors, so they can understand
        load patterns when tuning an agent.
    - name: Agent sample apps
      id: ai-agent-builder-sample-apps
      description: Wants to understand the use cases, solutions, and customization options
        of the AI Agent Builder's sample apps, so they can pick a starting point
        matching their business scenario.
    - name: Create agentic app
      id: create-agentic-app
      description: Wants to create a new agentic app in ODC Studio by clicking
        Create, selecting Agentic App, naming it, and selecting a previously
        configured AI model, so that they have a scaffolded AgentFlow
        template ready to customize.
    - name: Build agent flow
      id: build-agent-flow
      description: Wants to create an AI agent in ODC Studio by configuring an
        AI model, defining grounding data and system messages, adding actions,
        and building the AgentFlow server action, so that the agent can
        autonomously orchestrate responses in an agentic app.
    - name: AgentFlow logic overview
      id: agentflow-logic-overview
      description: Wants to understand the built-in AgentFlow server action
        structure in Agent Workbench (GetGroundingData, BuildMessages, Call
        Agent, StoreMemory) and what each step does by default in an agentic
        app template, so that they know the out-of-the-box logic before
        customizing any component.
    - name: Combine grounding sources
      id: combine-grounding-sources
      description: Wants to combine data from multiple entities into an AI
        agent's grounding data by serializing each entity's aggregate to
        JSON with separate JSON Serialize actions and combining the results
        in GetGroundingData, so that the agent can compare more than one
        data source (for example, applicant data and policy rules) in a
        single grounding context.
    - name: Add additional agents
      id: add-additional-agents
      description: Wants to add more than one agent to an existing agentic app
        in ODC Studio and wire each new agent into AgentFlow logic via the
        Toolbox's Call Agent element or the Automations tab, so that the app
        can serve multiple specialized purposes (for example, planning versus
        fast execution).
    - name: Configure action calling
      id: configure-action-calling
      description: Wants to expose server actions to an AI agent in ODC by
        writing clear action and parameter descriptions and marking parameters
        as Generated by AI, so that the agent can autonomously select and
        invoke the right action with correct inputs.
    - name: Set agent call condition
      id: set-call-condition
      description: Wants to define a call condition expression using ODC's internal
        variables that track resource usage and iteration counts, so that an AI
        agent stops calling actions after reaching a defined limit and avoids
        infinite loops or excessive resource consumption.
    - name: Action calling overview
      id: action-calling-overview
      description: Wants to understand how an AI agent decides which server
        actions to call within an Agent Flow, based on the user's prompt, the
        system prompt, and action metadata, so they can design actions that
        fit into the agent's autonomous action-calling process.
    - name: Add structured output
      id: add-structured-output
      description: Wants to add a structured output to a Call Agent action in
        ODC Studio by selecting an existing structure or creating a new one in
        the Structured output tab and setting the Call Agent's Response
        parameter to that structure type, so that the agent returns a
        predefined structured response instead of free text.
    - name: Store structured output
      id: store-structured-output
      description: Wants to adapt an AgentFlow to work with a structured
        output by updating the Response output parameter to the structure
        type and serializing the structured data before storing it in the
        Store Memory node, so that the structured response can be persisted
        and later deserialized on the consumer side.
    - name: Act on agent decision
      id: act-on-agent-decision
      description: Wants to convert an AI agent's structured text decision
        (for example "Approved" or "Rejected") into a business status by
        matching it with Index() and using the result to call a service
        action that persists the decision, so that the agent's
        recommendation drives downstream business logic instead of
        remaining free text.
    - name: Agent evaluations overview
      id: agent-evaluations-overview
      description: Wants to understand what agent evaluations are and how
        a run works end-to-end, including the black box methodology
        (evaluating a service action as the executable unit) and the flow
        from dataset through service action, execution trace, and platform
        judge to evaluation results, so they can decide when to use
        evaluations and interpret how results are produced.
    - name: Evaluation data retention
      id: evaluation-data-retention
      description: Wants to understand how long ODC retains agent evaluation data,
        distinguishing evaluation summaries (kept indefinitely) from evaluation
        details like per-test-case results and judge rationale (retained for a
        limited period before being automatically removed), so they know how
        long granular results stay available for review.
    - name: Create evaluation dataset
      id: create-evaluation-dataset
      description: Wants to create and upload a dataset for an agentic app
        evaluation in the ODC Portal, pairing it with the agentic app and
        service action being evaluated, downloading the JSON template, and
        uploading test case JSON matching the service action's inputs,
        expected outputs, and expected tool calls, so that the service
        action can be benchmarked with structured test cases.
    - name: Design dataset test cases
      id: design-dataset-test-cases
      description: Wants to design effective test cases for an agent evaluation dataset,
        covering real user scenarios, one intent per row, stable expected
        outputs, and data dependencies, and structure them as JSON that pairs
        inputs with expected responses, guidance notes, and expected tool calls,
        matching the service action contract, so that evaluation results give
        clear, reliable regression signal.
    - name: Edit evaluation dataset test cases
      id: edit-evaluation-dataset-test-cases
      description: Wants to edit test cases in a saved agent evaluation
        dataset in the ODC Portal, so that the dataset stays accurate as
        prompts, expected responses, or tools change.
    - name: Export evaluation dataset JSON
      id: export-evaluation-dataset-json
      description: Wants to export a saved agent evaluation dataset as JSON
        from the ODC Portal for backup or version control, so that they can
        track and restore dataset changes over time.
    - name: Configure evaluation setup and teardown actions
      id: configure-evaluation-setup-teardown-actions
      description: Wants to configure setup and teardown service actions for
        an agent evaluation in the ODC Portal, so that each run starts from
        a known state and cleans up after execution.
    - name: Run agent evaluation
      id: run-agent-evaluation
      description: Wants to create and save an agent evaluation in the ODC
        Portal by selecting a dataset and judge criteria, then start a run
        against the linked service action, so that test cases are scored
        and results become available for review.
    - name: Cancel evaluation run
      id: cancel-evaluation-run
      description: Wants to cancel an in-progress agent evaluation run in
        the ODC Portal, understanding that setup is aborted, in-progress
        test cases stop, teardown still executes, and no scores are
        returned, so that they can stop a run started with the wrong
        dataset or one taking too long.
    - name: Review run results
      id: review-run-results
      description: Wants to review agent evaluation results in the ODC
        Portal by checking run statuses, overall and judge criteria scores,
        dataset versions across runs, and failure details for setup or
        teardown stages, so that they can interpret run outcomes and know
        where to investigate failures.
    - name: Understand A2A protocol
      id: understand-a2a-protocol
      description: Wants to understand what agent-to-agent (A2A) communication is in ODC,
        including its benefits and the roles and architectural building blocks
        involved, so that they can decide whether it fits their agentic app
        design.
    - name: A2A communication flow
      id: a2a-communication-flow
      description: Wants to understand the phases of an A2A communication
        flow, discovery, authentication, and communication, between a
        client agent and a remote agent, so that they can implement or
        troubleshoot the integration correctly.
    - name: A2A vs MCP
      id: a2a-vs-mcp
      description: Wants to understand the difference between the MCP and
        A2A protocols and how they complement each other, so that they can
        decide which protocol to use for a specific agent integration
        scenario.
    - name: Raise agent timeout
      id: raise-agent-timeout
      description: Wants to fix AI agent call timeouts in ODC by raising the Server Request
        Timeout property to its maximum allowed value at the app level or on
        individual Call Agent actions, so that longer AI Model responses
        complete before the request times out.
    - name: Async AI agent calls
      id: async-agent-calls
      description: Wants to implement asynchronous processing for AI agent calls that
        exceed the platform's request timeout ceiling, by building client
        actions, queue and request entities, an event-triggered server action to
        call the agent, a polling action to check status, and a timer to clean
        up completed queue entries, so that long-running AI Model responses
        don't block the calling app.
    - name: Agent conversation memory
      id: agent-conversation-memory
      description: Wants to configure an AI agent's BuildMessages logic to
        include prior conversation turns and grounding data in each system
        message, so that the agent maintains contextual memory across
        multiple user prompts in a conversational agentic app.
    - name: Agent monitoring overview
      id: agent-monitoring-overview
      description: Wants to understand what agent monitoring in ODC shows, including its
        analytics groups, execution traces, and data layers, so they can decide
        where to look for visibility into an agent's behavior.
    - name: Agent monitoring access
      id: agent-monitoring-access
      description: Wants to know which permissions grant access to agent
        monitoring and its detailed execution traces in the ODC Portal, so
        they can confirm they can view an agent's logs and traces, given
        that detailed execution traces additionally require a separate
        per-agent toggle.
    - name: Answer relevance score
      id: answer-relevance-score
      description: Wants to understand what the answer relevance judge
        measures on an agent's CallAgent execution traces and what a low
        score indicates, so they can investigate whether a prompt or model
        change caused the agent to drift toward generic or off-topic
        responses.
    - name: Safety score
      id: safety-score
      description: Wants to understand what the safety judge measures on an
        agent's CallAgent execution traces, distinguishing unsafe input
        (jailbreak or harmful prompts) from unsafe output, and what a low
        score indicates, so they can determine whether the issue is on the
        input or output side and check it against their Guardrails policy.
    - name: Limit response length
      id: limit-response-length
      description: Wants to prevent an AI agent's free-text response from
        exceeding a downstream database column's character limit, by
        enforcing an explicit length rule in the SystemPrompt and adding a
        truncation fallback (e.g., Substr) before saving the response, so
        that the app avoids DB string-truncation errors and constraint
        violations.
  - name: Tools and connectors
    id: tools-and-connectors
    subtopics:
    - name: Choose tool source
      id: choose-tool-source
      description: Wants to decide whether to import external tools into an
        ODC app via a custom MCP server or a prebuilt connector,
        understanding that both produce callable server actions, so that
        they can choose based on portability and reuse versus faster
        guided setup.
    - name: Set up prebuilt connector
      id: set-up-prebuilt-connector
      description: Wants to create a connection to an external service using
        a prebuilt connector in ODC Portal, providing authentication and
        importing selected tools as server actions, so that the tools
        become available for use in apps and agents.
    - name: Set up MCP connection
      id: set-up-mcp-connection
      description: Wants to configure a connection to a custom MCP server in ODC Portal by
        selecting the matching transport type, choosing an authentication
        method, and providing the required credentials, then testing the
        connection, so that the server's tools become available for import.
    - name: Import MCP tools
      id: import-mcp-tools
      description: Wants to select and optionally rename specific tools
        from a tested MCP connection in ODC Portal so they import as
        server actions available in ODC Studio, understanding that ODC
        validates each tool's JSON schema and imports only supported input
        types during that process.
    - name: Use connector tools in agent
      id: use-connector-tools-in-agent
      description: Wants to add imported prebuilt-connector tools as
        dependencies in ODC Studio and invoke them in server logic or an
        agent's Call Agent element, mapping required inputs and handling
        common errors (invalid key, empty results, rate limit), so that
        external service functionality can be used within apps and agentic
        flows.
    - name: Use MCP tools in agent
      id: use-mcp-tools-in-agent
      description: Wants to add imported MCP tool server actions as
        dependencies in ODC Studio and use them as tools in an agent's
        Call Agent element, configuring parameters and mapping values, so
        that MCP capabilities can be invoked within an agentic app.
    - name: MCP unavailable patterns
      id: mcp-unavailable-patterns
      description: Wants to identify which JSON schema input patterns
        (multi-type anyOf/oneOf/allOf, mixed-type arrays, arrays without an
        items schema, arrays of arrays, external $refs, or objects without
        typed properties) make an entire MCP server action unavailable
        when ODC imports it, so they can diagnose why a tool didn't
        import.
    - name: MCP omitted fields
      id: mcp-omitted-fields
      description: Wants to understand which input schema patterns
        (recursive $ref, or $dynamicRef/$recursiveRef) cause ODC to omit a
        specific parameter or field, rather than block the entire action,
        when importing an MCP server action, so they can predict what will
        be missing from the action's signature.
    - name: Fix unsupported MCP tool
      id: fix-unsupported-mcp-tool
      description: Wants to make an unavailable MCP action importable into
        ODC by either simplifying the original tool's data structures on
        the external MCP server or creating a wrapper tool with a
        simplified input set, so the action becomes usable in an ODC app
        or agent.
  - name: Semantic Search
    id: semantic-search
    subtopics:
    - name: Implement semantic search
      id: implement-semantic-search
      description: Wants to implement semantic search in ODC by marking entity attributes as searchable and adding a Semantic Search element to a server action flow, so that the app can retrieve results ranked by meaning rather than exact keywords.
  - name: Troubleshoot Context Service
    id: troubleshoot-context-service
    subtopics:
    - name: Context Service errors
      id: context-service-errors
      description: Wants to locate and resolve ODC Context Service errors (OS-CTXS)
        by finding the error code in the reference, understanding whether it's
        caller-side or server-side, and following the recommended action, so
        that they can resolve failures affecting agent harness requests to the
        Enterprise Context Graph.
  - name: Troubleshoot Mentor Studio
    id: troubleshoot-mentor-studio
    subtopics:
    - name: Mentor Studio errors
      id: mentor-studio-errors
      description: Wants to locate and resolve Mentor Studio errors by finding
        the error code in the reference, understanding its cause and impact,
        and following the recommended action, so that they can continue
        working with Mentor Studio's conversational AI development flow in
        ODC Studio.
  - name: Troubleshoot Mentor Web
    id: troubleshoot-mentor-web
    subtopics:
    - name: Mentor Web errors
      id: mentor-web-errors
      description: Wants to locate and resolve Mentor Web errors by finding
        the error code in the reference, understanding whether it's a
        generation/refinement error, an AI Provider error, or a platform
        error, and following the recommended action, so that they can
        continue using Mentor Web to generate and refine apps in ODC
        Portal.
  - name: Mentor Web app generation
    id: mentor-web-app-generation
    subtopics:
    - name: Mentor Web workflow
      id: mentor-web-workflow
      description: Wants to understand the five phases of the Mentor Web
        workflow (input, blueprint, generate, refine, deploy or continue
        in ODC Studio) and the validation checkpoint at each phase, so
        that they know what to expect end-to-end before building an app
        with Mentor Web.
    - name: Generate app from document
      id: generate-app-from-document
      description: Wants to generate an ODC app in Mentor Web by uploading
        a requirement document and reviewing the resulting blueprint's
        entities, roles, screens, and stateflows before generating, so
        that misinterpretations are caught early and the generated app
        matches the requirements.
    - name: Deploy or continue
      id: mentor-web-deploy-or-continue
      description: Wants to decide whether to deploy a refined Mentor Web app to test or
        production through Portal, or continue building it in ODC Studio, based
        on which capabilities require ODC Studio, so that they choose the right
        next step after the refine phase.
    - name: Review MW blueprint
      id: review-mentor-web-blueprint
      description: Wants to review and edit the Mentor Web blueprint (entities,
        roles, screens, and stateflows) through conversational prompts before
        generation, including switching entities between local and referenced
        and setting the app theme, so that AI misinterpretations are caught
        and corrected before the app is generated.
    - name: Generate app from prompt
      id: generate-app-from-prompt
      description: Wants to generate an ODC app in Mentor Web by entering a
        natural-language prompt describing the entity, screens, and access
        rules, and reviewing the resulting blueprint before generating, so
        that the described app is produced in a single pass.
  - name: Mentor Web prompts
    id: mentor-web-prompts
    subtopics:
    - name: UI pattern prompts
      id: ui-pattern-prompts
      description: Wants to select and phrase prompts for Mentor Web to generate the right
        UI pattern for a given dataset shape, using prompt progressions from
        basic to detailed and respecting each pattern's entity and attribute
        constraints, so that the generated screen matches the intended layout
        and data density.
    - name: Data model prompts
      id: mentor-web-data-model-prompts
      description: Wants to write prompts for Mentor Web to add or delete
        entities, attributes, and static entity records, so that data model
        changes are generated correctly during the blueprint phase.
    - name: Authorization prompts
      id: mentor-web-authorization-prompts
      description: Wants to write prompts for Mentor Web to manage roles and
        permissions, so that authorization changes are generated correctly
        during the blueprint phase.
    - name: Theme prompts
      id: theme-prompts
      description: Wants to apply, change, or request a theme, including a
        dark theme via explicit request or primary color choice, through
        prompts or requirement documents in Mentor Web, meeting theme
        compatibility requirements, so that the generated app uses the
        intended visual theme.
    - name: Extend generated app
      id: extend-generated-app
      description: Wants to extend an already-generated Mentor Web app in
        the editor by writing follow-up prompts to add data model elements
        or modify screens (for example, a dashboard), iterating with
        additional prompts when results don't match expectations, so that
        the app meets requirements discovered after the initial generation.
  - name: Mentor Studio app modification
    id: mentor-studio-app-modification
    subtopics:
    - name: Modify app workflow
      id: mentor-studio-modify-workflow
      description: Wants to access Mentor Studio in ODC Studio and follow
        its describe-review-iterate workflow, including reviewing Proposed
        changes and viewing a summary of what changed, so that they can
        modify an app through conversation and refine the result until it
        meets requirements.
    - name: Generation scope
      id: mentor-studio-generation-scope
      description: Wants to know which elements (logic, UI, data, timers,
        events, code analysis) Mentor Studio can generate or modify for the
        open asset type in ODC Studio, since web apps, libraries, and
        agentic apps each support a different subset, so that they
        understand what changes are possible before describing a request.
    - name: Mentor Studio constraints
      id: mentor-studio-constraints
      description: Wants to know that Mentor Studio conversations aren't
        persistent and that agentic development capabilities evolve, so
        they save work frequently and check documentation for current
        constraints before relying on an open Mentor Studio session.
  - name: Mentor Studio prompts
    id: mentor-studio-prompts
    subtopics:
    - name: Logic prompts
      id: mentor-studio-logic-prompts
      description: Wants to write prompts for Mentor Studio to create or
        modify server actions, client actions, service actions, and
        aggregates, using a basic-to-detailed prompt progression, so that
        ODC Studio artifacts are generated or modified correctly.
    - name: UI prompts
      id: mentor-studio-ui-prompts
      description: Wants to write prompts for Mentor Studio to create or
        modify screens, web blocks, and layouts, using a basic-to-detailed
        prompt progression, so that ODC Studio artifacts are generated or
        modified correctly.
    - name: Data model prompts
      id: mentor-studio-data-model-prompts
      description: Wants to write prompts for Mentor Studio to create or
        modify entities, attributes, and relationships, using a
        basic-to-detailed prompt progression, so that ODC Studio artifacts
        are generated or modified correctly.
    - name: TrueChange error and warning prompts
      id: mentor-studio-truechange-prompts
      description: Wants to write prompts for Mentor Studio to resolve
        TrueChange errors and warnings, so that compile issues are
        diagnosed and corrected without manual debugging.
    - name: Behavior and bug fix prompts
      id: mentor-studio-behavior-bug-fix-prompts
      description: Wants to write prompts for Mentor Studio to identify and
        fix unexpected app behavior or bugs, so that logic issues are
        diagnosed and corrected without manual debugging.
    - name: Technical debt refactor prompts
      id: mentor-studio-technical-debt-refactor-prompts
      description: Wants to write prompts for Mentor Studio to identify and
        refactor technical debt, so that large or messy logic becomes
        maintainable.
    - name: Requirement breakdown prompts
      id: mentor-studio-requirement-breakdown-prompts
      description: Wants to write prompts for Mentor Studio to break down
        complex requirements into smaller implementation steps, so that
        large features become actionable to build incrementally.
    - name: Reuse public element prompts
      id: mentor-studio-reuse-public-element-prompts
      description: Wants to write prompts for Mentor Studio to reuse a
        public element exposed by another app in the tenant, using Add
        public elements or letting Mentor find and add it automatically,
        so that existing logic can be called from a new app without
        rebuilding it.
    - name: Explain and document prompts
      id: mentor-studio-explain-document-prompts
      description: Wants to write prompts for Mentor Studio to generate
        documentation for an element or explain unfamiliar logic and app
        structure, so that an element's intent and impact are understood
        before making changes.
    - name: Prompt best practices
      id: mentor-studio-prompt-best-practices
      description: Wants to write clear, specific prompts for Mentor Studio
        by referencing elements by name, sending one task per prompt, and
        providing context like data types or relationships, so that
        requests to modify logic, UI, or data produce predictable,
        correctly-scoped results.
  - name: AI models and search services
    id: ai-models-and-search-services
    subtopics:
    - name: Manage AI model connections
      id: manage-ai-model-connections
      description: Wants to understand how to connect apps to AI models in ODC, including
        using natively supported providers or bringing their own API key (BYOK),
        configuring endpoints, and setting daily usage limits, so they can set
        up AI model connections before writing app logic that calls the model.
    - name: Configure AI models
      id: configure-ai-models
      description: Wants to prepare a supported model provider for AI Agent Builder by
        gathering the required keys, endpoint details, and custom integration
        before creating agents, so the selected model can be used in the
        builder.
    - name: Configure AI Agent Builder with Amazon Bedrock
      id: configure-ai-agent-builder-amazon-bedrock
      description: Wants to configure AI Agent Builder to use a supported model
        provider by gathering the service keys and endpoint details and adding
        them to the builder, so that agents can use the selected model.
    - name: Custom model connections
      id: custom-model-connections-odc
      description: Wants to connect ODC to non-native LLM providers or
        private models by building a custom connector service that
        implements OutSystems' API contract, handling authentication,
        request and response formatting, and error codes, so that custom
        or third-party models can be called from ODC Studio like natively
        supported models.
    - name: Add custom AI model
      id: add-custom-ai-model
      description: Wants to add a custom AI model in the AI Agent Builder app by
        opening Configurations, selecting Custom connection, entering the model
        details, adding endpoints and authentication headers, and saving the
        configuration, so that the app can call a custom or third-party LLM.
    - name: Add Amazon Bedrock model
      id: add-amazon-bedrock-model
      description: Wants to add an Amazon Bedrock model in the AI Agent Builder app
        by opening Configurations, selecting Amazon Bedrock, entering the model
        details, adding one or more endpoints for the same model and version,
        setting endpoint priorities, and saving the configuration, so that the app
        can use Bedrock with prioritized failover endpoints.
    - name: Configure Amazon Bedrock
      id: configure-amazon-bedrock
      description: Wants to request access to Amazon Bedrock Claude foundation models,
        create an IAM user, and assign the minimum permissions needed to use the
        model in AI Agent Builder, so they can set up the model connection.
    - name: Configure Azure OpenAI
      id: configure-azure-openai
      description: Wants to create and deploy an Azure OpenAI service, retrieve the
        API keys and endpoint, and prepare them for AI Agent Builder, so that they
        can configure an Azure OpenAI model connection.
    - name: Model high availability
      id: model-high-availability
      description: Wants to configure multiple deployments or resources for an AI
        model in AI Agent Builder and distribute load between them so the system
        stays operational during upstream outages or when token-per-minute limits
        are reached.
    - name: Add Azure AI Search
      id: add-azure-ai-search-data-source
      description: Wants to add an Azure AI Search data source in the AI
        Agent Builder app by providing the connection details, testing the
        connection, and saving the source, so that the app can ground
        agents on Azure AI Search content.
  - name: Search services for RAG
    id: search-services-rag
    description: Wants to connect ODC apps to external knowledge bases
      like Azure AI Search or Amazon Kendra and retrieve semantically
      relevant data via a service action, then pass that data as context
      to an AI model call, so they can implement the
      Retrieval-augmented generation (RAG) pattern in app logic.
    subtopics:
    - name: Configure Azure AI Search
      id: configure-azure-ai-search
      description: Wants to create an Azure AI Search service backed by Azure Blob
        Storage, upload files, build an index, and retrieve the endpoint,
        key, and index name, so that they can add the source to AI Agent
        Builder.
    - name: Configure Amazon Kendra
      id: configure-amazon-kendra
      description: Wants to create an Amazon Kendra index backed by Amazon S3,
        upload source data, configure the data source, and retrieve the
        details needed to add the source to AI Agent Builder, so that they
        can ground agents on Amazon Kendra content.
  - name: Knowledge bases (ODC)
    id: knowledge-bases-odc
    subtopics:
    - name: Knowledge base overview
      id: knowledge-base-overview
      description: Wants to understand what a knowledge base is in ODC and
        how the ingestion pipeline (chunking, embedding, vector index) and
        semantic search enable retrieval-augmented generation (RAG) at
        runtime, so they can grasp how agents and apps retrieve relevant
        content from uploaded documents.
    - name: KB supported file types
      id: kb-supported-file-types
      description: Wants to know which file types and the maximum file size a knowledge
        base accepts, so they can prepare documents before uploading them.
    - name: KB stage isolation
      id: kb-stage-isolation
      description: Wants to understand how knowledge base files and indexes
        are isolated per ODC stage and don't auto-copy between stages, so
        they can plan deployment steps.
    - name: KB region and data residency limits
      id: kb-region-data-residency-limits
      description: Wants to understand which regions trigger cross-region
        data transfers for knowledge bases, so they can plan for data
        residency compliance.
    - name: Choose chunking strategy
      id: choose-chunking-strategy
      description: Wants to compare ODC knowledge base chunking strategies and their trade-
        offs, so they can select the strategy that optimizes retrieval quality
        for their data structure and expected query type.
    - name: Manage KB lifecycle
      id: manage-kb-lifecycle
      description: Wants to create, upload files to, view, edit, and delete
        a knowledge base in the ODC Portal, so they can manage a knowledge
        base's full life cycle without using ODC Studio.
    - name: Add KB server action
      id: add-kb-server-action
      description: Wants to add the auto-generated Server Action for a
        published knowledge base to an app in ODC Studio, so the app can
        run semantic search against that knowledge base's indexed content
        without custom integration code.
  - name: Agent guardrails
    id: agent-guardrails
    subtopics:
    - name: Configure stage guardrails
      id: configure-stage-guardrails
      description: Wants to configure baseline agent guardrail policies at the stage level
        in the ODC Portal, so that safety standards are enforced consistently
        across all agents in a given stage.
    - name: Configure agent-level guardrails
      id: configure-agent-level-guardrails
      description: Wants to configure or override guardrail policies for
        an individual AI agent in the ODC Portal, either overriding an
        inherited stage-level baseline policy or enabling guardrails
        directly when no baseline exists, so that specific high-risk or
        sensitive agents get tailored protection.
    - name: Handle guardrail violation exception
      id: handle-guardrail-violation-exception
      description: Wants to recognize and handle the exception code raised when an agent
        guardrail blocks a request, so that violations are surfaced to end users
        appropriately.
    - name: Review guardrail events
      id: review-guardrail-events
      description: Wants to review guardrail violation events in the ODC
        Portal's Monitor > Logs area, so that policy effectiveness can
        be assessed.
- name: Integrating with Plugins
  guid: 1af9eece-e820-40ec-8483-b1243267b038
  topics:
  - name: Using Cordova Plugins
    id: using-cordova-plugins
    subtopics:
    - name: MABS plugin versions
      id: mabs-plugin-versions
      description: Wants to determine which version of a supported OutSystems Cordova
        plugin to use for their MABS 4 or MABS 5 mobile app, by checking the
        recommended plugin version table for each MABS version, so that they
        avoid gradle dependency resolution and build compatibility issues.
    - name: Haptics plugin reference
      id: haptics-plugin-reference
      description: Wants to look up the Haptics plugin's client actions (CheckHapticsPlugin,
        Impact, Notification, Vibrate, SelectionStart, SelectionChanged, SelectionEnd),
        their parameters, related static entities (ImpactStyle, NotificationType), the
        HapticsError data structure, and error codes, so that they can trigger haptic
        feedback correctly in an ODC mobile app.
    - name: Trigger haptic feedback
      id: trigger-haptic-feedback
      description: Wants to add logic in ODC Studio to trigger haptic feedback (impact,
        notification, vibration, or selection hints) in a mobile app using the Haptics
        plugin's client actions, including checking the plugin's availability beforehand
        and handling the Success and Error output parameters, so that app users receive
        physical touch or vibration feedback during interactions.
    - name: CocoaPods support
      id: cocoapods-support-outsystems
      description: Wants to understand what CocoaPods support means in OutSystems, including
        what CocoaPods is, which plugin types it applies to (Objective-C Cordova plugins),
        and how it eliminates manual iOS dependency management, so that they can decide
        whether and how to use CocoaPods-dependent plugins in their mobile apps.
    - name: Check CocoaPods plugin
      id: check-plugin-uses-cocoapods
      description: Wants to determine whether a Cordova plugin uses CocoaPods by inspecting
        the plugin module's extensibility configurations JSON to locate the plugin URL,
        then checking the corresponding plugin.xml for a podspec framework reference,
        so that they know whether CocoaPods dependency handling applies before using
        the plugin.
    - name: Resolve podspec error
      id: resolve-podspec-error
      description: Wants to resolve the MABS build error caused by a Cordova plugin
        using a framework tag with type podspec, by updating the plugin to its latest
        version or converting the tag to a podspec tag in the plugin's plugin.xml, so
        that the iOS application package can be generated on MABS 10 or later.
    - name: Set up push notifications
      id: set-up-push-notifications
      description: Wants to set up push notifications in an OutSystems mobile app using
        the OneSignal plugin — including configuring the OneSignal account, installing
        the plugin, registering the device (with or without a user), handling notification
        events, and configuring deep-link support for Android — so that the app can
        receive push notifications on both iOS and Android.
    - name: Send push notifications
      id: send-push-notifications
      description: Wants to implement server-side logic in an OutSystems O11 app to
        send push notifications via the OneSignalAPI — including adding the API dependency,
        building the send logic, configuring the notification icon, and defining
        multi-language message content — so that the app can deliver targeted push
        notifications to users.
    - name: Set up Pushwoosh
      id: set-up-pushwoosh-notifications
      description: Wants to set up push notifications in an OutSystems O11 mobile app
        using the Pushwoosh plugin — including installing the plugin from Forge, adding
        the PushwooshNotifications block, configuring the ApplicationCode and FCMSenderId
        parameters, uploading google-services.json for Android, registering the device
        after login or via OnInitializeOver, and setting the aps-environment entitlement
        for iOS developer certificates — so that the app can receive push notifications
        on both iOS and Android.
    - name: Send Pushwoosh notification
      id: send-pushwoosh-notifications
      description: Wants to send push notifications from an OutSystems O11 app using
        the Pushwoosh plugin — including generating a Pushwoosh API Access Token and
        calling the SendPushNotificationToAll action with the ApplicationCode, ApiAccessToken,
        Message, Badge, and Deeplink parameters — so that the app can broadcast
        notifications to all registered devices.
    - name: Configure Pushwoosh iOS
      id: configure-pushwoosh-ios
      description: Wants to configure Pushwoosh for iOS push notifications by creating
        an Apple Developer App ID with Push Notifications enabled, generating and
        exporting an APNS certificate and private key from Keychain, and creating
        and configuring an iOS application in the Pushwoosh dashboard, so that the
        app is ready to send and receive push notifications via Pushwoosh on iOS.
    - name: Camera plugin reference
      id: camera-plugin-reference
      description: Wants to look up the Camera plugin's API reference so that they
        can implement camera and gallery functionality in a mobile app using this
        legacy plugin.
    - name: Control device keyboard
      id: control-device-keyboard
      description: Wants to show or hide the device's keyboard programmatically
        using the Keyboard plugin's Show and Hide client actions in ODC Studio,
        including checking the plugin's availability with CheckKeyboardPlugin
        and handling the Success and Error output parameters, so that the app
        can control keyboard visibility on Android and iOS.
    - name: Customize keyboard appearance
      id: customize-keyboard-appearance
      description: Wants to customize how the device's keyboard looks in a
        mobile app, including setting the accessory bar visibility with
        SetAccessoryBarVisible, changing the keyboard's light or dark style
        with SetStyle, and configuring the KeyboardBackdropBehavior
        extensibility setting to control the keyboard's background color on
        iOS 26 and later, so that the keyboard's appearance matches the app's
        design.
    - name: Handle keyboard events
      id: handle-keyboard-events
      description: Wants to detect when the device's keyboard is about to
        show, has shown, is about to hide, or has hidden, by adding the
        KeyboardEvents block to a screen and creating handler client actions
        for the OnKeyboardWillShow, OnKeyboardDidShow, OnKeyboardWillHide, and
        OnKeyboardDidHide events, so that the app can react to keyboard
        visibility changes.
    - name: KeyStore plugin actions
      id: keystore-plugin-actions
      description: Wants to use the KeyStore Plugin's client actions
        (CheckKeyStorePlugin, SetValue, GetValue, RemoveKey) in ODC Studio to
        securely set, get, and remove key-value secrets in a mobile app, and
        handle errors using each action's Success output, so that sensitive
        information like tokens or passwords is stored safely on iOS and
        Android devices.
    - name: Secure KeyStore access
      id: secure-keystore-access
      description: Wants to require additional user authentication (device
        PIN or biometrics) before accessing a KeyStore Plugin secret, by
        setting the KeyAuthentication parameter on SetValue, customizing the
        Android authentication dialog's title, subtitle, and cancel button
        via extensibility settings, enabling automatic invalidation of
        secrets on biometric change with InvalidateOnBiometricChange, and
        providing the required NSFaceIDUsageDescription iOS permission, so
        that access to sensitive key-value pairs is protected by the
        device's lock screen security.
    - name: Implement SSL Pinning
      id: implement-ssl-pinning-cordova
      description: Wants to implement SSL Pinning for an OutSystems Cordova
        mobile app, by generating certificate public key hashes, creating a
        JSON configuration file, installing the SSL Pinning plugin from
        Forge, uploading the configuration via extensibility settings, and
        validating certificates with the RequireSSLPinning block, so that
        mobile app HTTPS communications are protected against
        man-in-the-middle attacks via client-side certificate verification.
    - name: Check SSL Pinning validity
      id: check-ssl-pinning-hash-validity
      description: Wants to check whether a mobile app's bundled SSL Pinning
        certificate hash is still valid, using the client action
        CheckCertificateForUrl and handling whether the check succeeds or
        returns an error, so that users can be prompted to install a new app
        version before calls to server actions stop working due to a hash
        mismatch.
    - name: Contacts plugin reference
      id: contacts-plugin-reference
      description: Wants to look up the Contacts plugin's client actions
        (CheckContactsPlugin, AddToContacts, FindContact, PickContact,
        RemoveFromContacts, DEPRECATED_RemoveFromContacts), their
        parameters, and related error-handling variables, so that they can
        implement contact list access (add, find, pick, remove) in an O11
        mobile app.
    - name: Set contacts iOS permission
      id: set-contacts-ios-permission
      description: Wants to configure the NSContactsUsageDescription iOS
        permission for the Contacts plugin, using either the universal
        extensibility configurations schema or the Cordova-based schema for
        MABS versions lower than 12, so that the app can access the
        device's contact list on iOS without falling back to a default
        system message.
    - name: Remove contact by version
      id: remove-contact-by-version
      description: Wants to remove a contact from the device's contact list
        using the RemoveFromContacts action, providing only the ContactId
        input parameter on plugin version 5.0.0 and later, or the full
        Contact object on older versions (or via
        DEPRECATED_RemoveFromContacts if preferred), so that the correct
        action signature is used for their installed plugin version.
    - name: Calendar plugin reference
      id: calendar-plugin-reference
      description: Wants to look up the Calendar plugin's client actions
        (CheckCalendarPlugin, CreateEvent, CreateEventWithOptions,
        DeleteEvent, DeleteEventById, FindEvent, ListCalendars,
        OpenCalendar), their parameters, output parameters (Success, Error,
        Events), and MABS version compatibility, so that they can implement
        calendar event creation, deletion, and lookup in an O11 mobile app.
    - name: Set calendar iOS permission
      id: set-calendar-ios-permission
      description: Wants to configure the NSCalendarsUsageDescription and
        NSCalendarsFullAccessUsageDescription iOS permissions for the
        Calendar plugin, using either the universal extensibility
        configurations schema or the Cordova-based schema for MABS versions
        lower than 12, so that the app can access the device's calendar on
        iOS without falling back to default system messages.
    - name: Common plugin actions
      id: common-plugin-actions
      description: Wants to use the Common plugin's client actions to log to the device
        console and retrieve runtime environment information in an ODC mobile
        app, so that the app can identify its runtime environment.
    - name: Plugin manager scopes
      id: plugin-manager-scopes
      description: Wants to create and destroy PluginManager scopes in a
        mobile app screen lifecycle, using InitPluginManager, createScope,
        and destroyScope, so that native background work and callbacks
        follow the screen and UI block lifecycle safely.
  - name: Camera plugin migration
    id: migrate-camera-plugin
    description: Wants to migrate the Camera plugin from version 1 to version 2
      by replacing deprecated client actions and blocks with TakePhoto and
      ChooseFromGallery, updating renamed parameters and MediaResult output
      handling, and adapting to the new OnError and error code behavior, so the
      app keeps working after the upgrade.
    subtopics:
    - name: Migrate camera actions and blocks
      id: migrate-camera-actions-blocks
      description: Wants to replace deprecated Camera plugin actions and blocks
        with TakePhoto and ChooseFromGallery, update renamed inputs and outputs,
        and handle the gallery PWA limitations during migration, so the app uses
        the version 2 API correctly.
    - name: Handle TakePhoto migration errors
      id: handle-takephoto-migration-errors
      description: Wants to add the required OnError handler for the TakePhoto
        block and update related error handling logic, so the migrated block
        continues to fail safely.
    - name: Update camera error codes
      id: update-camera-error-codes
      description: Wants to remap the Camera plugin error codes after version 2
        renumbering, so existing error handling continues to route each failure
        to the correct branch.
  - name: Using Capacitor Plugins
    id: using-capacitor-plugins
    subtopics:
    - name: Configure SSL Pinning Capacitor
      id: configure-ssl-pinning-capacitor
      description: Wants to configure SSL Pinning in a Capacitor-based ODC
        mobile app, by creating a build action JSON file that injects
        certificate pin configuration into the Android network security
        config and iOS TSKPinnedDomains/TSKPublicKeyHashes during the build,
        and referencing that file in the app's extensibility configuration,
        so that Capacitor apps enforce certificate pinning without relying
        on Cordova extensibility settings.
    - name: Choose plugin source
      id: choose-plugin-source
      description: Wants to choose the safest plugin source for an ODC
        mobile app by checking OutSystems-supported plugins first, then
        following the plugin decision flow and reviewing source,
        maintenance activity, and security posture when no supported
        plugin fits.
    - name: Integrate plugin in app
      id: integrate-plugin-in-app
      description: Wants to integrate a Capacitor plugin (official, community,
        or custom-built) into an ODC app by creating a mobile library
        wrapper, configuring its extensibility settings to reference the
        plugin package and permissions, adding a client action that calls
        the plugin via window.CapacitorPlugins, using the library in the
        app, and building the mobile package, so that the plugin's native
        functionality becomes available to the app.
    - name: Vet plugin security
      id: vet-plugin-security
      description: Wants to assess whether a Capacitor plugin is safe to
        include in a mobile app, given that bundled plugins share the
        global JavaScript environment and can access device APIs, app
        data, and user information, by installing only from trusted
        sources, reviewing the plugin's source code and community
        signals, and keeping plugins updated or removing unused ones, so
        that the app's security risk from third-party plugin code stays
        low.
    - name: Pin plugin versions
      id: pin-plugin-versions
      description: Wants to pin plugin package versions in an ODC app
        extensibility configuration to exact values instead of floating
        ranges, so that MABS builds stay reproducible and plugin upgrades
        are deliberate.
  - name: Migrate Cordova plugins to Capacitor
    id: migrate-cordova-plugin-capacitor
    subtopics:
    - name: Migrate Cordova settings
      id: migrate-cordova-settings
      description: Wants to decide whether a Cordova plugin can be adapted by
        updating extensibility configurations only or needs a
        Capacitor-compatible plugin, then migrate its preference variables
        and config-file/edit-config declarations, which Capacitor doesn't
        support directly, to MABS 12 extensibility settings, plugin
        extensibility JSON, or build actions, so that plugin customization
        and native file changes (such as Info.plist entries or Android
        resource strings) keep working when building Capacitor apps.
    - name: Adapt Cordova hooks and APIs
      id: adapt-cordova-hooks-apis
      description: Wants to replace a Cordova plugin's build-time hooks with
        MABS 12 build actions (falling back to Capacitor Hooks when needed)
        and update Cordova-specific JavaScript API checks (such as typeof
        cordova and cordova.platformId) to also support Capacitor, so that
        the plugin's build process and client-side code work correctly in
        Capacitor apps.
    - name: iOS usage descriptions
      id: ios-usage-descriptions
      description: Wants to understand why omitting an iOS privacy usage
        description (for example, NSCameraUsageDescription) from a Capacitor
        plugin's extensibility JSON causes the app to fail or be rejected on
        iOS while Android continues to work, so they include the required
        iOS usage descriptions when configuring plugin permissions.
    - name: Build dual-stack plugin
      id: build-dual-stack-plugin
      description: Wants to implement a dual-stack plugin that supports both
        Cordova and Capacitor implementations, by creating an equivalent
        Capacitor plugin, declaring both sources in the OutSystems plugin's
        extensibility configuration, and updating JavaScript client actions
        to detect the runtime framework and route calls to the right
        implementation, so that the plugin works in both Cordova and
        Capacitor apps without breaking existing Cordova-based apps.
    - name: Decide dual-stack need
      id: decide-dual-stack-need
      description: Wants to decide whether to implement a dual-stack plugin
        (separate Cordova and Capacitor implementations) instead of fully
        adapting a Cordova plugin for Capacitor-only compatibility, based on
        criteria such as needing Capacitor-only APIs, better performance, a
        gradual Cordova phase-out, or complex compatibility logic, so that
        they choose the right migration approach for an OutSystems plugin.
- name: Lifecycle Independence
  guid: 4b3a44cd-2a86-462c-81b2-89a841284aab
  topics:
  - name: Lifecycle independence
    id: lifecycle-independence
    subtopics:
    - name: Lifecycle independence strategies in OutSystems
      id: lifecycle-independence-os
    - name: Domain Driven Design
      id: domain-driven-design
    - name: Team structure that promotes autonomy
      id: autonomous-team-structure
    - name: Strategies for development at scale
      id: scale-development-strategy
  - name: Basic feature fencing for independent releases
    id: feature-fencing-basics
    subtopics:
    - name: Feature fencing using site properties/settings
      id: feature-toggle-site-settings
    - name: Feature fencing strategies
      id: feature-fencing
- name: Platform lifecycle and support
  guid: 2a9c4b6f-7d5e-4a58-8f7e-3a5c5f0f0a61
  topics:
  - name: O11 lifecycle and support
    id: o11-lifecycle-support
    subtopics:
    - name: O11 lifecycle overview
      id: o11-lifecycle-overview
      description: Wants to understand the OutSystems 11 product lifecycle, including its
        release types and support calendar, so that they know how the platform
        stays supported over time.
    - name: Install O11 releases
      id: install-o11-releases
      description: Wants to plan how to install a new OutSystems 11 release,
        including the differences between self-managed upgrades,
        OutSystems 11 Cloud upgrades handled by the support team, and
        Service Studio auto-update, so that they know what to expect when
        updating the platform.
    - name: Update docs cadence
      id: update-docs-cadence
      description: Wants to know which documentation, training, installation
        checklist, release notes, and product update content change with
        major releases, regular releases, and cumulative patches in
        OutSystems 11, so that they can tell what gets refreshed for each
        release type.
- name: Load testing
  guid: f816091c-1508-4da9-b5f6-cffebb845234
  topics:
  - name: Fundamentals of load testing strategies
    id: load-test-strategy
    subtopics:
    - name: Post-release load testing
      id: post-release-load-testing
    - name: Load testing strategies
      id: load-testing-strategies
  - name: Load testing as part of CI/CD
    id: load-test-ci-cd
    subtopics:
    - name: Load tests in CI/CD pipeline
      id: load-tests-ci-cd
- name: Native Mobile apps
  guid: f67965c4-a2ef-4076-8676-c8b8203ab671
  topics:
  - name: Mobile best practices
    id: mobile-best-practices
    description: Wants to understand the mobile best-practices section for ODC apps,
      including what kinds of guidance it covers, so that they can find the
      right best-practice topic for their app.
  - name: MABS build process
    id: mabs-build-process
    description: Wants to understand how MABS turns an OutSystems mobile app into a
      signed native package, including the template, app resources, mobile
      platform selection, native plugins, signing keys, and build output, so
      they can follow the build flow.
    subtopics:
    - name: Generate iOS package
      id: generate-ios-development-package
      description: Wants to generate an iOS mobile app package for development tests
        in Service Studio or Service Center when the app uses Development build
        type, an iOS App Development certificate, and a matching provisioning
        profile.
    - name: Generate Android package
      id: generate-android-development-package
      description: Wants to generate an Android mobile app package for development
        tests in Service Studio or Service Center when the app uses Debug build
        type and, optionally, a custom keystore.
    - name: Distribute development package
      id: distribute-development-package
      description: Wants to share a generated mobile app package with testers and
        install it on devices when the package was created for development tests,
        using the QR code, installation link, or package file.
    - name: Limited iOS package
      id: limited-ios-package
      description: Wants to generate an iOS mobile app package for a limited group
        of end users by selecting the Ad Hoc build type and providing a matching
        certificate and provisioning profile, so that they can distribute it to
        selected testers before general release.
    - name: Limited Android package
      id: limited-android-package
      description: Wants to generate an Android mobile app package for a limited
        group of end users by selecting the Release build type and signing it with
        a keystore, so that they can distribute it to selected testers before
        general release.
    - name: Limited app distribution
      id: limited-app-distribution
      description: Wants to distribute a generated mobile app to a limited group of end
        users before general release, so that selected testers can install it
        through the supported limited-distribution methods.
    - name: App Store submission
      id: app-store-submission
      description: Wants to generate an App Store iOS package in Service Studio
        or Service Center and submit it to App Store Connect with Transporter,
        after meeting the Apple Developer and signing prerequisites, so that
        the app can be reviewed and published in the Apple App Store.
    - name: iOS signing setup
      id: ios-signing-setup
      description: Wants to enroll in the Apple Developer Program or Apple Developer
        Enterprise Program, create the signing certificate and provisioning
        profile needed to package iOS mobile apps, and renew the profile when it
        expires, so that they can generate signed iOS packages.
    - name: Android keystore setup
      id: android-keystore-setup
      description: Wants to create an Android keystore with keytool and keep it in
        a safe place for future app updates, so that Android mobile apps can be
        signed and published with the same key.
  - name: MABS environment configuration
    id: mabs-environment-configuration
    subtopics:
    - name: Enable MABS service
      id: enable-mabs-service
      description: Wants to enable or disable the Mobile Apps Build Service
        (MABS) for an OutSystems 11 environment in Service Center's
        Environment Configuration, understanding that disabling it prevents
        new mobile app package generation for that environment, so that
        they can control when new mobile builds are produced.
    - name: Monitor MABS status
      id: monitor-mabs-status
      description: Wants to check the operational status of the Mobile Apps
        Build Service (MABS) for an OutSystems 11 environment in Service
        Center's Environment Health screen, including per-platform status
        and generation logs, so that they can detect when the service is
        down and mobile app packages aren't being generated.
    - name: Access MABS generation log
      id: access-mabs-generation-log
      description: Wants to access the mobile app package generation log for
        a specific platform in OutSystems 11, using Application Management
        in Service Studio or the Distribute tab in Service Center, so that
        they can review or share generation details for troubleshooting.
    - name: Native platforms overview
      id: native-platforms-overview
      description: Wants to understand the Native Platforms section in Service Center's
        Distribute tab, including what the fields show, so that they can
        interpret mobile platform configuration and package status.
  - name: Troubleshoot MABS build errors
    id: troubleshoot-mabs-build-errors
    subtopics:
    - name: MABS build errors
      id: mabs-build-errors
      description: Wants to locate and resolve OutSystems Mobile Apps Build
        Service (MABS) build errors by finding the error code in the
        reference, understanding its cause and impact, and following the
        recommended action, so that they can resolve native mobile app
        build failures.
    - name: Cordova bootstrap error
      id: cordova-bootstrap-error
      description: 'Wants to resolve the OS-MABS-PLF-50000 "Something
        happened on our side. Please try again." error, caused by MABS
        failing while bootstrapping the base Cordova project with the
        OutSystems native shell, by retrying the generation, checking the
        Service Center build logs, and opening a support case if the
        problem persists, so that the mobile app package generates
        successfully.'
  - name: MABS release notes
    id: mabs-release-notes
    subtopics:
    - name: MABS 12 upgrade overview
      id: mabs-12-upgrade-overview
      description: Wants to understand what's new in MABS 12, including Capacitor
        support, the iOS 26 and Android API 36 SDK updates, build actions, and
        universal extensibility, so that they can plan whether and how to
        upgrade their mobile app's build service version.
    - name: MABS 12 breaking changes
      id: mabs-12-breaking-changes
      description: Wants to understand the breaking changes introduced in MABS
        12 for Capacitor apps (WebView version requirement, removed iOS
        Inspector, DeepLinksHandlerType no longer effective) and Cordova apps
        (Android status bar behavior, renamed Cordova template files), so that
        they can update their app before or after upgrading the MABS version.
    - name: MABS 12.1 splash screen changes
      id: mabs-12-1-splash-changes
      description: Wants to understand how splash screen behavior and configuration changed
        for Capacitor apps in MABS 12.1, including the native splash screen
        implementation, IDE-driven settings replacing extensibility
        configuration, and the new branding image size requirement, so that they
        can update their app's splash screen setup correctly.
    - name: MABS 6 breaking changes
      id: mabs-6-breaking-changes
      description: Wants to understand the breaking changes and known
        limitations introduced in MABS 6, including CORS enforcement for
        XHR/Fetch requests to OutSystems servers, document.cookie no longer
        working, RedirectToURL failures due to the outsystems:// scheme,
        missing Network information in Web Inspector, and Content Security
        Policy violations for images due to WKWebView, so that they can
        update their app before or after upgrading the MABS version.
    - name: MABS 8 upgrade overview
      id: mabs-8-upgrade-overview
      description: Wants to understand what's new in MABS 8.0 and 8.1, including
        iOS 15 and Android 12 (API 31) support, Gradle 7.1.1, Java 11, and
        updated Forge plugin version requirements, so that they can plan
        whether and how to upgrade their mobile app's build service version.
    - name: MABS 8 breaking changes
      id: mabs-8-breaking-changes
      description: 'Wants to understand the breaking changes introduced in
        MABS 8.0, including the mandatory android:exported attribute for
        Android 12 components, removal of Gradle''s compile configuration,
        the Cordova AllowList Plugin replacing Whitelist Plugin, and the
        native Android 12 splash screen behavior, so that they can update
        their app before or after upgrading the MABS version.'
    - name: MABS 10 upgrade overview
      id: mabs-10-upgrade-overview
      description: Wants to understand what's new in MABS 10, including iOS
        17 and Android 14 SDK targeting and the updated minimum Forge plugin
        version requirements for O11 and ODC, so that they can plan whether
        and how to upgrade their mobile app's build service version.
    - name: MABS 10 breaking changes
      id: mabs-10-breaking-changes
      description: Wants to understand the breaking changes introduced in
        MABS 10, including the removal of legacy Android Support Library
        (com.android.support) support in favor of mandatory AndroidX, the
        AndroidXEnabled preference no longer taking effect, and the removal
        of automatic aps-environments plist injection, so that they can
        update their app before or after upgrading the MABS version.
    - name: MABS version support
      id: mabs-version-support
      description: Wants to know which MABS versions are currently supported,
        deprecated, or obsolete, and which Android and iOS versions and build
        tools each version supports, so they can choose the correct MABS
        version for their mobile app and plan upgrades before a version
        becomes obsolete.
    - name: MABS lifecycle timeline
      id: mabs-lifecycle-timeline
      description: Wants to understand how long a MABS major or minor version stays
        supported before becoming deprecated and then obsolete relative to the
        release of the next major or minor version, so they can plan mobile app
        upgrades before their current MABS version loses support.
    - name: Choose MABS version
      id: choose-mabs-version
      description: Wants to select which MABS version to use when creating a
        mobile app's package for each app and mobile platform, either always
        using the latest available version or pinning a specific one, so
        they get a consistent app version across stages and the
        performance, security, and OS-compatibility benefits of newer MABS
        releases.
    - name: MABS 3.x release notes
      id: mabs-3x-release-notes
      description: Wants to review the release notes for MABS 3.x, including
        new features and bug fixes, so that they can troubleshoot mobile apps
        still running on a MABS 3.x version.
    - name: Pre-3.x MABS notes
      id: pre-3x-mabs-notes
      description: Wants to review the release notes for Mobile Apps Build
        Service versions before 3.3, from 2017 to 2018, including new
        features and bug fixes, so that they can troubleshoot mobile apps
        still running on these early builds.
    - name: MABS 4.x release notes
      id: mabs-4x-release-notes
      description: Wants to review the release notes for MABS 4.x, including
        new features, bug fixes, and the known viewport-fit issue, so that they
        can troubleshoot mobile apps still running on a MABS 4.x version.
    - name: MABS 5 breaking changes
      id: mabs-5-breaking-changes
      description: Wants to understand the breaking changes introduced when
        upgrading from MABS 4 to MABS 5, including the Cordova-android
        package update to 8.0.0 that moves application code into a new
        :app project directory structure and the Android target SDK
        increase to API 28 that requires updated Android support
        libraries, so that they can update their app and plugins before
        or after upgrading the MABS version.
    - name: MABS 11 upgrade overview
      id: mabs-11-upgrade-overview
      description: Wants to understand what's new in MABS 11, including iOS
        18 and Android 15 SDK targeting, the O11 and ODC mobile build service
        architecture unification introduced in MABS 11.2, and recommended
        plugin updates, so that they can plan whether and how to upgrade
        their mobile app's build service version.
    - name: MABS 11.1 upload widget changes
      id: mabs-11-1-upload-widget-changes
      description: Wants to understand how the Upload Widget's media
        selection behavior changed in MABS 11.1, including the new Android
        photo picker, the optional capture attribute for opening the camera
        directly, and the known limitation where capture doesn't work on
        Android 9, so that they can update their app's Upload Widget
        configuration correctly.
    - name: MABS 11 breaking changes
      id: mabs-11-breaking-changes
      description: Wants to understand the breaking changes introduced in
        MABS 11, including the AddUploadWidgetPermissions property default
        changing from true to false, the StatusBarOverlaysWebView property
        default changing from false to true, and the new StatusBarStyle
        property default, so that they can update their app before or after
        upgrading the MABS version.
    - name: MABS 7 upgrade overview
      id: mabs-7-upgrade-overview
      description: Wants to understand what's new in MABS 7.0 to 7.2, including
        Android 11 (API level 30), iOS 14, Cordova CLI 10, npm-based plugin
        installation, iOS Usage Descriptions at the application level,
        removing unwanted Upload Widget permissions, and improved Logger API
        and device UUID in HTTP headers, so that they can plan whether and
        how to upgrade their mobile app's build service version.
    - name: MABS 7 breaking changes
      id: mabs-7-breaking-changes
      description: Wants to understand the breaking changes and known
        limitations introduced in MABS 7.0, including plugins now installed
        via npm requiring the git+ protocol prefix, the commit/subdir
        dependency elements no longer taking effect,
        context.requireCordovaModule no longer loading non-Cordova modules,
        the xcode module needing to be declared in package.json, and
        user-agent no longer being retrievable through CDVUserAgentUtil, so
        that they can update their app before or after upgrading the MABS
        version.
  - name: MABS 6 upgrade overview
    id: mabs-6-upgrade-overview
    description: Wants to understand what's new across MABS 6.0 to 6.3,
      including the introduction of WKWebView as the default web view,
      Android 10 (API 29) and iOS 13 SDK support, removal of UIWebView,
      and the optional AndroidX support added in 6.3, so that they can
      plan whether and how to upgrade their mobile app's build service
      version.
    subtopics:
    - name: Enable AndroidX
      id: enable-androidx
      description: Wants to enable AndroidX for a mobile app by setting the AndroidXEnabled
        preference in the main module's Extensibility Configurations and
        publishing the app, so that supported MABS builds generate native builds
        with AndroidX dependencies.
  - name: OS and SDK support policy
    id: os-sdk-support-policy
    subtopics:
    - name: OS support process
      id: os-support-process
      description: Wants to understand how OutSystems supports new mobile
        operating system versions, by releasing a new major MABS version with
        updated runtime primitives and Forge components ahead of each new OS
        release, so they know when new SDKs become mandatory for publishing to
        the app stores.
    - name: Beta OS testing
      id: beta-os-testing
      description: Wants to know their responsibilities when testing mobile
        apps against new OS betas and SDKs, including testing custom and
        non-supported plugins and starting tests at the first public beta, so
        they understand support severity and stability expectations during
        Beta MABS versions.
  - name: Mobile app debugging
    id: mobile-app-debugging
    subtopics:
    - name: Inspect network traffic
      id: inspect-network-traffic
      description: Wants to inspect network requests and responses generated by
        their mobile app, using Chrome or Safari Developer Tools or network
        proxy tools (HTTP Toolkit, Mitmweb, Charles), so that they can debug
        HTTP traffic, check first-visit resource loading, and monitor OTA
        updates.
    - name: Simulate slow network
      id: simulate-mobile-network-conditions
      description: Wants to simulate degraded network conditions (slow,
        unreliable, or packet-dropping connections) in a mobile app using
        Chrome Developer Tools, Charles, or the iOS Network Link Conditioner,
        so that they can investigate bugs that occur only intermittently or
        under poor network conditions.
    - name: Mobile diagnostic logging
      id: mobile-production-logging
      description: Wants to diagnose production issues on a mobile app that
        cannot be reproduced with a debug build, by logging diagnostic
        messages with the LogMessage system action (viewable in ODC
        Portal's Monitoring > Logs), so that they can trace the sequence
        of events behind an issue without direct device access.
    - name: Debug mobile issues
      id: debug-mobile-issues
      description: Wants to choose the right debugging and diagnostics tool
        for a mobile app issue, using browser DevTools for web-layer
        problems, native IDEs for plugin crashes or build issues, network
        simulation for connectivity problems, and client-side logging or
        Crashlytics for production issues, so that they can inspect the
        layer that matches the problem.
    - name: Mobile native crash reporting
      id: mobile-native-crash-reporting
      description: Wants to diagnose production issues on a mobile app that
        cannot be reproduced with a debug build, by integrating the
        Firebase Crashlytics plugin for native crash reporting, so that
        they can capture crash reports needed to resolve the issue
        without direct device access.
    - name: Android device logs
      id: android-device-logs
      description: Wants to extract Android device logs for troubleshooting
        mobile app issues, by enabling USB debugging, downloading Android
        platform-tools, and using adb and logcat commands to capture the
        device log, so that they can send the log file to OutSystems Support
        for diagnosis.
    - name: iOS device logs
      id: ios-device-logs
      description: Wants to retrieve iOS device logs for troubleshooting
        mobile app issues, using Apple Configurator 2, Xcode, iTools, or
        iOSLogInfo, so that they can send the log file to OutSystems
        Support for diagnosis.
  - name: Mobile app testing practices
    id: mobile-app-testing-practices
    subtopics:
    - name: Test on real devices
      id: test-mobile-real-devices
      description: Wants to validate a mobile app's native behavior across a
        range of physical Android and iOS devices, by installing Debug
        (Android) or Development (iOS) builds via QR code from ODC Portal,
        so that they can catch manufacturer-specific rendering, permission,
        and hardware issues that simulators and emulators cannot reproduce.
    - name: Browser fallbacks
      id: browser-fallbacks
      description: Wants to add browser fallbacks for plugin calls in ODC
        library client actions, so the app still runs non-native flows in
        the browser, in the Chrome emulation target, and in PWAs when
        native plugins are unavailable.
  - name: Local Storage
    id: local-storage
    subtopics:
    - name: SQLite attribute limit
      id: sqlite-attribute-limit
      description: Wants to understand why Android mobile apps with local storage
        entities that have a large number of attributes, or complex queries
        returning many attributes, throw Out Of Memory exceptions due to a
        SQLite plugin limitation in how Android handles JSON objects
        internally, so that they can keep entities under the 50-attribute
        threshold and avoid the issue.
    - name: Persist screen state
      id: persist-screen-state
      description: Wants to maintain field values entered on a mobile screen (for
        example, a search form) across navigation to other screens and back, by
        storing the values in a Local Storage entity filtered by user ID and
        updating it via a client action on the field's OnChange event, in
        Service Studio for OutSystems 11, so that user input persists without
        relying on the back button.
    - name: Create local storage
      id: create-local-storage
      description: Wants to define local storage entities in ODC Studio by using
        the app's database entity structure as the basis for the local model, so
        mobile and native apps can store offline data.
    - name: Fetch local storage data
      id: fetch-local-storage-data
      description: Wants to display local storage data on a mobile screen by
        configuring the screen's local storage aggregate, adding filters and
        sorting, and refreshing it from screen lifecycle events or client
        actions so the screen shows the right records.
    - name: Load screen data locally
      id: load-screen-data-locally
      description: Wants to load only the data a mobile screen needs when the data
        changes infrequently or the screen should not depend on a live network
        round trip, by storing frequently accessed data in local storage and
        denormalizing the local model to match the screen.
  - name: Firebase plugin migration
    id: migrate-firebase-plugins
    description: Wants to migrate mobile app logic from unsupported Firebase-based
      plugins (Firebase Mobile) to the newer supported Firebase plugins (Analytics,
      Performance Monitoring, Crash Reporting, Cloud Messaging), by replacing
      actions with their equivalents, so the app benefits from dedicated release
      cycles, OutSystems support, and new features.
    subtopics:
    - name: Migrate LogEvent action
      id: migrate-analytics-logevent
      description: Wants to replace the LogEvent action of the unsupported Firebase
        Mobile plugin with the LogEvent action of the supported Analytics plugin
        in Service Studio, by deleting the old action and substituting its
        parameters with the new action's equivalents, so analytics logging keeps
        working after migration.
    - name: Migrate Cloud Messaging
      id: migrate-cloud-messaging-notifications
      description: Wants to migrate topic subscription and push notification
        handling from the unsupported Firebase plugin to the supported Cloud
        Messaging plugin, by calling the Subscribe action, adding the
        NotificationsHandler/NotificationDialog blocks, and using the Cloud
        Messaging Configurator's REST API, so notifications keep working after
        migration.
  - name: Firebase plugins
    id: firebase-plugins
    subtopics:
    - name: Add Firebase plugin
      id: add-firebase-plugin
      description: Wants to add a Firebase-based plugin to an OutSystems mobile app, by
        setting up a Firebase project, installing and referencing the plugin,
        uploading the GoogleService-Info.plist and google-services.json
        configuration files as extensibility resources, and calling the plugin's
        client actions from Service Studio logic, so that the app can use
        Firebase services.
    - name: Configure iOS tracking
      id: configure-ios-tracking-prompt
      description: Wants to configure the AppTrackingTransparency permission
        prompt for the Firebase Analytics plugin on iOS, by using the
        RequestTrackingAuthorization client action, customizing the
        ShowInformation alert's Title and Message, setting the
        USER_TRACKING_DESCRIPTION_IOS extensibility preference, or disabling
        the prompt entirely via the EnableAppTrackingTransparencyPrompt
        preference, so that the app requests user tracking permission the
        way its policy requires.
    - name: Log Firebase Analytics
      id: log-firebase-analytics-events
      description: Wants to log analytics data from a mobile app using the
        Firebase Analytics plugin, by calling the LogEvent client action
        with custom event names and parameters, using LogECommerceEvent
        with the supported ecommerce event types and required parameters,
        or toggling data collection with SetEnabled and the
        ANALYTICS_COLLECTION_ENABLED preference, so that user behavior and
        ecommerce activity are captured in Firebase Analytics.
    - name: Cloud Messaging APIs
      id: cloud-messaging-apis
      description: Wants to look up the deprecated Cloud Messaging Configurator REST
        API v1 and v2 endpoints for sending notifications to topics or users,
        including request and response parameters, so they can maintain an
        existing integration until they migrate to the Cloud Messaging plugin's
        server actions.
    - name: Cloud messaging setup
      id: cloud-messaging-setup
      description: Wants to set up Firebase Cloud Messaging server actions by creating
        the backend notification service, configuring service-account fields and
        access-token storage, and using the send and silent-send actions for users
        or topics, so that an ODC mobile app can deliver push notifications and
        authenticate FCM HTTP v1 requests.
  - name: Cloud Messaging reference
    id: cloud-messaging-plugin-reference
    description: Wants to look up the Firebase Cloud Messaging plugin's client actions,
      web blocks, and related data structures and static entities, so that
      they can implement push notification and deep-linking functionality in
      an O11 mobile app using this plugin version.
    subtopics:
    - name: Cloud Messaging actions
      id: cloud-messaging-actions
      description: Wants to implement push notification behavior in an ODC mobile app
        using the Firebase Cloud Messaging plugin client actions and blocks, by
        registering devices, retrieving tokens, managing topic subscriptions,
        handling notification clicks and custom actions, and configuring notification
        behavior, so that the app responds correctly to incoming notifications.
    - name: Cloud Messaging limitations
      id: cloud-messaging-limitations
      description: Wants to identify the known Firebase Cloud Messaging plugin limitations,
        so they can avoid unsupported configurations.
  - name: App distribution
    id: app-distribution
    subtopics:
    - name: Apple privacy manifest
      id: apple-privacy-manifest
      description: Wants to include a privacy manifest (PrivacyInfo.xcprivacy) in their ODC mobile app so that it meets Apple App Store submission requirements, with accurate API usage justifications.
    - name: Configure manifest in ODC
      id: configure-manifest-odc
      description: Wants to upload and configure a privacy manifest file in ODC Studio and the ODC Portal using extensibility settings, so that the file is packaged into their iOS app build.
    - name: File plugin manifest
      id: file-plugin-manifest
      description: Wants to declare the correct API reason keys (NSPrivacyAccessedAPICategoryFileTimestamp, NSPrivacyAccessedAPICategoryDiskSpace) and recommended reason values for the File plugin in a PrivacyInfo.xcprivacy file, so that the app passes Apple App Store privacy validation when using the File plugin.
    - name: App license compliance
      id: app-license-compliance
      description: Wants to publish the third-party license texts and copyright notices required by OutSystems UI and other plug-ins or components in a mobile app, so end users can access the required notices and the app meets license obligations.
    - name: Distribute as PWA
      id: distribute-as-pwa
      description: Wants to understand what a PWA is, how it compares to native mobile
        app distribution in ODC, and how to enable and try out the PWA on Android and
        iOS devices, so that they can offer their mobile app to users without requiring
        an app store installation.
    - name: Debug PWA
      id: debug-pwa
      description: Wants to debug a PWA built in ODC by emulating it in Google Chrome
        from ODC Studio's Debugger tab, so that they can inspect and troubleshoot the
        app without a physical device.
    - name: Troubleshoot PWA installation issues
      id: troubleshoot-pwa-issues
      description: Wants to troubleshoot common PWA installation issues in
        OutSystems 11 by applying the documented fixes, so that the app installs
        correctly on supported devices.
    - name: Edit PWA manifest
      id: edit-pwa-manifest
      description: Wants to edit the PWA manifest in Service Studio or LifeTime, and
        understand which location overrides which settings, so that the app uses the
        desired PWA configuration.
    - name: Add custom service worker
      id: add-custom-service-worker
      description: Wants to add a custom service worker to a PWA by creating the
        script, referencing it in the manifest, and accounting for the manual
        resource-management impact, so that the app can use custom offline or
        network behavior.
    - name: Keep PWA resources
      id: keep-pwa-resources
      description: Wants to keep the PWA static resources and URL mappings up to date
        when the app changes, so that a custom service worker keeps working after the
        app is republished.
    - name: Understand OTA upgrades
      id: understand-ota-upgrades
      description: Wants to understand how over-the-air upgrades deliver mobile app changes
        to installed devices without a store submission, including the server-
        side precache.manifest check, client-side version checks, and the
        situations that require a new build and redistribution, so they can
        decide when an installed mobile app will update OTA versus when it needs
        a new build.
    - name: Configure mobile updates
      id: configure-mobile-updates
      description: Wants to configure whether a mobile app receives store-only
        or hybrid updates, by activating the technical preview in LifeTime
        and setting the update preference per app and per target environment
        in a deployment plan's Distribution tab, so users get updates
        through the intended channel.
    - name: Check app update mode
      id: check-app-update-mode
      description: Wants to check whether a mobile app is configured for
        store-only or hybrid updates in a target environment, by viewing
        the Native Mobile Application Updates setting in the app's Settings
        screen in LifeTime, so they can confirm the deployment plan applied
        the intended update preference.
    - name: Develop store-only apps
      id: develop-store-only-apps
      description: Wants to develop and test a mobile app so it keeps
        working correctly when configured for store-only updates, by
        versioning builds, avoiding breaking API/logic changes, updating
        upgrade messages, and testing thoroughly before generating and
        submitting the native build to the app stores.
    - name: Prepare app store submission
      id: prepare-app-store-submission
      description: Wants to prepare the screenshots, metadata, and product
        details needed to publish a mobile app to Google Play or the Apple
        App Store when the app is ready for production, so that the store
        listing is complete before submission.
    - name: Google Play data safety
      id: google-play-data-safety
      description: Wants to complete the Google Play data safety disclosure for
        an Android app by declaring the default crash logs and device or other
        IDs collected by OutSystems apps and adding any extra
        plugin-collected data when the app uses custom or supported plugins, so
        that the app meets Google Play submission requirements.
  - name: Offline Synch
    id: offline-synch
    subtopics:
    - name: Basic read-only sync
      id: basic-read-only-sync
      description: Wants to understand how the basic Read-Only Data
        synchronization pattern works for mobile apps, including the OnSync
        client action logic (call ServerDataSync, delete all local records,
        and recreate them from the fetched data), the ServerDataSync server
        action logic (fetch all records from the database), and the
        underlying database entity and local storage entity data model, so
        that they can implement one-way (server-to-device) sync for read-only
        offline data.
    - name: Generate sync action
      id: generate-sync-action
      description: Wants to automatically generate the client and server
        actions needed to implement the Read-Only Data synchronization
        pattern for an entity, by right-clicking a local entity linked to a
        database entity via foreign key in ODC Studio's Data tab and
        choosing Create Action to Sync Data (Read-Only), so the
        SyncLocal<Entity> and Sync<Entity> actions are scaffolded without
        manual creation.
    - name: Generate RW sync action
      id: generate-rw-sync-action
      description: Wants to automatically generate the client and server actions needed to
        implement the Read/Write Last Write Wins synchronization pattern for an
        entity, by right-clicking a local entity linked to a database entity via
        foreign key in ODC Studio's Data tab and choosing Create Action to Sync
        Data (Read/Write), which scaffolds the sync actions, the metadata
        attributes needed to track record origin, modification, and active
        status, and the replacement local create, update, and delete actions
        used during sync, so that the synchronization logic is scaffolded
        without manual creation.
    - name: Complex mobile sync
      id: complex-mobile-sync
      description: Wants to optimize complex offline data synchronization in a mobile
        app — including when and what to sync, context-based sync moments, SyncProperties
        with time deltas, lightweight local database design, and minimizing server
        requests — so that the app performs efficiently and remains error-tolerant
        under demanding synchronization scenarios.
    - name: Fix missing device data
      id: fix-missing-device-data
      description: Wants to troubleshoot missing data on a mobile device, caused by
        offline synchronization errors, misconfigured sync filtering rules, or
        aggregates with restrictive JOIN "Only With"/"With or Without" settings on
        local storage entities, by checking server data, device local storage, and
        sync/aggregate configuration, so that expected data appears on the device.
    - name: Conflict sync pattern
      id: conflict-detection-sync-pattern
      description: Wants to implement the Read/Write Data with Conflict Detection offline
        synchronization pattern for ODC mobile apps, covering the client-side
        sync trigger logic, the server-side data reconciliation logic, and a
        local entity/conflict-shadow entity data model with metadata attributes
        tracking whether a record originated from the server, was modified
        locally, and remains active, so that concurrent offline edits are
        detected as conflicts instead of being silently overwritten.
    - name: Read-only sync pattern
      id: read-only-sync-pattern
      description: Wants to implement the Read-Only Data Optimized offline
        synchronization pattern for ODC mobile apps, covering the client-side
        OnSync logic, the server-side ServerDataSync logic, and a database/local
        storage data model tracking ModifiedOn and IsActive attributes, so that
        only changed or deleted records are synced to local storage for
        read-only offline data access.
    - name: One-to-many sync pattern
      id: one-to-many-sync-pattern
      description: Wants to implement the Read/Write Data One-to-Many offline
        synchronization pattern for ODC mobile apps, covering the client-side
        OnSync trigger logic, the server-side ServerDataSync reconciliation
        logic for the parent and child sync actions, a dictionary mapping local
        record IDs to newly created server record IDs to preserve the parent-
        child relationship, and a local entity data model with metadata
        attributes tracking whether a record originated from the server, was
        modified locally, and remains active, so that one-to-many master/detail
        records stay consistent under a "last write wins" strategy.
    - name: Last write wins pattern
      id: last-write-wins-pattern
      description: Wants to implement the Read/Write Data Last Write Wins
        offline synchronization pattern for mobile apps, covering the
        client-side sync logic that gathers locally added, updated, and
        deleted records, the server-side sync logic that reconciles the
        database and returns the full updated record set, and a local
        entity data model with metadata attributes tracking whether a
        record originated from the server, was modified locally, and
        remains active, so that local storage is deleted and recreated
        from server data under a simple last-write-wins strategy.
    - name: Sync actions overview
      id: sync-actions-procedure
      description: Wants to understand the out-of-the-box OfflineDataSync,
        ServerDataSync, and TriggerOfflineDataSync actions and the SyncUnit
        parameter, and how they call each other in sequence to run the data
        synchronization procedure, in OutSystems 11 mobile apps, so that they
        can build the client-side and server-side logic needed to keep local
        storage and server data consistent.
    - name: Avoid sync trigger loops
      id: avoid-onsync-loop
      description: Wants to avoid triggering multiple simultaneous
        synchronizations or an OnSync/OnSyncComplete loop, by not calling
        OnSync multiple times or from inside OnSyncComplete, and instead
        chaining OnSyncComplete calls sequentially, so that mobile app
        data doesn't sync incorrectly in OutSystems 11.
    - name: Sync conflict avoidance
      id: sync-conflict-avoidance
      description: Wants to decide, at the app-design stage, whether to
        avoid data-sync conflicts altogether (for example, using a
        last-write-wins approach or locking editing while offline) or to
        accept them and support conflict management in the UX, before
        choosing an offline sync implementation pattern for an OutSystems
        mobile app.
    - name: Other sync components
      id: other-sync-components
      description: Wants to understand the OfflineDataSyncEvents Block (used
        in the Layout Block to handle OnSyncStart, OnSyncComplete, and
        OnSyncError events), the NetworkStatusChanged Block, and the
        GetNetworkStatus action, in OutSystems 11 mobile apps, so that they
        can react to synchronization events and network connectivity changes
        in their application.
    - name: Implement manual sync
      id: implement-manual-sync
      description: Wants to manually implement offline data synchronization
        logic for an OutSystems Developer Cloud (ODC) mobile app, by creating
        a server action that fetches and processes data, a client action
        that calls the server action and updates local storage entities via
        CreateOrUpdate actions, and connecting that client action to the
        app's OnSync system event, so that custom sync logic runs when
        synchronization triggers, instead of relying on the Create Action to
        Sync Data accelerator.
    - name: Configure sync triggers
      id: configure-sync-triggers
      description: Wants to configure how a mobile app triggers data synchronization, by
        configuring automatic trigger conditions and retry behavior either in
        Mobile tab properties or through the OfflineDataSyncConfiguration
        action, so that data synchronization runs automatically under the right
        conditions.
    - name: Manually trigger sync
      id: manually-trigger-sync
      description: Wants to manually trigger data synchronization in a mobile
        app by dragging or calling the OnSync system event in the
        application flow, so that synchronization runs on demand when
        automatic triggers don't match the intended user interaction.
    - name: Sync status feedback events
      id: sync-status-feedback-events
      description: Wants to provide user feedback about synchronization
        status in a mobile app by handling OnSyncStart, OnSyncComplete, and
        OnSyncError events, so that users know when synchronization starts,
        finishes, or fails.
  - name: Mobile app performance troubleshooting
    id: mobile-app-performance-troubleshooting
    subtopics:
    - name: Fix mobile UI flicker
      id: fix-mobile-ui-flicker
      description: Wants to fix a screen element that flickers (rapid content or
        style changes) in a mobile app, caused by rapid data/state changes or
        delayed CSS style calculations, by reviewing conditional logic and
        aggregate timing or deferring element visibility until styles are ready,
        so that the transition is smooth.
    - name: iOS 12 viewport flicker
      id: ios-12-viewport-flicker
      description: Wants to fix the screen flickering that occurs on iPhones running iOS 12
        when the viewport-fit meta tag is set to cover on a screen with many
        input fields, in mobile apps built with MABS 4 or MABS 5, by applying
        the documented workaround for the shared native stack issue, so that the
        screen displays smoothly without flickering.
    - name: Fix slow transition
      id: fix-slow-transition
      description: Wants to fix a slow transition between two screens in a mobile
        app caused by slow operations (native plugin calls or server actions)
        running in the On Initialize event handler before the screen DOM loads,
        by moving them to On Ready or another later event handler, so that the
        destination screen loads quickly.
    - name: Fix viewport-fit empty bar
      id: fix-viewport-fit-empty-bar
      description: Wants to fix the empty bottom bar that appears above the home
        button on iPhone X devices after applying the documented viewport-fit
        meta tag workaround for the MABS 4.0 iOS 12 known issue, by changing
        the Status Bar background color and the WebView's default background
        color at runtime, so that the app appears full-screen without the
        empty bar.
    - name: iOS video upload issue
      id: ios-video-upload-issue
      description: 'Wants to understand why videos selected from the file
        system (not filmed in-app) fail to upload via the Upload widget
        in OutSystems mobile apps on iOS 10.3.1-10.3.3, due to Apple''s
        UIWebView changes to input type="file" fields that Apple has
        declined to fix, so they know this is a known, unresolved platform
        limitation rather than an OutSystems bug.'
  - name: Mobile app performance best practices
    id: mobile-app-performance-best-practices
    subtopics:
    - name: Prevent mobile content flicker
      id: prevent-mobile-content-flicker
      description: Wants to prevent flickering and layout shift while dynamic
        content loads on a mobile screen, by displaying an empty-state
        placeholder for asynchronously fetched content and setting explicit
        width and height on Image widgets, in OutSystems 11 mobile apps, so
        that the screen transitions smoothly without visual jank.
    - name: Prioritize mobile screen content
      id: prioritize-mobile-screen-content
      description: Wants to delay rendering of secondary screen content until
        after the main content loads, by placing the secondary content in a
        Block inside an If widget's True branch and toggling the condition
        variable from the On Render event, in OutSystems 11 mobile apps, so
        that the most relevant content appears first and perceived
        performance improves.
  - name: Data security
    id: mobile-data-security
    description: Wants to protect sensitive data in OutSystems Developer Cloud (ODC)
      mobile apps by minimizing local storage of sensitive information (keeping
      it server-side and fetching it only when needed over authenticated HTTPS
      connections), using the device's encrypted keystore, for example via the
      KeyStore Plugin, for small sensitive items like authentication tokens, and
      mandatorily re-validating all data server-side, so that mobile app data
      stays protected against device compromise and client-side tampering.
  - name: Mobile app security
    id: mobile-app-security
    description: Wants to reduce a mobile app's attack surface by limiting
      declared device permissions, configuring a restrictive CSP for the
      WebView, and pinning server certificates when the app loads external
      content or handles high-sensitivity data, so that the app resists common
      platform and network attacks.
  - name: Extensibility configurations
    id: extensibility-configurations
    subtopics:
    - name: App extensibility config
      id: app-extensibility-use-cases
      description: Wants to customize a mobile app's runtime and build-time behavior beyond
        the Mobile tab defaults, using the app extensibility configurations JSON
        schema, so that functionality not exposed by the Mobile tab UI can be
        configured.
    - name: Accessible domains
      id: accessible-domains
      description: 'Wants to configure the `access` extensibility configuration entry for a
        mobile app when they need to allowlist domains, block HTTP by allowing only HTTPS
        origins, or set the iOS-only optional fields that control ATS behavior.'
    - name: Custom splash screens
      id: custom-splash-screens
      description: Wants to customize a mobile app's native splash screen by packaging the
        required PNG variants in a ZIP file and configuring splash screen paths and
        preferences in the Extensibility Configurations JSON, so that the app shows the
        intended splash screen before it finishes loading.
    - name: Custom app icons
      id: custom-app-icons
      description: Wants to replace a mobile app's default icon with custom icon assets by
        packaging the icon variants in a ZIP file, importing it as a resource, and
        configuring the icons section of the app's Extensibility Configurations JSON, so
        that the published mobile app uses the custom icon.
    - name: Set app preferences
      id: set-app-preferences
      description: Wants to set mobile app preferences in the Extensibility Configurations JSON,
        including global and platform-specific values, so that the app uses the
        intended Cordova settings.
    - name: App icon sizes
      id: app-icon-sizes
      description: Wants to know the required iOS icon widths and heights and Android icon
        densities for a mobile app, so that they can include every icon variant
        in the app's extensibility configurations and avoid mobile package
        generation errors.
    - name: Override app config
      id: override-app-config
      description: Wants to override a mobile app's default extensibility configurations
        in LifeTime for a specific environment, so that different environments
        use different settings without manually changing and redeploying the
        app.
    - name: Extensibility JSON limit
      id: lifetime-extensibility-json-limit
      description: Wants to resolve the LifeTime Extensibility Configuration JSON character
        limit issue when the configuration exceeds the supported size, by
        upgrading to supported LifeTime and Platform Server versions or by
        editing the JSON in Service Studio, so they can keep the configuration
        within limits.
    - name: Library plugin config
      id: plugin-extensibility-use-cases
      description: Wants to customize a mobile library's plugin runtime and
        build-time behavior, such as specifying Cordova and Capacitor plugin
        sources, setting plugin preferences and configurations, declaring
        permissions and usage descriptions, defining installation variables,
        copying resources, and running build actions, using the library
        extensibility configurations JSON schema, so that Cordova and
        Capacitor plugins can be wrapped and configured for mobile apps.
    - name: Capacitor system bars
      id: capacitor-system-bars
      description: Wants to configure system bar style and status bar background settings in
        a Capacitor-based ODC mobile app, using systemBars.style and
        StatusBarBackgroundColor, so that Android and iOS system bars display
        with the intended icon contrast and background color.
    - name: Cordova system bars
      id: cordova-system-bars
      description: Wants to configure edge-to-edge behavior and system bar appearance in a
        Cordova-based ODC mobile app, using AndroidEdgeToEdge,
        EdgeToEdgeGlyphTheme, NavigationBarBackgroundColor, and
        StatusBarBackgroundColor, so that the app uses the right system bar
        backgrounds and glyph colors on Android.
    - name: Safe area insets
      id: safe-area-insets
      description: Wants to apply the safe-area-inset CSS variables, with env() fallbacks,
        in a mobile app layout, so that content stays clear of the status bar,
        navigation bar, and display cutouts when edge-to-edge is enabled.
  - name: Migrate Cordova app to Capacitor
    id: migrate-cordova-app-to-capacitor
    description: Wants to migrate an existing Cordova mobile app to Capacitor
      in ODC by updating its extensibility schema to the universal schema,
      checking plugin compatibility, rebuilding with MABS 12, and moving ODC
      Settings or Cordova hooks to supported Capacitor mechanisms, so that the
      app keeps working on the Capacitor runtime.
  - name: Mobile app updates
    id: mobile-app-updates
    subtopics:
    - name: Automatic app updates
      id: automatic-app-updates
      description: Wants to understand when a mobile app detects and applies
        published updates automatically, including when it checks for a new
        version and how seamless and attention-requiring upgrades behave, so
        that they can predict how production changes reach end users.
    - name: New build required
      id: new-build-required
      description: Wants to understand which mobile app changes require a new build or
        updated package on the device, including changes to branding, resources,
        plugins, and configuration, so that they can plan when a reinstall or
        republish is necessary.
  - name: OTA upgrade troubleshooting
    id: ota-upgrade-troubleshooting
    subtopics:
    - name: Fix cache manifest error
      id: cache-manifest-not-found-error
      description: 'Wants to resolve the "Failed to load cache manifest: File
        not found" error that leaves a mobile app stuck on the splash screen
        during an OTA upgrade, by reloading the app or reinstalling it, so
        that the app upgrade completes successfully.'
    - name: Fix corrupt cache manifest
      id: cache-manifest-corrupt-error
      description: 'Wants to resolve the "Cache manifest file is corrupt or
        invalid" error that causes a mobile app to stop working or fail to
        start, typically caused by insufficient device disk space corrupting
        the on-device cache manifest file, by checking available storage and
        reinstalling the app, so that the app works again.'
    - name: Fix hash mismatch error
      id: fix-hash-mismatch-error
      description: 'Wants to resolve the "Checksum failed for file" / "File
        is corrupt or invalid" hash mismatch errors that cause a mobile app
        OTA upgrade to roll back to the previous version, caused by network
        caching, CDN/proxy tampering, or deployment inconsistencies between
        the moduleinfo and the frontend running folder, by comparing hashes
        with the Hash Calculator tool and verifying that all frontends were
        deployed consistently, so that the OTA upgrade completes
        successfully.'
    - name: Fix local entity type error
      id: fix-local-entity-type-error
      description: 'Wants to resolve the "Upgrade failed - rolling back to
        previous application version" / "Unable to upgrade attribute" error
        caused by an incompatible Local Entity attribute data type change
        when the device already holds data for that entity, by rolling back
        the app and clearing the cache/data or reinstalling on affected
        devices, so that the OTA upgrade completes successfully.'
    - name: Database close error
      id: database-close-error
      description: 'Wants to understand the informative "failed to close
        database ... cannot be closed while a transaction is in progress"
        error that occurs when a mobile app closes its database connection
        on window unload while a transaction is still running, so that they
        can confirm it''s safe to ignore since it has no impact on the app.'
    - name: Resource not found error
      id: resource-not-found-error
      description: 'Wants to understand the informative "Unable to find
        resource <Resource>" error, visible only in Mobile Device Logs and
        accessible through ADB, that occurs when WebView requests a resource
        outside a cached frame or a locally-served Cordova plugin resource,
        so that they can confirm it''s safe to ignore since it has no impact
        on the end user.'
    - name: InputStream cache error
      id: inputstream-cache-error
      description: 'Wants to resolve the "Could not get InputStream while
        trying to get cache resource" error caused by a mobile app''s
        WebView requesting a file missing from the device file system,
        often due to low storage, which triggers the healing mechanism and
        may leave the app unusable if healing fails, by checking device
        storage, reloading the app, or reinstalling it, so that the cache
        resource loads and the app works again.'
    - name: Fix cache version switch error
      id: switch-cache-version-error
      description: 'Wants to resolve the "Unable to switch to cache version"
        error caused by a state mismatch between the client runtime and the
        native side during an OTA cache upgrade, by reloading the app or
        checking device storage and reinstalling it, so that the mobile app
        upgrades successfully.'
    - name: Fix OTA download timeout
      id: fix-ota-download-timeout
      description: Wants to resolve the "Aborting resources download... The request timed
        out" / "Upgrade failed - rolling back to the previous application
        version" error caused by a mobile device's poor network connection or
        large OTA resources exceeding the fixed per-resource download timeout,
        by reloading the app to retry the OTA upgrade or, if the new resources
        consistently fail, generating and distributing a new full application
        package, so that the OTA upgrade completes successfully.
    - name: OTA rollback behavior
      id: ota-rollback-behavior
      description: 'Wants to understand why a mobile app''s OTA upgrade rolls
        back to the previously cached version on failure and why
        already-downloaded resources are preserved via a checkpoint
        mechanism so the next OTA attempt doesn''t redownload everything,
        so they can anticipate runtime issues (like server action
        mismatches) after a failed OTA upgrade.'
    - name: Healing resource error
      id: healing-resource-error
      description: 'Wants to understand and resolve the "Healing resource
        <Resource> from prebundle: FAILED" error (and its "healing from the
        web" variant, sometimes accompanied by a "File is corrupt or
        invalid" checksum error), which leaves a mobile app in an invalid
        state recoverable only via an app store update, by reloading the
        app or reinstalling it from the app store, so that the app returns
        to a working state.'
  - name: Customize Mobile apps
    id: customize-mobile-apps
    subtopics:
    - name: Plugin not found error
      id: plugin-not-found-error
      description: 'Wants to resolve the OS-MABS-PLG-40000 "We couldn''t find
        Cordova plugin <plugin_name>" error, caused by MABS failing to
        resolve the specified Cordova plugin in the NPM registry or at the
        provided URL, by checking the build log and the plugin''s
        extensibility configurations, retrying app generation, and
        escalating to OutSystems Support if the problem persists, so that
        the mobile app package generates successfully.'
    - name: Invalid plugin error
      id: invalid-plugin-error
      description: 'Wants to resolve the OS-MABS-PLG-40001 "Invalid Cordova
        plugin <plugin_name>" error, caused by an invalid plugin identifier,
        URL, or resource (often a missing package.json) in the extensibility
        configurations, by checking the build logs on Service Center and
        fixing the configuration, so that the mobile app package generates
        successfully.'
    - name: Missing plugin variables error
      id: missing-plugin-variables-error
      description: 'Wants to resolve the OS-MABS-PLG-40002 "Couldn''t install
        the Cordova plugin <plugin_name> because one or more plugin
        variables are missing" error, caused by the plugin requiring
        variables that are missing from the extensibility configurations of
        the plugin module, by checking the build logs on Service Center and
        fixing the configuration, so that the mobile app package generates
        successfully.'
    - name: Detect device ready
      id: detect-device-ready
      description: Wants to wait until Cordova's native device APIs are fully
        loaded before running device-specific logic, by binding to the
        whenReady() JavaScript API promise instead of the deviceready DOM
        event, in ODC Mobile Apps, so that code safely accesses device
        properties (model, UUID, platform) only after Cordova initialization
        completes.
    - name: Build action errors
      id: build-action-errors
      description: Wants to troubleshoot a failed Capacitor build action caused
        by invalid JSON syntax, incorrect target selectors/XPath, unresolved
        variable references, or malformed merge/inject content in the build
        action configuration file, so that the mobile app build completes
        successfully.
    - name: Fix empty file error
      id: fix-empty-extensibility-file-error
      description: 'Wants to resolve the "Binary file for <resource-type>
        ''<resource-name>'' cannot be null or empty" error that blocks
        updating a mandatory extensibility setting or defining a mandatory
        mobile configuration during new package creation, caused by
        uploading a file with no contents or only zero bytes, by replacing
        it with a file that contains data, so that the extensibility
        setting update or package creation can proceed.'
    - name: Fix empty secret error
      id: fix-empty-secret-error
      description: 'Wants to resolve the "Secret for <resource-type>
        ''<resource-name>'' cannot be empty" error that blocks updating a
        mandatory extensibility setting or defining a mandatory mobile
        configuration during new package creation, caused by saving an
        empty-string value for a secret setting, by providing a valid
        value, so the extensibility setting update or package creation
        can proceed.'
    - name: Fix invalid property type
      id: invalid-property-type-error
      description: 'Wants to resolve the OS-MABS-CNF-40005 "Invalid {0} type
        in your extensibility configurations" error, caused by an invalid
        property type in the app''s extensibility configuration or by
        referencing a Secret/Binary Setting in ODC that has no value set,
        by reviewing and fixing the extensibility configuration or setting
        the referenced Setting''s value, so that the mobile app package
        generates successfully.'
    - name: Invalid preference key
      id: invalid-preference-key-error
      description: 'Wants to resolve the OS-MABS-PREF-40000 "Preference key
        <key> in the context <context> is not recognized" error, caused by
        an invalid key in the app''s extensibility configuration schema, by
        fixing the configuration according to the Property Schema
        documentation and rebuilding the app, so that the mobile app
        package generates successfully.'
    - name: Wrong preference type
      id: wrong-preference-type-error
      description: 'Wants to resolve the OS-MABS-PREF-40002 "Preference key
        <key> in <context> has a wrong type" error, caused by an invalid
        property type for a key in the app''s extensibility configuration
        schema, by fixing the configuration according to the Property
        Schema documentation and rebuilding the app, so that the mobile
        app package generates successfully.'
    - name: Fix array format error
      id: wrong-preference-format-error
      description: 'Wants to resolve the OS-MABS-PREF-40003 "<context> must
        have the format []" error, caused by a property in the app''s
        extensibility configuration that must be a JSON array but isn''t,
        by fixing the configuration according to the Property Schema
        documentation and rebuilding the app, so that the mobile app
        package generates successfully.'
    - name: Fix object format error
      id: wrong-preference-object-format-error
      description: 'Wants to resolve the OS-MABS-PREF-40004 "Preferences must
        have the format {}." error, caused by a property in the app''s
        extensibility configuration that must be a JSON object but isn''t,
        by fixing the configuration according to the Property Schema
        documentation and rebuilding the app, so that the mobile app
        package generates successfully.'
    - name: Missing preference key
      id: missing-preference-key-error
      description: 'Wants to resolve the OS-MABS-PREF-40001 "Preference key
        <key> in <context> wasn''t found" error, caused by a mandatory key
        missing from the app''s extensibility configuration, by adding the
        key to the context per the Property Schema documentation and
        rebuilding the app, so that the mobile app package generates
        successfully.'
    - name: Missing resource error
      id: missing-resource-error
      description: 'Wants to resolve the OS-MABS-CNF-40012 "resource {0} declared
        in your extensibility configurations can''t be found" error, caused by
        an extensibility configuration''s resources entry pointing to a missing
        file, by checking and correcting the resource declarations for their
        app and its dependencies, so that the mobile app package generates
        successfully.'
    - name: Resource file missing
      id: resource-file-missing-error
      description: 'Wants to resolve the OS-MABS-PRJ-40004 "Resource file declared
        in extensibility configurations can''t be found" error, caused by a
        literal file path pointing to a missing resource in the app''s or a
        library''s extensibility configuration, by replacing the literal path
        with a placeholder or correcting it to an existing file, so that the
        mobile app package generates successfully.'
    - name: No resources file error
      id: no-resources-file-error
      description: 'Wants to resolve the OS-MABS-CNF-40004 "No Resources file
        found. Check your extensibility configurations and try again." error,
        caused by a resource path declared in the extensibility configuration
        not existing, by checking the extensibility configurations to ensure
        the resource zip file exists and the declared path is correct, so
        that the mobile app package generates successfully.'
    - name: Restricted path error
      id: restricted-path-error
      description: 'Wants to resolve the OS-MABS-CNF-40011 "the {0} property
        of a resource declared in your extensibility configurations points
        to a restricted path" error, caused by a resource''s src or target
        property resolving outside the Cordova project root or platform
        project root folder, by checking and correcting the resource
        declarations in the extensibility configurations of their app and
        its dependencies, so that the mobile app package generates
        successfully.'
    - name: Android manifest error
      id: android-manifest-error
      description: 'Wants to resolve the OS-MABS-GEN-40001 "multiple errors merging
        the Android Manifest" error, by checking the Service Center build logs
        to identify the custom plugins causing the incompatibility, so that the
        Android mobile app package generates successfully.'
    - name: Missing exported attribute
      id: missing-exported-attribute-error
      description: 'Wants to resolve the OS-MABS-GEN-40016 "Couldn''t generate
        your app. For "<type>" type elements with defined intent filters, you
        must declare the value for the attribute "android:exported" in all
        AndroidManifest.xml files." error, caused by using custom plugins with
        MABS 8.0 or higher that define intent filters without declaring the
        android:exported attribute, by declaring the attribute and its value in
        the AndroidManifest.xml files for the affected type elements, so that
        the Android mobile app package generates successfully.'
    - name: Gradle dependency error
      id: gradle-dependency-error
      description: 'Wants to resolve the OS-MABS-GEN-40017 "There was an issue
        generating the app. Gradle now requires you to declare dependencies
        using ''implementation'' or ''api'' instead of ''compile''" error,
        caused by a custom plugin using a Gradle compile dependency in MABS
        8 or higher, by checking the Service Center build logs to identify
        the affected plugin and updating its Gradle files to use
        implementation or api instead of compile, so that the Android
        mobile app package generates successfully.'
    - name: Fix file not found error
      id: mabs5-file-not-found-error
      description: 'Wants to resolve a "wasn''t found" / ENOENT build error
        caused by a Cordova plugin trying to access a file at its old path
        after the Cordova-android package update (to 8.0.0) moved Android
        project files into a new :app subdirectory in MABS 5.0, by checking
        whether the OneSignal Plugin version matches the MABS version,
        updating or reconfiguring affected plugins, or adapting the plugin
        to the new directory, so that the mobile app build succeeds.'
    - name: Fix minimum SDK incompatibility
      id: min-sdk-incompatibility-error
      description: 'Wants to resolve a "requires minimum Android SDK X, which
        is higher than Y required by the application" build error caused by
        a plugin library needing a higher minimum Android SDK than the
        app''s configured value, by checking whether the minimum SDK is
        overridden in the Extensibility Configurations and adjusting it or
        using a compatible plugin library version, so that the mobile app
        build succeeds.'
    - name: PNG resource error
      id: png-resource-error
      description: 'Wants to resolve the OS-MABS-GEN-40002 "We couldn''t process
        your resources because at least one file isn''t in PNG format" error, by
        checking the Service Center build logs to identify the non-PNG resource
        file and replacing it with a valid PNG file, so that the Android mobile
        app package generates successfully.'
    - name: Missing plugin file error
      id: missing-plugin-file-error
      description: 'Wants to resolve the OS-MABS-GEN-40005 "There was an issue
        generating the app because {0} ''{1}'' wasn''t found" error, caused
        by a custom plugin referencing a file that doesn''t exist, by
        checking the Service Center build logs to identify the affected
        plugin and updating it, so that the Android mobile app package
        generates successfully.'
    - name: Support library error
      id: support-library-error
      description: 'Wants to resolve the OS-MABS-GEN-40006 "there was an issue
        generating the app" error, caused by a custom Cordova plugin using
        deprecated Android Support Library components, by identifying the
        plugin(s) via com.android.support references and migrating them to
        AndroidX, so that the Android mobile app package generates
        successfully.'
    - name: Swift language error
      id: swift-language-error
      description: 'Wants to resolve the OS-MABS-GEN-40003 "There was an issue
        generating the app. At least one Cordova plugin used in the build
        requires Swift, but no ''Swift Language Version'' build setting was
        defined" error, caused by a custom Cordova plugin requiring Swift
        without a Swift Language Version build setting defined for the iOS
        project, by checking the Service Center build logs to identify the
        plugin and configuring the required Swift Language Version setting,
        so that the iOS mobile app package generates successfully.'
    - name: Fix plugin Swift version
      id: fix-plugin-swift-version
      description: 'Wants to resolve the OS-MABS-GEN-40014 error ("...Cordova
        plugin {0} is not compatible with the ''Swift Language Version''
        build setting...") that blocks iOS app generation, by checking the
        build logs in Service Center to identify the incompatible custom
        plugin and updating its configuration to match the defined Swift
        Language Version, so that iOS app generation completes successfully.'
    - name: Metadata conflict error
      id: metadata-conflict-error
      description: 'Wants to resolve the OS-MABS-GEN-40008 "meta-data {0} is
        being defined more than once" error, by identifying which custom
        plugin is overriding the same Android manifest metadata attribute
        more than once, so that the Android mobile app package generates
        successfully.'
    - name: Google Services error
      id: google-services-error
      description: 'Wants to resolve the OS-MABS-GEN-40011 "There was an issue
        generating the app. Some plugins using Google Services aren''t
        correctly configured" error, by reviewing and correcting the Google
        Services plugin configuration file setup, so that the Android mobile
        app package generates successfully.'
    - name: Non-Cordova module load error
      id: non-cordova-module-load-error
      description: 'Wants to resolve the OS-MABS-GEN-40013 "Using
        requireCordovaModule to load a non-cordova module is not supported"
        error, by checking the Service Center build logs to identify the
        custom plugin using requireCordovaModule and replacing it with
        require, so that the mobile app package generates successfully.'
    - name: Whitelist plugin error
      id: whitelist-plugin-error
      description: 'Wants to resolve the OS-MABS-GEN-40018 "There was an issue
        generating the app. Whitelist Plugin was renamed AllowList in newer
        Cordova Android engine versions. Check your plugin code and try
        again." error, caused by a custom plugin referencing the Cordova
        Android Whitelist plugin that was renamed to AllowList starting in
        Cordova Android 10, by checking the Service Center build logs to
        identify the affected plugin and updating its Whitelist references
        to Allowlist, so that the Android mobile app package generates
        successfully.'
    - name: Cordova build failure
      id: cordova-build-failure-error
      description: 'Wants to resolve the OS-MABS-GEN-50000 "Couldn''t generate
        your app. If the problem persists, check our documentation for more
        information." error, by regenerating the app package and checking the
        build logs in Service Center for O11 or ODC Portal for ODC, so that
        the mobile app package generates successfully.'
    - name: Android build tools error
      id: android-build-tools-error
      description: 'Wants to resolve the OS-MABS-GEN-40020 "Couldn''t generate
        your app. It''s configured to use Android Build Tools version
        <used_version>, but the version can''t be modified. Check if it''s
        being overridden in your extensibility configurations or via a
        plugin." error, caused by the android-buildToolsVersion preference
        being overridden in the app''s extensibility configurations or via a
        specialized hook in a Cordova plugin, by removing the override from
        the extensibility configurations or identifying the offending
        plugin, so that the Android mobile app package generates
        successfully.'
    - name: Kotlin version error
      id: kotlin-version-error
      description: 'Wants to resolve the OS-MABS-GEN-40021 "Couldn''t generate
        your app. Kotlin version <used_version> isn''t compatible with this
        app. Update it to <minimum_version> or higher." error, caused by the
        GradlePluginKotlinVersion preference in the Extensibility
        Configurations or a plugin''s specialized hook setting an
        incompatible Kotlin version, by updating the preference to a
        compatible version or checking whether a plugin is overriding it,
        so that the mobile app package generates successfully.'
    - name: Invalid CocoaPods spec error
      id: invalid-cocoapods-spec-error
      description: 'Wants to resolve the OS-MABS-PLG-40005 "Spec attribute in
        the Framework element related to CocoaPods is invalid" error, by
        checking the Service Center build logs to identify the misconfigured
        Cordova plugin''s plugin.xml, so that the iOS mobile app package
        generates successfully.'
    - name: CocoaPods pod not found
      id: cocoapods-pod-not-found-error
      description: 'Wants to resolve the OS-MABS-PLG-40006 "Couldn''t install
        the Cordova plugin due to an error in the plugin.xml file. The
        specified pod wasn''t found in the CocoaPods repository" error, by
        checking the Service Center build logs and fixing the pod dependency
        on the affected Cordova plugin, so that the mobile app package
        generates successfully.'
    - name: Fix iOS deployment target
      id: fix-ios-deployment-target
      description: 'Wants to resolve the OS-MABS-PLG-40007 "Couldn''t install
        the Cordova plugin <plugin_name>. You must set a higher minimum iOS
        deployment target for a specific pod in the Extensibility
        Configurations property of your home module." error, caused by a
        Cordova plugin needing a higher minimum iOS deployment target than
        the app uses, by raising the deployment-target preference for the
        affected pod in the home module''s Extensibility Configurations or
        changing the referenced plugin, so that the app package generates
        successfully.'
    - name: Swift CocoaPods error
      id: swift-cocoapods-error
      description: 'Wants to resolve the OS-MABS-PLG-40008 "Couldn''t install
        the Cordova plugin because CocoaPods built in Swift are not
        supported" error, by importing the affected pod as a framework in
        the plugin''s podspec, so that the iOS mobile app package generates
        successfully.'
    - name: Cordova version mismatch
      id: cordova-version-mismatch-error
      description: 'Wants to resolve the OS-MABS-PLG-40009 "Couldn''t install
        the Cordova plugin because your plugin doesn''t support this
        Cordova version" error, by checking the build log to identify the
        incompatible plugin and either updating the plugin or changing the
        MABS version, so that the mobile app package generates
        successfully.'
    - name: Plugin config mismatch
      id: plugin-config-mismatch-error
      description: 'Wants to resolve the OS-MABS-PLG-40010 "Couldn''t install
        the Cordova plugin. Your plugin must have a package.json file that
        matches the configurations in the plugin.xml file" error, by checking
        the build logs on Service Center to identify the mismatch between the
        plugin''s package.json and plugin.xml files and correcting the plugin
        configuration, so that the mobile app package generates
        successfully.'
    - name: Invalid plugin spec error
      id: invalid-plugin-spec-error
      description: 'Wants to resolve the OS-MABS-PLG-40011 "Couldn''t install
        the Cordova plugin due to an issue with the plugin specification" error,
        by checking the build logs on Service Center to identify the mismatch
        in plugin name, version, or URL between the plugin.xml and package.json
        files, so that the mobile app package generates successfully.'
    - name: Plugin SDK tag error
      id: plugin-sdk-tag-error
      description: 'Wants to resolve the OS-MABS-PLG-40012 "Couldn''t install
        the Cordova plugin because it''s not supported by MABS 6.0 and later
        versions" error, caused by the plugin trying to change the
        <uses-sdk> tag in the AndroidManifest.xml file, by updating the
        plugin or changing the MABS version, so that the mobile app package
        generates successfully.'
    - name: Missing XML node error
      id: missing-xml-node-error
      description: 'Wants to resolve the OS-MABS-PLG-40013 "Couldn''t install
        the Cordova plugin because the plugin tried to edit a missing XML
        node" error, caused by an XML selector not finding the target node
        in the config file, by checking the Service Center build logs and
        fixing the selector path, so that the mobile app package generates
        successfully.'
    - name: Non-Cordova module error
      id: non-cordova-module-error
      description: 'Wants to resolve the OS-MABS-PLG-40015 "Couldn''t install
        the Cordova plugin. Using requireCordovaModule to load a non-Cordova
        module is not supported" error, by checking the Service Center build
        logs to identify the plugin using requireCordovaModule and replacing
        it with require, so that the mobile app package generates
        successfully.'
    - name: Missing module dependency error
      id: missing-module-dependency-error
      description: 'Wants to resolve the OS-MABS-PLG-40016 "Couldn''t install
        the Cordova plugin due to a missing dependency in module
        {node_module}" error, by checking the Service Center build logs to
        identify the affected plugin and fixing its missing node module
        dependency, including hooks dependencies declared in the plugin''s
        package.json for MABS 7 and later, so that the mobile app package
        generates successfully.'
    - name: Plugin dependency error
      id: plugin-dependency-error
      description: 'Wants to resolve the OS-MABS-PLG-40017 "Couldn''t install
        the Cordova plugin <plugin_name> because the plugin dependency
        <plugin_dependency> doesn''t support this Cordova version" error, by
        checking the build log on Service Center to identify the
        incompatible plugin dependency and either updating the plugin or
        changing the MABS version, so that the mobile app package generates
        successfully.'
    - name: Missing hook node module
      id: missing-hook-node-module-error
      description: 'Wants to resolve the OS-MABS-PLG-20000 "Couldn''t install
        the Cordova plugin <plugin_name> because the required dependency
        for the ''<dependency_name>'' node module in a plugin hook was
        missing" error, by declaring all hook dependencies in the plugin''s
        package.json, escalating to OutSystems Support if the problem
        persists, so that the mobile app package generates successfully.'
    - name: Blocked plugin security error
      id: blocked-plugin-security-error
      description: 'Wants to resolve the OS-MABS-PLG-20001 "Couldn''t install
        the Cordova plugin <plugin_name> because an action was blocked due
        to security concerns. Contact our support team if this problem
        persists." error, by checking the build logs and working around the
        imposed security limitation, escalating to OutSystems Support if the
        problem persists, so that the mobile app package generates
        successfully.'
    - name: Plugin min version error
      id: plugin-min-version-error
      description: 'Wants to resolve the OS-MABS-PLG-40018 "<plugin_id>
        version <plugin_version> must be <plugin_min_version> or higher"
        error, caused by using a Cordova plugin version that isn''t
        compatible with the MABS version (MABS 7 or higher), by updating
        the plugin to the minimum compatible version, so that the mobile
        app package generates successfully.'
    - name: Forge plugin version error
      id: forge-plugin-version-error
      description: 'Wants to resolve the OS-MABS-PLG-40019 "<plugin_name>"
        error, caused by using a Forge plugin version that isn''t compatible
        with MABS 8.0 or higher, by updating the plugin to its latest
        version, so that the mobile app package generates successfully.'
    - name: Plugin install failure
      id: plugin-install-failure
      description: 'Wants to resolve the OS-MABS-PLG-40021 "Couldn''t install
        the Cordova plugin <plugin_id>. <reason>" error, caused by the
        plugin failing to install for a reason specified by the plugin''s
        own developer, by following the developer''s instructions in the
        reported reason or contacting them for more information, so that
        the mobile app package generates successfully.'
    - name: Plugin source URL error
      id: plugin-source-url-error
      description: 'Wants to resolve the OS-MABS-PLG-50002 "Couldn''t fetch
        the Cordova plugin <plugin_name>. If the problem persists, check
        our documentation for more information." error, caused by an
        unreachable Cordova plugin source URL or a network issue while
        generating the mobile app package, by verifying the plugin URL and
        network connectivity and retrying the build, so that the mobile app
        package generates successfully.'
    - name: Generic plugin error
      id: generic-plugin-error
      description: 'Wants to resolve the OS-MABS-PLG-50003 "Couldn''t install
        the Cordova plugin <plugin_name>. If the problem persists, check our
        documentation for more information." error, caused by an issue with
        the plugin''s hook or a malformed plugin missing configurations or
        files, by confirming the plugin''s hooks and configurations are
        correct or checking the plugin''s documentation if it''s an
        OutSystems-supported plugin, and creating a support case if the
        problem persists, so that the mobile app package generates
        successfully.'
    - name: CocoaPods plugin fetch error
      id: cocoapods-plugin-fetch-error
      description: 'Wants to resolve the OS-MABS-PLG-50005 "Couldn''t install
        the Cordova plugin <plugin_name> because we couldn''t fetch its
        dependencies due to a CocoaPods server error. Please try again."
        error, caused by downtime on the CocoaPods CDN preventing MABS from
        fetching a Pod for the plugin, by checking the build logs on
        Service Center and retrying app generation, and creating a support
        case if the problem persists, so that the mobile app package
        generates successfully.'
    - name: Incompatible plugin error
      id: incompatible-plugin-error
      description: 'Wants to resolve the OS-MABS-PRJ-40003 "One or more
        Capacitor plugins aren''t compatible with <PackageManager>" error,
        caused by the code project''s selected package manager (SPM or
        CocoaPods) not supporting one or more required Capacitor plugins, by
        checking the build logs to identify the incompatible plugins and
        updating libraries, toggling the spmPreview configuration, or
        removing the affected plugins/libraries, so that the iOS mobile app
        package generates successfully.'
    - name: SSL Pinning domain error
      id: ssl-pinning-domain-error
      description: 'Wants to resolve the OS-MABS-RES-40001 "The configuration
        file for SSLPinning must not contain OutSystems domains" error, by
        reviewing the SSLPinning JSON configuration file and removing any
        OutSystems domains, so that the mobile app package generates
        successfully.'
    - name: SSL Pinning config error
      id: ssl-pinning-config-error
      description: 'Wants to resolve the OS-MABS-RES-40002 "The configuration
        file is missing. Add a valid configuration file for SSLPinning."
        error, by adding a valid SSLPinning configuration file and retrying
        app generation when the SSL Pinning plugin is in use, so that MABS
        can generate the mobile app package successfully.'
    - name: SSL Pinning format error
      id: ssl-pinning-format-error
      description: 'Wants to resolve the OS-MABS-RES-40003 "The configuration
        file has an invalid format. Add a file with a valid format for
        SSLPinning" error, by validating and correcting the SSLPinning
        configuration file''s format and content, so that the mobile app
        package generates successfully.'
    - name: Fetch resources error
      id: fetch-resources-error
      description: 'Wants to resolve the OS-MABS-RES-50002 "Couldn''t fetch
        your app resources" error, by checking the build logs for details
        and retrying application generation, escalating to OutSystems
        support if the problem persists, so that the mobile app package
        generates successfully.'
    - name: Resource processing error
      id: resource-processing-error
      description: 'Wants to resolve the OS-MABS-RES-50001 "Something happened
        on our side while packaging your app. Please try again." error
        that occurs when MABS fails while processing app resources such
        as icons, splash screens, or custom plugins, by validating the
        added resource files and reviewing the Service Center build logs,
        so that the mobile app package generates successfully.'
    - name: Resource load timeout
      id: resource-load-timeout-error
      description: 'Wants to resolve the OS-MABS-UNX-50000 "There was an issue
        loading your resources. Please try again." error, caused by MABS
        timing out while building the mobile app package (fetching
        application resources, installing custom plugins, or downloading
        native libraries from Maven or CocoaPods repositories), by retrying
        the operation and checking the build logs in Service Center,
        escalating to OutSystems Support if the problem persists, so that
        the mobile app package generates successfully.'
    - name: Signing certificate error
      id: signing-certificate-error
      description: 'Wants to resolve the OS-MABS-SIG-40010 "There are no
        certificates that match the provisioning profile" error, by
        confirming and updating the certificate and provisioning profile
        information so they match each other, so that the mobile app
        package generates successfully.'
    - name: Cert password error
      id: certificate-password-error
      description: 'Wants to resolve the OS-MABS-SIG-40003 "Please enter the correct password for this certificate." error by verifying that the password for the iOS signing certificate matches the uploaded certificate when generating a mobile app package in MABS, so that the app build completes successfully.'
    - name: App ID/profile mismatch
      id: app-id-provisioning-mismatch
      description: 'Wants to resolve the OS-MABS-SIG-40009 "Your application
        identifier and provisioning profile don''t match" error, by
        confirming and updating the application identifier and provisioning
        profile information so they match each other, so that the mobile
        app package generates successfully.'
    - name: Dev app type mismatch
      id: dev-app-type-mismatch
      description: 'Wants to resolve the OS-MABS-SIG-40011 "There was an
        issue generating the app. Please check if your app identifier,
        certificate, and provisioning profile match the Development
        application type." error, by confirming and updating the app
        identifier, certificate, and provisioning profile so they match the
        Development application type, so that the mobile app package
        generates successfully.'
    - name: Fix build type mismatch
      id: fix-build-type-mismatch
      description: Wants to fix the "application build type must match the
        provisioning profile" error when generating an iOS mobile app, by
        checking that the provisioning profile matches the requested build
        type and adjusting either the build type or the provisioning
        profile, so that the app package can be generated successfully.
    - name: APS environment mismatch
      id: aps-environment-mismatch-error
      description: 'Wants to resolve the OS-MABS-SIG-40001 "The APS Environment
        <value> doesn''t match your provisioning profile" error, caused by an
        iOS app''s APS Environment entitlement value not matching the
        provisioning profile''s declared value, by fixing the mismatch and
        escalating to OutSystems Support if the problem persists, so that the
        mobile app package generates successfully.'
    - name: Certificate format error
      id: certificate-format-error
      description: 'Wants to resolve the OS-MABS-SIG-40004 "This certificate
        has an unknown file format." error, caused by MABS being unable to
        parse an iOS app''s certificate file, by checking that the
        certificate uses the .p12 format and replacing it if needed, so that
        the mobile app package generates successfully.'
    - name: Provisioning format error
      id: provisioning-format-error
      description: 'Wants to resolve the OS-MABS-SIG-40006 "Your provisioning
        profile has an unknown format." error, caused by MABS being unable
        to parse an iOS app''s provisioning profile file, by checking that
        the provisioning profile is valid and uses the .mobileprovision
        format and replacing it if needed, so that the mobile app package
        generates successfully.'
    - name: Certificate keys error
      id: certificate-keys-error
      description: 'Wants to resolve the OS-MABS-SIG-40008 "There was an
        issue reading the certificate keys." error, caused by MABS failing
        to process or find the necessary information in an iOS app''s
        provisioning profile, by checking that the provisioning profile is
        valid and uses the .mobileprovision format and replacing it if
        needed, escalating to OutSystems Support if the problem persists,
        so that the mobile app package generates successfully.'
    - name: Invalid keystore error
      id: invalid-keystore-error
      description: 'Wants to resolve the OS-MABS-SIG-40012 "The keystore
        format is invalid" error, caused by MABS being unable to parse an
        Android app''s keystore file, by checking that the keystore is in
        the correct format and content and replacing it if needed, so that
        the Android mobile app package generates successfully.'
    - name: Keystore password error
      id: keystore-password-error
      description: 'Wants to resolve the OS-MABS-SIG-40013 "Please enter the
        right password for the keystore" error, caused by entering an
        incorrect password when generating an Android app, by checking and
        correcting the keystore password and retrying, escalating to
        OutSystems Support if the problem persists, so that the Android
        mobile app package generates successfully.'
    - name: Alias password error
      id: keystore-alias-password-error
      description: 'Wants to resolve the OS-MABS-SIG-40014 "Please enter the
        right password for the alias." error, caused by entering an
        incorrect alias password when generating an Android app, by
        checking and correcting the alias password and retrying,
        escalating to OutSystems Support if the problem persists, so that
        the Android mobile app package generates successfully.'
    - name: Provisioning expiry error
      id: provisioning-expiry-error
      description: 'Wants to resolve the OS-MABS-SIG-40015 "Your provisioning
        profile expired on <expiration_date>. Upload a new one and retry."
        error, caused by using an expired iOS provisioning profile, by
        renewing or uploading a valid provisioning profile with a valid
        expiration date, escalating to OutSystems Support if the problem
        persists, so that the mobile app package generates successfully.'
    - name: Missing entitlement error
      id: missing-entitlement-error
      description: 'Wants to resolve the OS-MABS-SIG-40016 "Your provisioning
        profile <provision_profile> is missing the <entitlement>
        entitlement" (or missing multiple entitlements) error, caused by the
        provisioning profile used to generate the iOS app lacking the
        required entitlements, by checking the provisioning profile''s
        capabilities against the needed entitlements and fixing the profile,
        so that the mobile app package generates successfully.'
    - name: Xcode profile mismatch
      id: xcode-profile-mismatch
      description: 'Wants to resolve the OS-MABS-SIG-40017 "Signing settings
        require a manually managed profile, but your provisioning profile
        <provision_profile> is Xcode managed." error, by replacing the Xcode
        managed provisioning profile with a manually created one, so that
        the iOS mobile app package generates successfully.'
    - name: Certificate expired error
      id: certificate-expired-error
      description: 'Wants to resolve the OS-MABS-SIG-40018 "This certificate
        expired on <expiration_date>. Create a new certificate or check our
        documentation to know how." error, caused by using an expired iOS
        signing certificate, by renewing or uploading a valid, unexpired
        certificate and retrying, escalating to OutSystems Support if the
        problem persists, so that the mobile app package generates
        successfully.'
    - name: Keystore alias cert error
      id: keystore-alias-cert-error
      description: 'Wants to resolve the OS-MABS-SIG-40019 "There was an
        issue reading the alias from keystore file. Please review both the
        keystore file and alias." (Android) or "There are no certificates
        available. Check your .p12 file or create a new one." (iOS) error,
        caused by an invalid Android keystore file or an iOS .p12 file
        lacking a valid certificate, by checking and replacing the keystore
        or certificate file as needed, escalating to OutSystems Support if
        the problem persists, so that the mobile app package generates
        successfully.'
    - name: App identifier error
      id: invalid-app-identifier-error
      description: 'Wants to resolve the OS-MABS-SIG-40020 "It looks like the
        application identifier is invalid." error, caused by the application
        identifier having an invalid format, by checking and correcting the
        identifier so it follows the required format, so that the mobile app
        package generates successfully.'
    - name: MABS version fetch error
      id: mabs-version-fetch-error
      description: 'Wants to resolve the OS-MCO-50020 "An error occurred
        while fetching the MABS version. If the problem persists, open a
        support case." error, an unspecified internal error with no more
        specific cause, by retrying the mobile compilation and opening a
        support case with OutSystems Support if the problem persists, so
        that the native mobile build completes successfully.'
    - name: MABS version not found error
      id: mabs-version-not-found-error
      description: 'Wants to resolve the OS-MCO-50021 "MABS version couldn''t
        be found or is obsolete. If the problem persists, open a support
        case." error, an unspecified internal error with no more specific
        cause, by retrying the mobile compilation and opening a support case
        with OutSystems Support if the problem persists, so that the native
        mobile build completes successfully.'
    - name: Invalid app icon
      id: invalid-app-icon-error
      description: 'Wants to resolve the OS-MCO-50014 "The app icon is
        invalid. If the problem persists, open a support case." error, an
        unspecified internal error with no more specific cause, by retrying
        the mobile compilation and opening a support case with OutSystems
        Support if the problem persists, so that the native mobile build
        completes successfully.'
    - name: Missing app icon
      id: missing-app-icon-error
      description: 'Wants to resolve the OS-MCO-40015 "The app doesn''t have
        an icon. Define an icon for the app in ODC Studio." error by
        defining an app icon in the app properties in ODC Studio and
        rebuilding the mobile app, so that the native build succeeds.'
    - name: Missing primary color
      id: missing-primary-color
      description: 'Wants to resolve the OS-MCO-40016 "The app doesn''t have a primary color. Define a primary color for the app in ODC Studio." error by defining a primary color in the app properties in ODC Studio and rebuilding the mobile app, so that the native build succeeds.'
    - name: Invalid primary color error
      id: invalid-primary-color-error
      description: 'Wants to resolve the OS-MCO-50015 "The primary color is
        invalid. If the problem persists, open a support case." error, an
        unspecified internal error with no more specific cause, by retrying
        the mobile compilation and opening a support case with OutSystems
        Support if the problem persists, so that the native mobile build
        completes successfully.'
    - name: Generic compile error
      id: generic-compile-error
      description: 'Wants to resolve the OS-MCO-50001 "An error occurred. We''ll
        look into it soon. Please try again, and if the problem persists, open
        a support case." error, an unspecified internal error with no more
        specific cause, by retrying the mobile compilation and opening a
        support case with OutSystems Support if the problem persists, so that
        the native mobile build completes successfully.'
    - name: Manifest unavailable error
      id: manifest-unavailable-error
      description: 'Wants to resolve the OS-MCO-50006 "The project manifest
        is not available. If the problem persists, open a support case."
        error, an unspecified internal error with no more specific cause,
        by retrying the mobile compilation and opening a support case with
        OutSystems Support if the problem persists, so that the native
        mobile build completes successfully.'
    - name: App manifest unavailable error
      id: app-manifest-unavailable-error
      description: 'Wants to resolve the OS-MCO-50007 "The app manifest is
        not available. If the problem persists, open a support case."
        error, an unspecified internal error with no more specific cause,
        by retrying the mobile compilation and opening a support case with
        OutSystems Support if the problem persists, so that the native
        mobile build completes successfully.'
    - name: Package listing error
      id: package-listing-error
      description: 'Wants to resolve the OS-MCO-50008 "An error occurred
        while listing the versions of a package. If the problem persists,
        open a support case." error, an unspecified internal error with no
        more specific cause, by retrying the mobile compilation and opening
        a support case with OutSystems Support if the problem persists, so
        that the native mobile build completes successfully.'
    - name: Package fetch error
      id: package-fetch-error
      description: 'Wants to resolve the OS-MCO-50009 "An error occurred
        while fetching package <Package>. If the problem persists, open a
        support case." error, an unspecified internal error with no more
        specific cause, by retrying the mobile compilation and opening a
        support case with OutSystems Support if the problem persists, so
        that the native mobile build completes successfully.'
    - name: AVS deserialize error
      id: avs-deserialize-error
      description: 'Wants to resolve the OS-MCO-50011 "An error occurred
        while deserializing data from AVS. If the problem persists, open a
        support case." error, an unspecified internal error with no more
        specific cause, by retrying the mobile compilation and opening a
        support case with OutSystems Support if the problem persists, so
        that the native mobile build completes successfully.'
    - name: BS fetch error
      id: bs-fetch-error
      description: 'Wants to resolve the OS-MCO-50012 "An error occurred
        while fetching data from BS. If the problem persists, open a
        support case." error, an unspecified internal error with no more
        specific cause, by retrying the mobile compilation and opening a
        support case with OutSystems Support if the problem persists, so
        that the native mobile build completes successfully.'
    - name: BS deserialize error
      id: bs-deserialize-error
      description: 'Wants to resolve the OS-MCO-50013 "An error occurred
        while deserializing data from BS. If the problem persists, open a
        support case." error, an unspecified internal error with no more
        specific cause, by retrying the mobile compilation and opening a
        support case with OutSystems Support if the problem persists, so
        that the native mobile build completes successfully.'
    - name: Document storage error
      id: document-storage-error
      description: 'Wants to resolve the OS-MCO-50002 "A document is not
        available in the document storage" error, by retrying the mobile
        compilation and escalating to OutSystems Support if the problem
        persists, so that the native mobile build completes successfully.'
    - name: Storage comms error
      id: storage-comms-error
      description: 'Wants to resolve the OS-MCO-50003 "An error occurred
        while communicating with the document storage" error, by retrying
        the mobile compilation and escalating to OutSystems Support if the
        problem persists, so that the native mobile build completes
        successfully.'
    - name: Object storage error
      id: object-storage-error
      description: 'Wants to resolve the OS-MCO-50004 "An error occurred
        while communicating with the object storage. If the problem
        persists, open a support case." error, by retrying the mobile
        compilation and escalating to OutSystems Support if the problem
        persists, so that the native mobile build completes successfully.'
    - name: Object deserialize error
      id: object-deserialize-error
      description: 'Wants to resolve the OS-MCO-50005 "An error occurred
        while attempting to deserialize an object from the object storage.
        If the problem persists, open a support case." error, by retrying
        the mobile compilation and escalating to OutSystems Support if the
        problem persists, so that the native mobile build completes
        successfully.'
    - name: Template package error
      id: template-package-error
      description: 'Wants to resolve the OS-MCO-50016 "The template NPM
        package is invalid. If the problem persists, open a support case."
        error, by retrying the mobile compilation and escalating to
        OutSystems Support if the problem persists, so that the native
        mobile build completes successfully.'
    - name: Config XML error
      id: config-xml-error
      description: 'Wants to resolve the OS-MCO-50017 "An error occurred
        while modifying the config.xml. If the problem persists, open a
        support case." error, by removing invalid XML characters from the
        extensibility configurations of the mobile app or its libraries,
        retrying the mobile compilation, and escalating to OutSystems
        Support if the problem persists, so that the native mobile build
        completes successfully.'
    - name: Mobile framework error
      id: mobile-framework-error
      description: 'Wants to resolve the OS-MCO-50022 "MABS version does not
        include <MobileFramework>. If the problem persists, open a support
        case." error, by retrying the mobile compilation and escalating to
        OutSystems Support if the problem persists, so that the native
        mobile build completes successfully.'
    - name: Asset deserialize error
      id: asset-deserialize-error
      description: 'Wants to resolve the OS-MCO-50018 "Extensibility
        Configurations of <Asset> could not be deserialized. If the problem
        persists, open a support case." error, caused by incorrect data
        types or values in the asset''s extensibility configuration
        properties, by correcting those issues, retrying the mobile
        compilation, and escalating to OutSystems Support if the problem
        persists, so that the native mobile build completes successfully.'
    - name: Branding image size error
      id: branding-image-size-error
      description: Wants to resolve the OS-MABS-PRJ-40001 "Branding image for <Screen> must
        be a specific pixel size, but is <Actual>" error, by resizing the splash
        screen branding image to match the required dimensions and updating the
        app's splash screen configuration, so that the mobile app package
        generates successfully.
    - name: Branding image format error
      id: branding-image-format-error
      description: Wants to resolve the OS-MABS-PRJ-40002 "Branding image for <Screen>
        couldn't be read. The file may be corrupted or in an unsupported
        format." error, by opening the branding image in an image editor to
        confirm it's valid, re-exporting it as a PNG in the required dimensions,
        and updating the app's splash screen configuration, so that the mobile
        app package generates successfully.
    - name: Invalid project config error
      id: invalid-project-config-error
      description: 'Wants to resolve the OS-MABS-PRJ-50001 "The project
        configuration is invalid. If the problem persists, open a support
        case." error, by regenerating the app package or contacting
        OutSystems Support if the problem persists, so that the mobile app
        package generates successfully.'
    - name: Splash image resize error
      id: splash-image-resize-error
      description: 'Wants to resolve the OS-MABS-PRJ-50002 "An error occurred
        while generating the splash screen images" error, caused by the
        branding image failing to be transformed or resized to match iOS or
        Android splash screen requirements, by regenerating the app package
        or contacting OutSystems Support if the problem persists, so that the
        mobile app package generates successfully.'
    - name: File copy/write error
      id: file-write-copy-error
      description: 'Wants to resolve the OS-MABS-PRJ-50003 "An error occurred
        while writing or copying a file. If the problem persists, open a
        support case." error, by regenerating the app package or contacting
        OutSystems Support if the problem persists, so that the mobile app
        package generates successfully.'
    - name: Invalid code structure error
      id: invalid-code-structure-error
      description: 'Wants to resolve the OS-MABS-PRJ-50004 "The code project
        structure is invalid. If the problem persists, open a support case."
        error, caused by a missing or malformed file or folder in the native
        project template, by regenerating the app package or contacting
        OutSystems Support if the problem persists, so that the mobile app
        package generates successfully.'
    - name: Cordova dependency error
      id: cordova-dependency-error
      description: 'Wants to resolve the OS-MABS-PRJ-50005 "An error occurred
        while resolving Cordova plugin dependencies. If the problem
        persists, open a support case." error, caused by a declared Cordova
        plugin not found among installed dependencies or a missing or
        unreadable dependency manifest, by regenerating the app package or
        contacting OutSystems Support if the problem persists, so that the
        mobile app package generates successfully.'
    - name: Template preparation error
      id: template-preparation-error
      description: 'Wants to resolve the OS-MABS-PRJ-50006 "An error occurred
        while preparing the project template. If the problem persists,
        open a support case." error, caused by a missing or unpreparable
        iOS or Android project template archive, by regenerating the app
        package or contacting OutSystems Support if the problem persists,
        so that the mobile app package generates successfully.'
    - name: Plugin auth error
      id: plugin-auth-error
      description: 'Wants to resolve the OS-MABS-PLG-20002 "Couldn''t fetch
        the Cordova plugin <plugin_name> due to an authentication error"
        error, by reviewing the server and plugin access permissions used
        to fetch the plugin, so that the mobile app package generates
        successfully.'
    - name: Fix plugin fetch timeout
      id: fix-plugin-fetch-timeout
      description: 'Wants to resolve the OS-MABS-PLG-10000 "There was an
        issue fetching the Cordova plugin <plugin_name> because the
        connection timed out. Please try again." error that blocks mobile
        app package generation, caused by MABS timing out while fetching a
        Cordova plugin, by checking the Service Center build logs and the
        plugin''s hosting location and retrying the build, escalating to
        OutSystems Support if the problem persists, so that the mobile app
        package generates successfully.'
    - name: Plugin fetch HTTP error
      id: plugin-fetch-http-error
      description: 'Wants to resolve the OS-MABS-PLG-40014 "There was an
        issue fetching the Cordova plugin: <plugin_name>. We couldn''t
        access the resource (http error <http_error_code>). Please review
        your plugin and server configurations." error, by checking the
        Service Center build logs and validating the plugin and server
        extensibility configurations, so that the mobile app package
        generates successfully.'
    - name: Plugin connection lost
      id: plugin-fetch-connection-lost
      description: 'Wants to resolve the OS-MABS-PLG-50001 "There was an
        issue fetching the Cordova plugin <plugin_name> because the
        connection to the server was lost. This might be caused by server
        downtime." error, by checking the Service Center build logs,
        validating the connection to the server hosting the plugin,
        retrying app generation, and creating a support case if the
        problem persists, so that the mobile app package can be generated
        successfully.'
    - name: Server config error
      id: server-config-error
      description: 'Wants to resolve the OS-MABS-CNF-50000 "There was an
        issue processing your extensibility configurations. Please try
        again." error, caused by an unexpected server-side failure while
        processing the app''s extensibility configurations, by checking the
        build logs in Service Center and reviewing the extensibility
        configurations for validity, escalating to OutSystems Support if
        the problem persists, so that the mobile app package generates
        successfully.'
    - name: Plugin compile error
      id: plugin-compile-error
      description: 'Wants to resolve the OS-MABS-GEN-40019 "Couldn''t compile
        the Cordova plugin <plugin_name>. Review your plugin configurations
        and check our documentation for more information if the problem
        persists." error, caused by a malformed plugin resource failing
        during the compilation phase of the application package, by checking
        the Service Center build logs to identify the affected plugin and
        correcting the malformed resource, so that the mobile app package
        generates successfully.'
    - name: Custom plugin error
      id: custom-plugin-error
      description: 'Wants to resolve the OS-MABS-GEN-40022 "There was an issue
        generating the app. A plugin compilation failed with the following
        reason: {REASON}" error, caused by a mobile plugin failing the build
        with a customizable message controlled by the plugin''s owner, by
        following the instructions or information given in the {REASON}
        message or contacting the plugin developer for more information, so
        that the mobile app package generates successfully.'
    - name: Renamed XML error
      id: renamed-xml-error
      description: 'Wants to resolve the OS-MABS-GEN-40023 "There was an issue
        generating the app. Plugin ''{plugin-name}'' is trying to access
        ''{filename}.xml'', but this file was renamed in Cordova Android 14"
        error, caused by a custom plugin referencing the old strings.xml,
        colors.xml, or themes.xml file names that were renamed with a
        ''cdv_'' prefix in Cordova Android 14, by updating the plugin to use
        the new file names, so that the Android mobile app package
        generates successfully.'
    - name: Undocumented MABS error
      id: undocumented-mabs-error
      description: Wants to resolve a MABS build error that isn't yet documented,
        by opening a support case with OutSystems Support, so that they can get
        help resolving their mobile app build issue.
    - name: Deprecated MABS version error
      id: deprecated-mabs-version-error
      description: Wants to resolve the error that occurs when creating a
        mobile app package using a deprecated MABS version, by switching to a
        MABS version that meets the minimum required version, so that the
        mobile app package generates successfully.
    - name: Too many plugins error
      id: too-many-plugins-error
      description: 'Wants to resolve the OS-MCO-40009 "The app includes too
        many plugins. Reduce the number of plugins." error, caused by a
        mobile app exceeding the maximum allowed number of plugins, by
        removing some plugins from the app and escalating to OutSystems
        Support if the problem persists, so that the native mobile build
        completes successfully.'
    - name: Fetch mobile config error
      id: fetch-mobile-config-error
      description: 'Wants to resolve the OS-NAOS-50024 "An internal error has
        occurred while fetching mobile configurations" error, caused by ODC
        failing to retrieve mobile configurations during mobile package
        creation, by canceling the current package creation attempt,
        starting a new one, and reapplying the mobile configurations,
        escalating to OutSystems Support if the problem persists, so that
        the mobile app package generates successfully.'
    - name: Fix extensibility error
      id: extensibility-fetch-error
      description: Wants to resolve the OS-NAOS-50005 "An error occurred while fetching the
        extensibility configurations" error, caused by malformed extensibility
        configuration JSON (a missing or invalid required property) or
        missing/inaccessible extensibility resources, by ensuring the necessary
        extensibility configurations are available and correctly set up,
        escalating to OutSystems Support if the problem persists, so that the
        mobile app build completes successfully.
    - name: Compilation state error
      id: compilation-state-error
      description: 'Wants to resolve the OS-NAOS-50019 "An error occurred due
        to an unexpected state in the compilation process" error, by
        reviewing logs for details about the compilation state or
        associated errors and contacting OutSystems Support if none are
        found, so that the application build process can proceed.'
    - name: Connection timeout error
      id: connection-timeout-error
      description: 'Wants to resolve the OS-MABS-CKT-10000 "It looks like the
        connection timed out" error that blocks mobile app package
        generation, by retrying the build and reviewing the Service Center
        build logs, escalating to OutSystems Support if the problem
        persists, so that the mobile app package generates successfully.'
    - name: Fix missing icon error
      id: fix-missing-icon-error
      description: 'Wants to resolve the OS-MABS-CNF-40000 "There''s an icon
        missing in your extensibility configurations" error that blocks
        mobile app package generation, caused by a missing icon density or
        size in an extensibility configuration, by checking and fixing the
        icon''s density/size in Service Studio (O11) or ODC Studio, so that
        the mobile app package generates successfully.'
    - name: Fix missing splash error
      id: fix-missing-splash-error
      description: 'Wants to resolve the OS-MABS-CNF-40001 "There''s a splash
        screen missing in your extensibility configurations with {0}" error
        that blocks mobile app package generation, caused by a missing
        splash screen density or size in an extensibility configuration, by
        checking and fixing the splash screen''s density/size in Service
        Studio (O11) or ODC Studio, so that the mobile app package
        generates successfully.'
    - name: Fix missing image error
      id: fix-missing-image-error
      description: 'Wants to resolve the OS-MABS-CNF-40002 "Unable to copy
        your {0}s. Check your Resources path and try again." error that
        blocks mobile app package generation, caused by an image file
        declared in an extensibility configuration not existing, by
        checking the extensibility configurations to ensure the declared
        image file exists and the path is correct, so that the mobile app
        package generates successfully.'
    - name: Missing property error
      id: missing-property-error
      description: 'Wants to resolve the OS-MABS-CNF-40006 "Missing property
        {0} on {1} in your extensibility configurations. Review it and try
        again." error that blocks mobile app package generation, caused by a
        required property missing in the app''s extensibility configuration,
        by reviewing the extensibility configuration and adding the missing
        property, so that the mobile app package generates successfully.'
    - name: Fix invalid icon format
      id: fix-invalid-icon-format-error
      description: 'Wants to resolve the OS-MABS-CST-40000 "This icon file is
        not supported, choose another one" error that blocks mobile app
        package generation, caused by an invalid or unparsable application
        icon image file, by replacing the icon with a valid image in the
        correct format, so that the mobile app package generates
        successfully.'
    - name: Fix app zip error
      id: fix-app-zip-error
      description: 'Wants to resolve the OS-MABS-FIN-50000 "Something happened
        on our side while packaging your app" error that blocks mobile app
        package generation, caused by MABS failing to zip the application
        source code at the end of the build process, by retrying the build
        and reviewing the Service Center build logs, escalating to
        OutSystems Support if the problem persists, so that the mobile app
        package generates successfully.'
    - name: Maven fetch error
      id: maven-fetch-error
      description: 'Wants to resolve the OS-MABS-GEN-10000 "Your app wasn''t
        generated because we couldn''t fetch its dependencies from the
        repository server" error that blocks mobile app package generation,
        caused by a failure to fetch build plugin dependencies from a Maven
        repository, by checking the Service Center build logs to identify
        the failing plugin and confirming access to the repository, so that
        the mobile app package generates successfully.'
    - name: Blocked plugin action
      id: blocked-plugin-action-error
      description: 'Wants to resolve the OS-MABS-GEN-20000 "Your app wasn''t
        generated because an action was blocked due to security concerns.
        Contact the support team if this problem persists." error, caused by
        a custom plugin attempting to execute non-permitted actions during
        mobile app package generation, by checking the Service Center build
        logs to identify the offending plugin, so that the mobile app
        package generates successfully.'
    - name: Missing hook dependency
      id: missing-hook-dependency-error
      description: 'Wants to resolve the OS-MABS-GEN-20001 "Your app wasn''t
        generated because the required dependency for the {0} node module in
        a plugin hook was missing" error, by checking the Service Center
        build logs to identify the custom plugin hook with the missing node
        module dependency, so that the mobile app package generates
        successfully.'
    - name: CocoaPods server error
      id: cocoapods-server-error
      description: 'Wants to resolve the OS-MABS-PLF-50002 "Your app wasn''t
        generated because we couldn''t fetch its dependencies due to a
        CocoaPods server error. Please try again." error, caused by an issue
        on the CocoaPods CDN while MABS bootstraps the iOS Cordova project''s
        dependencies, by checking the Service Center build logs and retrying
        application generation, escalating to OutSystems Support if the
        problem persists, so that the mobile app package generates
        successfully.'
    - name: Generic CocoaPods error
      id: cocoapods-config-error
      description: 'Wants to resolve the OS-MABS-GEN-40012 "There was an issue
        generating the app. Some plugins using CocoaPods aren''t correctly
        configured or a pod wasn''t found in the repository. Check your
        plugin configurations and try again." error, by checking the Service
        Center build logs to identify the affected pod and the custom plugin
        introducing it, then fixing that plugin''s CocoaPods configuration,
        so that the iOS mobile app package generates successfully.'
    - name: Generic validation error
      id: mabs-generic-validation-error
      description: 'Wants to resolve the OS-MABS-VLD-50000 "Something happened
        on our side. Please try again." error, caused by an unexpected
        failure while MABS validates the application package, plugins, or
        extensibility configurations, by repeating the operation and
        checking the build logs on Service Center, escalating to OutSystems
        Support if the problem persists, so that the mobile app package
        generates successfully.'
    - name: Empty plugin source error
      id: empty-plugin-source-error
      description: 'Wants to resolve the OS-MCO-40001 "A plugin in
        Extensibility Configurations is providing an empty source" error,
        caused by a plugin source in the app''s extensibility
        configurations being empty and not including an NPM package or a
        resource, by finding the affected plugin and updating it with a
        valid NPM package or resource, or removing the extensibility
        property or the library, so that the mobile app compiles and the
        native mobile build succeeds.'
    - name: Missing catalog image error
      id: missing-catalog-image-error
      description: 'Wants to resolve the OS-MCO-40002 "Extensibility
        Configurations are missing image <Identifier> in <Where>" error,
        caused by an incomplete custom icon or splash screen image catalog
        missing a required identifier in the app''s extensibility
        configurations, by adding the complete set of images for the icon
        or splash screen catalog or removing the custom icons and splash
        screens, escalating to OutSystems Support if the problem persists,
        so that the mobile app compiles and the native mobile build
        succeeds.'
    - name: Missing iOS usage description
      id: missing-ios-usage-description-error
      description: 'Wants to resolve the OS-MCO-40003 "Extensibility
        Configurations are missing a usage description for iOS permission
        <Permission>" error, caused by a library declaring an iOS permission
        in its extensibility configurations without a usage description, by
        adding the missing usage description in the mobile app''s
        extensibility configurations, removing the unneeded permission
        declaration from the library, or removing the library, so that the
        mobile app compiles and the native mobile build succeeds.'
    - name: Invalid schema error
      id: invalid-schema-error
      description: 'Wants to resolve the OS-MCO-40004 "Extensibility
        Configurations of <AssetName> do not follow a supported schema:
        <Detail>" error, caused by a mobile app''s or library''s
        extensibility configurations not conforming to either the Universal
        Extensibility or Cordova-based Extensibility JSON schema, by
        reviewing the error''s detail and updating the configurations to
        match the appropriate schema (Universal-only for Capacitor apps),
        escalating to OutSystems Support if the problem persists, so that
        the mobile app compiles and the native mobile build succeeds.'
    - name: Plugin source mismatch
      id: plugin-source-mismatch
      description: 'Wants to resolve the "Extensibility Configurations of
        <AssetName> include plugin sources, but none of them are compatible
        with <MobileFramework>" error that causes a mobile app''s native
        build to fail when a plugin only provides Capacitor sources for a
        Cordova-based build, by switching the mobile framework to Capacitor,
        removing the incompatible plugin, or providing a matching Cordova
        source for it, so that the native mobile build succeeds.'
    - name: Newer schema error
      id: newer-schema-error
      description: 'Wants to resolve the OS-MCO-40006 "Extensibility
        Configurations of <AssetName> must use a newer schema when
        targeting <MobileFramework>" error, caused by a Capacitor-targeted
        mobile app or a library it depends on still using Cordova-based
        Extensibility instead of Universal Extensibility, by updating the
        affected asset and its dependent libraries to Universal
        Extensibility, removing the incompatible library, or continuing to
        use Cordova as the mobile framework until the migration to
        Capacitor completes, so that the mobile app compiles and the
        native mobile build succeeds.'
    - name: Asset incompatible error
      id: asset-incompatible-error
      description: 'Wants to resolve the OS-MCO-40013 "<AssetName>
        <AssetVersion> is not compatible with MABS <MabsVersion>
        <MobileFramework>. Update the asset to the latest version." error,
        caused by using an asset version that isn''t compatible with the
        current MABS version or mobile framework, by updating the asset to
        the latest version or building with an older MABS version or a
        different mobile framework, escalating to OutSystems Support if the
        problem persists, so that the mobile app compiles and the native
        mobile build succeeds.'
    - name: Library framework error
      id: library-framework-error
      description: 'Wants to resolve the OS-MCO-40014 "The library
        <AssetName> does not support the mobile framework <MobileFramework>."
        error, caused by a mobile library not supporting the app''s mobile
        framework (Cordova or Capacitor), by updating a library they
        developed to support both frameworks, or for a third-party library,
        updating it, finding an alternative, or building with the supported
        framework, escalating to OutSystems Support if the problem persists,
        so that the mobile compilation and native mobile build succeed.'
  - name: AppShield
    id: appshield
    subtopics:
    - name: Retrace obfuscated logs
      id: retrace-obfuscated-logs
      description: Wants to deobfuscate an Android stack trace generated by a MABS
        AppShield-obfuscated build, using ProGuard's retrace CLI or proguardgui with
        the build's mapping.txt file, so that they can identify the root cause of
        a crash in an obfuscated app.
    - name: Plugin obfuscation errors
      id: plugin-obfuscation-errors
      description: Wants to understand why a customized or unsupported mobile plugin
        produces ClassNotFoundException or MethodNotFoundException errors at runtime
        after AppShield obfuscation, so that they can identify whether misconfigured
        dependencies, Gradle/JAR/AAR imports, or reflection-based code are causing
        the issue.
    - name: AppShield hardening
      id: appshield-hardening
      description: Wants to harden a high-security mobile app with AppShield by installing
        the plugin, keeping a Stable and Current version, configuring
        development-time disablement, and setting the optional protection
        preferences that match its security needs, so that the app binary
        resists reverse engineering, repackaging, and tampering.
    - name: Duplicate AppShield ref
      id: duplicate-appshield-reference
      description: 'Wants to resolve the OS-MABS-SHLD-40001 "The AppShield plugin
        has more than one reference in your app" error, by checking the
        extensibility configurations for duplicate references to the AppShield
        plugin and removing them, keeping only the reference from the AppShield
        plugin retrieved from Forge, so that the mobile app package generates
        successfully.'
    - name: Changed AppShield config
      id: changed-appshield-config
      description: 'Wants to resolve the OS-MABS-SHLD-40002 "The AppShield''s extensibility configurations can''t be changed." error, by installing the OutSystems AppShield plugin from Forge and requesting a new build without changing its extensibility configurations, escalating to OutSystems Support if the problem persists, so that the mobile app package generates successfully.'
    - name: AppShield settings error
      id: appshield-settings-error
      description: 'Wants to resolve the OS-MABS-SHLD-40003 "Some AppShield plugin
        settings have invalid values, please review them" error, by reviewing
        the AppShield preferences in their extensibility configurations
        against the documented configuration reference and correcting the
        invalid values, so that the mobile app package generates
        successfully.'
    - name: Configure Exit URL
      id: configure-exit-url
      description: 'Wants to configure AppShield''s ExitOnURL preference for a blocked
        mobile app so the app opens a landing page in the default browser,
        keeps the URL free of query parameters, and uses the generated query
        parameters to explain why the app shut down.'
    - name: Outdated MABS version error
      id: outdated-mabs-version-error
      description: Wants to resolve the OS-MABS-SHLD-40004 "It looks like your MABS version
        is outdated, please update it" error, by updating MABS to the minimum
        version required for the AppShield plugin, so that the AppShield plugin
        is supported and the mobile app package generates successfully.
    - name: License upgrade error
      id: license-upgrade-error
      description: 'Wants to resolve the OS-MABS-SHLD-40005 "It looks like
        OutSystems AppShield is not accessible with your current license" error,
        by contacting OutSystems Support to upgrade their license, so that the
        AppShield build succeeds and the mobile app package generates
        successfully.'
    - name: AppShield generic error
      id: appshield-generic-error
      description: 'Wants to resolve the OS-MABS-SHLD-50000 "Something happened
        and we couldn''t harden your app with AppShield. Please try again."
        error, by checking the build logs for issues related to the Shield
        phase and escalating to OutSystems Support if the problem persists,
        so that the mobile app package generates successfully.'
    - name: AppShield license error
      id: appshield-license-error
      description: 'Wants to resolve the OS-MABS-SHLD-50001 "Something happened
        and your request didn''t go through, please try again. If the problem
        persists, contact our support team." error, by checking the build
        logs on Service Center for AppShield license validation issues and
        repeating the operation, escalating to OutSystems Support if the
        problem persists, so that the mobile app package generates
        successfully.'
    - name: Settings validation error
      id: app-settings-validation-error
      description: 'Wants to resolve the OS-MABS-SHLD-50002 "Something went
        wrong when validating application settings." error, caused by invalid
        application settings or extensibility configurations, by reviewing
        and fixing them, checking the Service Center build logs, and
        retrying app generation, escalating to OutSystems Support if the
        problem persists, so that the mobile app package generates
        successfully.'
    - name: Deprecated signer cert
      id: deprecated-signer-cert-error
      description: 'Wants to resolve the OS-MABS-SHLD-40007 "The
        ''GooglePlayAppSigningCertificate'' AppShield preference is
        deprecated and no longer supported. Use
        ''ApplicationSignerCertificate'' instead." error, by replacing the
        deprecated GooglePlayAppSigningCertificate preference with
        ApplicationSignerCertificate in the app''s extensibility
        configurations, so that the mobile app package generates
        successfully.'
    - name: Encode signer certificate
      id: encode-signer-certificate
      description: 'Wants to fix an app crash (RepackagingException)
        affecting LifeTime 11.6.1/Platform Server 11.10 and earlier when
        Google Play App Signing is enabled, by URL-encoding the
        ApplicationSignerCertificate value in LifeTime''s Extensibility
        Configurations, so that the mobile app package builds
        successfully.'
    - name: AppShield sources error
      id: appshield-sources-error
      description: 'Wants to resolve the OS-MCO-40010 "Multiple AppShield
        plugin sources are present in the Extensibility Configurations. Use
        only one version of AppShield in the app." error, caused by cloning
        or modifying the OutSystems AppShield library so the app ends up
        with more than one AppShield version, by avoiding cloning or
        modifying the AppShield library and escalating to OutSystems
        Support if the problem persists, so that the mobile compilation and
        native mobile build succeed.'
    - name: Invalid AppShield config
      id: invalid-appshield-config-error
      description: 'Wants to resolve the OS-MCO-50019 "Extensibility
        Configurations of <Asset> are not valid for AppShield" error, caused
        by an internal issue unrelated to the asset''s own extensibility
        configurations, by retrying the operation and escalating to
        OutSystems Support if the problem persists, so that the mobile
        compilation and native mobile build succeed.'
    - name: Shutdown reasons
      id: appshield-shutdown-reasons
      description: 'Wants to map AppShield shutdown reason codes from Android crash logs
        and iOS ExitOnURL values to the documented reason when AppShield closes
        the app because it detects a security threat, so that they can identify
        why the app exited.'
  - name: Custom plugin development
    id: custom-plugin-development
    subtopics:
    - name: Choose plugin approach
      id: choose-plugin-approach
      description: Wants to decide whether to build a custom Capacitor plugin
        from scratch or reuse an existing option (Forge plugin, adapted Cordova
        plugin, or community Capacitor plugin), so that they avoid
        unnecessary investment in custom native plugin development for ODC
        mobile apps.
    - name: Plan custom Capacitor plugin
      id: plan-custom-capacitor-plugin
      description: Wants to plan a custom Capacitor plugin for an ODC mobile
        app after deciding that no existing Forge plugin, adapted Cordova
        plugin, or community Capacitor plugin meets their requirements, so
        that they can start building from scratch with the right development
        sequence.
    - name: Implement Android plugin
      id: android-plugin-implementation
      description: Wants to implement the native Android layer of a custom
        Capacitor plugin for an ODC mobile app, using Kotlin or Java to
        define the plugin's "bridge" file with @PluginMethod-annotated
        methods that receive a PluginCall parameter, so that they can
        implement Android-specific functionality declared in the plugin's
        web layer.
    - name: Capacitor plugin layers
      id: capacitor-plugin-layers
      description: Wants to understand how a Capacitor plugin's code is
        organized into web, Android, and iOS layers, and what each layer's
        directory (src, android, ios) is responsible for, so they can locate
        and modify the right layer when building or troubleshooting a
        plugin.
    - name: Capacitor bridge flow
      id: capacitor-bridge-flow
      description: Wants to understand how the Capacitor bridge routes a
        plugin call from the JavaScript layer through window.CapacitorPlugins
        to native Android or iOS code and back, in an ODC mobile app, so they
        can reason about plugin call behavior end to end.
    - name: Implement iOS plugin
      id: ios-plugin-implementation
      description: Wants to implement the native iOS layer of a custom
        Capacitor plugin for an ODC mobile app, using Swift to define the
        plugin's "bridge" file with @objc-annotated methods that receive a
        CAPPluginCall parameter, so that they can implement iOS-specific
        functionality declared in the plugin's web layer.
    - name: Add SPM support
      id: add-spm-support
      description: Wants to add Swift Package Manager (SPM) support to a
        custom Capacitor plugin by creating a Package.swift file declaring
        the plugin as a library target with its native dependencies and
        source path, so that the plugin builds correctly in ODC mobile apps
        using the SPM toolchain instead of, or alongside, CocoaPods.
    - name: Plugin privacy manifest
      id: plugin-privacy-manifest
      description: Wants to bundle a PrivacyInfo.xcprivacy file within a
        custom Capacitor plugin's iOS Sources directory and reference it as
        a resource bundle in the plugin's .podspec and Package.swift files,
        so that the manifest is correctly discovered by Xcode without
        colliding with the app's own privacy manifest.
    - name: Implement web plugin layer
      id: implement-web-plugin-layer
      description: Wants to scaffold a custom Capacitor plugin project and
        implement its web/TypeScript layer, by using the plugin generator to
        create the project structure, defining the plugin's TypeScript
        interface and method signatures in definitions.ts, and registering
        the plugin with registerPlugin() in index.ts, so that the web layer
        serves as the entry point that native Android and iOS
        implementations bind to.
    - name: Test custom Capacitor plugin
      id: test-custom-capacitor-plugin
      description: Wants to test a custom Capacitor plugin in an ODC mobile
        app after implementing its web and native layers, so that they can
        confirm it behaves correctly across supported platforms before
        release.
    - name: Package custom Capacitor plugin
      id: package-custom-capacitor-plugin
      description: Wants to package a custom Capacitor plugin for distribution
        after implementing and testing it, so that consumers can install the
        expected plugin files consistently in downstream projects.
    - name: Publish Capacitor plugin to npm
      id: publish-capacitor-plugin-npm
      description: Wants to publish a tested custom Capacitor plugin to npm, so
        that ODC wrapper libraries and consuming mobile apps can install a
        versioned release from a standard package registry.
    - name: Create ODC wrapper library for plugin
      id: create-odc-wrapper-library-plugin
      description: Wants to create an ODC wrapper library for a custom
        Capacitor plugin after the plugin package is ready, so that ODC mobile
        app developers can call the plugin through reusable OutSystems
        elements instead of hand-writing JavaScript integration code.
  - name: App lifecycle handling
    id: app-lifecycle-transitions
    description: Wants to react to foreground, background, resume, and pause
      transitions in an ODC mobile app by using the On Application Resume
      event for resume handling and a JavaScript pause listener for
      background handling, so that lifecycle-specific logic runs in the
      correct layer.
  - name: Deep linking
    id: deep-linking
    subtopics:
    - name: Configure mobile deep links
      id: configure-mobile-deep-links
      description: Wants to define a deep link to a specific screen of a
        module in an OutSystems Mobile App using the
        `<app-identifier>://<module>/<screen>` syntax, including passing
        input parameter values in the URL, so that end users and external
        systems can navigate directly to a specific screen of the app.
    - name: Configure Android deep link intents
      id: configure-android-deep-link-intents
      description: Wants to define an Android Intent for a Mobile App using
        the `intent://` syntax, including specifying a custom fallback URL,
        so that Android Chrome users without the app installed are
        redirected to the Google Play Store or another URL instead of the
        app.
    - name: Deep link handling
      id: deep-link-handling
      description: Wants to handle incoming deep links and custom URL schemes
        in ODC mobile apps by relying on the MABS-generated scheme and routing
        the link in Capacitor or Cordova, so that browsers, apps, and
        notifications open the right screen.
- name: OutSystems data centers
  guid: 5d6b141d-82f8-4949-af54-6593592e0327
  topics:
  - name: AWS Transit Gateway setup
    id: aws-transit-gateway-setup
    subtopics:
    - name: Transit Gateway benefits
      id: transit-gateway-benefits
      description: Wants to understand the advantages of connecting their
        OutSystems Cloud to an AWS Transit Gateway instead of a VPN, such
        as centralized network management, full control over on-premises
        VPN connections, and traffic staying on the AWS private network,
        so that they can decide whether Transit Gateway suits their
        connectivity needs.
    - name: Set up Transit Gateway connection
      id: setup-transit-gateway-connection
      description: Wants to connect their OutSystems Cloud to their own
        AWS Transit Gateway, by opening a support case, sharing the
        Transit Gateway resource via AWS Resource Access Manager,
        accepting the attachment, and updating route tables on both
        sides, so that they can centrally manage internal traffic to
        their OutSystems Cloud without relying on OutSystems-managed
        VPNs.
  - name: Firewall allowlisting
    id: firewall-allowlisting
    subtopics:
    - name: Allowlist front-end IPs
      id: allowlist-frontend-public-ips
      description: Wants to obtain the static public IPs of their
        OutSystems Cloud environment's front-end servers via Service
        Center and correctly allowlist them on their firewall instead of
        the load balancer's IP, so that outgoing connections to external
        services through the firewall aren't blocked.
    - name: Allowlist ODC public IPs
      id: allowlist-odc-public-ips
      description: Wants to identify which ODC public IP addresses to allowlist by region
        when private network endpoints need to accept traffic from ODC services,
        so that those endpoints can receive the correct ODC traffic.
    - name: Allowlist VPN IP range
      id: allowlist-vpn-private-ip-range
      description: Wants to allowlist the full private IP range of their
        OutSystems Cloud on their firewall when connecting via VPN instead
        of a single IP, so that traffic isn't blocked due to the
        non-static nature of individual private IP addresses.
  - name: Cloud architecture
    id: cloud-architecture
    description: Wants to understand the architecture and operational
      controls of OutSystems Cloud environments.
    subtopics:
    - name: Network architecture
      id: cloud-network-architecture
      description: Wants to understand the dedicated VPC architecture,
        environment segregation, WAF, security groups, administration
        access, and IP restrictions used in OutSystems Cloud
        environments, so that they can assess how the platform is
        isolated and operated.
    - name: HA and compliance
      id: cloud-ha-compliance
      description: Wants to understand the high-availability and compliance options for
        OutSystems Cloud environments, so that they can assess resilience and
        compliance posture.
  - name: VPN setup
    id: vpn-setup
    subtopics:
    - name: Request VPN connection
      id: request-vpn-connection
      description: Wants to set up a site-to-site VPN connection between
        their on-premises network and OutSystems Cloud, by requesting the
        VPN in LifeTime with the internet gateway IP, internal network IP
        range, and routing type, downloading the generated configuration
        file, and configuring their VPN gateway and firewall accordingly,
        so that they can establish secure connectivity between the two
        networks.
    - name: VPN gateway requirements
      id: vpn-gateway-requirements
      description: Wants to know the requirements and specifications for
        their on-premises VPN gateway, such as supported IKE protocols,
        encryption/hashing algorithms, and Diffie-Hellman groups, before
        setting up a site-to-site VPN to OutSystems Cloud, so that they
        can choose or configure a compatible gateway.
    - name: VPN traffic and redundancy
      id: vpn-traffic-redundancy
      description: Wants to understand how an OutSystems VPN connection
        generates traffic and handles redundancy, before setting up a
        site-to-site VPN, so that they can plan their on-premises network
        and firewall accordingly.
    - name: Change VPN IP range
      id: change-vpn-ip-range
      description: Wants to update the internal on-premises IP range
        associated with an existing VPN connection to OutSystems Cloud,
        using the VPN Management area in LifeTime for static routing or
        by opening a support case for dynamic routing, so that the VPN
        reflects a network change without needing to recreate the
        connection.
  - name: VPN connection troubleshooting
    id: vpn-connection-troubleshooting
    subtopics:
    - name: Fix VPN IP errors
      id: fix-vpn-ip-errors
      description: Wants to resolve VPN creation errors caused by an invalid or
        already-used Internet gateway public IP address when creating a VPN in
        the LifeTime console, so that they can provide a valid and unique
        public IP address.
    - name: Fix VPN connection limit errors
      id: fix-vpn-connection-limit-errors
      description: Wants to resolve VPN creation errors caused by exceeding the
        maximum number of VPN connections when creating a VPN in the LifeTime
        console, so that they can choose another supported setup to connect the
        infrastructure.
    - name: Fix VPN Transit Gateway conflict errors
      id: fix-vpn-transit-gateway-conflict-errors
      description: Wants to resolve VPN creation errors caused by an
        infrastructure already being connected to an AWS Transit Gateway when
        creating a VPN in the LifeTime console, so that they can use the
        existing Transit Gateway setup instead of creating a conflicting VPN.
    - name: Fix VPN ASN errors
      id: fix-vpn-asn-errors
      description: Wants to resolve VPN creation errors related to an invalid
        BGP ASN value, whether already in use, outside the 0-65535 range, or
        reserved by Amazon, when configuring a VPN connection in the LifeTime
        console, so that they can provide a valid, unreserved, and unique ASN
        value.
    - name: Fix VPN route errors
      id: fix-vpn-route-errors
      description: Wants to resolve errors when adding static routes to a VPN connection in
        the LifeTime console, including an invalid or overlapping internal
        network IP range, the disallowed 0.0.0.0/0 range, exceeding the maximum
        allowed number of routes, or a temporary "operation in progress"
        conflict, so that they can correct the route configuration and complete
        the request.
    - name: Fix VPN Dead Peer Detection (DPD) failures
      id: fix-vpn-dpd-failures
      description: Wants to diagnose and fix VPN tunnel instability caused by
        Dead Peer Detection (DPD) failures after setup, so that they can
        restore a stable OutSystems Cloud VPN connection.
    - name: Fix periodic IKEv2 tunnel drops
      id: fix-periodic-ikev2-tunnel-drops
      description: Wants to diagnose and fix periodic IKEv2 tunnel drops
        due to IKE/IPsec SA lifetime expiration and rekeying after setup, so
        that they can restore a stable OutSystems Cloud VPN connection.
    - name: Fix asymmetric routing with dual active VPN tunnels
      id: fix-vpn-asymmetric-routing-dual-active-tunnels
      description: Wants to diagnose and fix VPN tunnel instability caused by
        asymmetric routing with dual active tunnels after setup, so that they
        can restore a stable OutSystems Cloud VPN connection.
    - name: Fix VPN Phase 1 (IKE) and Phase 2 (IPsec) negotiation failures
      id: fix-vpn-phase-1-phase-2-negotiation-failures
      description: Wants to diagnose and fix VPN tunnel instability caused by
        Phase 1 (IKE) and Phase 2 (IPsec) negotiation failures after setup, so
        that they can restore a stable OutSystems Cloud VPN connection.
    - name: Test VPN connectivity
      id: test-vpn-connectivity
      description: Wants to test and troubleshoot VPN connectivity to
        OutSystems Cloud environments using ping, traceroute, telnet, and
        nslookup against the OutSystems DNS servers and front-end domain
        names, so that they can confirm whether DNS resolution and the
        network path to their environments works over the established VPN.
    - name: Configure VPN DNS resolution
      id: configure-vpn-dns-resolution
      description: Wants to configure conditional forwarders on their
        internal DNS servers to resolve OutSystems Cloud environment public
        domain names, internal network domain names, and external database
        hostnames over the VPN, so that traffic routes correctly through the
        internal load balancers instead of failing to resolve.
  - name: Organization region information
    id: organization-region-information
    subtopics:
    - name: View organization regions
      id: view-organization-regions
      description: Wants to view their ODC organization's Runtime region,
        Data platform region, and Platform region on the Organization page
        in ODC Portal, so that they can verify data residency for
        compliance and allowlist the correct IP addresses for networking.
- name: Product security
  guid: f5935fdf-7f64-46b8-bc71-dd69627c33d8
  topics:
  - name: Cloud shared responsibility model
    id: cloud-shared-responsibility-model
    subtopics:
    - name: Shared responsibility overview
      id: shared-responsibility-overview
      description: Wants to understand how security and infrastructure
        responsibilities are divided between OutSystems and the customer in
        OutSystems Cloud (ODC and OutSystems 11 Cloud) deployments, so that
        they know which protections OutSystems provides and where their own
        responsibility begins.
    - name: Cloud customer duties
      id: cloud-customer-duties
      description: Wants to understand which security and application
        lifecycle tasks customers must handle themselves in OutSystems Cloud,
        such as data management, secure development, vulnerability testing,
        and mitigation, so that they can meet their share of the shared
        responsibility model.
    - name: Platform server hardening
      id: platform-server-hardening
      description: Wants to harden an OutSystems Platform Server deployment
        by configuring security contacts, administrator passwords,
        authentication methods, encryption, network restrictions, and
        application security controls, so that the environment reduces its
        attack surface and follows security best practices.
    - name: OutSystems cloud duties
      id: outsystems-cloud-duties
      description: Wants to understand which infrastructure and platform
        security tasks OutSystems handles on their behalf in OutSystems
        Cloud, such as platform security, network/firewall configuration,
        encryption, and compute/storage/database management, so that they
        know what protections are already covered without needing action on
        their part.
  - name: Cloud connectivity
    id: cloud-connectivity
    description: Wants to understand the secure connection options between OutSystems Cloud
      and on-premises or AWS corporate systems, so that they can choose the
      right integration or troubleshooting path.
  - name: Cloud infrastructure maintenance
    id: cloud-infrastructure-maintenance
    subtopics:
    - name: Set maintenance window
      id: set-maintenance-window
      description: Wants to configure the maintenance window (day of week and
        time interval) for a production or non-production OutSystems Cloud
        environment via the LifeTime console, so that database maintenance
        and any related downtime occur at a time that minimizes disruption.
    - name: Cloud maintenance schedule
      id: cloud-maintenance-schedule
      description: Wants to understand the schedule, frequency, advance notice periods,
        and rescheduling rules for OutSystems Cloud system updates, so that they can
        plan around maintenance windows and avoid unexpected service disruption.
    - name: Cloud update downtime
      id: cloud-update-downtime
      description: Wants to understand the expected downtime during OutSystems Cloud
        system updates based on their infrastructure configuration (single front-end,
        multiple front-ends, load balancer), so that they can assess the impact on
        their production environment.
    - name: Schedule demo upgrade
      id: schedule-demo-upgrade
      description: Wants to schedule the self-service upgrade of an OutSystems Cloud demo
        environment to the latest available platform version within the self-
        service window, by using the "Check for a new platform release" link in
        the LifeTime console's Environments tab and upgrading Development before
        Production, so that they control the timing of the upgrade instead of
        waiting for automatic upgrade.
    - name: Cancel scheduled upgrade
      id: cancel-scheduled-demo-upgrade
      description: Wants to cancel an already scheduled self-service upgrade
        of an OutSystems Cloud demo environment, by selecting the
        environment in the LifeTime Environments tab, clicking "Upgrade
        scheduled for", and confirming the cancellation, so that they can
        reschedule it for a different time.
    - name: Demo upgrade timing
      id: demo-upgrade-timing
      description: Wants to understand how OutSystems Cloud demo environment upgrades are
        timed, including the automatic upgrade window that starts once the self-
        service period ends, the postponement option available during that
        window, the downtime expected during the upgrade, and that requesting an
        upgrade before the self-service period opens isn't possible, so that
        they can plan around the resulting downtime and deadlines.
    - name: SQL Server version upgrade
      id: sql-server-2022-upgrade
      description: Wants to understand what changes during the SQL Server 2022 upgrade of
        O11 Cloud databases (version and compatibility-level change) and whether
        it introduces breaking changes or requires application modifications, so
        that they can confirm whether their applications need any adaptation.
    - name: Database storage overview
      id: database-storage-overview-lifetime
      description: Wants to understand what the database storage figures shown
        in LifeTime's Environments screen mean for an OutSystems Cloud
        environment, including the occupied versus total space and the
        breakdown between System data (OutSystems system tables) and
        Application data (application Entities), so that they can interpret
        current storage usage, knowing the free-space panel is unavailable
        for Enterprise PaaS infrastructures since LifeTime Management Console
        11.25.0.
    - name: Database autogrow safety
      id: database-storage-autogrow-safety
      description: Wants to understand whether an OutSystems Cloud environment
        can stop running due to lack of database space, given that storage
        autogrows automatically and OutSystems health monitoring reacts
        promptly to gradual increases, so that they know a sudden, large
        storage spike is the main scenario that may not leave enough time to
        react before it affects the platform.
    - name: Windows Server upgrade breaking changes
      id: windows-server-upgrade-breaking-changes
      description: Wants to understand and prepare for the breaking changes
        introduced by the Windows Server 2022 upgrade of O11 Cloud environments
        (TLS protocol deprecation, OS-level dependency changes, private IP
        address changes, front-end storage loss, and required Platform Server
        upgrade), so that they can adapt their applications and infrastructure
        before their scheduled upgrade date.
    - name: Update lifecycle states
      id: update-lifecycle-states
      description: Wants to understand the states an ODC platform infrastructure update
        moves through during its life cycle, so that they can track update
        status and know what action, if any, is needed at each stage.
    - name: PCI DSS patching compliance
      id: pci-dss-patching-compliance
      description: Wants to understand how OutSystems Cloud system updates fulfill
        PCI DSS Requirement 6.2 for timely security patching, so that they can confirm
        their environment meets compliance obligations without taking manual action.
    - name: Reschedule platform updates
      id: reschedule-platform-updates
      description: Wants to view upcoming ODC platform infrastructure updates in
        the ODC Portal (Management > Maintain > Platform updates) and reschedule
        any of them to a different date within the allowed time window, so that
        they can prepare each tenant stage for the change at a convenient time.
    - name: Platform update notifications
      id: platform-update-notifications
      description: Wants to know who receives email notifications about ODC platform
        infrastructure updates and what permission determines that, so that they
        can ensure the right people stay informed of scheduled and rescheduled
        updates.
  - name: Automatic app patching
    id: automatic-app-patching
    description: Wants to understand how OutSystems Developer Cloud (ODC) scans
      app container images for vulnerabilities and automatically patches
      affected apps according to CVSS severity, including the difference
      between automatic and manual (1-Click Publish) patching, and the impact
      on active timers scheduled to run "When published", so that they can
      prepare for and manage vulnerability patch rollouts without disrupting
      running apps.
  - name: ODC data protection
    id: odc-data-protection
    description: Wants to understand how OutSystems Developer Cloud (ODC) protects customer
      production runtime data, including per-customer KMS-managed encryption at
      rest, data residency in the organization's specified region, continuous
      incremental backups retained for a defined retention period, and Aurora
      Serverless high-availability failover, so that they can assess data
      protection and disaster recovery guarantees for their apps.
  - name: Penetration testing
    id: penetration-testing
    subtopics:
    - name: Run penetration tests
      id: run-penetration-tests
      description: Wants to perform penetration tests or vulnerability scans
        against their own OutSystems Cloud, hybrid, or self-managed
        infrastructure, checking the OutSystems Platform Hardening guidance
        beforehand, so that they know which assets are in scope and avoid
        generating avoidable false positives.
    - name: Pentest false positives
      id: pentest-false-positives
      description: Wants to determine whether a penetration test or
        vulnerability scan finding against a known OutSystems platform
        component (for example jQuery, jQuery-UI, IdentityServer4,
        Newtonsoft.Json) is a false positive, so that they can distinguish
        already-mitigated findings from confirmed platform vulnerabilities
        before reporting them.
    - name: Pentest support process
      id: pentest-support-process
      description: Wants to understand the conditions under which
        OutSystems support and security teams review penetration test
        findings, and how responsibilities and expectations differ between
        the customer and OutSystems by deployment model (Cloud and hybrid
        versus self-managed), so that they know what to prepare before
        opening a support case.
    - name: Authorize cloud testing
      id: authorize-cloud-testing
      description: Wants to request OutSystems authorization to run penetration tests,
        vulnerability scans, or load tests against their own apps on OutSystems
        Cloud, by submitting a support case with contact info, scanning IP
        addresses, target environments, and a test schedule submitted with
        sufficient advance notice, so that testing stays within the customer's
        shared-responsibility scope and doesn't trigger platform protections.
  - name: Vulnerability disclosures
    id: vulnerability-disclosures
    subtopics:
    - name: Vulnerability disclosure
      id: public-vulnerability-disclosure
      description: Wants to understand a specific publicly disclosed OutSystems
        Platform Server vulnerability (affected component, CVSS risk score,
        embargo/disclosure timeline, patched versions), so that they can assess
        their exposure and apply the appropriate patch or workaround to protect
        their OutSystems 11 on-premises or cloud environment.
    - name: Activate remoting security
      id: activate-remoting-security
      description: Wants to activate the Enhanced security for Remoting requests
        setting per environment (including LifeTime) after upgrading to the
        RPM-3056 fixed Platform Server/LifeTime versions and upgrading their
        apps, so that they can complete the mandatory remediation for this
        vulnerability without breaking apps compiled on older, unpatched
        versions.
  - name: Regulatory compliance
    id: regulatory-compliance
    subtopics:
    - name: HIPAA requirements
      id: hipaa-compliance-requirements
      description: Wants to understand what protected health information (PHI) and
        electronic PHI (ePHI) are under HIPAA, the difference between the HIPAA
        Privacy Rule and Security Rule, and who counts as a covered entity or
        business associate, so that they can determine whether their OutSystems
        application handling health data falls under HIPAA obligations.
    - name: Achieve HIPAA compliance
      id: hipaa-odc-compliance
      description: Wants to understand what's required to achieve HIPAA compliance for an
        OutSystems Developer Cloud (ODC) application handling protected health
        information (PHI), such as obtaining a Business Associate Agreement
        (BAA), independent auditor attestation, and access to an audit trail, so
        that they can legally handle PHI in production and meet their
        organization's compliance obligations.
    - name: Achieve PCI compliance
      id: pci-compliance
      description: Wants to achieve PCI DSS compliance for an OutSystems application
        processing credit card payments, by delegating payment handling to a
        third-party PCI-compliant payment gateway via URL redirection, iFrame
        integration, or tokenization (direct post or secure APIs) rather than
        processing card data directly, so that cardholder data is never stored
        or processed within OutSystems applications and compliance obligations
        are met.
    - name: PCI security checklist O11
      id: pci-security-checklist-o11
      description: Wants to know which security configurations (HSTS, secure
        endpoints, Content Security Policy) they must enable in OutSystems 11
        Sentry to satisfy PCI DSS SAQ D attestation requirements, so that they
        can complete the required steps before pursuing attestation with a
        PCI Qualified Security Assessor.
    - name: Safe Harbor compliance
      id: safe-harbor-compliance
      description: Wants to understand why the Safe Harbor ECJ ruling doesn't apply
        to their OutSystems 11 public cloud infrastructure, given that
        OutSystems and AWS keep content within the customer's chosen AWS
        Region, so that they can confirm they aren't exposed to unauthorized
        EU-US data transfers and know when they still bear responsibility for
        their own data-handling compliance.
    - name: FedRAMP IT authentication
      id: fedramp-it-authentication
      description: Wants to understand how IT users authenticate in an
        OutSystems FedRAMP environment, exclusively through an external IdP
        using OIDC with MFA enforced, with no SAML or built-in IdP option
        for IT users, so that they can configure external IdP access
        correctly before provisioning and know how to regain access via a
        support case if lockout occurs due to IdP misconfiguration.
    - name: FIPS Service Studio
      id: fips-service-studio-config
      description: Wants to configure Service Studio for FIPS compliance in an OutSystems
        FedRAMP environment by editing the local Settings.xml file to disable
        features not compliant with the Federal Information Processing
        Standards, repeating the change on every developer's workstation, so
        that their organization can meet FedRAMP compliance requirements for
        developer tooling.
    - name: O11 compliance offerings
      id: o11-compliance-offerings-overview
      description: Wants to understand which compliance offerings OutSystems 11 provides
        and what certifications or regulatory scope each one covers, so that
        they can identify the right offering for their organization's regulatory
        and certification requirements.
    - name: FedRAMP control support
      id: fedramp-control-support
      description: Wants to understand how OutSystems FedRAMP and Knox Systems
        hosting support specific FedRAMP Certification Class C control areas
        (identification and authentication, access control, system and
        communications protection, audit and accountability, contingency
        planning, personnel security), so they can evaluate the platform for
        their own FedRAMP compliance process.
    - name: FedRAMP compliance artifacts
      id: fedramp-compliance-artifacts
      description: Wants to know which compliance artifacts (System Security
        Plan, Security Assessment Report, Plan of Action and Milestones)
        OutSystems and Knox Systems maintain for FedRAMP and how their agency
        uses them, so they can reference the right documents when pursuing
        an Agency-level ATO.
    - name: FedRAMP hosting details
      id: fedramp-hosting-details
      description: Wants to understand where and how OutSystems FedRAMP is
        hosted (Knox Systems on AWS in designated US regions, Certification
        Class C/Moderate impact level) and its platform specifics (region,
        domain, database engine, high availability, support), so that they
        can evaluate whether the hosting model and platform configuration
        meet their requirements before choosing OutSystems FedRAMP.
    - name: FedRAMP VPC architecture
      id: fedramp-vpc-architecture
      description: Wants to understand the network architecture of
        OutSystems FedRAMP, including the Knox Systems-managed VPC, Internet
        Gateway, WAF, load balancer, public/private subnets across two
        Availability Zones, and how the LifeTime, Development,
        Non-Production, and Production environments connect to their
        databases, so that they can assess the platform's infrastructure
        design for their security review.
    - name: FedRAMP ATO types
      id: fedramp-ato-types
      description: Wants to understand the difference between FedRAMP
        certification (awarded to Knox Systems by the FedRAMP PMO after a
        third-party audit) and Agency-level ATO (issued by each federal
        agency after reviewing compliance documentation), so that they know
        which ATO applies to OutSystems FedRAMP itself versus the
        applications they build on it.
    - name: FedRAMP feature limits
      id: fedramp-feature-limits
      description: Wants to review which kinds of OutSystems 11 features are unavailable in
        OutSystems FedRAMP before designing or planning a project, so they avoid
        unsupported platform capabilities.
    - name: Native apps
      id: fedramp-native-app-alternatives
      description: Wants to understand the FedRAMP-supported alternative for
        native apps, so that they can keep building within the authorization
        boundary.
    - name: Tooling
      id: fedramp-tooling-alternatives
      description: Wants to understand the FedRAMP-supported alternative for
        tooling, so that they can keep building within the authorization
        boundary.
    - name: Integrations
      id: fedramp-integration-alternatives
      description: Wants to understand the FedRAMP-supported alternative for
        integrations, so that they can keep building within the authorization
        boundary.
    - name: Disaster recovery
      id: fedramp-disaster-recovery-alternatives
      description: Wants to understand the FedRAMP-supported alternative for
        disaster recovery, so that they can keep building within the
        authorization boundary.
    - name: Log streaming
      id: fedramp-log-streaming-alternatives
      description: Wants to understand the FedRAMP-supported alternative for
        log streaming, so that they can keep building within the authorization
        boundary.
    - name: External services
      id: fedramp-external-service-alternatives
      description: Wants to understand the FedRAMP-supported alternative for
        external services, so that they can keep building within the
        authorization boundary.
  - name: Mobile certificate protection
    id: mobile-certificate-protection
    subtopics:
    - name: Protect signing key
      id: protect-signing-key
      description: Wants to understand how OutSystems protects the Android and iOS mobile
        app signing certificate and password throughout the build lifecycle,
        including encrypted database storage, HTTPS transmission to the Mobile
        Apps Builder Service, S3/queue storage protected by IAM permissions, and
        automatic deletion after a defined retention period, so that they can
        assess the security of uploading their own developer signing key to
        OutSystems.
    - name: Certificate access scope
      id: certificate-access-scope
      description: Wants to understand who can access their Android/iOS mobile
        app signing certificate and password, including that on-premises
        database readers can't decrypt it without file-system access, and
        that the OutSystems Support team has DBA/system-administrator
        equivalent access to Cloud environments and Mobile Apps Builder
        Service assets used only for troubleshooting or outage response, so
        that they know the scope and justification for third-party access to
        their signing credentials.
- name: Production Recovery
  guid: e392931b-251f-4d43-8c0b-c85bbc97e02e
  topics:
  - name: Production rollback
    id: production-rollback
    subtopics:
    - name: Production rollback strategies in OutSystems
      id: rollback-strategy-os
    - name: Rollback to an app's previous version
      id: rollback-app-version
  - name: Production recovery using roll forward
    id: recovery-roll-forward
  - name: Cross-region disaster recovery (XRDR)
    id: cross-region-disaster-recovery
    subtopics:
    - name: XRDR key concepts
      id: xrdr-key-concepts
      description: Wants to understand the key concepts of OutSystems
        cross-region disaster recovery (XRDR), including failover/failback,
        cross-region data backup, recovery point objective (RPO), and
        recovery time objective (RTO), including the supported RPO/RTO
        tiers, so that they can evaluate XRDR's business continuity and
        compliance benefits for their OutSystems Cloud platform.
    - name: How XRDR works
      id: how-xrdr-works
      description: Wants to understand how OutSystems cross-region disaster
        recovery works, including the roles of the primary and disaster
        recovery regions and the cross-region backup schedule between them,
        so that they can understand the mechanics behind XRDR's business
        continuity guarantee.
    - name: XRDR failover process
      id: xrdr-failover-process
      description: Wants to understand the end-to-end cross-region disaster
        recovery failover process, including how a failover is triggered
        and approved, what infrastructure and security configurations are
        automatically preserved in the recovery region, and what
        post-failover steps are required, so that they can plan for and
        execute a failover with the OutSystems team.
- name: Progressive Release
  guid: eb4c73d7-260f-4048-9a2f-f61333238db1
  topics:
  - name: Progressive releases
    id: progressive-releases
    subtopics:
    - name: Progressive release strategies
      id: progressive-release
    - name: Canary releases using feature toggles
      id: canary-release-toggle
    - name: AB releases using feature toggles
      id: ab-release-feature-toggle
  - name: Advanced progressive releases
    id: progressive-release-adv
    subtopics:
    - name: Releases using manual blue-green deployments
      id: manual-blue-green-release
    - name: Automated blue-green deployments
      id: blue-green-deployment
- name: Static Code Analysis
  guid: 931dbd7f-f26d-4499-b25a-e60f5c268f9d
  topics:
  - name: Basic technical debt maintenance
    id: tech-debt-basics
    subtopics:
    - name: Manage technical debt with AIMS
      id: manage-tech-debt
    - name: Remove disabled code
      id: remove-disabled-code
      description: Wants to remove code that has been disabled for a while,
        or when the app is already in production and behaving correctly, so
        that the app stays easier to read and maintain.
    - name: Remove unused action
      id: remove-unused-action
      description: Wants to delete an action that is not used in an app or
        library and is not exposed to other apps or libraries, so that the
        code stays smaller, easier to maintain, and less exposed to security
        risk.
    - name: Technical debt formula
      id: tech-debt-formula
      description: Wants to understand how OutSystems 11 Code Quality
        calculates the technical debt value for a module, app, or the
        Factory Application Portfolio, by summing the number of findings
        per pattern weighted by each pattern's impact level, so that they
        can interpret the resulting percentage and color-coded debt level
        shown across the Apps, Findings, and Overview areas.
    - name: Investigate finding causes
      id: investigate-finding-causes
      description: Wants to investigate the cause of an OutSystems 11 Code
        Quality technical debt finding, by filtering findings by team, app,
        module, category, or status, and reviewing a code pattern's Impact
        and How to resolve information, so that they can understand why it
        counts as debt and how to fix it.
    - name: Ignore modules
      id: ignore-modules
      description: Wants to exclude specific modules from the OutSystems 11
        Code Quality technical debt calculation, by setting their status to
        Ignored Modules in the Maintenance area and waiting for the next
        synchronization, so that findings from those modules are resolved
        and removed from the infrastructure's technical debt score.
    - name: Reclassify module
      id: reclassify-module
      description: Wants to change a module's classification (architecture
        layer) in the OutSystems 11 Code Quality canvas, by selecting a new
        layer in the module details area and confirming the override, so
        that the app's classification and technical debt score are
        recalculated to reflect the change.
    - name: Customize pattern impact
      id: customize-pattern-impact
      description: Wants to customize the impact level (weight) that a specific
        code pattern has on the overall technical debt calculation in O11 Code
        Quality, by changing its status from Default to Customized in the
        Maintenance area, so that technical debt priorities reflect their
        organization's specific needs.
    - name: Resolve findings status
      id: resolve-findings-status
      description: Wants to resolve OutSystems 11 Code Quality technical debt
        findings, by setting each finding's status to Already Fixed, False
        Positive, Won't Fix, or Remind Me Later in the module findings view,
        so that the finding is correctly counted or excluded from the
        infrastructure's technical debt score after the next synchronization.
  - name: Technical debt dashboards
    id: tech-debt-dashboards
    subtopics:
    - name: Interpret overview dashboard
      id: interpret-overview-dashboard
      description: Wants to understand the overall status, distribution by
        category, and evolution of their factory's technical debt using the
        Infrastructure Overview dashboard in OutSystems 11 Code Quality, so
        that they can decide which apps or teams need more investment to
        reduce technical debt.
    - name: Filter overview dashboard data
      id: filter-overview-dashboard-data
      description: Wants to filter the Infrastructure Overview dashboard by
        team, application, module, code pattern category, or analysis date
        range, so that they can get the most relevant technical debt data to
        define their reduction strategy.
    - name: Interpret debt variation chart
      id: interpret-debt-variation-chart
      description: Wants to interpret the Technical Debt Variation vs. Number
        of Findings chart, correlating the variation percentage against the
        number of new and solved findings, so that they can understand how
        much weight those findings had on their overall technical debt score.
  - name: Code Quality setup
    id: code-quality-setup
    subtopics:
    - name: Set up Code Quality
      id: setup-code-quality
      description: Wants to register their infrastructure in OutSystems 11 Code
        Quality and install the code analysis and LifeTime probes, so that
        their apps' technical debt data starts being collected and appears in
        Code Quality.
    - name: Configure proxy access
      id: configure-code-quality-proxy
      description: Wants to configure a forward proxy for Code Quality by
        turning on proxy authentication, entering the proxy URL, and
        providing credentials in the LifeTime plugin, so that the plugin can
        connect to Code Quality SaaS through the proxy.
    - name: Connect performance analytics
      id: connect-performance-analytics-code-quality
      description: Wants to enable Code Quality to use runtime performance data from LifeTime Analytics by selecting the Runtime Performance probe environment, resuming monitoring for the app, republishing it, and waiting for synchronization, so that findings can show performance metrics.
    - name: Associate IT user
      id: associate-code-quality-user
      description: Wants to associate their IT user account with Code Quality
        after their infrastructure is already registered, using either an
        OutSystems account or an IT user account login flow through LifeTime,
        so that they can access Code Quality with their credentials.
    - name: Understand metric differences
      id: understand-code-quality-performance-differences
      description: Wants to understand why runtime performance metrics differ between
        LifeTime Analytics and Code Quality because of environment selection,
        synchronization cadence, and the data window, so that they can interpret
        the values correctly.
    - name: Fix Code Quality 401
      id: fix-code-quality-401
      description: Wants to resolve a 401 Unauthorized error encountered while
        logging into, registering infrastructure with, or associating an IT
        user with OutSystems 11 Code Quality, by identifying whether the cause
        is a wrong authentication mode, an incomplete IT user association, or
        invalid credentials, so that they can complete setup successfully.
    - name: Troubleshoot missing performance data
      id: troubleshoot-code-quality-performance-data
      description: Wants to troubleshoot missing runtime performance data in Code Quality after enabling the feature by checking the version prerequisites, monitoring status, republish state, traffic, recent analytics data, and findings, so that performance metrics appear after synchronization.
    - name: IT user login errors
      id: fix-code-quality-login-error
      description: Wants to resolve an "It was not possible to sign into Code
        Quality" login error in OutSystems 11, by checking whether the
        username doesn't exist, the password is incorrect, or the account is
        blocked due to failed attempts, and asking their LifeTime
        administrator to verify the username or unblock the account, so that
        they can regain access to Code Quality.
  - name: Code Quality API access
    id: code-quality-api-access
    subtopics:
    - name: Code Quality API auth
      id: code-quality-api-authentication
      description: Wants to authenticate requests to the OutSystems 11 Code
        Quality REST API by including the x-api-key and x-activation-code
        HTTP headers over an HTTPS connection, understanding that
        unauthenticated or plain HTTP calls fail, that full control
        permissions are required, and that the API key must be kept secure
        since Code Quality doesn't store it, so that they can successfully
        call the Code Quality API.
    - name: Manage Code Quality API key
      id: manage-code-quality-api-key
      description: Wants to generate, replace, or revoke their OutSystems 11
        Code Quality API key from the Maintenance > Configurations area,
        understanding that a lost key can't be retrieved (only regenerated by
        replacing it) and that revoking the key immediately blocks further
        API access, so that they can control and rotate access to the Code
        Quality API.
    - name: Retrieve tech debt data
      id: retrieve-tech-debt-data
      description: Wants to retrieve Code Quality technical debt data from the
        Swagger reference, including the technical debt endpoints and their
        filters, so that they can integrate the data into third-party tools
        or CI/CD quality gates.
  - name: SAST integration
    id: sast-integration
    subtopics:
    - name: Use third party SAST tools
      id: use-third-party-sast
    - name: Code Quality security scans overview
      id: code-quality-security-scans-overview
      description: Wants to understand which security issues OutSystems Code
        Quality scans can detect, and how that detection scope compares with
        third-party SAST tools, so that they can evaluate whether the built-in
        analysis covers the security checks they need before adding another
        SAST tool.
  - name: Automated SAST integration in dev
    id: sast-integration-dev
  - name: Automated SAST in CI/CD pipelines
    id: automated-sast-ci-cd
    subtopics:
    - name: Automated low-code analysis and SAST integrated in a CI/CD pipeline
      id: low-code-sast-ci-cd
  - name: Code Quality product FAQs
    id: code-quality-product-faqs
    subtopics:
    - name: Code Quality licensing
      id: code-quality-licensing
      description: Wants to understand how the OutSystems 11 licensing model limits the
        number of code analysis environments that can be linked to an Activation
        Code, so that they can determine whether multiple development
        environments can be analyzed separately.
    - name: Code Quality probes
      id: code-quality-probes
      description: Wants to check, update, or uninstall their Code Quality
        probes, and change the target code analysis environment, so that they
        keep the Code Quality Environment Probe, LifeTime Probe, and Shared
        components current and correctly configured without losing collected
        technical debt data.
    - name: Code Quality data privacy
      id: code-quality-data-privacy
      description: Wants to understand what platform, module, and personal
        data Code Quality collects, stores, and retains, and how to give or
        withdraw GDPR consent or request erasure, so that they can assess
        compliance before registering their infrastructure.
    - name: Request unscheduled sync
      id: request-unscheduled-sync
      description: Wants to request an unscheduled data synchronization between the Code
        Quality LifeTime probe and Code Quality SaaS in OutSystems 11, by
        selecting Request Synchronization in the Code Quality LifeTime plugin's
        Monitor page, understanding the daily limit on unscheduled requests per
        scheduled sync, so that they get updated technical debt data sooner than
        the default sync schedule.
    - name: Code Quality sync issues
      id: troubleshoot-code-quality-sync
      description: Wants to diagnose why Code Quality technical debt data
        appears stale or inconsistent across views in OutSystems 11, by
        checking the Last Sync date in the Apps tab, reviewing LifeTime
        Service Center Monitoring logs for ArPr_Communication probe errors,
        confirming connectivity to the Code Quality Web Service, and
        accounting for target-environment changes that require deleting
        existing data first, so that they can restore accurate
        synchronization and know when to open a support case.
    - name: Plugin communication
      id: plugin-communication-flow
      description: Wants to understand how the authentication method
        (OutSystems account versus IT User account) determines whether
        communication between the Code Quality LifeTime plugin and the Code
        Quality SaaS is plugin-initiated only or bidirectional, and that a
        forward proxy can be used with either method, so that they can plan
        the network connectivity required for Code Quality in OutSystems 11.
  - name: Code Quality permissions
    id: code-quality-permissions
    description: Wants to know how LifeTime permission levels and role
      assignment (default role, team, or app) determine which Code Quality
      features an IT user can access, including viewing teams and apps,
      managing findings, dashboards, and maintenance operations, so that
      they can plan and troubleshoot IT user access to Code Quality in
      OutSystems 11.
- name: Testing practice
  guid: 0e440c0c-7db6-41cd-a51c-0c00ccb15e72
  topics:
  - name: Fundamentals of testing strategies
    id: testing-strategy-basics
    subtopics:
    - name: Testing strategies
      id: testing-strategies
    - name: Manual testing strategies
      id: manual-testing-strategies
  - name: Fundamentals of testing in SDLC
    id: fund-testing-in-sdlc
    subtopics:
    - name: Pre-release testing
      id: pre-release-testing
    - name: Test phase in SDLC
      id: test-phase-in-sdlc
  - name: Testing in SDLC
    id: testing-in-sdlc
    subtopics:
    - name: End of sprint testing
      id: end-of-sprint-testing
    - name: Testing in iterations
      id: testing-in-iterations
      description: Wants to understand what a tester does during an iteration, including coordinating user acceptance testing, creating the next iteration test plan, and supporting shaping and build work, so that they can manage testing activities across the iteration.
  - name: Fundamentals of testing practice
    id: testing-practices
    subtopics:
    - name: Test scripts
      id: test-scripts
  - name: Road to automation
    id: road-to-automation
    subtopics:
    - name: Test pyramid
      id: test-pyramid
    - name: Prioritizing automation of tests
      id: prioritize-test-automation
    - name: Automated testing strategies
      id: automated-test-strategy
    - name: Team automation roles
      id: team-automation-roles
      description: Wants to assign test automation responsibilities across product
        owners, business analysts, engagement managers, tech leads, developers,
        and manual testers when implementing a test automation strategy with a
        team, so that the team plans, builds, triages, and maintains automated
        tests as a shared responsibility.
    - name: Infra integration testing
      id: infra-integration-testing
      description: Wants to automate integration tests for non-API dependencies,
        such as virtualization tools, external databases, mail servers, and DNS
        or proxy servers, by identifying their most critical failure points, so
        that automated checks can quickly pinpoint or rule out the source of an
        issue when problems occur.
    - name: Choose testing tool
      id: choose-testing-tool
      description: Wants to select the right automated testing tool for their
        OutSystems project by weighing their team's coding skills, the app's UI
        complexity, licensing budget, data-driven testing needs, and
        cross-technology test coverage, so that they pick a tool suited to
        their context instead of over- or under-investing.
  - name: Acceptance criteria
    id: acceptance-criteria
    subtopics:
    - name: Three amigos sessions
      id: three-amigos-sessions
    - name: Gherkin syntax for functional acceptance criteria
      id: gherkin-functional-criteria
- name: Traceability
  guid: da3a0cdb-4d5e-4281-add1-df304cd6db5f
  topics:
  - name: Basic traceability
    id: basic-tracebility
    subtopics:
    - name: Track work using tag and deployment notes
      id: track-work-tag-deploy
    - name: Traceability strategies in OutSystems
      id: traceability-outsystems
  - name: Traceability for automated deployments
    id: traceability-deployments
    subtopics:
    - name: Integrate with ALM tools
      id: integrate-with-alm-tools
  - name: Traceability in CI/CD pipelines
    id: traceability-ci-cd
    subtopics:
    - name: Integrate with external version control systems in a CI/CD pipeline
      id: external-version-control-ci
- name: UI testing
  guid: 0a465f0d-889b-4ff4-8903-9f3acd3c98c3
  topics:
  - name: Fundamentals of UI testing strategies
    id: ui-testing-strategy
    subtopics:
    - name: Manual ad-hoc UI tests
      id: manual-ad-hoc-ui-tests
    - name: UI testing strategies
      id: ui-testing-strategies
  - name: Manual UI testing
    id: manual-ui-testing
    subtopics:
    - name: Manual UI tests using testing scripts
      id: manual-ui-tests
    - name: Ad-hoc visual defect reporting
      id: ad-hoc-defect-reporting
  - name: Automated UI testing
    id: automated-ui-testing
    subtopics:
    - name: Automated UI tests
      id: automated-ui-tests
    - name: Page object model
      id: page-object-model
      description: Wants to structure automated web UI tests around reusable page objects, page widgets, interactions, assertions, and templates so the test framework stays maintainable as the UI changes.
- name: User Management
  guid: 30a6f6ca-c898-4e05-b5d6-88a6789ca36c
  topics:
  - name: Authentication
    id: authentication
    subtopics:
    - name: IT Users external IdP OIDC overview
      id: it-users-external-idp-oidc-overview
      description: Wants to understand what the IT Users Integration with
        External IdP (OIDC) feature does and why it's used, including
        reducing password-related risk, consolidating identity providers,
        and enabling single sign-on for IT users across OutSystems tools, in
        OutSystems 11, so that they can decide whether to adopt it for
        securing IT user access and centralize IT user authentication without
        deploying a separate IAM solution.
    - name: IT Users external IdP login flow
      id: it-users-external-idp-login-flow
      description: Wants to understand how IT users authenticate through
        the external IdP OIDC login flow when accessing OutSystems tools
        such as Service Center, LifeTime, Service Studio, and Integration
        Studio, including the token exchange sequence between the tool,
        the Server API, and the identity provider, in OutSystems 11, so
        that they can explain how authentication is handled end-to-end for
        each tool.
    - name: External IdP flow exceptions
      id: external-idp-flow-exceptions
      description: Wants to know which OutSystems tools and custom apps
        don't enforce the External IdP (OIDC) login flow for IT users,
        such as the OSP Tool and apps not using the User_GetUnifiedLoginUrl
        API, and instead continue to use username and password or LT login
        authentication plugins, in OutSystems 11, so that they can identify
        exceptions when planning an External IdP rollout.
    - name: Change authentication provider
      id: change-authentication-provider
      description: Wants to change an OutSystems 11 environment's
        authentication provider in LifeTime by choosing the new method,
        configuring it, testing it, propagating the settings, and applying
        them across environments, so that IT users sign in through the
        selected provider.
    - name: Custom federated SSO
      id: custom-federated-sso
      description: Wants to understand how to integrate a centralized federated
        single sign-on setup into template-based OutSystems apps by using a
        central authentication server, an authentication library, and the
        SAML authentication flow, so that users sign in once and return to the
        original app URL.
    - name: Configure Okta SAML
      id: configure-okta-saml
      description: Wants to configure an Okta SAML application and the matching
        IdP Forge settings for an OutSystems 11 Traditional Web exercise,
        including the Single sign on URL, Audience URI, certificate import,
        attribute statements, and claim mapping, so that users can
        authenticate through Okta with SAML.
    - name: Built-in Authentication
      id: built-in-authentication
    - name: Custom authentication
      id: custom-authentication
      description: Wants to understand when OutSystems built-in authentication
        is not enough and how to choose a custom authentication approach,
        including external corporate authentication through REST or SOAP and
        federated single sign-on through OAuth or SAML, so that they can decide
        how to integrate identity systems that the built-in methods do not
        cover.
    - name: Get authenticated user id
      id: get-authenticated-user-id
      description: Wants to retrieve the identifier of the user currently
        authenticated with the server, or a null identifier when
        unauthenticated, inside a server action, service action, or database
        aggregate, using the GetUserId built-in function, so that logic can
        reference or filter data by the current user's identity.
    - name: Integrated authentication
      id: integrated-windows-authentication
      description: Wants to understand how OutSystems 11 Integrated Windows
        Authentication (NTLM/Kerberos) works, including the HTTP 401
        challenge-response flow and domain-credential prompt, and which
        elements (Web Screens, Web Flows, exposed/consumed SOAP Web Services)
        support enabling the Integrated Authentication property, so that end
        users can be automatically authenticated using their Windows domain
        credentials without a separate login screen, provided the client and
        front-end server share the same domain and Active Directory.
    - name: Windows auth actions
      id: windows-auth-actions
      description: Wants to use the IntegratedSecurityGetDetails and
        IntegratedSecurityCheckRole built-in server actions to get the
        current Windows user's details and check their Windows role
        membership, in Traditional Web Apps using Integrated Windows
        Authentication, so that app logic can act on the authenticated
        Windows user's identity and role without a custom login mechanism.
    - name: Fix recurring WIA popup
      id: fix-recurring-wia-popup
      description: Wants to stop the Windows Integrated Authentication (WIA)
        login popup from repeatedly reappearing for end users in an
        OutSystems 11 .NET Traditional Web app, by restricting WIA to the
        login page instead of every page (relying on Roles for access
        control) or by configuring the browser to trust the server for
        implicit authentication, so that users aren't repeatedly prompted
        for credentials during normal use.
    - name: Fix WIA Negotiate Login
      id: fix-wia-negotiate-login
      description: Wants to resolve the Windows Integrated Authentication login
        failure in Internet Explorer or Edge caused by the NEGOTIATE protocol,
        by disabling NEGOTIATE in IIS or on the client workstation, so that
        users can sign in successfully with Integrated Authentication.
    - name: Extend login validation
      id: extend-login-validation
      description: Wants to add custom server-side validation to the built-in
        end-user login flow by extending the Users data model with a 1-to-1
        related entity, creating a server action that validates the extra data,
        and wiring that action into the Login/DoLogin flow, in OutSystems 11
        Traditional Web, Reactive Web, and Mobile apps, so that additional
        business rules (for example, account expiration or external-system
        checks) can block or allow login beyond the default username/password
        check.
    - name: Custom auth screens
      id: custom-auth-screens
      description: Wants to use and edit the pre-built authentication screens and
        blocks (Login, ChangePassword, RecoverPassword, UserProfile, UserInfo, PasswordPolicy)
        available in the Common UI folder in ODC Studio, so that they can implement
        a custom-branded authentication flow without building it from scratch.
    - name: External login redirect
      id: external-login-redirect
      description: Wants to configure the OnException global handler's
        SecurityException flow to call GetExternalLoginURL and set the
        Destination to the Login screen, in ODC Studio, so that unauthenticated
        users accessing a protected screen are automatically redirected to the
        configured external identity provider's login page.
    - name: External authentication flow
      id: external-authentication-flow
      description: 'Wants to add external authentication to a template-based
        OutSystems app by isolating the login logic in a reusable library,
        validating credentials in an external system, updating the user in the
        Users component, and redirecting back to the original URL, so that the
        app can use an external identity system while keeping the local
        session.'
    - name: External IDPs
      id: external-idps
    - name: Replace external IdP
      id: replace-external-idp
      description: Wants to replace an external IdP in ODC without disrupting
        authentication, by configuring and assigning the new provider,
        updating apps with custom login logic (settings or the
        IdentityProvider property) to reference it, verifying successful
        authentication, and then unassigning the old provider, so that user
        profile associations are preserved and login continuity isn't
        interrupted, including when only the issuer changes.
    - name: Remove built-in IdP
      id: remove-built-in-idp
      description: Wants to remove the built-in identity provider assignment
        for an organization or app stage in ODC Portal (Management > Govern
        > Identity providers > Built-in provider > Unassign), by clearing
        the scope checkboxes to unassign, so that end users authenticate
        only via the assigned external IdP(s), understanding this action
        can cause lockout if the external IdP isn't working correctly.
    - name: Assign external IdP
      id: assign-external-idp
      description: Wants to assign an existing external identity provider to their
        organization or to specific app stages in ODC Portal (Management >
        Govern > Identity providers), by selecting the target scopes and
        confirming the change, so that users can authenticate with that
        provider, understanding that the change doesn't apply instantly to
        already signed-in users and that propagation time varies depending on
        the provider's configuration.
    - name: Unassign external IdP
      id: unassign-external-idp
      description: Wants to unassign an external identity provider from an
        organization or app stage in ODC Portal (Management > Govern >
        Identity providers) without replacing it, so that they can stop using
        that provider for the selected scope.
    - name: Edit external IdP
      id: edit-external-idp
      description: Wants to edit the configuration of an external identity
        provider that is not currently assigned in ODC Portal (Management >
        Govern > Identity providers), so that they can update its settings
        before assigning it.
    - name: Delete external IdP
      id: delete-external-idp
      description: Wants to delete an external identity provider that is not
        currently assigned in ODC Portal (Management > Govern > Identity
        providers), so that they can remove an unused configuration.
    - name: External IdP concepts
      id: external-idp-concepts
      description: Wants to understand what external identity providers (IdPs) are,
        why they are beneficial (reduced overhead, enhanced security, SSO), and the
        core concepts of authentication, authorization, and single sign-on in the
        context of ODC, so that they can explain external IdP integration and its
        value before configuring it.
    - name: IdP assignment scopes
      id: idp-assignment-scopes
      description: Wants to understand the difference between assigning an
        external IdP at organization scope (for member sign-in to ODC Portal
        and ODC Studio) versus stage scope (for end-user app sign-in),
        including that multiple IdPs can be active per scope and that
        stage-level assignment is portfolio-scoped in multi-portfolio
        organizations, so that they can decide the correct scope when
        planning an external IdP rollout.
    - name: Add SAML 2.0 provider
      id: add-saml-provider-odc
      description: Wants to add a SAML 2.0 identity provider in ODC Portal
        by naming the provider, selecting its scope (organization or a
        specific stage), exchanging SP and IdP metadata, configuring claim
        mapping and advanced signing options, and reviewing the
        configuration before adding it, so that end users can authenticate
        via that provider.
    - name: SAML endpoint access
      id: saml-endpoint-access
      description: Wants to know which SAML endpoints (metadata, single
        logout, and single sign-on) must be reachable from ODC to their
        identity provider before configuring a SAML 2.0 provider, including
        which ones can be skipped when uploading metadata as an XML file,
        so that they can expose or allowlist only the required endpoints.
    - name: SAML single logout
      id: saml-single-logout
      description: Wants to understand what the single logout service URL
        does and when it needs to be configured for a SAML 2.0 provider in
        ODC, including the consequences of leaving it unconfigured (users
        remaining signed in at the identity provider), so that they can
        decide whether to enable SAML single logout for their setup.
    - name: Lockout
      id: lockout
    - name: Avoid IdP lockout
      id: avoid-idp-lockout
      description: Wants to avoid a total IdP lockout (loss of ODC Portal, ODC
        Studio, or app access) when removing the built-in IdP assignment, by
        fully testing the external IdP beforehand and verifying its SSO
        session timeout is long enough to cover the complete login flow
        (including MFA, consent, or profile matching steps), so that they
        don't need to rely on a support ticket to reactivate the built-in
        provider.
    - name: App brute force config
      id: app-brute-force-config
      description: Wants to configure the brute force login protection thresholds and
        backoff delays for application (end) users via the Users module's Site
        Properties in Service Center, including enabling the protection and
        setting the username- and IP-level attempt thresholds and backoff delay
        windows, in OutSystems 11, so that they can tune how aggressively user-
        and IP-level brute force attacks are throttled.
    - name: IT brute force config
      id: it-brute-force-config
      description: Wants to configure the brute force login protection settings
        for IT users signing in to Service Center and LifeTime, using the
        Factory Configuration Forge application deployed to all environments,
        in OutSystems 11, so that the same backoff protection behavior is
        applied consistently for IT users across every application and
        LifeTime environment.
    - name: Diagnose failed login block
      id: diagnose-failed-login
      description: 'Wants to understand why they''re seeing a "Too many failed
        login attempts" error when signing into an OutSystems application or
        management console (LifeTime/Service Center), by identifying the
        possible causes (mistyped password, stale credentials in Service
        Studio, brute-force attack on the account or the network''s IP), so
        they know whether the account, the IP, or both were blocked by
        brute-force protection, in OutSystems 11.'
    - name: Unblock user after failed logins
      id: unblock-user-login
      description: Wants to unblock an end user or IT user account, or the IP
        address, blocked by the brute-force login protection, using LifeTime
        or Service Center (for IT users) or the Users application (for end
        users), as an administrator, so the blocked user can sign in again,
        in OutSystems 11.
    - name: Passwords
      id: passwords
    - name: Update user email
      id: update-user-email
      description: Wants to change a logged-in end-user's email address in
        ODC's built-in identity provider by calling StartUpdateEmail to send
        a verification code and FinishUpdateEmail to confirm it, so that the
        user's login email is updated without requiring ODC Portal access.
    - name: Automatic user creation
      id: automatic-user-creation
      description: Wants to resolve the OS-IDCO-CONF-40102 login error, caused
        by the automatic user creation setting being disabled for an external
        IdP configuration in ODC, by either enabling automatic user creation
        or manually provisioning the user account, so that end users can log
        in successfully.
    - name: Username claim mapping
      id: username-claim-mapping
      description: Wants to resolve the OS-IDCO-CONF-40001 login error, caused
        by a missing or misconfigured username claim mapping in the external
        IdP, by verifying the IdP's claim mapping configuration and confirming
        it matches the mapping configured in ODC, so that end users can
        authenticate successfully.
    - name: Duplicate IdP email conflict
      id: duplicate-idp-email-conflict
      description: Wants to resolve the OS-IDCO-CONF-40002 login error, caused
        by an external IdP trying to update a user profile with an email
        address already associated with a different existing user profile
        from another IdP, by removing the conflicting duplicate profile and
        reviewing IdP account-linking configuration, so that end users can
        log in successfully.
    - name: Email verified claim
      id: email-verified-claim
      description: Wants to resolve the OS-UAUC-60040 login error, caused by
        a missing or invalid email_verified claim in the external IdP
        configuration when using the Trust identity provider setting, by
        verifying the IdP's claim mapping to ensure it sends the correct
        data, so that end users can log in successfully.
    - name: Trust IdP email setting
      id: trust-idp-email-setting
      description: 'Wants to know when to set organization user email
        verification to "Trust identity provider" in ODC, so that ODC honors
        the IdP''s email_verified claim instead of performing its own
        verification, when the IdP is known to guarantee verified emails.'
    - name: Fix invalid invitation
      id: fix-invalid-invitation
      description: Wants to resolve the OS-UAUC-60043 onboarding error,
        caused by the invitation's associated profile target becoming
        invalid after an authentication configuration change, by verifying
        identity provider configurations with the administrator and having
        the administrator resend the invitation, so that the end user can
        complete onboarding.
    - name: Add social accelerator
      id: add-social-accelerator
      description: Wants to add a social identity provider to ODC using the accelerator
        configuration screen in the ODC Portal, including entering the
        provider's client credentials and choosing email verification and
        profile matching settings, so that end users can authenticate with that
        social provider.
    - name: Social accelerator info
      id: social-accelerator-info
      description: Wants to know what credentials to gather from Apple,
        Google, Facebook, or LinkedIn (for example, Client ID, Client
        Secret, or Apple's Key ID and private key) before configuring that
        provider as a social accelerator in ODC, so that the accelerator
        configuration screen can be completed without missing information.
    - name: External IDPs - User Mapping
      id: user-mapping
    - name: Map users from external systems
      id: map-external-auth-users
      description: Wants to map IT users from an external authentication
        provider to matching LifeTime users, either manually or by using
        the LifeTime Services API, so that each external user can
        authenticate to LifeTime with an existing OutSystems account.
    - name: External IDPs - LinkedIn
      id: idp-linkedin
    - name: External IDPs - Google
      id: idp-google
    - name: External IDPs - Facebook
      id: idp-facebook
    - name: External IDPs - Apple
      id: idp-apple
    - name: External IDPs - OpenIDP
      id: idp-openidp
    - name: External IDPs - Auth0
      id: idp-auth0
      description: Wants to integrate Auth0 as an external identity provider with
        an OutSystems 11 (Traditional Web) application using the IdP Forge component
        and the SAML protocol, including installing IdP, signing up for Auth0,
        configuring the SAML connection and callback URL in Auth0, and mapping the
        SAML configuration parameters between Auth0 and the IdP Configuration page,
        so that end users can log in via Auth0 single sign-on.
    - name: Migrate SAML provider for domain change
      id: migrate-saml-domain-change
      description: Wants to create and cut over to a new SAML 2.0 provider
        in ODC after an organization or stage domain change, understanding
        that the SP metadata's EntityID and ACS URL are immutable and tied
        to the domain, so that end users can keep authenticating once the
        previous domain is deleted.
    - name: Configure Okta IdP
      id: idp-okta
      description: Wants to register ODC as a trusted OIDC application in
        Okta (grants, redirect URIs, access policies) and configure the
        matching connection in the ODC Portal using the Issuer Metadata URI
        as the Discovery endpoint, entering the Client ID/Secret, and
        confirming claim mapping, so that they can establish a secure OIDC
        handshake between ODC and Okta.
    - name: Map AD Groups to Roles
      id: map-ad-groups-to-roles
      description: Wants to map Active Directory Groups to OutSystems Roles using
        the Active Directory and Active Directory Group Mapping Forge components —
        including installing the components, creating an access token, defining role
        mappings, and updating the login flow to call RefreshUserRoles before session
        creation — in OutSystems 11, so that user permissions managed in Active Directory
        are automatically reflected in OutSystems application roles.
    - name: Sync AD roles schedule
      id: sync-ad-roles-schedule
      description: Wants to configure the schedule of the SyncRoles, SyncGroups,
        and SyncUsers timers provided by the Active Directory Group Mapping component
        — including setting timer schedules in Service Center and triggering on-demand
        synchronisation — in OutSystems 11, so that changes to Active Directory Groups
        are regularly propagated to OutSystems user roles without manual intervention.
    - name: Map O11-ODC groups
      id: map-o11-odc-groups
      description: Wants to map existing O11 end-user groups to their corresponding
        ODC end-user groups by creating an end-user group mapping for each ODC group,
        including enabling the Include Groups Claim option when using the UsersIdP
        component, after setting up single sign-on between O11 and ODC, so that end
        users authenticating through O11 receive the correct permissions in their
        ODC apps.
    - name: Coordinate SSO sessions
      id: coordinate-sso-sessions
      description: Wants to implement coordinated logout pages on both O11 and
        ODC sides (an O11 logout page redirecting to ODC, and an ODC logout
        page redirecting back to O11), work around GetExternalLogoutURL's
        automatic addition of the ODC stage domain to ReturnToURL by adding an
        intermediate PostLogout ODC screen that then redirects to the O11
        logout page, after setting up O11 and ODC single sign-on for external
        authentication, so that terminating one platform's session properly
        ends the shared IdP session instead of triggering an automatic
        re-login, and so that the external IdP session terminates and both O11
        and ODC sessions end correctly during external authentication SSO.
    - name: Fix invalid email
      id: fix-invalid-email
      description: Wants to resolve the "User with invalid email" error that
        appears when an end user authenticates via O11-ODC single sign-on
        (UsersIdP) but lacks a valid email address configured in the O11
        Users app.
    - name: Install UsersIdP for SSO
      id: install-usersidp-sso
      description: Wants to install and configure the UsersIdP Forge component
        in an O11 environment, including creating the UsersIdP client, adding
        O11 as an external identity provider in ODC, assigning it to an ODC
        stage, and configuring redirect URIs, so that end users authenticating
        with O11 built-in authentication get single sign-on access to ODC apps.
    - name: O11-ODC interop limits
      id: o11-odc-interop-limitations
      description: Wants to know the limitations of O11-ODC user
        interoperability before setting up SSO, including that the UsersIdP
        component only supports O11 built-in authentication,
        external-authentication interoperability is limited to protocols ODC
        supports, and O11 and ODC apps can't be served from the same
        subdomain, so they can plan realistic domain and authentication
        architecture.
    - name: Configure auth settings
      id: configure-app-authentication-settings
      description: Wants to configure OutSystems 11 app authentication settings
        in Service Center's Applications Authentication area, including Cache
        Time In Minutes, Single Sign-On Between App Types, and the persistent
        (Max Idle Time, Cookie Expiration) and session (Max Idle Time)
        authentication settings, so that app authentication behavior meets
        their environment's security and user-experience requirements.
    - name: Authentication cookies
      id: authentication-cookies
      description: Wants to understand how OutSystems 11 authenticates app
        requests, including the difference between session and persistent
        authentication (the RememberLogin parameter), the purpose and
        HttpOnly flags of the nr1 and nr2 authentication cookies, and how the
        server verifies the X-CSRF-Token header and cookies on each request,
        so that they can reason about how their app's login state is
        maintained and validated.
    - name: Auth cache and session extension
      id: auth-cache-session-extension
      description: Wants to understand how the OutSystems 11 authentication
        cache window affects when a session's login expiration is extended, so
        that they can predict session expiration behavior across their apps.
    - name: Logout propagation across app types
      id: logout-propagation-app-types
      description: Wants to understand how logging out of an app propagates
        across Reactive, Mobile, and Traditional apps sharing the same browser
        scope when Single Sign-On Between App Types is configured, so that
        they can predict logout behavior across their apps.
    - name: Configure SAML authentication
      id: configure-saml-authentication-o11
      description: Wants to configure SAML 2.0 authentication in the Users
        application by selecting the SAML 2.0 authentication method, entering
        the Identity Provider's configuration values, enabling signed login
        response verification to match their IdP's assertion signing setting,
        and exchanging SP/IdP metadata XML files, in OutSystems 11, so that
        end users can authenticate via their enterprise Identity Provider.
    - name: Mobile SSO login flow
      id: mobile-sso-login-flow
      description: Wants to implement the mobile SSO login flow for a mobile
        app by installing the SSO Mobile plugin, checking whether SSO is
        available, and wiring the LoginSSO action to the identity provider,
        when the app uses SAML 2.0 single sign-on.
    - name: Mobile SSO logout flow
      id: mobile-sso-logout-flow
      description: Wants to implement the mobile SSO logout flow for a mobile
        app by updating the UserInfo block to call the right logout actions
        for PWA and native users, when the app uses SAML 2.0 single sign-on.
    - name: Troubleshoot SAML errors
      id: troubleshoot-saml-errors
      description: 'Wants to troubleshoot SAML 2.0 authentication issues in
        OutSystems 11 by reviewing the SAML Message Logs, adjusting the
        DeleteSAMLLogsOlderThen site property retention period, regaining
        access via the default Users app login page when locked out of a
        misconfigured authentication method, and resolving common SAML
        configuration errors, so that they can diagnose and fix broken SAML
        logins.'
    - name: Map IdP groups
      id: map-idp-groups
      description: Wants to understand how OutSystems maps external SAML
        Identity Provider group names to OutSystems groups via the Groups
        claim in the Configure Authentication screen of the Users
        application, including auto-creation of unmatched groups and the
        Microsoft Entra GUID-naming limitation, so that they can align their
        OutSystems group names with their IdP's group data.
    - name: Auth methods overview
      id: auth-methods-overview
      description: Wants to understand the end-user authentication methods available in
        OutSystems 11 and select one via the Configure Authentication dropdown
        in the Users application, so that they can choose and set up the
        appropriate identity source for their end users.
    - name: End-user login flow
      id: end-user-login-flow
      description: Wants to understand how OutSystems 11 handles end-user
        login depending on the configured authentication method, including
        SSO redirects for SAML 2.0, Microsoft Entra, and Okta, the Integrated
        Windows Authentication challenge flow, and the Login screen
        credential validation against Active Directory or LDAP, so that they
        can predict how and where end users authenticate.
    - name: User data sync
      id: user-data-sync
      description: Wants to understand how OutSystems 11 synchronizes
        end-user Name, Email, and Mobile Phone data from Active Directory,
        LDAP, SAML 2.0, Microsoft Entra, or Okta after login and via the
        daily SynchronizeDomainUsers timer, including the known issue where a
        reactivated Active Directory user's status isn't automatically
        updated to active, so that they can diagnose synchronization
        behavior and apply the manual status-update workaround.
    - name: UsersIdP security risks
      id: usersidp-security-risks
      description: Wants to understand the security characteristics and
        identity-spoofing risks of the UsersIdP component (token validation,
        no password sync, unvalidated email change risk) before deploying
        O11-ODC SSO, so that they monitor O11 and ODC for suspicious activity
        to prevent user impersonation.
    - name: UsersIdP limitations
      id: usersidp-limitations
      description: Wants to confirm which authentication methods, Users app configuration,
        and user provider the UsersIdP component supports, before installing it,
        so that they can determine whether O11-ODC SSO via UsersIdP is viable
        for their environment.
    - name: O11-ODC SSO overview
      id: o11-odc-sso-overview
      description: Wants to understand how SSO lets O11 end users access
        both O11 and ODC apps without re-authenticating, including the two
        authentication scenarios (O11 built-in authentication, where the
        Users app acts as an external OIDC provider for ODC, versus O11
        external authentication, where both platforms federate with a
        shared external identity provider), so that they can plan which
        SSO setup path applies to their environment.
    - name: Set up O11 external SSO
      id: setup-o11-external-sso
      description: Wants to configure O11 and ODC single sign-on for the O11
        external authentication scenario by adding the external IdP in ODC,
        configuring redirect URIs, and assigning the IdP to ODC stages, when
        O11 apps authenticate end users through an external identity provider
        (OIDC or SAML) instead of O11 built-in authentication, so that end
        users sign in once at the IdP and reuse that session across O11 and
        ODC apps without separate ODC credentials.
    - name: Refresh token rotation
      id: refresh-token-rotation
      description: Wants to understand how ODC's refresh token rotation
        invalidates the previous token on each refresh and revokes all
        related tokens if a used token is replayed, so that they can explain
        unexpected forced logouts and reason about session security.
    - name: Self-hosted IdP requirements
      id: self-hosted-idp-requirements
      description: Wants to understand why self-hosted tenants require an
        external OIDC identity provider for every stage, including
        Development, and why two separate connector configurations (For
        Cloud vs. For self-hosted) are needed, so that they can plan and
        configure authentication correctly for a self-hosted tenant.
    - name: External IdP system limits
      id: external-idp-system-limits
      description: Wants to know the technical requirements and protocol
        limitations for external IdPs in ODC (endpoint network
        reachability or firewall allowlisting, unsupported dynamic issuer
        URIs, PKCE-only OIDC flows, SP-initiated-only SAML), so that they
        can confirm their IdP is compatible before configuring it.
    - name: Validate IdP setup
      id: validate-idp-setup
      description: Wants to verify that an external IdP's claim mappings
        and end-to-end login flow work correctly in ODC, by inspecting
        token claims against portal configuration and testing complete
        login flows across different user types and groups, before
        deploying the IdP to production, so that they can catch mapping
        issues and authentication failures early.
  - name: Authorization
    id: authorization
    subtopics:
    - name: Manage end-user roles
      id: assign-roles-o11
      description: Wants to grant or revoke an end user's application role,
        using the Applications tab in the Users application to add or remove
        specific users under a role, in OutSystems 11, so that end users
        gain or lose access to the app's associated screens and tasks.
    - name: Users app visibility
      id: users-app-visibility
      description: Wants to make an application appear in the Users
        application so that application roles can be assigned, when the app
        has no Home module defined.
    - name: Creating app role
      id: create-app-role
    - name: Application Roles
      id: app-roles
    - name: Creating Organization Role
      id: create-org-role
    - name: Delete app roles
      id: delete-app-role
    - name: Updating app role
      id: update-app-role
    - name: Assign Users to Organization Roles
      id: assign-to-org-roles
    - name: Assign org-scope role
      id: assign-org-scope-role
      description: Wants to grant or revoke a member's (IT-user's) role at
        the organization scope in the ODC Portal, using Edit role on the
        Organization access tab to pick a single role or No role, so that
        the member's tenant-wide permissions update immediately.
    - name: Assign asset-scope roles
      id: assign-asset-scope-roles
      description: Wants to grant or revoke asset-scope (app-specific) roles
        for a member (IT-user) in the ODC Portal, using the Assign roles
        option on the Organization access tab, so that the member's
        permissions are scoped to specific apps rather than the whole
        organization.
    - name: Assign end-user roles
      id: assign-end-user-roles-odc
      description: Wants to grant or revoke an end-user's role for a specific
        app and stage (Development, QA, or Production) in the ODC Portal,
        either by inviting a new end-user from the Users section, selecting
        End user access and the role to grant, or by using Manage roles for
        an existing end-user in the End-user access tab, so that role-based
        access control (RBAC) updates the end-user's access to app features
        and data after external IdP login or sign-up completion.
    - name: Grant/revoke role action
      id: grant-revoke-role-action-odc
      description: Wants to grant or revoke an end-user's application role
        programmatically, using the built-in GrantRole and RevokeRole
        Actions inside a Server Action logic flow, in ODC, including
        granting the role immediately inside the same custom server action
        that finishes user activation or registration, so that role
        assignment can be automated instead of done manually through the
        ODC Portal.
    - name: Create user & assign role via API
      id: create-user-assign-role-rest-api
      description: Wants to create a new end user and grant them an
        application role programmatically, using the ODC public REST API
        endpoints (POST /users, GET /assets, GET /application-roles, POST
        /users/{key}/application-roles/{roleKey}), so that user
        provisioning and access control can be automated instead of done
        manually through the ODC Portal.
    - name: Define Permissions
      id: define-permissions
    - name: Permissions
      id: permissions
    - name: Assign Users to User Groups
      id: assign-to-user-groups
    - name: User Groups
      id: user-groups
    - name: Deleting Organization Role
      id: delete-org-roles
    - name: Duplicating Organization Role
      id: duplicate-org-roles
    - name: Updating Organization Role
      id: update-org-roles
    - name: Organizational Roles
      id: org-roles
    - name: Fix ODC permissions
      id: fix-odc-permissions
      description: Wants to resolve the invalid permissions screen an end
        user sees in an ODC app after O11-ODC SSO login, caused by a missing
        or unassigned ODC app role for that end user.
  - name: Users in OutSystems
    id: users-in-os
    subtopics:
    - name: Creating Users
      id: creating-users-o11
      description: Wants to understand how end-users are created in
        OutSystems 11, using the built-in Users application or
        programmatically via the Users module's actions, so that new
        employees, customers, or partners can be provisioned with app
        access without going through public self-registration.
    - name: Register user actions
      id: register-user-system-actions-odc
      description: Wants to register a new end user in ODC's built-in
        identity provider by calling StartUserRegistration to create the
        user and obtain a temporary password, then FinishUserRegistration
        to activate the password and log the user in, including when
        completing a custom self-registration flow outside the standard
        self-registration screens, so that a new user account becomes
        usable immediately.
    - name: End-user management
      id: end-user-management
      description: Wants to understand how end-users are managed in ODC
        Portal, including that they are not created only programmatically
        and that each end-user needs at least one application role to
        access protected screens, so that they can identify the correct
        statement about ODC user administration and access rules.
    - name: Login actions
      id: login-actions
      description: Wants to know which built-in login actions are available in
        ODC, so they can build sign-in flows.
    - name: Deleting Users
      id: deleting-users
    - name: Updating Users
      id: updating-users
    - name: Activate deactivated user
      id: activate-deactivated-user
      description: Wants to reactivate a deactivated end-user account in ODC,
        using either the ODC Portal's Management > Users page (Activate User)
        or the ODC user management APIs, so that the user can resolve the
        OS-IDCO-AUTH-40101 error and resume authenticating through the
        external IdP.
    - name: Self Registration
      id: self-registration
    - name: Build self-reg form
      id: build-self-registration-form
      description: Wants to build the verification code and password inputs
        for a self-registration form in ODC Studio, by adding a boolean
        condition to swap form content, inserting header and input widgets,
        and wiring local variables, so that a new user can enter their
        verification code and set a password after signing up.
    - name: Validate password confirmation
      id: validate-password-confirmation
      description: Wants to validate a new user's password against the
        platform's complexity policy and confirm it matches a re-entered
        value, using the PasswordPolicy block and an OnChange client action
        comparing Input_ConfirmPassword to Password, in ODC Studio, so that
        the self-registration form blocks submission until both checks pass.
    - name: Finish user registration
      id: finish-user-registration
      description: Wants to complete a self-registration flow in ODC
        Studio by calling FinishUserRegistration to activate the account,
        running a FinishSignUp server action to grant the app role, and
        redirecting to the Login screen on success or showing an error for
        an invalid verification code or failed password policy.
    - name: Users and Licensing
      id: users-licensing
    - name: User Types
      id: user-types
    - name: Configure user domains
      id: configure-user-domains
      description: Wants to configure internal user domains in the ODC Portal
        by adding organization email domains under Management > Admin >
        Subscription, so that end users are correctly classified as internal
        or external for separate licensing purposes.
    - name: IT vs App Users
      id: it-vs-app-users
      description: Wants to understand the difference between IT Users and Application
        Users in O11, including how each is managed under a separate User Provider
        (Service Center vs Users app) and how multi-tenancy can create further isolated
        user spaces, so that they can correctly identify which user space a given login
        belongs to.
    - name: Login provider mismatch
      id: login-provider-mismatch
      description: Wants to diagnose why a login works in one OutSystems user provider
        (for example, Service Center) but fails in another (for example, the Users
        app), by understanding the common causes — inactive user, different password,
        external authentication configuration, or multi-tenancy — so that they can
        resolve the login failure.
    - name: Built-in member workflow
      id: builtin-member-workflow
      description: Wants to manage members (IT-users) end-to-end using ODC's
        built-in authentication, by optionally creating custom roles, creating
        members in the ODC Portal, and granting or revoking their roles, so
        that they can onboard and manage members without an external identity
        provider.
    - name: IdP member workflow
      id: external-idp-member-workflow
      description: Wants to manage members (IT-users) end-to-end using an
        external identity provider, by optionally creating custom roles,
        configuring and assigning an external IdP, adding members manually,
        programmatically, or automatically, and granting or revoking their
        roles, so that they can onboard and manage members backed by an
        external IdP.
  - name: Governance principles
    id: governance-principles
    subtopics:
    - name: Deleting apps and libraries
      id: delete-apps-libraries
    - name: Modify IT Users in LT
      id: modify-it-users-in-lt
    - name: Delete IT Users in LT
      id: delete-it-users-in-lt
    - name: Create IT Users in LT
      id: create-it-users-in-lt
    - name: Built-in Roles
      id: built-in-roles
    - name: IT User management in LT
      id: it-user-management-in-lt
    - name: Governance strategies
      id: governance-strategies
    - name: Permission model in LT
      id: permission-model-in-lt
    - name: Permission model for semi-autonomous dev deployments up to QA
      id: permission-model-qa
    - name: Role assignment
      id: role-assignment
    - name: Create IT role
      id: create-it-role
      description: Wants to create a new custom IT role in the LifeTime
        console, defining environment-specific permission levels,
        per-application permission toggles, and infrastructure-wide
        permissions, in OutSystems 11, so that they can implement a more
        granular security policy beyond the built-in Developer and
        Administrator roles.
    - name: Permission governance
      id: permission-governance
      description: Wants to know the recommended practices for managing permissions
        in ODC, including starting with least privilege, using clear naming, avoiding
        the Administrator role unless necessary, testing roles before rollout, and
        auditing permissions regularly, so that they can govern access securely and
        maintainably across their organization.
  - name: Multi-portfolio user management
    id: multi-portfolio-user-management
    subtopics:
    - name: Centralized user access
      id: centralized-user-access
      description: Wants to understand how ODC manages users (members,
        end-users, and API clients) at the organization level rather than
        per portfolio, with a single user profile and organization roles
        that include portfolio-scoped permissions, in a multi-portfolio
        organization, so that they don't create redundant per-portfolio
        accounts and understand the centralized user model.
    - name: Configure portfolio roles
      id: configure-portfolio-roles
      description: Wants to configure organization roles' portfolio-level permissions
        across the available permission categories for one or more portfolios,
        including relying on the built-in Administrator role for automatic
        access to all portfolios or the Developer role scoped to the main
        portfolio, and designing custom cross-portfolio or portfolio-specific
        roles, in a multi-portfolio ODC organization, so that each user gets the
        correct level of access per portfolio.
    - name: End-users in portfolios
      id: end-users-in-portfolios
      description: Wants to understand how end-users are managed in a
        multi-portfolio ODC organization, including that a single end-user
        profile is shared and counted once toward the organization-level
        end-user limit while end-user groups and IdP assignments remain
        portfolio-specific, so that they can plan end-user access and
        authentication correctly across portfolios.
  - name: Subscription and entitlement management
    id: subscription-entitlement-management
    subtopics:
    - name: Retrieve subscription data
      id: retrieve-subscription-data
      description: Wants to retrieve ODC subscription, entitlement, and usage
        data by asset and environment, using the Subscription API v1 REST
        endpoints, so they can programmatically monitor account status and
        resource consumption.
    - name: AO counting rules
      id: ao-counting-rules
      description: Wants to know how application objects (AOs) are counted for
        screens, entities/database tables, API methods, and events in
        OutSystems 11 and OutSystems Developer Cloud, including special cases
        such as libraries, imported Forge components, and objects reused
        across multiple apps, so that they can accurately estimate the AO
        footprint of their apps.
    - name: AO limits and usage
      id: ao-limits-usage
      description: Wants to check their licensed AO capacity and current AO
        usage across production runtimes, using Service Center in OutSystems
        11 or the Subscription section of the ODC Portal, so that they can
        manage AO consumption and know when to request a capacity upgrade.
    - name: Check license limits
      id: check-license-limits-o11
      description: Wants to check the licensing limits and current usage
        statistics for various licensed resource types for their OutSystems 11
        environment, using the Licensing submenu under the Administration tab
        in Service Center, so that they can monitor consumption before hitting
        a limit.
    - name: View subscription entitlements
      id: view-subscription-entitlements-odc
      description: Wants to view their organization's and portfolios' subscription details
        and entitlement consumption across resource types, along with available
        add-ons, using the Subscription page in ODC Portal, so that they can
        monitor usage and know when to request an upgrade.
    - name: Agent Execution counting
      id: agent-execution-counting
      description: Wants to know how Agent Executions are counted in OutSystems,
        understanding that each CallAgent action call at runtime counts as one
        Execution regardless of the number of actions, tools, or AI model calls
        performed within that call, so that they can accurately estimate Agent
        Execution consumption for licensing purposes.
    - name: Interop end-user licensing
      id: interop-end-user-licensing
      description: Wants to understand how end-user licensing is counted
        when O11-ODC user interoperability is enabled, since the same
        individual is tracked as an end user separately on each platform and
        only exceeds capacity if either platform's own limit is surpassed
        individually, so that they can accurately assess licensed end-user
        capacity across both platforms.
  - name: Expanded governance
    id: expanded-governance
    subtopics:
    - name: Assign a role for an app
      id: assign-a-role-for-an-app
    - name: Permission model for autonomous dev deployments up to PP
      id: permission-model-pp
    - name: Assign a role within a team
      id: assign-team-role
      description: Wants to assign roles within a LifeTime team, so that they can
        control team members' access across the applications included in the team.
    - name: Create a team
      id: create-a-team
      description: Wants to create a LifeTime team, so that they can organize
        related applications and the people who manage them.
    - name: Edit a team
      id: edit-a-team
      description: Wants to edit an existing LifeTime team, so that they can
        update the applications or people assigned to the team.
    - name: App-specific permissions
      id: app-specific-permissions
      description: Wants to assign an infrastructure role to a user for a specific
        application or set of applications in LifeTime, including when
        application-specific access overrides the user's default role and when No
        Access blocks sign-in, so they can control app-level access per
        environment.
    - name: Permission precedence
      id: permission-precedence
      description: Wants to understand how LifeTime combines application-specific,
        team, and infrastructure-level permissions for a user, including which
        level overrides the others and how No Access affects sign-in, so they can
        reason about the effective access a user receives.
    - name: Add IT users and applications
      id: manage-teams
      description: Wants to add IT users and applications to a LifeTime team, so
        that they can keep the team membership and application scope aligned.
    - name: Manage teams overview
      id: manage-teams-overview
      description: Wants to manage a LifeTime team and its related users and
        applications, so that they can control team-based access across related
        applications.
- name: Using audit logs for compliance and troubleshooting
  guid: c96ed7dd-90d2-406d-a816-c47269c0d20b
  topics:
  - name: Using audit logs for operational alerts
    id: alerts-audit-logs
    description: Can I detect when a deployment happened outside defined hours?
  - name: Using audit logs for troubleshooting
    id: troubleshoot-audit-logs
  - name: Which compliances can audit trail help me achieve
    id: compliance-audit-logs
  - name: User permissions for audit log access
    id: user-perms-audit-logs
    description: What is the permission model? For example, if I have access to only
      some apps, can I see audit logs for other apps for which I don't have permission?
      Is it per stage?
  - name: Events logged in the audit trail
    id: events-audit-logs
    description: 'Is operation XYZ in ODC Portal audited?

      What is the list of operations and for what scope for auditing?'
  - name: Understanding the structure and fields of an audit log
    id: struct-audit-logs
    description: 'What does each field mean?

      What fields exist?

      What values can each field hold?

      What does each possible value mean?'
  - name: Retention period available for audit trail
    id: retention-audit-logs
  - name: Methods for accessing audit log data
    id: access-audit-logs
    subtopics:
    - name: Stream audit logs
      id: stream-audit-logs
      description: Can I stream audit logs?
    - name: Exporting logs via CSV
      id: export-audit-logs
    - name: Viewing logs in the ODC Portal
      id: view-audit-logs
- name: Business Analysis
  guid: a3f7c821-56e4-4b2d-9d83-1e4c0f2d8b95
  topics:
  - name: Business Vision
    id: business-vision
    subtopics:
    - name: Vision document
      id: vision-document
      description: Wants to understand what a business vision document is and what
        goals it serves, when starting a new project, so that they can align business
        strategy, context, and challenges before development begins.
    - name: Vision statement
      id: vision-statement
      description: Wants to create a vision statement and vision cloud for a project,
        when conducting business analysis, so that they can articulate the primary
        business goals in an inspirational and actionable way.
    - name: Process diagrams
      id: process-diagrams
      description: Wants to create and scope process diagrams for a business vision
        document, when conducting business analysis, so that all stakeholders can
        visually understand the full business process beyond just the application scope.
    - name: Information architecture
      id: information-architecture
      description: Wants to create an information architecture for a vision document
        by representing the application structure and navigation hierarchy, so that
        stakeholders understand where concepts live and how users move through the
        application.
    - name: 'Low-fi mockups'
      id: low-fi-mockups
      description: Wants to design low-fi mockups with seed data from the business
        processes, information architecture, and user stories in the vision document,
        so that the team can validate the user experience and get useful user feedback
        early.
    - name: Personas
      id: personas
      description: Wants to create personas for a business vision document by observing
        users in action and collecting statistics and context information for each,
        when conducting business analysis, so that they can understand each persona's
        contribution to strategic or tactical goals.
    - name: Identify user roles
      id: identify-user-roles
      description: Wants to identify the user roles that matter when creating personas
        by grouping users with specific capabilities or different purposes, so that
        they can define personas with the right scope.
    - name: Workshop roles
      id: workshop-roles
      description: Wants to identify the key roles that drive a Business Vision workshop,
        when planning the session, so that responsibilities and deliverables are
        clear.
    - name: Workshop participants
      id: workshop-participants
      description: Wants to identify the additional business and technical participant
        groups needed for a Business Vision workshop, when organizing the
        session, so that business context, technical alignment, and coordination
        are covered.
    - name: Workshop planning
      id: workshop-planning
      description: Wants to prepare and run a Business Vision workshop, including
        the kickoff, preparation, stage sequence, and two-week agenda, when setting
        up the workshop, so that participants align on activities, deliverables,
        and timing.
    - name: OutSystems 6 dimensions
      id: outsystems-6-dimensions
      description: Wants to identify the six dimensions used to optimize an
        OutSystems delivery methodology, including that speed is not one of them,
        so they can answer questions about the value-realization model.
  - name: Business processes
    id: business-processes
    description: Wants to understand what business processes are, why organizations
      use them, and common examples such as approval and support flows, so that they
      can recognize process-driven work in an organization.
- name: Agile methodology
  guid: ''
  topics:
  - name: Developer responsibilities
    id: developer-responsibilities
    description: Wants to understand what is expected from a developer in an OutSystems
      delivery team, including responsibilities before and during iterations and
      the developer mindset, so that they can contribute effectively to solution
      design, implementation, quality, usability, scalability, performance, testing,
      and team collaboration.
  - name: Business analyst responsibilities
    id: business-analyst-responsibilities
    description: Wants to understand what is expected from a Business Analyst in an
      OutSystems delivery team, including representing the business, gathering
      requirements, creating the vision document, preparing and developing
      iterations, assuring quality, supporting release planning, and contributing
      additional competencies such as product ownership, UX promotion, and quality
      control, so that they can perform the role effectively.
    subtopics:
    - name: Business analyst skills
      id: business-analyst-skills
      description: Wants to identify additional skills that help a Business Analyst
        perform better in an OutSystems project, when the role extends beyond core
        responsibilities, so they can strengthen brainstorming, negotiation,
        presentation, elicitation, and analysis.
  - name: Business analyst mindset
    id: business-analyst-mindset
    description: Wants to understand the Business Analyst mindset in an OutSystems
      delivery team, including business alignment, quality assurance,
      organizational context awareness, user focus, and project steering, so that
      they can challenge decisions against the real business vision and guide
      delivery toward the right outcomes.
  - name: Product owner mindset
    id: product-owner-mindset
    description: Wants to understand the Product Owner mindset in an OutSystems
      delivery team, including business alignment, communication hub behavior,
      organizational context awareness, user focus, and project steering, so that
      they can challenge decisions against the business vision and guide delivery
      toward the right outcomes.
  - name: Product owner responsibilities
    id: product-owner-responsibilities
    description: Wants to understand what is expected from a Product Owner in an
      OutSystems delivery team, including core responsibilities like solution
      ownership, backlog management, organizational awareness, and change management,
      plus additional competencies such as project management, budget control, UX
      promotion, delivery process ownership, and quality control, so that they can
      perform the role effectively.
    subtopics:
    - name: Create backlog structure
      id: create-backlog-structure
      description: Wants to create the backlog from user story mapping by turning
        user needs into themes, epics, and high-level user stories, sequencing the
        epics, and keeping the structure focused on the minimum viable product, so
        that they can shape the initial project scope.
    - name: Prioritize user stories
      id: prioritize-user-stories
      description: Wants to prioritize user stories with the MoSCoW technique by
        classifying them as must have, should have, could have, or will not have,
        so that they can rank work inside each epic.
    - name: Define product minimums
      id: define-product-minimums
      description: Wants to define the minimum viable product and the minimum
        lovable product from the prioritized backlog by separating must-have stories
        from lower-priority ones and deciding which desirable enhancements deserve
        extra effort, so that they can plan incremental deliveries that are usable
        and appealing.
    - name: Drill down stories
      id: drill-down-user-stories
      description: Wants to drill down high-level user stories into small, valuable,
        end-to-end slices that fit within a few days of work and still support demo
        feedback, so that each iteration delivers usable value without overengineering.
    - name: Personal story framing
      id: personal-user-story-framing
      description: Wants to frame a user story from a specific user profile with the
        who, what, why, and collaboratively defined how, so that the story unfolds
        progressively and stays personal and actionable for the team.
    - name: Connected story structure
      id: story-collaboration-structure
      description: Wants to structure a user story with relevant context,
        prerequisites, next steps, acceptance criteria, examples, and a collaboration
        zone, so that the team can plan, test, and update it while keeping it as the
        single source of truth.
  - name: Tech lead duties
    id: tech-lead-duties
    description: Wants to understand what is expected from a Tech Lead in an
      OutSystems delivery team, including responsibilities before and during
      iterations such as business goals, technical solution, non-functional
      requirements, architecture, task breakdown, team leadership, quality
      assurance, and delivery to production, so that they can perform the role
      effectively.
  - name: Tech lead mindset
    id: tech-lead-mindset
    description: Wants to understand the Tech Lead mindset in an OutSystems
      delivery team, including solution architecture, team building, quality
      assurance, acting as the user, and project steering, so that they can guide
      delivery with a balance of architecture, quality, and business value.
  - name: Project timebox phases
    id: project-timebox-phases
    description: Wants to understand the project timebox lifecycle in OutSystems
      agile methodology, including initiation, iteration development, solution
      release, and post-production, so that they can see how the overall delivery
      timeline is organized.
    subtopics:
    - name: Product team lifecycle
      id: product-team-lifecycle
      description: Wants to understand how Product teams, CoE teams, release staging,
        testing, monitoring, and production support fit into the OutSystems delivery
        lifecycle, so that they can organize releases and handoffs effectively.
    - name: Solution release phase
      id: solution-release-phase
      description: Wants to understand the solution release phase in OutSystems agile
        methodology, including change and release management, rollout planning,
        training, documentation, project control, testing, stabilization, and user
        acceptance, so that they can prepare the solution for go-live.
    - name: Release testing and UAT
      id: release-testing-and-uat
      description: Wants to validate a solution during the release phase by running
        business testing, load and stress tests, stabilization activities, and user
        acceptance testing, so that they can confirm it is ready to go live.
  - name: Iteration structure
    id: iteration-structure
    description: Wants to understand how a single iteration is organized in the
      OutSystems project timebox, including development, analysis, stabilization,
      daily stand-ups, and retrospectives, so that they can follow the work tracks
      and ceremonies within each iteration.
  - name: Project role allocation
    id: project-role-allocation
    description: Wants to understand how project roles are allocated across the
      project timebox phases, including which roles are involved at initiation,
      iteration development, solution release, and post-production, so that they
      can plan team involvement across the lifecycle.
  - name: OutSystems delivery methodology
    id: outsystems-delivery-methodology
    description: Wants to understand the OutSystems delivery methodology baseline,
      including that it is based on Scrum but changes team roles, delivery
      approach, and adaptations for speed, risk reduction, and value delivery,
      so they can align their working method with OutSystems projects.
  - name: Agile rationale
    id: agile-rationale
    description: Wants to understand why OutSystems uses agile methodology instead
      of waterfall, including user value, time to market, and response to
      change, so that they can explain the motivation for the approach.
  - name: Agile software development manifesto
    id: agile-software-development-manifesto
    description: Wants to understand the Agile software development manifesto,
      including its core values and how they guide collaborative delivery, so that
      they can work with the mindset expected in agile projects.
  - name: Software craftsmanship manifesto
    id: software-craftsmanship-manifesto
    description: Wants to understand the software craftsmanship manifesto,
      including its core values and how they guide collaborative delivery and
      professional practice, so that they can contribute to sustainable agile
      delivery.
  - name: Working in an agile way
    id: working-in-an-agile-way
    description: Wants to understand how to work in an agile way in OutSystems
      projects, including timeboxing, iteration-based feedback, and adapting
      delivery as requirements evolve, so that they can apply the approach in
      practice.
- name: O11 to ODC Migration
  guid: ''
  topics:
  - name: O11 to ODC overview
    id: o11-to-odc-overview
    description: Wants to understand the main terminology, app model, user model,
      portal, deployment, configuration, and monitoring differences between
      OutSystems 11 and ODC, so they can map concepts quickly when reading the
      cheatsheet or moving between platforms.
  - name: Code Conversion
    id: code-conversion
    subtopics:
    - name: Convert O11 code
      id: convert-o11-code
      description: Wants to understand how to convert O11 app code to ODC using the
        app conversion console, including what the tool does automatically, so that
        they can initiate the code conversion step in their migration plan.
    - name: Publish converted apps
      id: publish-converted-apps
      description: Wants to publish converted ODC apps and libraries after code conversion
        — including downloading each app file, opening it in ODC Studio, fixing dependency
        and TrueChange errors, and publishing producers before consumers — so that
        the converted apps are live in the target ODC stage.
    - name: Tag app before conversion
      id: tag-app-before-conversion
      description: Wants to know how to tag an O11 app in the source environment
        before starting the code conversion process, so that the correct app version
        is picked up by the app conversion console.
    - name: Troubleshoot code conversion
      id: troubleshoot-code-conversion
      description: Wants to identify and collect troubleshooting information when
        an O11 to ODC code conversion ends with errors — including reading conversion
        logs and exporting the troubleshooting zip file — so that they can diagnose
        the failure or provide the necessary data to OutSystems Support.
    - name: Adapt converted SQL
      id: adapt-converted-sql
      description: Wants to validate and adapt SQL nodes in a converted ODC app to
        ensure compatibility with Aurora PostgreSQL — including reviewing auto-converted
        queries, handling limitations (Expand Inline, external entities, BEGIN-END
        blocks), and testing all SQL nodes — so that the converted app's data queries
        work correctly in ODC.
    - name: SQL conversion mapping
      id: sql-conversion-mapping
      description: Wants to understand the exact syntax mappings applied automatically
        when converting O11 SQL nodes to ODC PostgreSQL — including general clauses,
        Oracle-specific functions, data type translations, string concatenation rules,
        temporary tables, and pattern matching — so that they can verify converted
        queries and anticipate what the conversion tool produces.
    - name: Adapt login flows
      id: adapt-login-flows
      description: Wants to manually adapt the login and logout flows of a converted
        ODC app so that end users can authenticate using their existing O11 credentials,
        so that the converted app is usable after migration.
    - name: Review converted app
      id: review-converted-app
      description: Wants to review the On Application Ready and On Application Resume
        event actions and the app and library properties of a converted ODC app, after
        the code conversion tool has merged multiple O11 modules, so that the resulting
        app behaves as expected and has the correct settings.
    - name: Configure converted apps
      id: configure-converted-apps
      description: Wants to configure a converted ODC app after code conversion —
        including Site Properties/Settings (secret and non-secret), custom domains,
        external databases, REST service endpoints, private gateways, and SMTP email
        — so that the app works correctly in ODC before testing and deploying it.
    - name: Connect ODC to O11
      id: connect-odc-to-o11
      description: Wants to verify that their ODC organization is connected to the
        correct O11 infrastructure and, in a multi-portfolio organization, set the
        target ODC portfolio for app conversion, in the ODC Portal, so that conversion
        plans from the Conversion Assessment Tool are available in the app conversion
        console.
    - name: Conversion prerequisites
      id: conversion-prerequisites
      description: Wants to understand the prerequisites and limitations for executing
        an O11 to ODC app conversion — including required infrastructure type, LifeTime
        version, prior preparation steps, and unsupported scenarios (self-managed,
        hybrid, demo environments, ODC self-hosted) — so that they can confirm whether
        their environment is ready before starting the conversion process.
  - name: Code Patterns
    id: code-patterns
    subtopics:
    - name: Fix built-in function pattern
      id: fix-built-in-function-pattern
      description: Wants to manually replace O11 built-in functions that don't exist in ODC
        with their ODC equivalents or manual workarounds after O11 to ODC code
        conversion, so that migrated app logic keeps working under ODC's
        architecture.
    - name: Multipart REST pattern
      id: multipart-rest-pattern
      description: Wants to unblock an O11 to ODC code conversion when the app consumes
        a REST API using a multipart/form-data request, by removing that REST call
        in O11 before converting, since ODC doesn't yet support that request format.
    - name: Incompatible built-in functions
      id: incompatible-built-in-functions
      description: Wants to identify O11 built-in functions incompatible with ODC
        architecture, either restricted from use inside aggregates or removed
        entirely, after O11 to ODC code conversion, so that they can rework the
        affected logic instead of applying a direct fix.
    - name: Traditional Web modules
      id: trad-web-modules-pattern
      description: Wants to convert Traditional Web modules to Service or Reactive
        Web modules in O11 before starting O11 to ODC code conversion, since ODC
        doesn't support Traditional Web apps, so that the app asset isn't blocked
        from code conversion.
    - name: Additional pipelines pattern
      id: additional-pipelines-pattern
      description: Wants to understand why O11 to ODC conversion isn't supported
        yet for infrastructures with additional pipelines, so that they can focus
        on preparing code and data for ODC instead of attempting conversion.
    - name: Self-managed infra pattern
      id: self-managed-infra-pattern
      description: Wants to understand why O11 to ODC conversion isn't supported
        yet for self-managed infrastructures, so that they can focus on preparing
        code and data for ODC instead of attempting conversion.
    - name: Align persistent login
      id: align-persistent-login-timeout
      description: Wants to align an O11 Reactive Web or Mobile app's customizable
        persistent login duration with ODC's fixed access token limit before
        migrating, by testing the shorter duration in a non-production O11
        environment and then propagating it to production, so that app behavior
        and any O11-ODC single sign-on stay compatible after conversion to ODC.
  - name: Data Migration
    id: data-migration
    subtopics:
    - name: Data migration scope
      id: data-migration-scope
      description: Wants to understand what is included in an O11 to ODC data migration
        (entity data, repeat migrations, data model consistency requirements) and the
        available migration types (non-production vs production final), so that they
        can choose the right approach for their migration phase.
    - name: Migrate end users
      id: migrate-end-users
      description: Wants to understand how end-user migration works during O11 to ODC
        data migration, including IdP configuration requirements, one IdP per O11
        environment, user extension tables, role migration, and the option to skip
        end-user migration, so that they can prepare their environment and identity
        setup before migrating.
    - name: Cancel data migration
      id: cancel-data-migration
      description: Wants to know when and how to cancel an in-progress O11 to ODC data
        migration, and understand the impact on app data and end users depending on
        which phase is canceled, so that they can recover safely if a running migration
        needs to be aborted.
    - name: Execute data migration
      id: execute-data-migration
      description: Wants to execute the O11 to ODC data migration using the app conversion
        console in the ODC Portal — including selecting migration type, source environment,
        target stage, end-user migration options, and confirming the setup — so that
        app entity data and end users are transferred to ODC.
    - name: Configure end-user IdP
      id: configure-end-user-idp
      description: Wants to configure an identity provider in ODC for each O11 source
        environment and map it to a target ODC stage in the infrastructure settings,
        including selecting the user profile mapping and username prefix, so that end
        users can be migrated from that O11 environment.
    - name: Fix data migration errors
      id: fix-data-migration-errors
      description: Wants to identify and fix data model inconsistency errors that
        stop an O11 to ODC data migration — including missing entities or attributes,
        attribute property mismatches, and reference attribute mismatches — so that
        they can resolve the root cause and successfully retry the migration.
  - name: Conversion Assessment
    id: conversion-assessment
    subtopics:
    - name: Review assessment report
      id: review-assessment-report
      description: Wants to view and validate the ODC Conversion Assessment Tool's
        report, including navigating to it via ODC Blueprint or a conversion plan,
        and validating Code, Data, and Infrastructure pattern findings by reviewing
        solutions and rerunning the assessment, so that they can plan the refactoring
        effort needed before converting O11 apps to ODC.
  - name: Preparation
    id: preparation
    subtopics:
    - name: Prepare O11 for ODC
      id: prepare-o11-for-odc
      description: Wants to understand the high-level steps involved in preparing
        an O11 app for ODC conversion, adjusting the app to be ODC-compatible,
        deploying it to QA, and testing it, so that they know what is needed before
        starting the code conversion process.
- name: Infrastructure Setup
  guid: a1c2e3f4-5b6d-7e8f-9a0b-1c2d3e4f5a6b
  topics:
  - name: Configuration Tool
    id: configuration-tool
    subtopics:
    - name: Sync front-end configs
      id: sync-front-end-configs
      description: Wants to keep configuration settings identical across a
        Deployment Controller and multiple front-end servers, by exporting
        the Deployment Controller's configuration with the Configuration
        Tool's Export Configuration option and importing it on each
        front-end server with Import Configuration, so that all front-end
        servers share the same configuration.
    - name: Generate setup scripts
      id: generate-setup-scripts
      description: Wants to create and export the required SQL and PowerShell setup scripts
        from the OutSystems 11 Configuration Tool's File menu, so that they can
        inspect the scripts before manually running them to set up the required
        on-premises databases and cache invalidation service.
  - name: Certificate Management
    id: certificate-management
    subtopics:
    - name: App Server Certificates
      id: app-server-certificates
      description: Wants to understand how application server SSL
        certificates are managed in OutSystems cloud (default
        outsystemsenterprise.com wildcard certificate versus a custom
        domain certificate) and in self-managed environments, so that they
        know who's responsible for installation and renewal and which
        article to follow for their deployment type.
    - name: Trust self-signed cert
      id: trust-self-signed-cert-o11
      description: Wants to add a self-signed certificate to the trusted root
        store on O11 Platform Server front-end servers, by importing it via
        the Microsoft Management Console (MMC) Certificates snap-in into the
        Trusted Root Certification Authorities store and exporting a
        DER-encoded copy for reuse, so that HTTPS connections to a
        self-signed web service can establish trust instead of failing with
        "Could not establish trust relationship for the SSL/TLS" errors.
    - name: Add cloud trusted cert
      id: add-cloud-trusted-cert
      description: 'Wants to add a self-signed certificate to the trusted
        root store on O11 Cloud environment front-end servers, by opening a
        support case with the certificate in .CER format and specifying the
        target environments, so that OutSystems staff can install it and
        HTTPS connections to the external system establish trust instead of
        failing with "Could not establish trust relationship for the
        SSL/TLS" errors.'
    - name: Generate CSR keytool
      id: generate-csr-keytool
      description: Wants to generate a PKCS#10 Certificate Signing Request
        (CSR) for an OutSystems 11 Java/Linux front-end (JBoss, Wildfly, or
        WebLogic), by creating a Java keystore and key pair with keytool and
        generating a .csr file, so that they can submit it to a
        certification authority to obtain an SSL certificate.
    - name: Generate CSR request
      id: generate-csr-request
      description: Wants to generate a Certificate Signing Request (CSR)
        and keep the private key safe when using IIS, OpenSSL, or another
        supported tool, so that they can submit the CSR to a certificate
        authority and preserve the key for later certificate installation.
    - name: Import SSL cert keystore
      id: import-ssl-cert-keystore
      description: Wants to import an acquired SSL certificate, along with
        its root and intermediate CA certificates, into a Java keystore
        using keytool on an OutSystems 11 Linux front-end, and set correct
        keystore file ownership, so that the certificate chain is trusted
        and ready for the application server to use.
    - name: Configure app server SSL
      id: configure-app-server-ssl
      description: Wants to configure JBoss EAP/Community, Wildfly, or WebLogic to use an
        installed SSL certificate and keystore (editing standalone-
        outsystems.xml or the WebLogic Administration Console keystore/SSL
        settings) and restart the server, so that OutSystems 11 applications on
        Linux become accessible over HTTPS.
    - name: Install client-side cert
      id: install-client-side-cert-o11
      description: Wants to install a client-side certificate (in .PFX format
        with password, plus any required root certificates in .CER format) on
        self-managed O11 front-end servers, by installing it directly on the
        front-ends, so that the platform can present it when authenticating
        outbound webservice calls that require
        client-certificate authentication.
    - name: Install cloud client-side cert
      id: install-client-side-cert-cloud
      description: Wants to install a client-side certificate (in .PFX format
        with password, plus any required root certificates in .CER format) on
        O11 Cloud environment front-end servers, by opening a support case
        with the certificate files and specifying the target environments, so
        that OutSystems support installs it and the platform can present it
        when authenticating outbound webservice calls that require
        client-certificate authentication.
    - name: Configure Cloud SSL domain
      id: configure-cloud-ssl-domain
      description: Wants to configure their own custom SSL domain for an
        OutSystems Cloud environment, by meeting the domain and certificate
        requirements (CSR, CNAME record, complete certificate chain),
        uploading the PEM, PFX, or ZIP-bundled certificate in LifeTime, and
        assigning it and its hostname to the environment, so that the
        environment's apps and web services become reachable over HTTPS
        under the company's own domain instead of the default
        `outsystemsenterprise.com` domain.
    - name: SSL domain errors
      id: ssl-domain-errors
      description: Wants to troubleshoot SSL domain certificate validation
        errors in LifeTime while configuring a custom SSL domain on
        OutSystems Cloud, by identifying each validation message, its
        cause, and the recommended fix for PFX, PEM, ZIP, password,
        private key, expiration, and chain problems, so that the
        certificate validates successfully.
    - name: Assign SSL domain to LifeTime
      id: assign-ssl-domain-lifetime
      description: Wants to customize the domain of their LifeTime
        infrastructure management console environment on OutSystems Cloud,
        by first uploading the domain certificate on another environment
        (or reusing an existing wildcard certificate) and then opening a
        support ticket for OutSystems to manually assign it, so that
        LifeTime becomes reachable under their own domain without
        disrupting active user sessions.
    - name: Renew Cloud SSL certificate
      id: renew-cloud-ssl-cert
      description: Wants to renew an SSL domain certificate that's about to
        expire on an OutSystems Cloud environment, by using the Renew
        option under Manage Domain Certificates, or by opening a new
        support ticket if the certificate is assigned to the LifeTime
        environment, so that HTTPS access under their custom domain
        continues without interruption.
  - name: Reverse Proxy Configuration
    id: reverse-proxy-configuration
    subtopics:
    - name: Configure SSL offloading
      id: configure-ssl-offloading
      description: Wants to configure SSL offloading for OutSystems 11 by setting
        the X-Forwarded-Proto HTTP header at the reverse proxy, inserting the HTTPtoHTTPSproxyHeader
        parameter into the OutSystems database, and configuring the trusted proxy
        address in Service Center, so that the platform correctly identifies and processes
        HTTPS requests when SSL is terminated at the proxy level.
    - name: Rewrite resource URLs
      id: rewrite-resource-urls
      description: Wants to rewrite absolute URLs in OutSystems application resources
        (HTML, CSS, JavaScript) at the reverse proxy level and disable content compression
        at both the platform and application-server level, so that all resource links
        remain valid when an extra path segment is introduced by the reverse proxy.
    - name: Expose app addresses
      id: expose-app-addresses
      description: Wants to configure the reverse proxy to translate their OutSystems
        application's internal address to its externally exposed address, including
        referenced content such as RichWidgets and other cross-app references, so
        that the application and its dependent resources are correctly reachable
        from the internet.
    - name: Adjust keep-alive timeout
      id: adjust-keep-alive-timeout
      description: Wants to configure the keep-alive connection timeout on an Apache-based
        reverse proxy to at least 30 seconds using the KeepAliveTimeout directive,
        so that OutSystems Mobile and Reactive Web apps do not encounter connection
        errors caused by the default 5-second timeout.
    - name: Reverse proxy requirements
      id: reverse-proxy-requirements
      description: Wants to know the technical requirements to serve OutSystems
        Traditional Web, Mobile, and Reactive Web apps through a reverse proxy,
        including maintaining the original host header, using only default HTTP/HTTPS
        ports, disabling the NEGOTIATE protocol, keeping URL paths unchanged, exposing
        apps via their Service Center domain over HTTPS, and not caching Mobile/Reactive
        App resources, so that the reverse proxy setup doesn't break URL generation,
        authentication, or app functionality.
    - name: Reverse proxy basics
      id: reverse-proxy-basics
      description: Wants to understand what a reverse proxy is and its capabilities
        (load balancing, SSL offloading, caching, compression, content switching,
        application firewall, server obfuscation, authentication, and single sign-on),
        so they can determine whether it fits their OutSystems infrastructure needs.
    - name: Custom 404 pages
      id: custom-404-pages
      description: Wants to serve a custom 404 page through a reverse proxy or
        CDN when a requested URL is not found, so that end users and search
        engines reach a branded error page instead of a generic not found
        response.
    - name: Reverse proxy benefits
      id: reverse-proxy-benefits
      description: Wants to understand the benefits of placing a reverse proxy
        in front of OutSystems applications, including reducing load on application
        servers, increasing security and traceability, and ensuring high availability
        through load balancing and failover, so they can justify adopting a reverse
        proxy in their OutSystems architecture.
    - name: Reverse proxy scenarios
      id: reverse-proxy-scenarios
      description: Wants to determine which reverse proxy configurations to combine
        (SSL offloading, end-to-end SSL, header manipulation with referenced content
        exposure, URL rewriting, keep-alive timeout adjustment) for common OutSystems
        deployment scenarios such as plain HTTP redirection, end-to-end HTTPS, SSL
        offload, and multi-app path-based routing, so they can implement the correct
        configuration set for their specific setup.
    - name: Configure Android HTTP/2
      id: configure-android-http2
      description: Wants to enable reverse proxy support with NGINX and HTTP/2 for
        Android apps built with MABS 4.1 or later, by configuring the platform
        server's SSL settings to support at least one of the required cipher
        suites, so that Android devices can connect over HTTP/2 through the
        reverse proxy.
    - name: Fix broken app links
      id: fix-broken-app-links
      description: Wants to fix links in their OutSystems 11 app that unexpectedly
        switch from HTTPS to HTTP or expose an internal server hostname instead
        of the expected domain, caused by a reverse proxy or load balancer not
        passing through the original protocol and Host header, so that they can
        restore correct URLs by enabling SSL offload pass-through and preserving
        the host header at the network layer.
    - name: Host header mismatch
      id: host-header-mismatch
      description: Wants to diagnose and fix the 503 "Host header does not match"
        error by checking secure endpoints, redirect rules, and DNS entries so
        that legitimate domains are allowed through the cloud environment.
  - name: Web Application Firewall
    id: web-application-firewall
    subtopics:
    - name: Configure Akamai WAF
      id: configure-akamai-waf
      description: Wants to set up Akamai's App & API Protector product and
        configure the CDN/WAF property to protect their OutSystems web app,
        by creating an Akamai account, configuring the CDN property with
        origin hostname, DNS, and edge certificate, and enabling the
        out-of-the-box WAF protections, so that traffic to their OutSystems
        environment is filtered through Akamai's WAF.
    - name: Customize Akamai WAF rules
      id: customize-akamai-waf-rules
      description: Wants to customize Akamai WAF protections for their
        OutSystems web app, by creating a new security policy version,
        adjusting attack group actions, and creating custom rules for
        specific rule IDs, so that they can tailor the WAF's blocking
        behavior to their application's needs.
    - name: Configure Cloudflare WAF
      id: configure-cloudflare-waf
      description: Wants to set up a Cloudflare account and integrate
        Cloudflare's WAF with their OutSystems web app, by creating an
        account, adding their domain, configuring DNS records and SSL/TLS
        settings, and enabling the built-in managed WAF rule sets, so that
        traffic to their OutSystems application is filtered through
        Cloudflare's WAF.
    - name: Customize Cloudflare WAF rules
      id: customize-cloudflare-waf-rules
      description: Wants to customize Cloudflare WAF protections for their
        OutSystems web app by creating custom rules with specific match
        criteria and actions (for example, a JS Challenge triggered by a
        Threat Score threshold), so that they can tailor the WAF's blocking
        behavior to their application's needs.
    - name: WAF overview
      id: waf-overview
      description: Wants to understand what a Web Application Firewall is,
        including its key features (traffic filtering and monitoring,
        rule-based protection, real-time response, compliance support, and
        logging and reporting) and its three deployment types (network-based,
        host-based, cloud-based), so that they can evaluate how a WAF fits
        into a layered defense strategy for their OutSystems web application.
    - name: OWASP Top 10 awareness
      id: owasp-top10-awareness
      description: Wants to understand what the OWASP Top 10 is and why
        keeping up with each new release matters, so that they can identify
        which attack vectors their WAF rules need to protect their
        OutSystems web applications against.
    - name: Configure customer-managed WAF
      id: configure-customer-managed-waf
      description: Wants to set up a customer-managed WAF for OutSystems Cloud alongside
        the built-in Application Load Balancer protections, understanding the
        added benefits of geo-blocking, custom rules, CDN performance offload,
        and security monitoring, so that they can protect their environment with
        capabilities the built-in ALB doesn't provide.
    - name: Customize customer-managed WAF rules
      id: customize-customer-managed-waf-rules
      description: Wants to customize rules on a customer-managed WAF
        protecting OutSystems Cloud, so that application-specific traffic
        patterns and new attack behavior are handled without blocking
        legitimate requests.
    - name: Monitor customer-managed WAF security analytics
      id: monitor-customer-managed-waf-security-analytics
      description: Wants to monitor the security analytics from a
        customer-managed WAF protecting OutSystems Cloud, so that they can
        detect repeated attack patterns and false positives before they affect
        users.
    - name: Decide when to involve a WAF security expert
      id: decide-waf-security-expert-involvement
      description: Wants to know when to involve a WAF security expert while
        operating a customer-managed WAF for OutSystems Cloud, so that complex
        tuning or incident response work is escalated before configuration
        gaps leave the application exposed.
    - name: Recommended WAF rules
      id: recommended-waf-rules
      description: Wants to look up the recommended ModSecurity-style WAF rule IDs and
        names to always enable on a customer-managed WAF protecting OutSystems
        Cloud applications, so that they configure baseline protection against
        common attack types.
    - name: WAF mobile integrity
      id: waf-mobile-integrity
      description: Wants to configure their WAF to exclude mobile app
        static asset paths (css, img, scripts, fonts, assets) for each
        mobile app OS, retrieved via the moduleinfo URL, so that WAF
        features like bot protection, file compression, or code injection
        don't break MABS Over-the-Air update checksum validation.
  - name: Redis in-memory sessions
    id: redis-in-memory-sessions
    subtopics:
    - name: Redis session overview
      id: redis-session-overview
      description: Wants to understand why OutSystems supports Redis for in-memory
        session storage in Traditional Web Apps on self-managed infrastructures
        — including the drawbacks of storing sessions as a serialized column in
        a relational database and the difference between a single Redis Server
        and a Redis Cluster configuration — so they can decide whether this advanced
        scenario fits their factory before setting it up.
    - name: Redis software prerequisites
      id: redis-software-prereqs
      description: Wants to know which Platform Server and Redis Server versions
        are required to use Redis for in-memory session storage in O11, so they
        can verify their environment meets the minimum software requirements before
        starting setup.
    - name: Redis hardware sizing
      id: redis-hardware-sizing
      description: Wants to understand the minimum hardware specifications for a
        Redis infrastructure — covering both non-productive and production (Redis
        Cluster) environments — so they can provision servers with the right CPU,
        RAM, storage, and network capacity before deploying Redis for OutSystems.
    - name: Enable Redis SSL
      id: enable-redis-ssl
      description: Wants to configure Platform Server to connect to Redis over
        SSL/TLS on self-managed O11 infrastructures, by setting the required
        SERedis_* certificate environment variables on every front-end server,
        enabling the Use SSL option in the Configuration Tool, and reapplying
        settings or republishing the factory, so that session traffic between
        Platform Server and Redis is encrypted.
    - name: Scale Redis Cluster nodes
      id: redis-cluster-scale-nodes
      description: Wants to scale an existing Redis Cluster by adding a new pair
        of server machines to it, by setting up their Master and Replica processes,
        joining the new Master processes with redis-cli --cluster add-node,
        resharding key slots across all Master processes, and joining the new
        Replica processes with cross-replication to the paired new Master, so
        that they can grow their self-managed O11 Redis Cluster's capacity while
        preserving its high-availability architecture.
    - name: Test Redis failover
      id: redis-cluster-failover-test
      description: Wants to verify that a Redis Cluster's failover configuration
        works correctly by using redis-cli to simulate a Master process outage
        and confirming that the associated Replica process on another server
        promotes itself to Master, so they can validate their self-managed O11
        Redis Cluster setup before relying on it in production.
    - name: Set up non-prod Redis
      id: redis-nonprod-setup
      description: Wants to set up a single Redis server for OutSystems 11 non-productive
        environments, including the recommended network deployment (a dedicated
        subnet, inbound connections on port 6379/TCP) and installing and configuring
        Redis Server on Ubuntu (commenting the bind directive, disabling protected-mode,
        setting a password via requirepass, restarting the service), so that the
        server is ready to accept connections from OutSystems front-end servers
        for in-memory session storage.
    - name: Set up prod Redis
      id: redis-prod-setup
      description: Wants to set up a three-server Redis Cluster for OutSystems
        11 production environments on self-managed infrastructures, including
        hardening each Ubuntu node (disabling the default redis-server service,
        opening the cluster ports, disabling transparent huge pages, tuning
        overcommit memory), configuring paired Master and Replica redis-server
        processes with cluster mode enabled and systemd services, creating the
        cluster with redis-cli --cluster create, and validating that the
        resulting nodes and hash slots are healthy, so that the cluster is
        ready to accept connections from OutSystems front-end servers for
        in-memory session storage.
    - name: Configure Redis sessions
      id: configure-redis-sessions
      description: Wants to configure OutSystems 11 front-end servers to use Redis
        for in-memory session storage via the Configuration Tool's Session tab,
        including entering the Redis hosts, password, and optional SSL settings,
        testing the connection, applying the change, and republishing modules,
        so that their self-managed Platform Server environment stores sessions
        in Redis instead of the database.
    - name: Verify Redis sessions
      id: verify-redis-sessions
      description: Wants to confirm that Platform Server is storing sessions in
        Redis after configuring in-memory session storage, by connecting to the
        Redis Cluster or single Redis server with redis-cli and checking for the
        expected _Data and _Internal session keys, so they can validate their
        self-managed O11 Redis session configuration is working.
  - name: CDN Configuration
    id: cdn-configuration
    subtopics:
    - name: Configure CDN
      id: configure-cdn
      description: Wants to set up and configure a CDN for OutSystems 11
        mobile and reactive web apps, including the CNAME DNS record, SSL
        certificate installation, supported HTTP versions, pointing the
        origin to the load balancer, matching ForwardCookies to the cache
        policy, forcing HTTPS redirects, setting OriginKeepaliveTimeout and
        OriginReadTimeout, and choosing which static file types to cache,
        so that static content is served correctly and securely from the
        CDN instead of the front-end servers.
    - name: CDN stale cache
      id: cdn-stale-cache
      description: Wants to prevent users from receiving outdated application resources
        from the CDN after a new app version is deployed, by using Platform
        Server 11.30.0+'s stale cache prevention mechanism in Factory
        Configuration, or the IIS cache-timeout fallback (reducing static
        content expiration to a short duration) on earlier versions, in
        OutSystems 11, so that they can set long cache expiration times without
        risking stale content.
    - name: CDN brute-force fix
      id: cdn-brute-force-fix
      description: Wants to prevent a CDN from triggering the Users
        module's brute-force protection against legitimate users, because
        the CDN masks each end-user's real IP address, by setting the
        EnableBruteForceProtectionPerIP site property to False in Service
        Center, in OutSystems 11, so that valid login attempts through the
        CDN aren't blocked.
  - name: Self-hosted cluster services
    id: self-hosted-cluster-services
    subtopics:
    - name: Cluster services overview
      id: cluster-services-overview
      description: Wants to understand what services are installed in an ODC self-hosted
        Kubernetes cluster and why each is necessary, so that they can
        understand the platform's architecture and their own operational
        responsibilities versus OutSystems'.
  - name: Self-hosted inbound traffic configuration
    id: self-hosted-inbound-traffic
    subtopics:
    - name: Configure self-hosted domains
      id: configure-self-hosted-domains
      description: Wants to create and manage DNS domains for each self-hosted ODC
        application stage that resolve to the load balancer's public address, so
        that user requests can reach the load balancer over HTTPS.
    - name: Set up load balancer TLS
      id: set-up-load-balancer-tls
      description: Wants to set up a layer 7 load balancer to terminate TLS for
        self-hosted ODC domains and forward traffic to the Gloo ingress gateway
        over HTTP, so that HTTPS access to apps stays secure while TLS management
        is offloaded from the cluster.
    - name: Configure Gloo routing headers
      id: configure-gloo-routing-headers
      description: Wants to configure the X-Forwarded-Host and X-OSEnvironment HTTP
        headers at the load balancer, or another upstream proxy, so that Gloo can
        identify the target runtime stage and route each inbound request correctly
        in a self-hosted ODC cluster.
    - name: Finish stage setup
      id: finish-stage-setup
      description: Wants to complete the final ODC Portal setup for a self-hosted
        ODC stage after the platform services are deployed, including the domain
        and identity provider configuration, so that users can reach the stage and
        sign in successfully.
  - name: Self-hosted configurator
    id: self-hosted-configurator
    subtopics:
    - name: Stage prerequisites
      id: stage-prerequisites
      description: Wants to prepare the infrastructure, PostgreSQL database, and
        optional custom OCI registry before starting the Self-hosted configurator
        for a self-hosted ODC stage, so that the stage can be installed on a ready
        environment.
    - name: Configure registry credentials
      id: configure-registry-credentials
      description: Wants to prepare provider-specific credentials for a custom OCI
        registry before starting the Self-hosted configurator, including the
        registry endpoint, required permissions, and expiration value, so that
        the configurator can authenticate to the registry and sync images.
    - name: Install self-hosted stage
      id: install-self-hosted-stage
      description: Wants to install and configure a self-hosted ODC stage through
        the Self-hosted configurator, including onboarding in the ODC Portal,
        authenticating, providing registry, APM, and database details, deploying
        platform services, and capturing the stage ID, so that the cluster is
        bootstrapped correctly.
    - name: Reopen self-hosted configurator
      id: reopen-self-hosted-configurator
      description: Wants to reopen the Self-hosted configurator on an already-bootstrapped
        ODC cluster via the ODC Portal's Self-hosted tab and configurator command,
        so that they can update APM tool, stage database connection, or container
        registry settings without rerunning the full installation wizard.
    - name: Redis HA architecture
      id: redis-ha-architecture
      description: Wants to understand the 3-node Master/Replica Redis Cluster
        high-availability architecture for OutSystems 11 self-managed infrastructures,
        including how crossed replica placement enables automatic failover to a
        surviving server, so that they can reason about Redis Cluster resilience
        before deploying it.
  - name: Front-end server scaling
    id: front-end-server-scaling
    subtopics:
    - name: Install front-end server
      id: install-front-end-server-o11
      description: Wants to install and configure a new front-end server on a
        self-managed OutSystems 11 environment, by exporting the private key
        and configuration files from the Controller, running the Platform
        Server installation checklist with the First install task and the
        Server (or Front-end Server) role, so that the environment gains
        additional horizontal scalability without disrupting ongoing
        deployments.
    - name: Request Cloud horizontal scaling
      id: request-cloud-horizontal-front-end-scaling
      description: Wants to request horizontal scaling of front-end servers
        for an OutSystems Cloud environment, by submitting a support case
        and understanding that OutSystems provisions new front-end servers
        automatically, so that they can plan for the resulting capacity
        increase without manual server installation.
    - name: Request Cloud vertical scaling
      id: request-cloud-vertical-front-end-scaling
      description: Wants to request vertical scaling of front-end servers
        for an OutSystems Cloud environment, by submitting a support case
        and understanding that OutSystems proposes an upgrade schedule that
        can involve downtime, so that they can plan for the resulting
        capacity change.
    - name: Automate server scaling
      id: automate-server-scaling-o11
      description: Wants to automate the horizontal scaling of front-end
        servers in a self-managed OutSystems 11 environment, using
        OutSystems unattended installation commands or, on Microsoft Azure,
        Azure scale sets, so that new front-end servers can be provisioned
        without manual installation steps.
  - name: Database Collation
    id: database-collation
    subtopics:
    - name: Collation requirements
      id: collation-requirements
      description: Wants to understand OutSystems' case-insensitive collation
        requirements at the SQL Server instance, database, and column level,
        and why collations must stay consistent across all environments in
        an on-premises infrastructure, so that they configure a compliant
        and consistent database backend when installing OutSystems Platform
        Server on SQL Server.
    - name: Avoid collation conflicts
      id: avoid-collation-conflicts
      description: Wants to understand why mismatched SQL Server column
        collations cause errors in join conditions and foreign keys between
        different data models, in an on-premises OutSystems 11
        installation, so that they account for future integration needs
        before finalizing their database's collation choice.
    - name: Change DB collation
      id: change-db-collation
      description: 'Wants to change the collation of an existing OutSystems
        SQL Server database, understanding that changing only the
        database-level collation doesn''t update existing column
        collations, that OutSystems Support doesn''t assist with the change
        itself, and how to interpret the resulting "Cannot resolve the
        collation conflict" error, so that they plan and safely execute (or
        troubleshoot) a collation change with the appropriate database
        professionals.'
  - name: Database connection pools (O11)
    id: database-connection-pools-o11
    subtopics:
    - name: Configure pool size
      id: configure-connection-pool-size
      description: Wants to control the minimum and maximum number of database
        connections in the connection pool for the .NET stack (via the
        Configuration Tool's Database > Advanced connection string) and the
        Java stack (via the Configuration Tool's Advanced Configurations), in
        OutSystems 11, so that they can tune connection pool size for their
        environment's performance needs.
    - name: Calculate runtime connections
      id: calculate-runtime-connections
      description: Wants to calculate the number of database connections used
        at runtime, using the (connections per module) x (application
        modules) x (front-end servers) formula for the .NET stack and the
        (connections configured) x (front-end servers) formula for the Java
        stack, including how minimum and maximum connections behave under
        concurrent load, in OutSystems 11, so that they can plan capacity and
        avoid connection-pool exhaustion or timeouts.
  - name: Oracle Database Configuration
    id: oracle-database-configuration
    subtopics:
    - name: Configure Oracle connection
      id: configure-oracle-connection
      description: Wants to configure an Oracle database connection for the
        OutSystems 11 Platform Server using the Configuration Tool, by
        choosing between the Service Name or TNS Name naming method
        (including SID-based TNS connect descriptors) for both the .NET and
        Java stacks, so that the platform database connects successfully to
        the target Oracle service or instance.
    - name: External Oracle DB connection
      id: external-oracle-db-connection
      description: Wants to configure an external Oracle database connection
        for use in extensions via Service Center, using Basic Configuration
        for a Service Name or Advanced Configuration for an explicit TNS
        definition (following ADO.NET syntax on .NET or JDBC syntax on Java),
        so that extensions can connect to external Oracle databases without
        needing a tnsnames.ora file.
    - name: Configure Oracle sorting
      id: configure-oracle-linguistic-sorting
      description: Wants to change the linguistic sorting (NLS_SORT) of an
        on-premises OutSystems 11 platform database from Oracle's default
        BINARY_AI (accent-insensitive) to BINARY_CI (accent-sensitive),
        using the Configuration Tool's Advanced Settings, understanding
        that applications must be republished for the change to take
        effect, so that character searches on the Oracle-backed platform
        database return accent-sensitive results.
  - name: Database and farm server migration
    id: database-farm-server-migration
    subtopics:
    - name: Reinstall on new path
      id: reinstall-different-path
      description: Wants to move an on-premises OutSystems 11 Platform Server
        installation from one folder path to another, by uninstalling
        RabbitMQ, Erlang, and Platform Server, cleaning up database and .NET
        configuration entries and environment variables tied to the old
        path, reinstalling on the new path, and restoring the private key
        and server configuration files, so that the environment continues
        to run correctly from the new location without losing its identity
        or license.
    - name: Restore database server
      id: restore-database-server
      description: Wants to restore the OutSystems 11 database to a new
        database server in a self-managed .NET stack farm, by running the
        Configuration Tool on each application server (starting with the
        deployment controller, then each front-end) to update database
        connection settings, then creating and applying an All Components
        solution across all modules, so that all servers point to the new
        database server without altering application data.
    - name: Change DB user passwords
      id: change-database-user-passwords
      description: Wants to change the passwords of OutSystems Platform,
        Log, and Session database users on a self-managed O11
        infrastructure, by updating credentials directly in each database,
        then in the Configuration Tool on the deployment controller and
        each front-end server, and finally applying settings to all
        modules in Service Center so republished apps use the new
        credentials, so that they can comply with password rotation
        requirements without leaving modules unable to log in to the
        database.
    - name: Change DB auth mode
      id: change-db-authentication-mode
      description: Wants to change the SQL Server database authentication
        mode (Windows Authentication or Database Authentication) for a
        self-managed OutSystems 11 standalone or farm installation, using
        the Configuration Tool to set credentials, grant permissions, and
        create the session database, then updating Database Catalog
        permissions and Application Pool identities, so that the platform
        continues running correctly under the new authentication mode
        without undesired behavior.
    - name: Change DB address
      id: change-database-address
      description: Wants to change the address of an OutSystems database
        (Platform, Log, or Session) used by applications on a self-managed
        OutSystems 11 infrastructure, by updating it in the Configuration
        Tool on each application server and then creating and applying an
        All Components solution across all modules, so that the
        applications reconnect to the new database address without
        altering application data.
  - name: LifeTime environment setup
    id: lifetime-environment-setup
    subtopics:
    - name: Relocate LifeTime console
      id: relocate-lifetime-console
      description: Wants to relocate the infrastructure management console
        (LifeTime) from a shared on-premises environment to a new dedicated
        environment, by installing LifeTime in the new environment, manually
        recreating Roles, Teams, Authentication Mode, and Service Account
        configurations, registering the OutSystems environments, and then
        disabling and deleting LifeTime's applications from the original
        environment, so that LifeTime runs independently, accepting the
        loss of LifeTime-specific data such as Performance Monitor history,
        application tagging history, and the change log.
    - name: Remove LifeTime console
      id: remove-lifetime-console
      description: Wants to remove OutSystems infrastructure management console
        (LifeTime) from an on-premises environment by unregistering all
        registered environments in LifeTime and then deleting the
        LifeTimePerformanceMonitor, LifeTime, and LifeTimeSDK applications
        in that order in Service Center, or disabling those applications
        instead if the environment runs a Platform Server version lower
        than 10.0.804.0, so that LifeTime is uninstalled without deleting
        any table or data from the database.
    - name: LifeTime dedicated env
      id: lifetime-dedicated-env
      description: Wants to understand that OutSystems 11 requires installing
        the infrastructure management console (LifeTime) in its own dedicated
        environment, with separate application servers and distinct database
        catalogs or schemas, rather than reusing an existing environment, so
        that they plan the installation correctly.
    - name: Size LifeTime storage
      id: lifetime-storage-sizing
      description: Wants to estimate the storage capacity needed for the
        LifeTime infrastructure management console environment, including
        Production monitoring data, LifeTime database growth, and
        transaction log growth, based on average page views of monitored
        applications, so that they can properly size the dedicated
        environment's database.
    - name: Start demo infrastructure
      id: start-demo-infrastructure
      description: Wants to start the demo infrastructure VMs and open the LifeTime console
        so they can prepare the infrastructure demo environment before running
        the demo steps.
  - name: Add new cloud environment
    id: add-new-cloud-environment
    description: Wants to add a new Cloud environment to their OutSystems
      infrastructure by contacting sales, who provision it and confirm by
      email, so that the new environment is ready for use.
  - name: Add new self-managed environment
    id: add-new-self-managed-environment
    description: Wants to add a new self-managed environment to their
      OutSystems infrastructure by verifying system and network requirements,
      installing the environment, registering the serial number and
      installing the license, and registering it in the infrastructure
      management console (LifeTime), so that the new environment is ready for
      use.
  - name: Env registration error
    id: env-registration-error
    description: Wants to troubleshoot why registering a new environment in
      LifeTime fails by checking bidirectional network connectivity, reviewing
      the environment's Integration logs for SOAP requests from LifeTime, and
      confirming valid SSL certificates on LifeTime and the target environment,
      so that the environment can be registered successfully.
  - name: Hybrid env addition
    id: hybrid-env-addition
    description: Wants to know how to add an environment when running a hybrid
      configuration (some environments in OutSystems Cloud, others self-
      managed), by contacting technical support instead of following the
      standard Cloud or self-managed steps, understanding this option only
      applies to licenses purchased before a specific cutoff date, so that they
      add environments correctly under a hybrid setup.
  - name: IIS application pool management
    id: iis-application-pool-management
    subtopics:
    - name: Move app to new pool
      id: move-app-to-new-pool
      description: Wants to isolate a critical or misbehaving app from
        resource contention with other apps by creating a new IIS
        application pool (with matching .NET CLR version and managed
        pipeline mode, tuned recycling settings, and immediate start), then
        moving the app to it via Change Application Pool, in a self-managed
        OutSystems 11 environment, so that the app runs on its own worker
        process without impacting or being impacted by other apps.
    - name: Revert app pool move
      id: revert-app-pool-move
      description: Wants to move an app back to the default
        OutSystemsApplications IIS application pool and remove the custom
        application pool they created, in a self-managed OutSystems 11
        environment, so that the isolation change can be undone once no
        longer needed.
    - name: Tune app pool recycling
      id: tune-app-pool-recycling
      description: Wants to understand application pool recycling in IIS, including the
        overlapped recycle method, and configure automatic recycling settings
        through the IIS Manager Recycling wizard, then set memory-based recycle
        limits high enough to avoid unnecessary recycles and low enough to avoid
        affecting other pools' available memory, fine-tuning those values using
        real-world memory usage data, in a self-managed OutSystems 11 on-
        premises or private cloud deployment, so that application pools remain
        performant and stable.
    - name: Tune IIS thread pool
      id: tune-iis-thread-pool
      description: Wants to tune the ASP.NET worker process thread pool
        settings (the processModel element in machine.config, including
        autoConfig, minWorkerThreads, and minIoThreads) on a self-managed
        OutSystems 11 IIS front-end, and to understand why the default
        autoConfig="true" behavior doesn't react fast enough to bursty
        Mobile/Reactive traffic, so that the front-end has enough ready
        threads to absorb request bursts without queuing.
  - name: Database Catalogs and Schemas
    id: database-catalogs-and-schemas
    subtopics:
    - name: Set up DB catalog/schema
      id: setup-database-catalog-schema
      description: Wants to create a new SQL Server database catalog or
        Oracle schema and configure it in Service Center (setting
        permissions, downloading/running the configuration script, and
        testing the configuration), so that OutSystems modules can be
        published to a dedicated database, in a self-managed OutSystems 11
        environment.
    - name: Publish module to catalog
      id: publish-module-to-catalog-schema
      description: Wants to specify the target database catalog or schema
        when publishing a new module or a solution containing new modules,
        in Service Studio or Service Center, so that the module's database
        objects are created in the intended catalog/schema, in a
        self-managed OutSystems 11 environment.
    - name: Move module database
      id: move-module-database-catalog-schema
      description: Wants to move an existing module to a different database
        catalog or schema (SQL Server or Oracle), including backing up
        databases, copying module data, and republishing or staging the
        solution, so that the module operates in its new database without
        losing data or breaking dependent modules, in a self-managed
        OutSystems 11 environment.
  - name: Direct database access (Cloud)
    id: direct-database-access-cloud
    subtopics:
    - name: Temporary database access
      id: temporary-database-access
      description: Wants to request a temporary database user for an OutSystems Cloud
        environment through LifeTime's self-service "Create database user"
        option, receiving time-limited read-and-write credentials for ad-hoc
        troubleshooting, provided their LifeTime version supports the feature
        and they have an existing VPN or AWS Transit Gateway connection.
    - name: Permanent database access
      id: permanent-database-access
      description: Wants to request a permanent database user for an OutSystems Cloud
        environment by opening a support case specifying the environment, access
        level, and connection method, so that they get long-term credentials for
        automated integration or data synchronization.
    - name: DB user permissions
      id: database-user-permissions
      description: Wants to know the exact permissions granted to OutSystems Cloud direct
        database access users, comparing SQL Server and Oracle, for read-only
        versus read-and-write credential sets, so that they can determine which
        access level meets their needs.
  - name: SQL Server Compatibility Upgrade
    id: sql-server-compatibility-upgrade
    subtopics:
    - name: Upgrade SQL compatibility
      id: upgrade-sql-compatibility
      description: Wants to upgrade the SQL Server compatibility level of a
        self-managed OutSystems 11 installation, using either an
        upgrade-in-place or a fresh-install migration path, so that they
        keep the database engine current or meet company compliance
        requirements while safeguarding correct application behavior and
        query performance for databases created at a lower compatibility
        level.
  - name: Technical Preview features
    id: technical-preview-features
    subtopics:
    - name: Understand Technical Preview
      id: understand-technical-preview
      description: Wants to understand what Technical Preview features are in
        OutSystems, including that they aren't production-ready, are disabled
        by default, receive no OutSystems support, and may change
        significantly or be removed, so that they can decide whether to opt
        in.
    - name: Enable Technical Preview
      id: enable-technical-preview
      description: Wants to activate a Technical Preview feature in LifeTime,
        either in OutSystems Cloud under Environment > Options or in a
        self-managed installation under Infrastructure > Technical Preview,
        requiring the "Manage Infrastructure and Users" infrastructure
        permission, so that they can start using the feature before it's
        generally available.
  - name: Platform Server Setup
    id: platform-server-setup
    subtopics:
    - name: Install prerequisites
      id: platform-install-prerequisites
      description: Wants to understand the default hardware, software, and
        file system requirements for installing OutSystems 11 Platform
        Server, including the fixed MAC address hardware requirement, the
        Platform Server installation checklist, default installation and
        registry locations, and the Deployment Controller, Deployment, and
        Scheduler Windows services with their executables and running
        accounts, so that they can prepare their on-premises infrastructure
        and grant the necessary file system, service, and port permissions
        before installing.
    - name: Database requirements
      id: platform-database-requirements
      description: Wants to understand the database configuration
        requirements for the OutSystems 11 platform database across SQL
        Server, Azure SQL Database, Oracle, and DB2, including
        authentication mode, case-insensitive collation, NLS_CHARACTERSET
        encoding, and IBM iAccess software prerequisites, so that they
        configure a compliant database backend before installing
        OutSystems 11.
    - name: App server requirements
      id: platform-app-server-requirements
      description: Wants to understand the IIS Application Server and
        deployed application requirements for OutSystems 11, including
        enabling IIS6 compatibility mode, keeping the Default Web Site
        listening on 127.0.0.1:80 without host headers, allowing the HTTP
        PATCH method for Server.API endpoints, and where deployed
        application source code and compiled applications are stored, so
        that they can prepare the application server layer before
        deployment.
    - name: Deploy All feature
      id: deploy-all-feature-upgrade
      description: Wants to use the optional modules deployment step (Deploy All)
        introduced in Platform Server 11.19.0+ during an environment upgrade, by
        going to Factory > Upgrades > Deploy in Service Center, understanding
        its benefits over manual publishing, and that only modules with a failed
        compilation or an already-deployed latest version are skipped, so that
        they can deploy all newly compiled modules after a Platform Server
        upgrade without publishing each one manually.
    - name: Check Deploy All impact
      id: check-deploy-all-impact
      description: Wants to determine whether an on-premises environment is
        affected by RPM-3448 after using Deploy All in Platform Server 11.18.0
        by checking Service Center > Factory > Upgrades for the Deployment step,
        so that they know whether the issue applies before they try the cleanup
        procedure.
    - name: Clean Deploy All tenants
      id: clean-deploy-all-tenants
      description: Wants to clean up invalid records created by Deploy All in an affected
        on-premises environment by running the platform-specific cleanup scripts
        and validation steps after an affected Platform Server upgrade, so that
        they can remove the duplicated records safely.
    - name: Track deployment progress
      id: track-deployment-progress-upgrade
      description: Wants to monitor an in-progress modules deployment step
        during a Platform Server upgrade in Service Center, by revisiting
        Factory > Upgrades > Deployment for the ongoing run, viewing the
        upgrades report once it finishes, and knowing that relaunching
        Deploy after an error retries only the modules that failed, so that
        they can follow the deployment to completion and recover from
        partial failures.
    - name: Modules prep overview
      id: modules-prep-overview
      description: Wants to understand the modules preparation step
        introduced in Platform Server 11.12.0+ during an environment
        upgrade, including that it performs a background compilation of
        previously published modules without deploying them, that
        publishing remains gradual and optional until they choose to
        deploy, and how progressive modules preparation (grouped
        compilation, dependency-ordered processing, and retry of only
        failed modules) evolved from 11.12.0 through 11.17.0+, so that they
        understand what happens automatically after an upgrade and when
        they still need to publish modules manually.
    - name: Track modules prep
      id: track-modules-prep
      description: Wants to monitor an in-progress modules preparation step
        during a Platform Server upgrade in Service Center, by checking the
        current preparation details banner or More details link, viewing
        group-by-group progress on the Upgrades tab, or in self-managed
        installations checking the Configuration Tool's Apply/Exit window,
        so that they can follow progress and know when modules in a group
        become available again for publish, configuration, or delete
        operations.
    - name: Module prep errors
      id: module-prep-errors
      description: 'Wants to locate and resolve errors and lock notifications
        related to the modules preparation step of a Platform Server upgrade
        in Service Center (failed or skipped module preparation, publish,
        configuration, or delete lock messages during an ongoing upgrade,
        and module version mismatch messages), by identifying the specific
        message and following the recommended fix (checking the Error Log,
        waiting for the preparation step to finish, or republishing the
        affected modules), so that they can restore normal publishing and
        configuration in OutSystems 11 after a platform upgrade.'
    - name: Modules prep known issues
      id: modules-prep-known-issues
      description: Wants to know about a known issue affecting the modules
        preparation step when directly upgrading from Platform Server
        11.10.0 or earlier to a version with Flexible Upgrades (11.12.0+),
        where applications compiled in previous versions combined with new
        configurations added in 11.10.0 can cause high CPU usage on
        frontends until republished, by publishing all applications instead
        of relying on modules preparation, so that they avoid this issue
        after the upgrade.
    - name: Plan platform upgrade
      id: plan-platform-upgrade
      description: Wants to plan an OutSystems Platform Server upgrade by
        following the four-phase process (analysis and preparation,
        planning, execution, testing), including checking release notes
        and breaking-changes documents and scheduling the upgrade around
        development sprints, so that the upgrade doesn't disrupt ongoing
        development and application impact is minimized.
    - name: Choose upgrade go-live strategy
      id: upgrade-golive-strategy
      description: Wants to decide between a downtime or zero-downtime
        approach for upgrading the Production environment's Platform
        Server, weighing the safer rollback guarantees of a downtime
        upgrade against the multiple-front-end requirement and potential
        data loss of a zero-downtime upgrade, so that they select the
        go-live strategy that fits their availability needs.
    - name: Publish apps after upgrade
      id: publish-apps-after-upgrade
      description: Wants to know whether to publish all applications
        immediately or publish them gradually after a Platform Server
        upgrade, based on the source and target version (Platform Server
        11.12.0+ enabling gradual publishing at their teams' pace, versus
        earlier versions requiring all applications to be published), so
        that they follow the correct application rollout approach for
        their specific upgrade scenario.
    - name: Infrastructure architecture
      id: infrastructure-architecture
      description: Wants to understand how an OutSystems infrastructure is
        organized around environments, LifeTime, pipelines, and SaaS tools,
        so that they can reason about the platform architecture and
        management model.
    - name: Environment architecture
      id: environment-architecture
      description: Wants to understand how an OutSystems environment is
        built from application servers, databases, Platform Server
        services, and standalone or server-farm configurations, so that
        they can plan and troubleshoot the runtime topology for each
        environment.
    - name: Schedule index/statistics maintenance
      id: schedule-index-statistics-maintenance
      description: Wants to schedule regular index reorganize/rebuild
        operations and statistics updates for OutSystems 11 self-managed
        installations, so that database query performance stays stable as
        fragmentation and stale statistics accumulate.
  - name: Schedule transaction log backups
    id: schedule-transaction-log-backups
    description: Wants to schedule transaction log backups across all
      environments for OutSystems 11 self-managed installations, so that
      transaction log growth stays under control and point-in-time recovery
      remains available.
  - name: Configure data/log file growth and storage isolation
    id: configure-data-log-file-growth-storage-isolation
    description: Wants to set large fixed database file growth and keep
      data and log files on separate storage for OutSystems 11
      self-managed installations, so that database files avoid
      fragmentation and storage contention as capacity grows.
  - name: Server performance tuning
    id: server-performance-tuning
    description: Wants to apply OutSystems 11 self-managed server-level
      performance tuning practices, including disabling unnecessary
      Virtual Memory/system indexers and Data Execution Prevention,
      isolating heavy background timers on dedicated servers, preferring
      64-bit server architecture, and excluding Platform Server, ASP.NET
      temp, and Deployment Controller Service folders from real-time
      antivirus scanning, so that frontend and timer processing avoid
      unnecessary CPU, memory, and I/O overhead.
- name: Log streaming (O11)
  guid: 7f3c2a1e-84d0-4b5f-a9c3-e1d06f82b347
  topics:
  - name: Log Streaming overview
    id: log-streaming-overview
    subtopics:
    - name: Understand Log Streaming
      id: log-streaming-overview-selfmanaged
      description: Wants to understand what Log Streaming is for a self-managed O11
        installation — including how the bundled Fluent Bit service reads JSON log
        files, transforms them into the Flat Format, and forwards them to an external
        tool — so that they can decide whether to adopt it and know what to expect
        before setup.
    - name: Enable Log Streaming
      id: log-streaming-enable
      description: Wants to enable and configure Log Streaming on a self-managed O11
        installation using the Configuration Tool GUI or CLI, including meeting
        prerequisites, configuring settings, setting up a multi-front-end farm, and
        disabling the feature, so that application logs are forwarded to an external
        observability or SIEM tool.
    - name: Cloud log streaming
      id: log-streaming-overview-cloud
      description: Wants to understand what log streaming is for an OutSystems 11
        app running on OutSystems Cloud, including how the OpenTelemetry Protocol
        (OTLP) is used to export application and platform logs to third-party APM
        and observability tools, and the benefits it provides, so they can decide
        whether to adopt it.
    - name: Log streaming prerequisites
      id: log-streaming-cloud-prerequisites
      description: Wants to know the licensing, Platform Server and LifeTime
        version, user permission, and connectivity requirements to enable log
        streaming for an OutSystems 11 app on OutSystems Cloud, so they can
        confirm they meet the prerequisites before configuring a stream to an
        APM tool.
    - name: OpenTelemetry field mapping
      id: log-streaming-field-mapping
      description: Wants to look up how existing OutSystems 11 log data fields map to their
        OpenTelemetry equivalents across all supported O11 log types, so they
        can correctly interpret or query streamed log data in an APM tool.
  - name: Log Streaming operations
    id: log-streaming-ops
    subtopics:
    - name: Understand stream errors
      id: understand-stream-errors
      description: Wants to understand why a streaming error occurs when configuring
        a connection to an APM tool in O11 or ODC, and to find the resolution for
        a specific error code, so they can navigate to the correct error page and
        fix the connection.
    - name: Troubleshoot Log Streaming
      id: log-streaming-troubleshoot
      description: Wants to diagnose and resolve Log Streaming issues on a self-managed
        O11 installation — using Test Connection, the Fluent Bit service log, the
        Windows Service console, and the metrics endpoint — so that they can identify
        why logs aren't arriving at a destination or the service isn't running.
    - name: Fix streaming cancelled
      id: fix-streaming-cancelled
      description: Wants to understand and resolve the OS-DP-STREAM-00001 "operation
        was cancelled" error — where the destination server responds with a gRPC 1
        (Cancelled) error when testing the streaming connection in O11 or ODC — so
        that they can restore log and observability data streaming by verifying the
        destination works correctly and re-establishing the connection.
    - name: Fix streaming timeout
      id: fix-streaming-timeout
      description: Wants to understand and resolve the OS-DP-STREAM-00004 "deadline
        exceeded" error — where the destination server responds with a gRPC 4 error
        when testing the streaming connection in O11 or ODC — so that they can restore
        log and observability data streaming to the destination APM tool.
    - name: Fix streaming not found
      id: fix-streaming-not-found
      description: Wants to understand and resolve the OS-DP-STREAM-00005 "not found"
        error — where the destination server responds with a gRPC 5 error when testing
        the streaming connection in O11 or ODC — so that they can restore log and
        observability data streaming by correcting the endpoint URL in LifeTime Log
        Streaming or the ODC Portal.
    - name: Fix streaming already exists
      id: fix-streaming-already-exists
      description: Wants to understand and resolve the OS-DP-STREAM-00006 "already
        exists" error — where the destination server responds with a gRPC 6 error
        when testing the streaming connection in O11 or ODC — so that they can restore
        log and observability data streaming by verifying the APM tool is working
        and re-establishing the connection.
    - name: Fix streaming permission denied
      id: fix-streaming-permission-denied
      description: Wants to understand and resolve the OS-DP-STREAM-00007 "permission
        denied" error — where the destination server responds with a gRPC 7 error
        when testing the streaming connection in O11 or ODC — so that they can restore
        log and observability data streaming by correcting the authentication credentials
        or endpoint URL in LifeTime Log Streaming or the ODC Portal.
    - name: Fix streaming resource exhausted
      id: fix-streaming-resource-exhausted
      description: Wants to understand and resolve the OS-DP-STREAM-00008 "resource
        exhausted" error — where the destination server responds with a gRPC 8 error
        when testing the streaming connection in O11 or ODC — so that they can restore
        log and observability data streaming by verifying the APM tool is working
        and re-establishing the connection.
    - name: Fix streaming failed precondition
      id: fix-streaming-failed-precondition
      description: Wants to understand and resolve the OS-DP-STREAM-00009 "failed
        precondition" error — where the destination server responds with a gRPC 9
        error when testing the streaming connection in O11 or ODC — so that they
        can restore log and observability data streaming by verifying the APM tool
        is working and re-establishing the connection.
    - name: Fix streaming aborted
      id: fix-streaming-aborted
      description: Wants to understand and resolve the OS-DP-STREAM-00010 "aborted"
        error — where the destination server responds with a gRPC 10 (Aborted)
        error when testing the streaming connection in O11 or ODC — so that they
        can restore log and observability data streaming by verifying the APM
        tool is working and re-establishing the connection.
    - name: Fix streaming unknown error
      id: fix-streaming-unknown
      description: Wants to understand and resolve the OS-DP-STREAM-00002 "unknown
        error reaching your destination server" error — where the destination server
        responds with a gRPC 2 (Unknown) error when testing the streaming connection
        in O11 or ODC — so that they can restore log and observability data streaming
        to the destination APM tool.
    - name: Fix streaming invalid argument
      id: fix-streaming-invalid-argument
      description: Wants to understand and resolve the OS-DP-STREAM-00003 "invalid
        argument" error — where the destination server responds with a gRPC 3 (Invalid
        argument) error when testing the streaming connection in O11 or ODC — so
        that they can restore log and observability data streaming by correcting
        the authentication credentials or endpoint URL in LifeTime Log Streaming
        or the ODC Portal.
    - name: Fix streaming out of range
      id: fix-streaming-out-of-range
      description: Wants to understand and resolve the OS-DP-STREAM-00011 "out
        of range" error — where the destination server responds with a gRPC 11
        (Out of range) error when testing the streaming connection in O11 or ODC
        — so that they can restore log and observability data streaming by verifying
        the APM tool is working and re-establishing the connection.
    - name: Fix streaming unimplemented
      id: fix-streaming-unimplemented
      description: Wants to understand and resolve the OS-DP-STREAM-00012 "unimplemented"
        error — where the destination server responds with a gRPC 12 (Unimplemented)
        error when testing the streaming connection in O11 or ODC — so that they
        can restore log and observability data streaming by verifying the APM tool
        is working and re-establishing the connection.
    - name: Fix streaming internal
      id: fix-streaming-internal
      description: Wants to understand and resolve the OS-DP-STREAM-00013 "internal
        error" error — where the destination server responds with a gRPC 13 (Internal)
        error when testing the streaming connection in O11 or ODC — so that they
        can restore log and observability data streaming by verifying the APM tool
        is working and re-establishing the connection.
    - name: Fix streaming unavailable
      id: fix-streaming-unavailable
      description: Wants to understand and resolve the OS-DP-STREAM-00014 "unavailable"
        error — where the destination server responds with a gRPC 14 (Unavailable)
        error when testing the streaming connection in O11 or ODC — so that they
        can restore log and observability data streaming by verifying the destination
        server is reachable and correcting the server URL.
    - name: Fix streaming data loss
      id: fix-streaming-data-loss
      description: Wants to understand and resolve the OS-DP-STREAM-00015 "data
        loss" error — where the destination server responds with a gRPC 15 (Data
        loss) error when testing the streaming connection in O11 or ODC — so that
        they can restore log and observability data streaming by verifying the
        APM tool is working and re-establishing the connection.
    - name: Fix streaming unauthenticated
      id: fix-streaming-unauthenticated
      description: Wants to understand and resolve the OS-DP-STREAM-00016 "unauthenticated"
        error — where the destination server responds with a gRPC 16 (Unauthenticated)
        error when testing the streaming connection in O11 or ODC — so that they
        can restore log and observability data streaming by correcting the authentication
        credentials or endpoint URL in LifeTime Log Streaming or the ODC Portal.
    - name: Fix streaming redirect
      id: fix-streaming-redirect
      description: Wants to understand and resolve the OS-DP-STREAM-30101 "moved
        permanently" error — where the destination server responds with HTTP 301
        when testing the streaming connection in O11 or ODC — so that they can
        restore log and observability data streaming by updating the endpoint
        URL to the new destination location.
    - name: Fix streaming auth error
      id: fix-streaming-auth
      description: Wants to understand and resolve the OS-DP-STREAM-40101 "authentication
        error" — where the destination APM tool server rejects an authentication
        attempt when streaming audit/observability data (ODC) or logs (O11) — so
        that they can restore data streaming by correcting the authentication
        credentials or endpoint URL in the ODC Portal or LifeTime Log Streaming
        destination configuration.
    - name: Fix streaming bad gateway
      id: fix-streaming-bad-gateway
      description: Wants to understand and resolve the OS-DP-STREAM-50002 "internal
        or gateway communication error" — where the destination server responds
        with HTTP 502 when testing the streaming connection in O11 or ODC — so
        that they can restore log and observability data streaming by verifying
        the APM tool is working and re-establishing the connection.
    - name: Fix streaming authz error
      id: fix-streaming-authz
      description: Wants to understand and resolve the OS-DP-STREAM-40301 authorization
        error, where the APM tool server rejects an authorization attempt when
        streaming audit/observability data in ODC or logs in O11, so that they
        can restore data streaming by reviewing the APM server configuration and
        verifying that the credentials have the correct permissions.
    - name: Fix streaming 40401
      id: fix-streaming-40401
      description: Wants to understand and resolve the OS-DP-STREAM-40401 error,
        where an authorization failure blocks the connection to the APM tool
        server when streaming audit/observability data in ODC or logs in O11,
        so that they can restore data streaming by reviewing the APM server
        configuration and verifying the server URL in the ODC Portal or LifeTime
        Log Streaming destination configuration.
    - name: Fix streaming HTTP 503
      id: fix-streaming-http-503
      description: Wants to understand and resolve the OS-DP-STREAM-50003 "unknown
        error reaching your APM tool server" error, where the destination server
        responds with HTTP 503 when testing the streaming connection in O11 or
        ODC, so that they can restore audit, observability, and log data streaming
        by verifying the APM tool is working and re-establishing the connection.
  - name: Fluent Bit configuration
    id: fluentbit-configuration
    subtopics:
    - name: Configure Fluent Bit output
      id: fluentbit-configure-output
      description: Wants to set up a Fluent Bit output plugin in custom.yaml on a
        self-managed O11 installation, including specifying the output name, match
        tags, match_regex, alias, and reliability settings such as retry_limit, so
        that logs are routed to the chosen APM or SIEM destination reliably.
    - name: Filter streaming logs
      id: fluentbit-filter-logs
      description: Wants to filter streamed logs in Fluent Bit on a self-managed O11
        installation by log type (using tag matching) or by application name (using
        the modify+grep workaround for flat-format field names), so that only the
        desired subset of records reaches a given output.
    - name: Enrich logs with metadata
      id: fluentbit-enrich-logs
      description: Wants to add static metadata fields, copy fields, or rename fields
        in Fluent Bit log records on a self-managed O11 installation using the modify
        filter and dotted-key names, so that streamed logs carry the additional context
        needed by the destination tool.
  - name: APM destination configuration
    id: log-streaming-destination-config
    subtopics:
    - name: Configure Splunk streaming
      id: configure-splunk-streaming
      description: Wants to set up log streaming from OutSystems 11 applications
        to Splunk by configuring the OpenTelemetry Collector with an HTTP Event
        Collector receiver and Splunk Exporter, updating the collector image
        version, and registering the collector endpoint and credentials in
        LifeTime, so that logs stream to Splunk in near real time.
    - name: Configure Datadog streaming
      id: configure-datadog-streaming
      description: Wants to set up log streaming from ODC apps to Datadog by
        configuring the OpenTelemetry Collector with the Datadog exporter,
        getting the Datadog API key and collector endpoint credentials, and
        registering the stream in the ODC Portal, so that observability data
        streams to Datadog.
    - name: Configure New Relic streaming
      id: configure-new-relic-streaming
      description: Wants to set up log streaming from ODC apps to New Relic by
        retrieving the New Relic API key and account region, configuring the
        stream in the ODC Portal, and sending observability data to New Relic,
        so that app telemetry reaches the New Relic APM tool.
    - name: Set up OpenTelemetry Collector
      id: set-up-open-telemetry-collector
      description: Wants to set up the OpenTelemetry Collector with the appropriate
        exporter and security settings for destinations that do not support
        native OTLP ingestion, so they can receive streamed logs, traces, and
        metrics.
- name: Log streaming (ODC)
  guid: 5e4d826e-41ed-47ae-9f2e-9c87b56d41f2
  topics:
  - name: Stream overview
    id: odc-stream-overview
    subtopics:
    - name: Analytics Stream overview
      id: understand-analytics-stream
      description: Wants to understand what streaming observability data
        (Analytics Stream) is, how ODC uses the OpenTelemetry standard and
        OTLP to export logs, traces, and metrics to third-party APM tools,
        and its benefits, so they can decide whether and how to adopt it
        for monitoring ODC apps, agents, and workflows.
    - name: Streamed log data
      id: streamed-log-data
      description: Wants to look up which log attributes ODC streams to APM tools,
        so they can interpret the streamed log records when analyzing app
        telemetry in an APM tool.
    - name: Streamed metrics data
      id: streamed-metrics-data
      description: Wants to look up which metrics ODC streams to APM tools and
        the request, invocation, and resource attributes associated with them,
        so they can interpret streamed metric records in an APM tool.
    - name: Streamed trace data
      id: streamed-trace-data
      description: Wants to look up which trace span attributes ODC streams to
        APM tools, so they can interpret streamed trace records in an APM tool.
    - name: Supported APM tools
      id: supported-apm-tools
      description: Wants to know which APM tools the streaming feature supports
        natively vs. via the OpenTelemetry collector, and which categories of
        observability data and application and platform log data stream to them,
        so they can pick a compatible destination for their observability and
        log data.
    - name: Connectivity requirements
      id: stream-connectivity-requirements
      description: Wants to understand the connectivity and security
        requirements (public endpoint exposure, encryption, authentication,
        IP allowlisting) for streaming observability data from ODC to an
        APM tool, so they can prepare their APM tool's ingestion endpoint
        before configuring a stream.
  - name: Stream configuration
    id: odc-stream-configuration
    subtopics:
    - name: Create analytics stream
      id: create-analytics-stream
      description: Wants to create a stream in the ODC Portal by selecting a destination
        APM tool, entering destination and source environment details, and
        testing the connection, so that observability data (logs, metrics, and
        traces) streams from their app to that APM tool.
    - name: Stream to Elastic
      id: stream-to-elastic
      description: Wants to stream observability data from ODC apps to Elastic APM
        by deploying the required Elastic Observability services and Kibana plugins
        alongside Elasticsearch, providing the Elastic URL and Secret token, and
        configuring the stream in the ODC Portal, so that observability data reaches
        Elastic.
    - name: Stream to Amazon S3
      id: stream-to-amazon-s3
      description: Wants to stream observability data from ODC apps to Amazon S3 by
        setting up the OpenTelemetry Collector with the AWS S3 contrib exporter and
        configuring the stream in the ODC Portal, so that observability data lands
        in Amazon S3 when that is the destination.
    - name: Stream to Splunk
      id: stream-to-splunk
      description: Wants to stream observability data from ODC apps to Splunk APM
        by setting up the HTTP Event Collector, configuring the OpenTelemetry
        Collector with an HTTP or gRPC receiver and Splunk Exporter, updating the
        collector image version, and registering the stream in the ODC Portal,
        so that observability data reaches Splunk.
    - name: Stream status meanings
      id: stream-status-meanings
      description: Wants to understand what the Active, Active with errors, and
        Inactive statuses mean for a stream in the ODC Portal, so they can tell
        whether observability data is being delivered correctly to the
        destination APM tool.
- name: O11 Data Migration
  guid: ''
  topics:
  - name: Environment Data Migration
    id: environment-data-migration
    subtopics:
    - name: Factory metamodel
      id: factory-metamodel
      description: Wants to understand how OutSystems manages the factory's
        Applications, Espaces, Modules, and Extensions, and their system
        entities (physical tables, producers, and consumers, SS_Key
        uniqueness, and Is Active flags), so that they can correctly
        interpret this data model when planning an O11 data migration
        between environments.
    - name: Plan data migration
      id: plan-data-migration
      description: Wants to understand the recommendations, prerequisites, and
        planning considerations for migrating pure application and hybrid data
        from an O11 production environment to a non-production environment,
        including platform version alignment, database definition differences,
        and downtime considerations, so that they can plan a safe data migration
        process.
    - name: Platform dependencies
      id: map-platform-dependencies
      description: Wants to identify OutSystems Platform system entities (Users,
        Tenants, Multi-Tenant Entities, BPT) that applicational entities have
        foreign key dependencies on, so that they can map and align those
        dependencies between environments before migrating application data.
    - name: Match migration identifiers
      id: match-migration-identifiers
      description: Wants to match entity and attribute physical names, static
        entity record identifiers, and build a foreign keys map between source
        and destination O11 environments, so that migrated data correctly
        references the right records after an O11 data migration.
    - name: BPT entity model
      id: bpt-entity-model
      description: Wants to understand the entity-relationship model of BPT
        Process and Activity definition and instance entities, and how
        Service Studio, Service Center, and Lifetime populate them, so that
        they can correctly interpret this data model when planning an O11
        BPT data migration.
    - name: Migrate hybrid BPT and users
      id: migrate-hybrid-bpt-users
      description: Wants to migrate hybrid data, including BPT process/activity
        instances, BPT trigger events, and Users/Groups/Roles, between O11
        environments, so that migrated applicational data remains functionally
        consistent after the migration.
    - name: GDPR and data masking
      id: migration-gdpr-data-masking
      description: Wants to account for GDPR compliance requirements and data
        masking when planning an O11 application data migration, so that
        sensitive data stays protected throughout the migration process.
    - name: Date formats and timezones
      id: migration-date-timezone-differences
      description: Wants to reconcile date format and timezone differences
        between source and destination O11 environments during an application
        data migration, so that migrated date and time values remain accurate
        after the move.
    - name: Database catalog metadata
      id: migration-database-catalog-metadata
      description: Wants to account for database catalog metadata when planning
        an O11 application data migration, so that supporting schema details
        needed by the migrated data aren't missed.
    - name: Binary data (BLOB) migration
      id: migration-binary-blob-data
      description: Wants to migrate binary data (BLOB) between O11
        environments as part of an application data migration, so that files
        and other binary content remain available after the migration.
    - name: Espace bootstraps
      id: migration-espace-bootstraps
      description: Wants to handle Espace bootstraps when planning an O11
        application data migration, so that initialization logic and migrated
        data remain aligned in the destination environment.
    - name: Map entity dependencies
      id: map-entity-dependencies
      description: Wants to identify primary keys, foreign keys, other
        constraints, cyclic entity references, and soft key usage in
        application entities, so that they can migrate application data
        without breaking referential integrity or introducing broken
        business logic during an O11 data migration.
    - name: Classify entity types
      id: classify-entity-types
      description: Wants to classify OutSystems Platform entities into System,
        Application, and Hybrid types, including their naming conventions
        (OSSYS_/OSUSR_ prefixes), read/write permissions, and subgroupings,
        so that they can correctly plan which entities to read or migrate
        during an O11 data migration.
    - name: Users/Roles/Groups model
      id: users-roles-groups-entity-model
      description: Wants to understand the physical tables, producers, and
        consumers for the User, Role, Group, User_Role, Group_Role, and
        Group_User system entities, so that they can correctly interpret
        this entity-relationship model when planning an O11 environment
        data migration.
    - name: Trace BPT process data
      id: trace-bpt-process-data
      description: Wants to trace a BPT process instance across the Process,
        Process_Input, Process_Output, Activity, and Activity_Output
        entities using SQL queries in Service Center, so that they can
        identify actual field values and hybrid entity relationships before
        planning a BPT data migration.
- name: Application Monitoring and Alerts
  guid: 5a7f74cd-1f74-4499-a5db-c8761f47512f
  topics:
  - name: APDEX score explained
    id: apdex-score-explained
    description: Wants to understand what an OutSystems 11 APDEX score means and how it's
      calculated, based on the percentage of page loads rated poor, fair, or
      good against page load time thresholds, so they can interpret an
      application's performance rating shown as a percentage from 0% to 100%.
  - name: LifeTime Analytics sampling
    id: adjust-request-sampling
    description: Wants to adjust the LifeTime Analytics request sampling rate for
      Reactive and Mobile apps by changing the PerformanceMonitoringReactiveSampleRate
      parameter in Factory Configuration and republishing the app, so that the
      collection rate matches the app's traffic volume.
  - name: Enable performance analytics
    id: enable-performance-analytics
    description: Wants to enable LifeTime Analytics for Reactive and Mobile apps
      by activating the Technical Preview feature in LifeTime, resuming monitoring
      for each app, republishing the app, and waiting for data synchronization,
      so that performance metrics appear in LifeTime once the environment meets
      the version prerequisites and LifeTime Analytics is enabled.
  - name: App performance analytics
    id: app-performance-analytics
    description: Wants to understand how LifeTime performance analytics measures Reactive
      and Mobile app performance, so they can interpret the metrics correctly.
  - name: LifeTime analytics
    id: lifetime-analytics
    description: Wants to use LifeTime Analytics in Traditional Web apps to inspect
      end-user experience and performance by drilling from the environment to the
      application, module, screen, and screen action levels, reading client, network,
      server, and APDEX metrics, and troubleshooting performance issues.
  - name: Custom alerts
    id: custom-alerts
    subtopics:
    - name: Gather monitoring data
      id: gather-monitoring-api-data
      description: Wants to gather application operations data (request duration,
        browser load time, server time, error counts) using the Monitoring API's
        GET RequestEvents method or by querying log data, and optionally register
        custom events via POST RequestEvents or LogMessage, so that they have the
        raw data needed to build custom alerts.
    - name: Define alert thresholds
      id: define-alert-thresholds
      description: Wants to consume Monitoring API data or log data and define custom
        logic with thresholds that trigger alerts, using OutSystems logic or a
        third-party application, so that they can detect when application metrics
        cross acceptable limits.
    - name: Integrate alerts ITSM
      id: integrate-alerts-itsm
      description: Wants to trigger email notifications or call webservices when
        an alert threshold is met, to create tickets in ITSM tools such as
        ServiceNow, Zendesk, Jira, or Opsgenie, send Slack messages, or integrate
        with any webservice-exposing external service, so that operational alerts
        reach the right external system.
  - name: App Feedback
    id: app-feedback
    subtopics:
    - name: Send mobile app feedback
      id: send-mobile-app-feedback
      description: Wants to send feedback from a screen in a mobile app by
        long-pressing with two fingers, marking the area of concern, and
        entering a text message or voice recording (which requires microphone
        permission), so that end users can report issues or comments for
        review in the App Feedback app, provided the device has network
        access.
    - name: Send web app feedback
      id: send-web-app-feedback
      description: Wants to send feedback in a Traditional Web App or Reactive
        Web App by clicking the glowing circle indicator, typing a message,
        optionally pointing to a specific screen area or element, and
        pressing SEND, so that developers can better understand reported
        issues, provided the feedback feature is activated in the
        environment.
  - name: SLOWSQL log messages
    id: slowsql-log-messages
    description: Wants to interpret SLOWSQL log messages in Service Center's
      General Logs, with a configurable threshold set via Factory
      Configuration, in OutSystems 11, so that they can identify when a query
      has been flagged as slow during performance troubleshooting.
  - name: SLOWSQL duration behavior
    id: slowsql-duration-behavior
    description: Wants to understand why the duration reported by SLOWSQL log
      messages, with a configurable threshold set via Factory Configuration,
      excludes network fetch time and changes with progressive or
      optimizer-driven data fetching in OutSystems 11, so that they can
      interpret the logged duration correctly during performance
      troubleshooting.
  - name: Measure true query elapsed time
    id: measure-true-query-elapsed-time
    description: Wants to measure the true elapsed time of a query or screen in
      OutSystems 11 when SLOWSQL log messages, with a configurable threshold
      set via Factory Configuration, do not reflect the complete operation, by
      using LogMessage calls or an isolated screen copy, so that they can
      diagnose performance issues without relying only on SLOWSQL timings.
- name: Monitor and Troubleshoot (O11)
  guid: efd7e36b-05ea-4fe3-bf72-80617a1557e8
  topics:
  - name: Test environment connectivity
    id: test-environment-connectivity
    description: Wants to verify bidirectional HTTPS connectivity between the
      LifeTime environment and an application environment in OutSystems 11,
      by remoting into one environment's server and accessing the other
      environment's Service Center console over HTTPS, so that they can
      confirm the network path required by the OutSystems architecture is
      working.
  - name: Check OutSystems version
    id: check-outsystems-version
    description: Wants to check the installed version and revision of Service
      Studio, Integration Studio, LifeTime, Service Center, and Platform
      Server (including System Components) in OutSystems 11, so they can
      provide accurate version information when opening a support case or
      verifying environment consistency.
  - name: Debugging toolkit overview
    id: debugging-toolkit-overview
    description: Wants to understand what tools are available for debugging
      and troubleshooting Mobile and Reactive Web apps in OutSystems 11,
      including the Service Studio debugger, Service Center application
      logs, and modern browser developer tools, so that they know where to
      start when investigating unexpected application behavior.
    subtopics:
    - name: Fix debugger Chrome issue
      id: fix-debugger-chrome-issue
      description: Wants to fix the Service Studio debugger failing to start (with an
        "unresponsive browser" error) when using an incompatible Chrome version,
        by upgrading Service Studio to a version that includes the fix, so that
        they can resume debugging their app in OutSystems 11.
    - name: Fix debugger hang
      id: fix-debugger-hang
      description: Wants to fix the Service Studio debugger hanging or never
        reaching a breakpoint during a debug session in OutSystems 11,
        caused by antivirus, firewall, or network elements closing the
        connection to the environment, by enabling the "Use One Connection
        per Request in Debugger" option in Service Studio's Preferences, so
        debugging can continue without the connection being dropped.
    - name: Fix stopped debug session
      id: fix-stopped-debug-session
      description: 'Wants to fix the "The server has stopped the debug
        session" / "HTTP Forbidden" error in a self-managed OutSystems 11
        environment, by trusting the addresses that inject X-Forwarded-For
        headers under Service Center''s Administration > Security > Network
        Security > Trusted proxy addresses, so debug sessions behind load
        balancers or reverse proxies aren''t rejected by the Platform
        Server.'
    - name: Fix Chrome debug delay
      id: fix-chrome-debug-delay
      description: 'Wants to fix the "There was an error loading the
        application for debugging" message that stops a Reactive Web app
        debug session in Service Studio, caused by a proxy auto-detect
        delay connecting Service Studio and Chrome, by reusing the existing
        Chrome window or disabling "Automatically detect settings" in
        Chrome''s proxy settings, so debugging starts successfully.'
    - name: Capture HAR file
      id: capture-har-file
      description: Wants to capture a Network HAR file for troubleshooting an
        application issue, by opening the browser's developer tools,
        enabling network log recording and preservation, reproducing the
        issue, and exporting the captured traffic, across Google Chrome,
        Firefox, Edge, and IE11, so they have the network trace needed to
        diagnose the problem in OutSystems 11.
    - name: Browser-first debugging
      id: browser-first-debugging
      description: Wants to troubleshoot a Mobile App faster by first trying
        to reproduce the issue in a desktop browser (Chrome or Safari) or
        inspecting the WebView via Chrome Dev Tools while the app runs on a
        connected device, but always confirm the fix on a real device before
        considering it resolved, in OutSystems 11, so that they debug
        efficiently without missing device-only behavior differences.
  - name: Log source catalog
    id: log-source-catalog
    description: Wants to know which log or report file to collect for a
      specific OutSystems 11 component (Service Center, mobile app
      generation, Service Studio, LifeTime, Windows Event Viewer,
      Android/iOS devices, IIS, Java, OutSystems services, or the
      database) when preparing a support case, so they know where each
      log is available and which one applies to their issue.
    subtopics:
    - name: Use OSDiagTool
      id: use-osdiagtool
      description: Wants to collect Windows server system information and logs for an
        OutSystems 11 support case, by downloading, configuring, and running
        OSDiagTool and sending the generated ZIP file to OutSystems Support, so
        that Support has the diagnostic information needed to investigate the
        issue.
    - name: Obtain Java logs
      id: obtain-java-logs
      description: Wants to obtain JBoss, WildFly, or WebLogic server logs
        for a self-managed OutSystems 11 environment, by connecting to the
        server and locating the log files at their default installation
        paths for each app server, so that they can zip and attach them to
        a support case.
    - name: Export Windows event logs
      id: export-windows-event-logs
      description: Wants to export a copy of Windows event logs (Application, Security,
        or System) for a specific OutSystems 11 environment, by connecting via Remote
        Desktop, opening Windows Event Viewer, and using Save All Events As, so that
        they have a log file to review or send to OutSystems Support.
    - name: LifeTime infrastructure report
      id: lifetime-infrastructure-report
      description: Wants to download the LifeTime infrastructure report to
        attach to an OutSystems Support case, by accessing the LifeTime
        troubleshoot page (OutSystems 11) or using the Send us your feedback
        icon in the LifeTime console (OutSystems 10) to save the file, so
        that Support has the information needed to investigate an
        infrastructure-level issue.
    - name: LifeTime staging report
      id: lifetime-staging-report
      description: Wants to download the staging report for a specific
        deployment plan to attach to an OutSystems Support case, by using
        the Deployment/Staging list on the LifeTime troubleshoot page
        (OutSystems 11) or the environment's Change Log and DebugStaging
        page (OutSystems 10), so that Support has the deployment
        diagnostic information needed to investigate a staging issue.
    - name: LifeTime user permissions report
      id: lifetime-user-permissions-report
      description: Wants to download the user permissions report to attach
        to an OutSystems Support case, by using the LifeTime troubleshoot
        page (OutSystems 11) or the DebugPermissions page (OutSystems 10)
        to save the file, so that Support has the information needed to
        investigate a permissions issue.
    - name: Export SC logs
      id: export-sc-logs
      description: Wants to export Service Center application logs (such as
        Errors or General) in OutSystems 11, by selecting the log type in the
        Monitoring section, resetting filters, and using Export to Excel, so
        they have a log file to review or attach to a support case, given
        that Service Center has a limited log retention window.
  - name: Troubleshooting overview
    id: troubleshooting-overview
    description: Wants to find the troubleshooting guides and known issues
      in the section, then check their causes and how to solve them, so
      that they can diagnose the issue from the right article.
  - name: Troubleshoot infra issues
    id: troubleshoot-infra-issues
    description: Wants to find the right troubleshooting article for O11 infrastructure
      issues, including installation, upgrade, configuration, connectivity,
      application server, database, and platform service problems, so that they can
      diagnose the problem in the correct section.
  - name: Fix cross-site cookies (O11)
    id: fix-cross-site-cookies-o11
    description: Wants to identify and fix cross-site cookie problems in
      OutSystems 11 apps by using the LifeTime SameSite/Secure settings when
      stricter SameSite cookie policies cause issues such as broken iframes
      displaying third-party or OutSystems content, failing OAuth/OpenID/SAML
      authentication flows, and custom JavaScript-set cookies, so their
      applications keep working correctly.
  - name: Cookie reference (O11)
    id: cookie-reference-o11
    description: Wants to look up the OutSystems 11 cookies used by default
      (name, size, expiration, and purpose), covering session, visitor,
      login, pagination, and device-detection cookies, so they can
      document cookie usage for EU cookie-consent compliance.
  - name: Disabled cookies impact
    id: disabled-cookies-impact-o11
    description: Wants to know what happens when end-users disable cookies
      in their browser for an OutSystems 11 application, so they
      understand unauthenticated pages stay accessible but
      login-dependent functionality may break.
  - name: Fix CSP headers (O11)
    id: fix-csp-headers-o11
    description: Wants to troubleshoot missing or incorrect Content Security
      Policy headers in an OutSystems 11 app by verifying the headers sent
      in the browser's developer tools and checking common causes, including
      republishing the app and its dependencies after CSP configuration changes,
      application-level CSP settings overriding environment-level ones, and a Shared
      Configuration overwriting the CSP headers, so they can restore the
      expected CSP header behavior.
  - name: Fix ViewState decrypt error
    id: fix-viewstate-decrypt-error
    description: 'Wants to fix the "Error Deserializing ViewState: Cannot
      decrypt the content" error in OutSystems 11 Traditional Web apps,
      caused by a mismatch between the request''s cookies and the ones
      stored in the view state (cookie caching by reverse proxies/CDNs,
      network components manipulating cookies/view state, Secure Session
      Cookies enforced over HTTP, application logic manipulating
      OutSystems cookies, stale browser sessions after a
      deployment/upgrade, browsers blocking the osVisitor cookie due to
      SameSite, switching from the personal area to the public app, or
      cached screens with PostBacks), by applying the fix for the matching
      cause or temporarily disabling Encrypt ViewState in Factory
      Configuration as mitigation, so postbacks stop failing with this
      error.'
  - name: EPA taskbox administration
    id: epa-taskbox-admin
    description: Wants to hide or show the EPA Taskbox per eSpace by activating EPA
      Administration and setting which applications/eSpaces display it, in
      OutSystems 11, from the EPA administration backoffice screen, so that they
      can control which apps expose the taskbox to end users and keep it hidden
      where it isn't needed.
  - name: Resolve internal error
    id: resolve-internal-error-o11
    description: Wants to know what to do when OutSystems 11 shows a generic
      "Internal Error" caused by an unexpected situation in the application
      or environment, so they can submit feedback through the tool or
      contact OutSystems Support if the issue persists.
  - name: Resolve app not found
    id: resolve-app-not-found
    description: Wants to know what to do when OutSystems 11 shows an "Application
      not found" error while opening or editing an application that no
      longer exists, so they can confirm the app was removed and contact
      OutSystems Support if the issue persists.
  - name: Resolve shared config
    id: resolve-shared-config
    description: 'Wants to know what to do when OutSystems 11 shows a "Shared
      Configuration ''<Shared Configuration>'' is invalid and will be
      ignored" warning, so they can ask the application manager or Service
      Center administrator to correct it using the Service Center Factory
      Configuration solution and republish the module.'
  - name: Resolve FIPS crypto error
    id: resolve-fips-crypto-error
    description: 'Wants to resolve the "This implementation is not part of the
      Windows Platform FIPS validated cryptographic algorithms" error that occurs
      when running OutSystems Platform Server versions earlier than 11.38.0 on a
      system enforcing FIPS policy, by disabling the local FIPS compliance security
      policy, overriding the .NET configuration to allow non-FIPS-validated
      algorithms, or upgrading to Platform Server 11.38.0+ for native FIPS-compliant
      environment support, so that the platform runs without being blocked by the
      system''s FIPS enforcement.'
  - name: Fix ORA-00054 error
    id: fix-ora-00054-error
    description: 'Wants to resolve the "ORA-00054: resource busy and acquire
      with NOWAIT specified or timeout expired" error when publishing an
      OutSystems 11 module to an Oracle-backed environment, by increasing the
      Oracle DDL Lock Timeout setting in server.hsconf on all front-ends and
      reapplying configuration via the Configuration Tool, so that ALTER
      statements succeed despite objects being locked by other transactions.'
  - name: Fix open cursors error
    id: fix-open-cursors-error
    description: 'Wants to resolve the "ORA-01000: maximum open cursors
      exceeded" error in OutSystems 11, by increasing the Oracle
      OPEN_CURSORS database setting or by refactoring application logic to
      avoid reusing aggregate output directly as a server action/loop
      input (assigning it to a local variable instead), so that sessions
      stop exceeding the open cursor limit.'
  - name: Fix registration mismatch
    id: fix-registration-mismatch
    description: 'Wants to fix the "Unable to register the Front-end Server"
      error caused by a mismatched, case-sensitive database connection string
      between a front-end server and the Deployment Controller in a
      self-managed OutSystems 11 farm environment, with the Deployment and
      Scheduler services not starting and the error appearing in Event Viewer
      (.NET) or DeployService.log (Java), by exporting the Controller''s
      configuration and importing it into the front-end (or copying
      server.hsconf and rerunning the configuration tool on Java), so that the
      Deployment and Scheduler services start successfully.'
  - name: AD auth login workaround
    id: ad-auth-login-workaround
    description: Wants to temporarily disable Active Directory (AD)
      authentication for IT users on a self-managed OutSystems 11
      environment, by updating the OSSYS_AUTHPROVIDER and admin user
      records directly in the Platform database and reapplying settings in
      the Configuration Tool, so that they can log in to Service Center
      with the built-in admin account and apply settings when AD-based
      login is otherwise blocked.
  - name: Reset LifeTime auth
    id: reset-lifetime-auth
    description: 'Wants to regain access to LifeTime, Service Center, and
      Service Studio after being locked out due to a misconfigured external
      authentication provider (for example, Active Directory or OpenID
      Connect), by disabling all authentication providers (UPDATE
      OSSYS_AUTHPROVIDER SET ISACTIVE = 0) in the LifeTime database and all
      environments registered in it, so that they can log back in with
      their previous credentials, in a self-managed OutSystems 11
      infrastructure.'
  - name: Reset IT admin password
    id: reset-it-admin-password
    description: Wants to reset the admin password for IT users in
      OutSystems 11, by asking another administrator or opening a Support
      Portal case in Cloud, or by hashing a new password and running a
      direct UPDATE OSSYS_USER database query in self-managed
      infrastructure, so that IT users regain administrator access to
      Service Center.
  - name: Reset end-user admin password
    id: reset-end-user-admin-password
    description: Wants to reset the admin password for the Users
      application (end users) in OutSystems 11, using Service Center's
      Users tool on supported Platform Server versions, opening a Support
      Portal case on older Cloud versions, or hashing a new password and
      running a direct UPDATE OSSYS_USER database query on any self-managed
      version, so that end users regain access to the Users app
      administrator account.
  - name: Restore console access
    id: restore-console-access
    description: 'Wants to restore access to Service Center and LifeTime when
      the browser shows "You are not authorized to view this page" because
      the machine is outside the Internal Network, by updating the Internal
      Network configuration in the Configuration Tool for OutSystems 9.1 or
      in the Service Center Network Security screen for OutSystems 11 and
      restarting the required services, so that the management consoles are
      reachable again.'
  - name: Fix SAML decrypt error
    id: fix-saml-decrypt-error
    description: 'Wants to resolve the "Unable to decrypt the assertion" error
      that occurs during end-user SAML authentication in OutSystems 11,       
      caused by a certificate mismatch between the Service Provider (SP)
      and Identity Provider (IdP), by regenerating the SP KeyStore in the
      Users application, downloading the new KeyStore and SP metadata XML,
      and re-uploading both to the IdP, so that end users can successfully
      authenticate.'
  - name: Fix SAML signature error
    id: fix-saml-signature-error
    description: 'Wants to resolve the "Unable to create SAML signature" error
      that occurs during end-user SAML logout in an OutSystems 11 personal
      environment, understanding it''s caused by personal environment
      hosting infrastructure security constraints and only affects the free
      personal environment offering, so they know this is expected behavior
      rather than a misconfiguration.'
  - name: Release environment serial
    id: release-environment-serial
    description: Wants to release an OutSystems 11 environment's serial
      number through the Customer Portal when performing planned operations
      (Platform Server upgrade, server transfer, hardware change, or
      environment sunset), so that they can register a new serial number
      and download an updated license file.
  - name: Renew server license
    id: renew-server-license
    description: Wants to know how to respond to a Service Center warning
      that their OutSystems 11 server license will expire soon or has
      already expired, by contacting their Service Center administrator to
      request a new license from OutSystems before losing access to
      licensed features.
  - name: License limit exceeded
    id: license-limit-exceeded
    description: Wants to resolve a Service Center warning that their
      OutSystems 11 server has exceeded its licensed resource usage or
      Software Units publishing limit, by cleaning up obsolete users,
      modules, or data, or asking their Service Center administrator to
      change the license contract, so that they can keep using licensed
      features and publish new modules.
  - name: License limits reached
    id: license-limits-reached
    description: Wants to resolve reaching their OutSystems 11 license usage
      limits (users, Application Objects, or environments), by contacting
      OutSystems Support with their Audit License and Activation Code, so
      that they can troubleshoot their license usage or upgrade their
      license.
  - name: License verification
    id: license-verification
    description: Wants to manually verify their OutSystems 11 license
      consumption by downloading the license verification file
      (OutSystems.Audit.txt) from Service Center's Licensing menu and
      uploading it to the Customer Portal, so that self-managed
      environments without automatic telemetry communication remain
      license compliant each quarter.
  - name: License invalid hardware
    id: license-invalid-hardware
    description: Wants to resolve an OutSystems 11 license invalid error
      caused by a hardware change (Deployment Controller VM restart,
      physical network device change, or promoting a front-end server to
      Deployment Controller) that alters the environment's serial number,
      by releasing the previous environment, registering it with the new
      serial number on the Licensing portal, and installing the new
      license file.
  - name: Retain VM MAC
    id: retain-vm-mac
    description: Wants to prevent a Virtual Machine hosting the Deployment
      Controller from getting a new environment serial number after a
      restart, by configuring the VM to retain its MAC address across
      restarts instead of assigning a new one.
  - name: Migrate Activation Code
    id: migrate-activation-code
    description: Wants to move a full self-managed OutSystems 11
      infrastructure (including LifeTime) from one Activation Code to
      another, by backing up existing license files, checking for
      IP-protected modules, registering and uploading new license files for
      the destination Activation Code, and republishing a test module to
      confirm the change, so that development activities and app publishing
      continue without unexpected Intellectual Property errors.
  - name: Rollback Activation Code
    id: rollback-activation-code
    description: Wants to revert a self-managed OutSystems 11 infrastructure
      back to its previous Activation Code after an Intellectual Property
      error occurs during a migration, by pausing Production deployments
      and restoring each environment's backed-up license file, so that they
      can confirm the old Activation Code is active again and contact
      OutSystems Support for further help.
  - name: AC migration issues
    id: ac-migration-issues
    description: Wants to know how to respond to common issues during an
      Activation Code migration (a missing serial number under the old
      Activation Code, or a license under either the old or new Activation
      Code not prepared for migration), by gathering the affected
      environments and contacting OutSystems Support, so they can get
      assistance completing the license change for all environments at
      once.
  - name: Find Activation Code
    id: find-activation-code
    description: 'Wants to locate their OutSystems 11 infrastructure''s
      Activation Code and each environment''s Serial Number, either from the
      subscription confirmation email or from Service Center''s
      Administration and Licensing pages, so that they can reference these
      license identifiers when contacting OutSystems Support or performing
      license management tasks.'
  - name: Licensing identifiers
    id: licensing-identifiers
    description: Wants to understand what an Activation Code and Serial
      Number are, how they differ (infrastructure-wide identifier vs.
      per-environment, hardware-based identifier), and how they relate to
      each other, so that they can correctly interpret and use these
      licensing identifiers when managing their OutSystems 11
      infrastructure.
  - name: Get environment license
    id: get-environment-license
    description: Wants to obtain and install a license file for a
      self-managed OutSystems 11 environment by registering the
      environment's serial number in Customer Portal, downloading the
      license file, and installing it, so that the environment can be
      activated or upgraded.
  - name: Enable environment analytics
    id: enable-environment-analytics
    description: Wants to enable performance and end-user-experience analytics for
      a non-Production Traditional Web Apps environment (for example, Quality Assurance)
      in OutSystems 11, using the Configuration link and toggle on the LifeTime Analytics
      Configuration page, so that performance issues can be detected before the app
      reaches Production.
  - name: Analytics data retention
    id: analytics-data-retention-o11
    description: Wants to understand and adjust how long LifeTime Analytics retains
      collected data by setting a retention period in the Factory Configuration
      application's Platform Configurations tab, and to archive data before
      shortening the period since older data is purged permanently, so that
      database growth is controlled without losing needed historical analytics.
  - name: IP rights not granted
    id: ip-rights-not-granted
    description: 'Wants to resolve the "The IP rights of your applications could
      not be granted" email received after submitting an app through the IPP
      Portal, caused by changing the activation code of an IPP-protected
      application on an infrastructure that doesn''t allow it, by contacting
      their OutSystems Platform administrator to confirm IPP is properly set
      up, or contacting OutSystems Support if that doesn''t resolve it.'
  - name: IPP email delay
    id: ipp-email-delay
    description: Wants to know what to do when they used the IPP Portal to change an
      application's activation code but haven't received a confirmation email
      within the expected time, so they check their spam folder or contact
      OutSystems Support if the email still doesn't arrive.
  - name: Fix Windows token error
    id: fix-windows-token-error
    description: 'Wants to resolve the "Could not create Windows user token
      from the credentials specified in the config file" error that blocks
      access to an eSpace (screens, web services, BPT processes, timers, or
      publishing) in OutSystems 11 on the .NET stack, caused by a
      misconfigured "Run As" user, by removing the Run As credentials or
      adding the specified user to the IIS_IUSRS group, so that the eSpace
      becomes accessible again.'
  - name: Diagnose SSL trust error
    id: diagnose-ssl-trust-error-o11
    description: 'Wants to identify the cause of a "Could not establish trust
      relationship for the SSL/TLS secure channel" error when consuming a web
      service in OutSystems 11, by checking the service URL in a browser for
      certificate warnings/details or running openssl s_client from the
      command line, so they can determine whether the cause is a hostname
      mismatch, expired/invalid certificate, untrusted CA, self-signed
      certificate, or protocol/cipher mismatch and apply the matching fix.'
  - name: Forge component known issues
    id: forge-component-known-issues
    subtopics:
    - name: Map component issues
      id: map-component-issues
      description: Wants to check known issues with the Map Forge component for Reactive
        Web and Mobile Apps in OutSystems 11, covering issues such as inaccurate
        positioning behavior with Auto Zoom, marker popup interaction problems,
        shape dragging issues with Polyline and Polygon, and search UI scrolling
        problems, so they can identify whether an issue they're facing is
        already known, apply available workarounds, and decide when to upgrade.
    - name: Data Grid known issues
      id: data-grid-known-issues
      description: Wants to check known issues with the Data Grid component for Reactive
        Web apps in OutSystems 11, so they can tell whether a problem is already
        known and decide whether to use a workaround or upgrade.
  - name: Generate BPT troubleshooting report
    id: generate-bpt-troubleshooting-report
    description: Wants to generate a BPT troubleshooting report by installing
      the BPT Utils component in their LifeTime environment (with Full
      Control permissions) and downloading it from the Troubleshooting
      Processes page, in OutSystems 11, so that they have diagnostic
      information for investigating BPT process issues.
  - name: Diagnose timeout errors
    id: diagnose-timeout-errors
    description: Wants to identify which type of OutSystems 11 timeout (SQL
      query, application request, web service, service action, server
      request, or timer) caused a runtime error, by matching the error
      message and stack trace in Service Center's Error Logs to the
      timeout event's known symptoms, so they can address the correct
      timeout setting.
  - name: Service Action timeout
    id: service-action-timeout
    description: 'Wants to understand why a Service Action call between OutSystems 11
      modules fails after a fixed period of time with "The request was aborted:
      The operation has timed out", caused by a fixed, non-configurable client-
      side timeout that SetRequestTimeout cannot extend, so they know to break
      down long-running logic instead of trying to raise the limit.'
  - name: Diagnose HTTP 502 errors
    id: diagnose-http-502-errors
    description: Wants to identify why an OutSystems 11 application returns
      an HTTP 502 bad gateway error, by comparing IIS/proxy access logs to
      distinguish a proxy timeout, a dropped proxy-to-server connection, or
      queued requests exceeding the proxy timeout, so they can pinpoint the
      correct cause at the network layer.
  - name: React 16 runtime (technical preview)
    id: react-16-runtime-technical-preview
    subtopics:
    - name: React 16 preview prereqs
      id: react-16-preview-prereqs
      description: Wants to know the requirements (the minimum required versions of
        Platform Server, LifeTime, and Service Studio, and how to activate the
        React 16 technical preview in LifeTime) to test their Reactive Web and
        Mobile apps on the React 16 runtime in OutSystems 11 before it becomes
        the default, so they can anticipate breaking changes ahead of the
        upgrade.
    - name: React 16 runtime side effects
      id: react-16-runtime-side-effects
      description: Wants to identify the behavioral differences introduced
        by the React 16 runtime in Reactive Web and Mobile apps (widgets
        ignoring JavaScript-tampered events, all unknown HTML attributes
        now rendering, and `option` element children being stringified),
        so they can fix or work around app issues before React 16 becomes
        the default runtime in OutSystems 11.
    - name: React 16 known issues
      id: react-16-known-issues
      description: Wants to check the known issues (pending and fixed) with
        the React 16 runtime technical preview for Reactive Web and Mobile
        apps in OutSystems 11, so they can tell whether an issue they're
        facing is already known and in which Platform Server version it
        was fixed.
- name: Monitor and Troubleshoot (O10)
  guid: 69e74025-d666-40fd-b224-c96f5cbba50c
  topics:
  - name: Fix cross-site cookies (O10)
    id: fix-cross-site-cookies-o10
    description: Wants to identify and fix cross-site cookie problems in
      OutSystems 10 apps by using Factory Configuration when stricter
      SameSite cookie policies cause issues such as broken iframes displaying
      third-party or OutSystems content, failing OAuth/OpenID/SAML
      authentication flows, and custom JavaScript-set cookies, so their
      applications keep working correctly.
- name: Monitor and Troubleshoot (ODC)
  guid: ''
  topics:
  - name: Resolve internal error (ODC)
    id: resolve-internal-error-odc
    description: 'Wants to know what to do when ODC Mentor Web shows the generic
      OS-IDE-CORE-50001 internal server error (a catch-all code for failures
      without a more specific cause), so they can retry the action or contact
      OutSystems Support with the trace ID if the problem persists.'
  - name: Traces and spans
    id: understand-traces-spans
    description: Wants to understand what a trace and its spans represent in
      ODC Portal, and how to review the Traces screen to identify which span
      caused an error or performance issue in a server-side request, so they
      can locate the root cause before resolving it.
    subtopics:
    - name: Client-side trace behavior
      id: client-side-trace-behavior
      description: Wants to understand client-side trace behavior in ODC Portal,
        including trace types, missing or out-of-order spans, empty blocks,
        and additional spans after screen interactions, so they can
        interpret trace data while troubleshooting an app.
    - name: Enable client-side traces
      id: enable-client-side-traces
      description: Wants to enable client-side traces for an ODC app in ODC
        Portal by turning on Capture client-side traces and metrics under
        Monitoring & Troubleshooting, so they can collect traces after
        publishing the app.
    - name: Client-side trace limits
      id: client-side-trace-limits
      description: Wants to understand client-side trace limits and known limitations, so
        they can decide when to use client-side traces.
    - name: Capture and view traces
      id: capture-and-view-traces
      description: Wants to turn server-side traces on or off and enable
        client-side trace capture in ODC Portal, then open the Traces screen
        and trace details to inspect spans and related logs, so they can
        collect and review request traces with the required permissions.
    - name: Trace retention rules
      id: trace-retention-rules
      description: Wants to understand which traces ODC retains or samples, and the rules
        that determine that, so they can predict which requests remain available
        for troubleshooting.
  - name: ODC observability retention window
    id: odc-observability-data-retention
    description: Wants to know how long logs, traces, and metrics remain
      available in ODC Portal, so they can understand the default retention
      window before it expires.
  - name: Monitor app issues
    id: monitor-app-issues
    description: Wants to monitor and troubleshoot apps in ODC Portal by using
      the Logs, Traces, and Analytics screens, so they can inspect errors,
      performance, and usage trends.
  - name: Resource capacity
    id: resource-capacity
    description: Wants to understand ODC resource capacity limits, scaling
      behavior, and what happens when resources approach or exceed those
      limits, so they can plan usage and avoid performance or deployment
      issues.
    subtopics:
    - name: Capacity limits
      id: capacity-limits
      description: Wants to understand which ODC resource types have stage-level limits and
        how resources scale against those limits, so they can plan capacity.
    - name: Optimize usage
      id: optimize-usage
      description: Wants to optimize ODC resource consumption and understand
        the effect of hitting or exceeding a resource limit, so they can
        reduce wasted capacity and avoid performance issues, failed publishes,
        or deployment errors.
  - name: Agent cost and performance analytics
    id: agent-cost-performance-analytics
    subtopics:
    - name: View agent token analytics
      id: view-agent-token-latency
      description: Wants to view token consumption and P90 response-time data
        for AI agents in the ODC Portal Analytics console, both as sortable
        columns on the agent overview page and as trend charts on the agent
        detail page, so they can identify which agents are the most expensive
        or slowest and investigate what changed.
    - name: Distinguish agent nodes
      id: distinguish-agent-nodes
      description: Wants to distinguish CallAgent nodes from generic Service
        Action elements in the Analytics console's element breakdown, so they
        can focus on agent-specific behavior when diagnosing a performance
        issue.
    - name: View agent trace
      id: view-agent-trace
      description: Wants to open a production execution trace for an AI agent
        in the ODC Portal Analytics console and read what each CallAgent node
        was prompted with, what it responded, and which tools it called,
        plus the execution's overall duration and token counts, so they can
        inspect agent behavior in detail beyond what general trace search
        shows.
- name: Project Tracking and Collaboration
  guid: ''
  topics:
  - name: OnTrack (O11)
    id: ontrack-o11
    subtopics:
    - name: OnTrack overview
      id: ontrack-overview
      description: Wants to understand what OnTrack offers for backlog management,
        project tracking, and continuous delivery collaboration in OutSystems 11,
        so that they can decide whether to use it to define requirements, prioritize
        work items, confirm user acceptance, and monitor project progress.
    - name: Link feedback to OnTrack
      id: link-feedback-to-ontrack
      description: Wants to configure ECT_Client_Connector and map eSpaces/modules
        between environments so that end-user feedback submitted through OutSystems
        App Feedback is automatically synced into OnTrack as trackable work items.
    - name: OnTrack colour scheme
      id: ontrack-work-item-color-scheme
      description: Wants to interpret the color-coded notification icons on OnTrack work
        items, so they can quickly tell whether a work item has an unread
        update, was rejected, or was accepted.
  - name: Project management metrics
    id: project-management-metrics
    subtopics:
    - name: Project metrics overview
      id: project-metrics-overview
      description: Wants to understand how OutSystems project management metrics are
        grouped and what each category is used to monitor, so that they can
        interpret the overall metrics overview and choose the right metric
        family for a delivery problem.
    - name: Project metrics tooling
      id: project-metrics-tooling
      description: Wants to understand how to collect, analyze, and visualize
        project management metrics with tools such as JIRA and EazyBI, including
        using the metric template to structure title, purpose, inputs, charts,
        and signs, so that they can implement the metrics with the tool set
        available to them.
    - name: Project metrics ceremonies
      id: project-metrics-ceremonies
      description: Wants to understand how to use project management metrics in
        planning, status meetings, scrum, and retrospectives, so that they can
        discuss scope, estimation, productivity, quality, and lead-time findings
        in the right ceremony.
- name: Community and Training
  guid: 336189f2-2366-41d7-9dfa-6819b924a0ae
  topics:
  - name: Boot Camps
    id: boot-camps
    subtopics:
    - name: Boot Camp registration
      id: boot-camp-registration
      description: Wants to find the price of an OutSystems Boot Camp and register
        for one, when planning to attend instructor-led training, so that they
        can enroll through a Training Partner and receive schedule, location,
        and payment details.
  - name: Training videos
    id: training-videos
    subtopics:
    - name: Access blocked videos
      id: access-blocked-videos
      description: Wants to watch OutSystems training videos when the streaming
        platform (for example Vimeo) is blocked, by downloading the video and
        subtitle files from the Training portal and playing them locally with
        a media player like VLC, so that they can continue training without
        guaranteed progress tracking.
  - name: Certification exams
    id: certification-exams
    subtopics:
    - name: Access exam score report
      id: access-exam-score-report
      description: Wants to access their certification or specialization exam
        score report when it wasn't received by email and isn't in the spam
        or junk folders, by using the confirmation number and last name on
        the Prometric Score Report portal, so that they can see which exam
        topics they passed or failed.
    - name: Missing exam appointment
      id: missing-exam-appointment
      description: 'Wants to get their exam appointment details when they
        didn''t receive the appointment confirmation email, by checking
        spam and junk folders and opening a support case if it''s still
        missing, so that they can continue with their exam registration.'
    - name: Exam refund eligibility
      id: exam-refund-eligibility
      description: Wants to cancel a scheduled certification exam and receive a refund, by
        canceling before the refund eligibility cutoff relative to the exam's
        scheduled date and time, so that they know whether they qualify for a
        refund or forfeit the fee.
    - name: Exam reschedule windows
      id: exam-reschedule-windows
      description: Wants to reschedule a scheduled certification exam without
        losing the fee, by understanding the free, paid, and not-allowed
        rescheduling windows based on how many days remain before the exam,
        so that they can reschedule at the right time.
    - name: Exam payment receipt
      id: exam-payment-receipt
      description: Wants to get a receipt or invoice for their certification or
        specialization exam payment, when Prometric (the exam vendor) doesn't
        issue invoices, by requesting the payment receipt through the
        Prometric confirmation portal, so that they have a document to
        support the expense with their company or tax authority.
    - name: Download exam certificate
      id: download-exam-certificate
      description: Wants to download the PDF certificate for a passed
        OutSystems certification exam, either via the public certifications
        page or via the Learn tab on their OutSystems profile, so that they
        have proof of certification once results are processed.
    - name: Download team certs
      id: download-team-certs
      description: Wants to download certification documents for their team
        members, by accessing the Certifications tab in the Customer Portal
        (as an infrastructure/company admin) or the Certifications option in
        the Partner Center (as a partner manager), so that they can track
        and share proof of certification for people they manage.
    - name: Use exam voucher
      id: use-exam-voucher
      description: Wants to redeem a promotional exam voucher code during
        OutSystems Certification exam registration, understanding its
        expiration date rules (the exam must be scheduled before it expires)
        and the reasons behind an "Invalid voucher" error (expired, wrong
        exam type, or already used), so that they can successfully apply the
        discount without registration issues.
  - name: Community account overview
    id: community-account-overview
    subtopics:
    - name: Community participation overview
      id: community-participation-overview
      description: Wants to understand how an OutSystems community account
        helps them participate in community spaces and activities, so they can
        decide how to engage with other community members.
    - name: Training access overview
      id: training-access-overview
      description: Wants to understand how an OutSystems community account
        connects them to OutSystems learning resources, so they can plan how
        to use the platform for training and skill development.
    - name: Support access overview
      id: support-access-overview
      description: Wants to understand how an OutSystems community account
        relates to support interactions and when extra permissions affect that
        access, so they know which support tasks they can perform.
    - name: Community account settings overview
      id: community-account-settings-overview
      description: Wants to understand which profile and account settings they
        can manage with an OutSystems community account, so they know where to
        update their personal details and account preferences.
  - name: Support Portal
    id: support-portal
    subtopics:
    - name: Open support case
      id: open-support-case
      description: Wants to open a support case with OutSystems through the Support Portal,
        by logging in, choosing the affected infrastructure, and selecting the
        appropriate case type, so that the case is routed to the right team and
        resolved quickly.
    - name: Add case followers
      id: add-case-followers
      description: Wants to add colleagues as followers to an OutSystems
        support case, either while opening the case in Support Portal or
        afterward by entering their email addresses on an existing case,
        so that those colleagues receive email notifications and can
        contribute information to the case.
    - name: Get support case approved
      id: get-support-case-approved
      description: Wants to get their OutSystems support case approved when
        they aren't listed as an authorized contact in Customer Portal, by
        having a company or infrastructure administrator authorize the
        request via email or the Support Portal, so that the case isn't
        held or suspended and future cases from them proceed without
        approval.
  - name: Community Badges
    id: community-badges
    subtopics:
    - name: Community badge criteria
      id: community-badge-criteria
      description: Wants to know what action or achievement unlocks each
        OutSystems Community badge, when participating in the community
        (posting, contributing Forge assets, attending events, publishing
        content, and similar activities), so that they can understand
        which recognition badges they've earned or how to earn new ones.
  - name: Customer Portal
    id: customer-portal
    subtopics:
    - name: Customer Portal access levels
      id: customer-portal-access-levels
      description: Wants to understand the access and permission structure of OutSystems
        Customer Portal, including the different member types and role tiers, so
        that they can determine who has access to what within their team.
    - name: Add Customer Portal member
      id: add-customer-portal-member
      description: Wants to add a new team member to OutSystems Customer
        Portal for an O11 infrastructure, by inviting them via email,
        assigning company and infrastructure permissions, and optionally
        configuring their initial LifeTime developer account, so that the
        new member gets appropriate access.
    - name: Set Customer Portal security contact
      id: set-customer-portal-security-contact
      description: Wants to designate a security contact in OutSystems
        Customer Portal, by assigning the Security role to a member with
        Company admin or Infrastructure admin permissions, so that person
        receives all security-related communications and notifications.
  - name: Account Settings
    id: account-settings
    subtopics:
    - name: Transfer certifications
      id: transfer-certifications
      description: Wants to transfer their OutSystems certifications from
        one community account to another, by logging into the receiving
        account's Settings and using the Move certifications option with
        the other account's email and password, so that certifications
        earned under a different email consolidate onto one account.
    - name: Change community email
      id: change-community-email
      description: Wants to change their OutSystems community account email
        through the Settings > Change email option on the community website
        and confirm it via the verification email sent, so that the new
        email also syncs as their Personal Environment login on next
        sign-in.
    - name: Change community password
      id: change-community-password
      description: Wants to change their OutSystems community account
        password by logging into their profile Settings and using the
        Change password form, understanding that the new password syncs
        to their Personal Environment after a short delay, so that they
        can update their login credentials for both the community website
        and their Personal Environment.
    - name: Merge community accounts
      id: merge-community-accounts
      description: Wants to merge two OutSystems community accounts into one, understanding
        what information transfers automatically and what doesn't between the
        two accounts, by designating which account to keep and which to delete,
        so that they consolidate their community history without losing needed
        data unexpectedly.
    - name: Close community account
      id: close-community-account
      description: Wants to close their OutSystems community account, understanding it's a
        destructive operation that permanently deletes their community data and
        Personal Environment access, by confirming the action with their
        password in Settings > Close my account, so that they stop being part of
        the OutSystems community and their data isn't retained.
- name: SEO
  guid: 54a1d9e5-0f3e-4a8b-b897-7c5c8838b0ff
  topics:
  - name: SEO fundamentals
    id: seo-fundamentals
    subtopics:
    - name: Content vs technical SEO
      id: content-vs-technical-seo
      description: Wants to understand the difference between content SEO and
        technical SEO in OutSystems 11, and which of the platform's SEO-related
        features and configurations fall into each category, so that they can
        improve their app's search-engine ranking and user experience.
    - name: SEO benefits overview
      id: seo-benefits-overview
      description: Wants to understand the benefits of implementing SEO in OutSystems apps,
        so that they can decide to adopt SEO strategies for their app.
  - name: SEO rules performance
    id: seo-rules-performance
    subtopics:
    - name: SEO rule sync limits
      id: seo-rule-sync-limits
      description: Wants to understand why performance degrades in OutSystems 11 when a
        large number of SEO Rules are configured, and which platform operations
        trigger the ISAPIFilter synchronization delay, so that they can plan
        around this known limitation.
  - name: Sitemap and robots configuration
    id: sitemap-and-robots-configuration
    subtopics:
    - name: Generate dynamic sitemap
      id: generate-dynamic-sitemap
      description: 'Wants to generate a valid sitemap.xml for their Reactive
        Web app by creating a REST API endpoint that outputs an
        XML-formatted list of static and dynamic screen URLs and deploying
        it as a static resource at the domain root, so that search engine
        crawlers can discover and index all public pages.'
    - name: Create robots.txt file
      id: create-robots-txt-file-odc
      description: 'Wants to control which parts of their site search engine
        crawlers can access by creating a static robots.txt file that
        defines allow/disallow rules and references the sitemap.xml
        endpoint, in ODC, so that crawler behavior is properly restricted.'
    - name: Create O11 robots.txt
      id: create-robots-txt-file-o11
      description: 'Wants to control which parts of their site search engine
        crawlers can access by creating a static robots.txt file that
        references the sitemap.xml endpoint and deploying it at the domain
        root, in OutSystems 11, so that crawler behavior is properly
        restricted.'
    - name: Deploy O11 sitemap flow
      id: deploy-o11-sitemap-workflow
      description: 'Wants to understand the developer-deployer workflow for
        creating, deploying, and validating sitemap.xml and robots.txt
        files across environments up to production, using Service Studio,
        Service Center, and LifeTime, in OutSystems 11, so that the SEO
        configuration is correctly published.'
  - name: Prerendering for SPAs
    id: prerendering-for-spas
    subtopics:
    - name: Integrate SEO prerender
      id: integrate-seo-prerender-odc
      description: 'Wants to improve SEO for a reactive or mobile ODC app
        affected by client-side-rendering indexing issues by integrating a
        third-party prerender solution via a reverse proxy or CDN, in ODC, so
        that search engine bots receive fully rendered pages for indexing.'
    - name: Configure prerender custom domain and SSL
      id: configure-prerender-custom-domain-ssl
      description: 'Wants to configure the custom domain and SSL certificates
        for a reverse proxy or CDN that fronts a prerendered reactive or mobile
        ODC app, so that prerendered pages are served securely on the intended
        public domain.'
    - name: Expose sitemap and robots through prerender
      id: expose-sitemap-robots-through-prerender
      description: 'Wants to expose sitemap.xml and robots.txt through the
        prerender reverse proxy or CDN for a reactive or mobile ODC app, so
        that search engine crawlers can discover crawl rules and public URLs
        through the same SEO entry point.'
    - name: Validate prerender integration
      id: validate-prerender-integration
      description: 'Wants to validate that the prerender integration serves
        fully rendered pages to search engine bots through the reverse proxy or
        CDN, by using the Prerender.io dashboard test tool and comparing curl
        requests with a bot user agent against a normal browser request, so
        that they can confirm the app is ready for indexing.'
    - name: Prerender via CloudFront
      id: integrate-seo-prerender-o11
      description: 'Wants to integrate an OutSystems 11 reactive or mobile
        app with Prerender.io using AWS CloudFront and Lambda@Edge functions
        to intercept viewer and origin requests, so that search engine bots
        receive fully rendered pages for improved SEO without affecting
        end-user experience.'
    - name: Reduce Prerender.io costs
      id: reduce-prerender-costs
      description: 'Wants to minimize Prerender.io caching costs in
        OutSystems 11 by excluding unnecessary content from caching, limiting
        the bot user agents forwarded to Prerender.io, and adopting an
        optimal cache/re-cache strategy, so that render and re-cache charges
        stay under control.'
    - name: Prerender.io sitemap sync
      id: prerender-sitemap-sync
      description: 'Wants to keep Prerender.io''s cache aligned with current
        site content by uploading and periodically synchronizing their
        sitemap.xml with Prerender.io, in OutSystems 11, so that new and
        updated URLs get queued for caching automatically.'
    - name: Set prerender HTTP status
      id: set-prerender-http-status
      description: 'Wants to make Prerender.io report the correct HTTP status
        code (for example, 301 or 404) for pages that don''t return a default
        200, by adding prerender-status-code and prerender-header meta tags
        via JavaScript in Service Studio, in OutSystems 11, so that crawler
        history accurately reflects page status.'
  - name: Root domain redirection
    id: root-domain-redirection
    subtopics:
    - name: Redirect root domain
      id: redirect-root-domain
      description: 'Wants to redirect a root domain (or subdomain) to a
        default ODC app screen using a reverse proxy such as Nginx or a
        CDN, in OutSystems Developer Cloud, so that end users and search
        engine crawlers land on a functional app entry point for improved
        SEO and usability.'
  - name: Reactive Web SEO configuration
    id: reactive-web-seo-configuration
    subtopics:
    - name: Configure custom screen URLs
      id: configure-custom-screen-urls
      description: 'Wants to configure SEO-friendly custom URLs for Reactive
        Web app screens, by meeting the Platform Server and Service Studio
        version prerequisites and setting the Custom URL, Page Name, and URL
        Structure screen properties in Service Studio, so that screens have
        clean, keyword-friendly URLs instead of default query strings.'
    - name: Create dynamic page titles
      id: create-dynamic-page-titles
      description: 'Wants to create dynamic page titles for Reactive Web
        app screens using expressions in the Service Studio Title property,
        so that browser tabs, bookmarks, and search results reflect page
        content dynamically.'
  - name: Traditional Web SEO configuration
    id: traditional-web-seo-configuration
    subtopics:
    - name: Configure SEO URL rules
      id: configure-seo-url-rules
      description: 'Wants to configure SEO-friendly URLs for Traditional Web
        apps in OutSystems 11 by setting up Page Rules and Module Aliases in
        Service Center, so that URLs are simplified for search engines.'
    - name: Manage SEO redirect rules
      id: manage-seo-redirect-rules
      description: 'Wants to create, edit, and bulk-import Redirect Rules in
        Service Center, including permanent 301 and temporary 302 rules, so
        that old links keep working after domain or URL changes.'
    - name: Install SEO ISAPI filter
      id: install-seo-isapi-filter
      description: 'Wants to install the OutSystems ISAPI Filter in
        Microsoft IIS on an on-premises Platform Server, by adding the
        filter through IIS Manager and setting the Root Application''s
        application pool to OutSystemsApplications, in OutSystems 11, so
        that SEO-friendly URLs work correctly on on-premises environments.'
    - name: Configure SEO logging
      id: configure-seo-logging
      description: 'Wants to turn detailed logging of SEO-friendly URL
        transformations and ISAPI Filter status on or off, by editing the
        OsISAPIConf.cache file on each Front-end server, in OutSystems 11
        on-premises environments, so that they can troubleshoot URL
        rewriting issues without permanently degrading Platform Server
        performance.'
- name: Traditional to Reactive Conversion
  guid: ''
  topics:
  - name: Conversion process
    id: conversion-process
    subtopics:
    - name: Convert to Reactive
      id: convert-to-reactive
      description: Wants to convert an OutSystems 11 Traditional Web module
        to Reactive using Service Studio's conversion accelerator, including
        checking prerequisites, deciding conversion order for multiple
        modules, and handling entities and roles, so that the module is
        transformed with minimal manual rework.
  - name: Post-conversion security
    id: post-conversion-security
    subtopics:
    - name: Validate security after conversion
      id: validate-security-after-conversion
      description: Wants to review security-sensitive elements introduced
        by an OutSystems 11 Traditional-to-Reactive conversion (client
        variables, converted Preparation logic, exposed server actions,
        client-side role checks, and dynamic sorting queries), so that no
        previously server-protected data or operations become exposed in
        the Reactive module.
  - name: Post-conversion fixes
    id: post-conversion-fixes
    subtopics:
    - name: Fix mock elements
      id: fix-mock-elements
      description: Wants to identify and replace mock elements (mocked
        actions and "to be fixed" web block placeholders) created by an
        OutSystems 11 Traditional-to-Reactive conversion, so they can
        complete the manual conversion work Service Studio couldn't
        automate.
- name: Creating components
  guid: cc7329da-a219-487c-b72f-2ef30bb1676d
  topics:
  - name: Plan and design components
    id: plan-design-components
    description: Wants to plan and design a reusable OutSystems component by
      deciding its purpose, defining a small API with sensible defaults and
      extensibility options, and choosing between a block or a module (with
      or without a UI template), so it addresses its main use cases and
      stays easy to reuse and maintain.
  - name: Component best practices
    id: component-best-practices
    description: Groups build-time guidance for reusable OutSystems
      components, including customization points, lifecycle events, Service
      Studio preview, and scale, security, and performance concerns.
    subtopics:
    - name: Expose component styling and script customization points
      id: component-styling-script-customization
      description: Wants to decide how a reusable OutSystems component should
        reuse shared CSS and JavaScript while still exposing safe
        customization points, so consuming apps can adapt its look and
        behavior without forking the component.
    - name: Handle component lifecycle events
      id: component-lifecycle-events
      description: Wants to decide which lifecycle events a reusable
        OutSystems component should handle and where that logic belongs, so
        initialization, updates, and cleanup happen predictably in every app
        that uses it.
    - name: Support Service Studio preview
      id: component-service-studio-preview
      description: Wants to ensure a reusable OutSystems component renders and
        behaves correctly in Service Studio preview, so developers can inspect
        and configure it accurately while building apps.
    - name: Design for scale, security, and performance
      id: component-scale-security-performance
      description: Wants to design a reusable OutSystems component to meet
        scalability, security, and performance requirements, so it remains
        safe and responsive as more apps and users depend on it.
  - name: Present and share components
    id: present-share-components
    description: Wants to prepare a reusable OutSystems component for others
      to adopt, by following naming, description, and icon conventions and
      the Forge sharing and trusted component requirements, so other
      developers can discover, understand, and trust the component before
      using it.
- name: Detach from OutSystems
  guid: 6c31f58f-e910-49bf-8c27-bf14bd8cd714
  topics:
  - name: Detach process (.NET)
    id: detach-process-net
    subtopics:
    - name: Detach process overview
      id: detach-process-overview
      description: Wants to understand the overall process, requirements, and
        consequences of detaching .NET web and mobile applications from
        OutSystems 10/11, including what functionality is lost and the
        personnel skills needed, so that they can plan a full detach from
        the platform.
    - name: Prepare detach infrastructure
      id: prepare-detach-infrastructure
      description: Wants to prepare the Application Server (IIS) and Database
        Server to host a detached OutSystems .NET application, by
        reconfiguring hostnames, connection strings, encryption keys, and
        cache control paths, so that the detached application runs
        correctly outside OutSystems.
    - name: Compile deploy detached web app
      id: compile-deploy-detached-web-app
      description: Wants to obtain, compile, and deploy the detached source
        code of an OutSystems web application using the supported development
        tools, so that
        the application runs independently of OutSystems after detaching.
    - name: Compile deploy detached mobile native shell
      id: compile-deploy-detached-mobile-native-shell
      description: Wants to obtain, compile, and deploy the detached source
        code of an OutSystems mobile native shell using the supported
        development tools, so that the application runs independently of
        OutSystems after detaching.
