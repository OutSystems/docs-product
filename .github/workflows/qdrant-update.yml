name: Qdrant update

on:
  push:
    branches: [master]

jobs:
  call_shared_workflow:
    name: Qdrant update
    uses: OutSystems/tk-cicd/.github/workflows/qdrant-update.yml@main
    with:
      GITHUB_REPOSITORY: ${{ github.repository }}
    secrets: inherit
